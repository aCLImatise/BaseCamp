!Command
positional: []
named:
- !Flag
  description: The input PSL file (output of BLAT) containing the reads mapping gene-gene
    sequence.
  synonyms:
  - --input_psl
  args: !SimpleFlagArg
    name: INPUT_PSL_FILENAME
  optional: true
- !Flag
  description: The input MAP file containing the reads mapping on transcriptome.
  synonyms:
  - --input_transcriptome
  args: !SimpleFlagArg
    name: INPUT_TRANSCRIPTOME_FILENAME
  optional: true
- !Flag
  description: The output text file containing all reads mapping on the from the input
    PSL file except the ones which have been removed (because their mates map on other
    genes than those from which form the candidate fusion).
  synonyms:
  - --output_psl
  args: !SimpleFlagArg
    name: OUTPUT_PSL_FILENAME
  optional: true
command:
- remove_reads_exon_exon_psl.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: show program's version number and exit
  synonyms:
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "Usage: remove_reads_exon_exon_psl.py [options]\n\nGiven a input MAP file\
  \ containing reads mapping on                      exon-\nexon junctions it will\
  \ remove the reads which have                      the\nmate mapping (i.e. second\
  \ MAP input file                      containing reads\nmapping on transcriptom)\
  \ on other genes                      than those\ncontaining the exons which form\
  \ the exon-exon                      junction.\n\nOptions:\n  --version        \
  \     show program's version number and exit\n  -h, --help            show this\
  \ help message and exit\n  --input_psl=INPUT_PSL_FILENAME\n                    \
  \    The input PSL file (output of BLAT) containing\n                        the\
  \ reads mapping gene-gene sequence.\n  --input_transcriptome=INPUT_TRANSCRIPTOME_FILENAME\n\
  \                        The input MAP file containing the reads mapping\n     \
  \                   on transcriptome.\n  --output_psl=OUTPUT_PSL_FILENAME\n    \
  \                    The output text file containing all reads mapping\n       \
  \                 on the from the input PSL file\n                        except\
  \ the ones which have been removed (because\n                        their mates\
  \ map on other genes than those from which\n                        form the candidate\
  \ fusion).\n"
generated_using:
- --help
