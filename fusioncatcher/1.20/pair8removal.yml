!Command
positional: []
named:
- !Flag
  description: Tab separeted text file such that one line has two reads which are
    paired (i.e. generated via 'paste - - - - - - - -').
  synonyms:
  - -i
  - --input
  args: !SimpleFlagArg
    name: INPUT_FILENAME
  optional: true
- !Flag
  description: Tab separeted text file such that one line has two reads which are
    paired (i.e. generated via 'paste - - - - - - - -').
  synonyms:
  - -o
  - --output
  args: !SimpleFlagArg
    name: OUTPUT_FILENAME
  optional: true
- !Flag
  description: The minimum length of a read. All reads shorter than this will be removed.
    Default is 25.
  synonyms:
  - -l
  - --length
  args: !SimpleFlagArg
    name: LENGTH
  optional: true
command:
- pair8removal.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: show program's version number and exit
  synonyms:
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "Usage: pair8removal.py [options] --input <tab separated file>  --output\
  \ <tab separated file>\n\nIt removes the pairs of reads where one or both of the\
  \ reads are shorter than\na given threshold.\n\nOptions:\n  --version          \
  \   show program's version number and exit\n  -h, --help            show this help\
  \ message and exit\n  -i INPUT_FILENAME, --input=INPUT_FILENAME\n              \
  \          Tab separeted text file such that one line has two\n                \
  \        reads which are paired (i.e. generated via 'paste - -\n               \
  \         - - - - - -').\n  -o OUTPUT_FILENAME, --output=OUTPUT_FILENAME\n     \
  \                   Tab separeted text file such that one line has two\n       \
  \                 reads which are paired (i.e. generated via 'paste - -\n      \
  \                  - - - - - -').\n  -l LENGTH, --length=LENGTH\n              \
  \          The minimum length of a read. All reads shorter than\n              \
  \          this will be removed. Default is 25.\n\n\nAuthor: Daniel Nicorici\nEmail:\
  \ Daniel.Nicorici@gmail.com\nCopyright (c) 2009-2019 Daniel Nicorici\n\n"
generated_using:
- --help
