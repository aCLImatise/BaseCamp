!Command
positional: []
named:
- !Flag
  description: File containing the candidate fusion genes.
  synonyms:
  - --input
  args: !SimpleFlagArg
    name: INPUT_FILENAME
  optional: true
- !Flag
  description: File containing the pairs of genes and their corresponding number of
    reads which map ambiguously on each other.
  synonyms:
  - --input_ambiguous
  args: !SimpleFlagArg
    name: INPUT_AMBIGUOUS_FILENAME
  optional: true
- !Flag
  description: File containing the candidate fusion genes from the input and an extra
    column with their associatied counts of common mapping reads.
  synonyms:
  - --output
  args: !SimpleFlagArg
    name: OUTPUT_FILENAME
  optional: true
command:
- add_ambiguous_counts.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: show program's version number and exit
  synonyms:
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "Usage: add_ambiguous_counts.py [options]\n\nIt adds an extra column with\
  \ the counts of reads which are ambiguous.\n\nOptions:\n  --version            \
  \ show program's version number and exit\n  -h, --help            show this help\
  \ message and exit\n  --input=INPUT_FILENAME\n                        File containing\
  \ the candidate fusion genes.\n  --input_ambiguous=INPUT_AMBIGUOUS_FILENAME\n  \
  \                      File containing the pairs of genes and their\n          \
  \              corresponding number of reads which map ambiguously on\n        \
  \                each other.\n  --output=OUTPUT_FILENAME\n                     \
  \   File containing the candidate fusion genes from the\n                      \
  \  input and an extra column with their associatied\n                        counts\
  \ of common mapping reads.\n"
generated_using:
- --help
