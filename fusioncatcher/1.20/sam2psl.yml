!Command
positional: []
named:
- !Flag
  description: The input file in SAM format.
  synonyms:
  - -i
  - --input
  args: !SimpleFlagArg
    name: INPUT_FILENAME
  optional: true
- !Flag
  description: By default if the CIGAR strings in the input SAM file are in the format
    defined in SAM version 1.4 (i.e. there are 'X' and '=') then the CIGAR string
    will be first converted into CIGAR string, which is described in SAM version 1.3,
    (i.e. there are no 'X' and '=' which are replaced with 'M') and afterwards into
    PSL format. Default is 'False'.
  synonyms:
  - '-4'
  - --skip-conversion-cigar-1.3
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: It adds to the PSL output as column 22, the sequence of the read. This
    is not anymore a valid PSL format.
  synonyms:
  - -s
  - --read-seq
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: In the reads ids (also known as query name in PSL) the string specified
    here will be replaced with '/' (which is used in Solexa for /1 and /2).
  synonyms:
  - -r
  - --replace-read-ids
  args: !SimpleFlagArg
    name: REPLACE_READS_IDS
  optional: true
- !Flag
  description: The output file in PSL format.
  synonyms:
  - -o
  - --output
  args: !SimpleFlagArg
    name: OUTPUT_FILENAME
  optional: true
command:
- sam2psl.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: show program's version number and exit
  synonyms:
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "Usage: sam2psl.py [options]\n\nIt takes as input a file in SAM format\
  \ and it converts into a PSL format file.\n\nOptions:\n  --version             show\
  \ program's version number and exit\n  -h, --help            show this help message\
  \ and exit\n  -i INPUT_FILENAME, --input=INPUT_FILENAME\n                      \
  \  The input file in SAM format.\n  -4, --skip-conversion-cigar-1.3\n          \
  \              By default if the CIGAR strings in the input SAM file\n         \
  \               are in the format defined in SAM version 1.4 (i.e.\n           \
  \             there are 'X' and '=') then the CIGAR string will be\n           \
  \             first converted into CIGAR string, which is described\n          \
  \              in SAM version 1.3, (i.e. there are no 'X' and '='\n            \
  \            which are replaced with 'M') and afterwards into PSL\n            \
  \            format. Default is 'False'.\n  -s, --read-seq        It adds to the\
  \ PSL output as column 22, the sequence\n                        of the read. This\
  \ is not anymore a valid PSL format.\n  -r REPLACE_READS_IDS, --replace-read-ids=REPLACE_READS_IDS\n\
  \                        In the reads ids (also known as query name in PSL) the\n\
  \                        string specified here will be replaced with '/' (which\n\
  \                        is used in Solexa for /1 and /2).\n  -o OUTPUT_FILENAME,\
  \ --output=OUTPUT_FILENAME\n                        The output file in PSL format.\n"
generated_using:
- --help
