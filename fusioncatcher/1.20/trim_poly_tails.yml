!Command
positional: []
named:
- !Flag
  description: The input file in FASTQ format.
  synonyms:
  - -i
  - --input
  args: !SimpleFlagArg
    name: INPUT_FILENAME
  optional: true
- !Flag
  description: The output FASTQ file containing all the trimmed sequences.
  synonyms:
  - -o
  - --output
  args: !SimpleFlagArg
    name: OUTPUT_FILENAME
  optional: true
- !Flag
  description: The number of times a nucleotide specified with '-- nucleotide' should
    be repeated in order to be considered a poly Default is 9.
  synonyms:
  - -r
  - --repeats
  args: !SimpleFlagArg
    name: REPEATS
  optional: true
- !Flag
  description: If this is specified then the reads which are having poly tails are
    filtered out (i.e. not written to the output) instead of trimming. Default is
    False.
  synonyms:
  - -s
  - --skip_reads
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: If this is specified then the reads which are less than N bp will be
    kept, where N is set using '--keep- too-short-length'. Default is False.
  synonyms:
  - -k
  - --keep-too-short
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: The threshold used to decide when a read is too short. Default is 20.
  synonyms:
  - -l
  - --keep-too-short-length
  args: !SimpleFlagArg
    name: KEEP_TOO_SHORT_LENGTH
  optional: true
command:
- trim_poly_tails.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: show program's version number and exit
  synonyms:
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "Usage: trim_poly_tails.py [options]\n\nIt trims the leading or trailing\
  \ poly-A/C/G/T tails of read sequences from a\ninput FASTQ file.\n\nOptions:\n \
  \ --version             show program's version number and exit\n  -h, --help   \
  \         show this help message and exit\n  -i INPUT_FILENAME, --input=INPUT_FILENAME\n\
  \                        The input file in FASTQ format.\n  -o OUTPUT_FILENAME,\
  \ --output=OUTPUT_FILENAME\n                        The output FASTQ file containing\
  \ all the trimmed\n                        sequences.\n  -r REPEATS, --repeats=REPEATS\n\
  \                        The number of times a nucleotide specified with '--\n \
  \                       nucleotide' should be repeated in order to be\n        \
  \                considered a poly Default is 9.\n  -s, --skip_reads      If this\
  \ is specified then the reads which are having\n                        poly tails\
  \ are filtered out (i.e. not written to the\n                        output) instead\
  \ of trimming. Default is False.\n  -k, --keep-too-short  If this is specified then\
  \ the reads which are less\n                        than N bp will be kept, where\
  \ N is set using '--keep-\n                        too-short-length'. Default is\
  \ False.\n  -l KEEP_TOO_SHORT_LENGTH, --keep-too-short-length=KEEP_TOO_SHORT_LENGTH\n\
  \                        The threshold used to decide when a read is too short.\n\
  \                        Default is 20.\n"
generated_using:
- --help
