!Command
positional: []
named:
- !Flag
  description: The input MAP file containing the reads mapping on exon-exon junctions.
  synonyms:
  - --input_exon_exon
  args: !SimpleFlagArg
    name: INPUT_EXON_EXON_FILENAME
  optional: true
- !Flag
  description: The input MAP file containing the reads mapping on transcriptome.
  synonyms:
  - --input_transcriptome
  args: !SimpleFlagArg
    name: INPUT_TRANSCRIPTOME_FILENAME
  optional: true
- !Flag
  description: Only reads which form a pair are kept for further analysis (i.e. one
    read maps on one of the known                       transcripts of the genes involved
    in the candidate                       fusion and its corresponding mate maps
    on fusion point which is the exon-exon junction). Default is False.
  synonyms:
  - --only_pairs
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: The output text file containing all reads mapping on the exon-exon
    junctions from the input MAP file except the ones which have been removed (because
    their mates map on other genes than those from which the exons which form the
    exon-exon junction).
  synonyms:
  - --output_exon_exon
  args: !SimpleFlagArg
    name: OUTPUT_EXON_EXON_FILENAME
  optional: true
command:
- remove_reads_exon_exon_map.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: show program's version number and exit
  synonyms:
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "Usage: remove_reads_exon_exon_map.py [options]\n\nGiven a input MAP file\
  \ containing reads mapping on                      exon-\nexon junctions it will\
  \ remove the reads which have                      the\nmate mapping (i.e. second\
  \ MAP input file                      containing reads\nmapping on transcriptom)\
  \ on other genes                      than those\ncontaining the exons which form\
  \ the exon-exon                      junction.\n\nOptions:\n  --version        \
  \     show program's version number and exit\n  -h, --help            show this\
  \ help message and exit\n  --input_exon_exon=INPUT_EXON_EXON_FILENAME\n        \
  \                The input MAP file containing the reads mapping\n             \
  \           on exon-exon junctions.\n  --input_transcriptome=INPUT_TRANSCRIPTOME_FILENAME\n\
  \                        The input MAP file containing the reads mapping\n     \
  \                   on transcriptome.\n  --only_pairs          Only reads which\
  \ form a pair are kept for\n                        further analysis (i.e. one read\
  \ maps on one of the\n                        known                       transcripts\
  \ of the genes\n                        involved in the candidate              \
  \         fusion\n                        and its corresponding mate maps on fusion\
  \ point\n                        which is the exon-exon junction). Default is False.\n\
  \  --output_exon_exon=OUTPUT_EXON_EXON_FILENAME\n                        The output\
  \ text file containing all reads mapping\n                        on the exon-exon\
  \ junctions from the input MAP file\n                        except the ones which\
  \ have been removed (because\n                        their mates map on other genes\
  \ than those from which\n                        the exons which form the exon-exon\
  \ junction).\n"
generated_using:
- --help
