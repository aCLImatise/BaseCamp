!Command
positional: []
named:
- !Flag
  description: The first input file containing homolog genes (generated using find_homolog_genes.py).
  synonyms:
  - --input_1
  args: !SimpleFlagArg
    name: INPUT_1_FILENAME
  optional: true
- !Flag
  description: The second input file containing homolog genes (generated using find_homolog_genes.py).
  synonyms:
  - --input_2
  args: !SimpleFlagArg
    name: INPUT_2_FILENAME
  optional: true
- !Flag
  description: The output text tab-separated file containing the candidate homologous
    genes (the genes are sorted alphabetically on the each line).
  synonyms:
  - --output
  args: !SimpleFlagArg
    name: OUTPUT_FILENAME
  optional: true
- !Flag
  description: The output text tab-separated file containing the candidate homologous
    genes (the genes are sorted alphabetically on the each line) which were found
    to have less reads than the threshold specified using --reads.
  synonyms:
  - --rest
  args: !SimpleFlagArg
    name: OUTPUT_REST_FILENAME
  optional: true
- !Flag
  description: The output text tab-separated file containing all candidate homologous
    genes (the genes are sorted alphabetically on the each line) and the corresponding
    counts of ambiguous reads.
  synonyms:
  - --all
  args: !SimpleFlagArg
    name: OUTPUT_ALL_FILENAME
  optional: true
- !Flag
  description: The minimum number of reads which map simultaneously on two genes in
    order to be considered as homolog genes (after the joining is done). Default is
    1.
  synonyms:
  - --reads
  args: !SimpleFlagArg
    name: READS
  optional: true
command:
- join_homolog_genes.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: show program's version number and exit
  synonyms:
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "Usage: join_homolog_genes.py [options]\n\nIt joins a list of homologous\
  \ gene (found using find_homolog_genes.py) by\nadding also their corresponding counts.\n\
  \nOptions:\n  --version             show program's version number and exit\n  -h,\
  \ --help            show this help message and exit\n  --input_1=INPUT_1_FILENAME\n\
  \                        The first input file containing homolog genes\n       \
  \                 (generated using find_homolog_genes.py).\n  --input_2=INPUT_2_FILENAME\n\
  \                        The second input file containing homolog genes\n      \
  \                  (generated using find_homolog_genes.py).\n  --output=OUTPUT_FILENAME\n\
  \                        The output text tab-separated file containing the\n   \
  \                     candidate homologous genes (the genes are sorted\n       \
  \                 alphabetically on the each line).\n  --rest=OUTPUT_REST_FILENAME\n\
  \                        The output text tab-separated file containing the\n   \
  \                     candidate homologous genes (the genes are sorted\n       \
  \                 alphabetically on the each line) which were found to\n       \
  \                 have less reads than the threshold specified using\n         \
  \               --reads.\n  --all=OUTPUT_ALL_FILENAME\n                        The\
  \ output text tab-separated file containing all\n                        candidate\
  \ homologous genes (the genes are sorted\n                        alphabetically\
  \ on the each line) and the corresponding\n                        counts of ambiguous\
  \ reads.\n  --reads=READS         The minimum number of reads which map simultaneously\n\
  \                        on two genes in order to be considered as homolog\n   \
  \                     genes (after the joining is done). Default is 1.\n"
generated_using:
- --help
