!Command
positional: []
named:
- !Flag
  description: The input file containing the list of reads names to be removed from
    the MAP BOWTIE file.
  synonyms:
  - --input_reads
  args: !SimpleFlagArg
    name: INPUT_READS_FILENAME
  optional: true
- !Flag
  description: The input file in Bowtie MAP format from where the reads will be removed.
  synonyms:
  - --input_map
  args: !SimpleFlagArg
    name: INPUT_MAP_FILENAME
  optional: true
- !Flag
  description: The output text file containing all reads from the input MAP BOWTIE
    file except the ones which have been removed.
  synonyms:
  - --output_map
  args: !SimpleFlagArg
    name: OUTPUT_FILENAME
  optional: true
- !Flag
  description: Type of operation to be performed. The choices are ['remove','extract'].
    Default is 'remove'.
  synonyms:
  - --operation
  args: !SimpleFlagArg
    name: KIND
  optional: true
command:
- reads_from_map.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: show program's version number and exit
  synonyms:
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "Usage: reads_from_map.py [options]\n\nGiven a list of reads names it removes/extracts\
  \ them from a MAP BOWTIE file.\n\nOptions:\n  --version             show program's\
  \ version number and exit\n  -h, --help            show this help message and exit\n\
  \  --input_reads=INPUT_READS_FILENAME\n                        The input file containing\
  \ the list of reads names to\n                        be removed from the MAP BOWTIE\
  \ file.\n  --input_map=INPUT_MAP_FILENAME\n                        The input file\
  \ in Bowtie MAP format from where the\n                        reads will be removed.\n\
  \  --output_map=OUTPUT_FILENAME\n                        The output text file containing\
  \ all reads from the\n                        input MAP BOWTIE file except the ones\
  \ which have been\n                        removed.\n  --operation=KIND      Type\
  \ of operation to be performed. The choices are\n                        ['remove','extract'].\
  \ Default is 'remove'.\n"
generated_using:
- --help
