!Command
positional: []
named:
- !Flag
  description: The input file (in FASTQ format) containing the short reads to be processed.
  synonyms:
  - --input
  args: !SimpleFlagArg
    name: INPUT_FILENAME
  optional: true
- !Flag
  description: The output FASTQ file containing the short reads where the reads which
    are shorter then a given threshold are removed.
  synonyms:
  - --output
  args: !SimpleFlagArg
    name: OUTPUT_FILENAME
  optional: true
- !Flag
  description: If if this is larger than zero then all the short reads strictly shorter
    than this threshold are removed. Default is 0.
  synonyms:
  - --threshold
  args: !SimpleFlagArg
    name: THRESHOLD
  optional: true
command:
- remove_shorter_reads.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: show program's version number and exit
  synonyms:
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "Usage: remove_shorter_reads.py [options]\n\nIt takes a FASTQ file and\
  \ removes all reads which are shorter strictly than a\ngiven threshold.\n\nOptions:\n\
  \  --version             show program's version number and exit\n  -h, --help  \
  \          show this help message and exit\n  --input=INPUT_FILENAME\n         \
  \               The input file (in FASTQ format) containing the short\n        \
  \                reads to be processed.\n  --output=OUTPUT_FILENAME\n          \
  \              The output FASTQ file containing the short reads where\n        \
  \                the reads which are shorter then a given threshold are\n      \
  \                  removed.\n  --threshold=THRESHOLD\n                        If\
  \ if this is larger than zero then all the short\n                        reads\
  \ strictly shorter than this threshold are\n                        removed. Default\
  \ is 0.\n"
generated_using:
- --help
