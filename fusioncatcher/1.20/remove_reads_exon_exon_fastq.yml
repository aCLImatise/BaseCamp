!Command
positional: []
named:
- !Flag
  description: The input FASTQ file containing the reads.
  synonyms:
  - --input_fastq
  args: !SimpleFlagArg
    name: INPUT_FASTQ_FILENAME
  optional: true
- !Flag
  description: The input file containing the list of candidate fusion genes.
  synonyms:
  - --input_fusions
  args: !SimpleFlagArg
    name: INPUT_FUSIONS_FILENAME
  optional: true
- !Flag
  description: The input MAP file containing the reads mapping on transcriptome.
  synonyms:
  - --input_transcriptome
  args: !SimpleFlagArg
    name: INPUT_TRANSCRIPTOME_FILENAME
  optional: true
- !Flag
  description: The output FASTQ file containing all reads which have their mate mapping
    on the candidate fusion genes.
  synonyms:
  - --output_fastq
  args: !SimpleFlagArg
    name: OUTPUT_FASTQ_FILENAME
  optional: true
- !Flag
  description: The output log file.
  synonyms:
  - --log
  args: !SimpleFlagArg
    name: OUTPUT_LOG_FILENAME
  optional: true
command:
- remove_reads_exon_exon_fastq.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: show program's version number and exit
  synonyms:
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "Usage: remove_reads_exon_exon_fastq.py [options]\n\nGiven a input FASTQ\
  \ file containing unmapped reads mapping\nit will remove the reads which have  \
  \                    the mate mapping\n(i.e. second MAP input file             \
  \         containing reads mapping on\ntranscriptom) on other genes            \
  \          than the list of candidate\nfusion genes.\n\nOptions:\n  --version  \
  \           show program's version number and exit\n  -h, --help            show\
  \ this help message and exit\n  --input_fastq=INPUT_FASTQ_FILENAME\n           \
  \             The input FASTQ file containing the reads.\n  --input_fusions=INPUT_FUSIONS_FILENAME\n\
  \                        The input file containing the list of candidate fusion\n\
  \                        genes.\n  --input_transcriptome=INPUT_TRANSCRIPTOME_FILENAME\n\
  \                        The input MAP file containing the reads mapping\n     \
  \                   on transcriptome.\n  --output_fastq=OUTPUT_FASTQ_FILENAME\n\
  \                        The output FASTQ file containing all reads which\n    \
  \                    have their mate mapping on the candidate fusion\n         \
  \               genes.\n  --log=OUTPUT_LOG_FILENAME\n                        The\
  \ output log file.\n"
generated_using:
- --help
