!Command
positional: []
named:
- !Flag
  description: The input file in FASTQ Solexa file (also given thru stdin or as gzipped
    file).
  synonyms:
  - -i
  - --input
  args: !SimpleFlagArg
    name: INPUT_FILENAME
  optional: true
- !Flag
  description: The output text file containg the unique lengths of the reads found
    in the input file. The unique lengths are sorted in descending order.
  synonyms:
  - -o
  - --output
  args: !SimpleFlagArg
    name: OUTPUT_FILENAME
  optional: true
- !Flag
  description: The output text file containg the counts of reads found in the input
    file.
  synonyms:
  - -c
  - --counts
  args: !SimpleFlagArg
    name: COUNTS_FILENAME
  optional: true
command:
- lengths_reads.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: show program's version number and exit
  synonyms:
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "Usage: lengths_reads.py [options]\n\nIt writes into a text files the lengths\
  \ (sorted in descending order) of the\nreads found in a FASTQ Solexa file.\n\nOptions:\n\
  \  --version             show program's version number and exit\n  -h, --help  \
  \          show this help message and exit\n  -i INPUT_FILENAME, --input=INPUT_FILENAME\n\
  \                        The input file in FASTQ Solexa file (also given thru\n\
  \                        stdin or as gzipped file).\n  -o OUTPUT_FILENAME, --output=OUTPUT_FILENAME\n\
  \                        The output text file containg the unique lengths of\n \
  \                       the reads found in the input file. The unique lengths\n\
  \                        are sorted in descending order.\n  -c COUNTS_FILENAME,\
  \ --counts=COUNTS_FILENAME\n                        The output text file containg\
  \ the counts of reads\n                        found in the input file.\n"
generated_using:
- --help
