!Command
command:
- kissnp2
positional: []
named:
- !Flag
  description: '(0 arg) :    keep truncated bubbles, that have no successors on the
    2 paths at the same position'
  synonyms:
  - -x
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "(1 arg) :    maximal number of polymorphism per bubble  [default '1']"
  synonyms:
  - -P
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "(1 arg) :    maximal size of a predicted indel  [default '0']"
  synonyms:
  - -D
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "(0 arg) :    Don't output the first coverage threshold. Use this option\
    \ whent the refernece file is used for finding the variants"
  synonyms:
  - -dont_output_first_coverage
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: '(1 arg) :    input file (likely a hdf5 file)'
  synonyms:
  - -in
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "(1 arg) :    File (.h5) generated by kissnp2, containing the coverage\
    \ threshold per read set  [default '_removemeplease']"
  synonyms:
  - -coverage_file
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: '(1 arg) :    output name'
  synonyms:
  - -out
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: '(0 arg) :    extend found and stop at large polymorphism (extension=contigs)
    SNPs. Uncompatible with -t'
  synonyms:
  - -T
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: '(0 arg) :    extend found and stop at first polymorphism (strict extension=unitigs)
    SNPs. Uncompatible with -T'
  synonyms:
  - -t
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "(1 arg) :    In b2 mode only: maximal number of symmetrical croasroads\
    \ traversed while trying to close a bubble. Default: no limit  [default '-1']"
  synonyms:
  - -max_symmetrical_crossroads
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "(1 arg) :    branching mode\n0: forbid SNPs for wich any of the two\
    \ paths is branching (high precision, low recall)\n1: forbid SNPs for wich the\
    \ two paths are branching (e.g. the two paths can be created either with a 'A'\
    \ or a 'C' at the same position (default value)\n2: No limitation on branching\
    \ (low precision, high recall)  [default '1']"
  synonyms:
  - -b
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "(1 arg) :    Maximal size of ambiguity of INDELs. INDELS whose ambiguity\
    \ is higher than this value are not output  [default '20']"
  synonyms:
  - -max_ambigous_indel
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: '(0 arg) :    conserve low complexity SNPs'
  synonyms:
  - -l
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "(1 arg) :    number of cores  [default '0']"
  synonyms:
  - -nb-cores
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "(1 arg) :    verbosity level  [default '1']"
  synonyms:
  - -verbose
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: '(0 arg) :    version'
  synonyms:
  - -version
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "(1 arg) :    maximum depth for BFS  [default '200']"
  synonyms:
  - -bfs-max-depth
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "(1 arg) :    maximum breadth for BFS  [default '20']"
  synonyms:
  - -bfs-max-breadth
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "(1 arg) :    Longest accepted difference length between two paths\
    \ of a truncated bubbleS  [default '0']"
  synonyms:
  - -max_truncated_path_length_difference
  args: !EmptyFlagArg {}
  optional: true
parent:
subcommands: []
usage: []
help_flag: !Flag
  description: '(0 arg) :    help'
  synonyms:
  - -help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "ERROR: Unknown parameter '--help'\nERROR: Option '-in' is mandatory\n\
  ERROR: Option '-out' is mandatory\n\n[Kissnp2 options]\n       -x              \
  \                      (0 arg) :    keep truncated bubbles, that have no successors\
  \ on the 2 paths at the same position\n       -P                               \
  \     (1 arg) :    maximal number of polymorphism per bubble  [default '1']\n  \
  \     -D                                    (1 arg) :    maximal size of a predicted\
  \ indel  [default '0']\n       -dont_output_first_coverage           (0 arg) : \
  \   Don't output the first coverage threshold. Use this option whent the refernece\
  \ file is used for finding the variants\n       -in                            \
  \       (1 arg) :    input file (likely a hdf5 file)\n       -coverage_file    \
  \                    (1 arg) :    File (.h5) generated by kissnp2, containing the\
  \ coverage threshold per read set  [default '_removemeplease']\n       -out    \
  \                              (1 arg) :    output name\n       -T             \
  \                       (0 arg) :    extend found and stop at large polymorphism\
  \ (extension=contigs) SNPs. Uncompatible with -t\n       -t                    \
  \                (0 arg) :    extend found and stop at first polymorphism (strict\
  \ extension=unitigs) SNPs. Uncompatible with -T\n       -max_symmetrical_crossroads\
  \           (1 arg) :    In b2 mode only: maximal number of symmetrical croasroads\
  \ traversed while trying to close a bubble. Default: no limit  [default '-1']\n\
  \       -b                                    (1 arg) :    branching mode\n\t\t\
  0: forbid SNPs for wich any of the two paths is branching (high precision, low recall)\n\
  \t\t1: forbid SNPs for wich the two paths are branching (e.g. the two paths can\
  \ be created either with a 'A' or a 'C' at the same position (default value)\n\t\
  \t2: No limitation on branching (low precision, high recall)  [default '1']\n  \
  \     -max_ambigous_indel                   (1 arg) :    Maximal size of ambiguity\
  \ of INDELs. INDELS whose ambiguity is higher than this value are not output  [default\
  \ '20']\n       -l                                    (0 arg) :    conserve low\
  \ complexity SNPs\n       -nb-cores                             (1 arg) :    number\
  \ of cores  [default '0']\n       -verbose                              (1 arg)\
  \ :    verbosity level  [default '1']\n       -version                         \
  \     (0 arg) :    version\n       -help                                 (0 arg)\
  \ :    help\n       -bfs-max-depth                        (1 arg) :    maximum depth\
  \ for BFS  [default '200']\n       -bfs-max-breadth                      (1 arg)\
  \ :    maximum breadth for BFS  [default '20']\n       -max_truncated_path_length_difference\
  \ (1 arg) :    Longest accepted difference length between two paths of a truncated\
  \ bubbleS  [default '0']\n"
generated_using:
- --help
