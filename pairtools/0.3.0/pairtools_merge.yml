&id003 !Command
positional:
- !Positional
  description: ''
  position: 0
  name: PAIRS_PATH
  optional: true
named:
- !Flag
  description: output file. If the path ends with .gz/.lz4, the output is compressed
    by pbgzip/lz4c. By default, the output is printed into stdout.
  synonyms:
  - -o
  - --output
  args: !SimpleFlagArg
    name: TEXT
  optional: true
- !Flag
  description: 'The maximal number of inputs merged at once. For more, store merged
    intermediates in temporary files.  [default: 8]'
  synonyms:
  - --max-nmerge
  args: !SimpleFlagArg
    name: INTEGER
  optional: true
- !Flag
  description: Custom temporary folder for merged intermediates.
  synonyms:
  - --tmpdir
  args: !SimpleFlagArg
    name: TEXT
  optional: true
- !Flag
  description: 'The amount of memory used by default.  [default: 2G]'
  synonyms:
  - --memory
  args: !SimpleFlagArg
    name: TEXT
  optional: true
- !Flag
  description: 'A binary to compress temporary merged chunks. Must decompress input
    when the flag -d is provided. Suggested alternatives: lz4c, gzip, lzop, snzip.
    NOTE: fails silently if the command syntax is wrong.   [default: ]'
  synonyms:
  - --compress-program
  args: !SimpleFlagArg
    name: TEXT
  optional: true
- !Flag
  description: 'Number of threads for merging.  [default: 8]'
  synonyms:
  - --nproc
  args: !SimpleFlagArg
    name: INTEGER
  optional: true
- !Flag
  description: 'Number of processes used by the auto-guessed input decompressing command.  [default:
    1]'
  synonyms:
  - --nproc-in
  args: !SimpleFlagArg
    name: INTEGER
  optional: true
- !Flag
  description: 'Number of processes used by the auto-guessed output compressing command.  [default:
    8]'
  synonyms:
  - --nproc-out
  args: !SimpleFlagArg
    name: INTEGER
  optional: true
- !Flag
  description: 'A command to decompress the input. If provided, fully overrides the
    auto-guessed command. Does not work with stdin. Must read input from stdin and
    print output into stdout. EXAMPLE: pbgzip -dc -n 3'
  synonyms:
  - --cmd-in
  args: !SimpleFlagArg
    name: TEXT
  optional: true
- !Flag
  description: 'A command to compress the output. If provided, fully overrides the
    auto-guessed command. Does not work with stdout. Must read input from stdin and
    print output into stdout. EXAMPLE: pbgzip -c -n 8'
  synonyms:
  - --cmd-out
  args: !SimpleFlagArg
    name: TEXT
  optional: true
command:
- pairtools
- merge
parent: &id001 !Command
  positional: []
  named:
  - !Flag
    description: Post mortem debugging
    synonyms:
    - --post-mortem
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: Profile performance with Python cProfile and dump the statistics
      into a binary file
    synonyms:
    - --output-profile
    args: !SimpleFlagArg
      name: TEXT
    optional: true
  command:
  - pairtools
  parent:
  subcommands:
  - !Command
    positional:
    - !Positional
      description: ''
      position: 0
      name: PAIRS_PATH
      optional: true
    named:
    - !Flag
      description: output file for pairs after duplicate removal. If the path ends
        with .gz or .lz4, the output is pbgzip-/lz4c-compressed. By default, the output
        is printed into stdout.
      synonyms:
      - -o
      - --output
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: output file for duplicated pairs.  If the path ends with .gz or
        .lz4, the output is pbgzip-/lz4c-compressed. If the path is the same as in
        --output or -, output duplicates together  with deduped pairs. By default,
        duplicates are dropped.
      synonyms:
      - --output-dups
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: output file for unmapped pairs. If the path ends with .gz or .lz4,
        the output is pbgzip-/lz4c-compressed. If the path is the same as in --output
        or -, output unmapped pairs together with deduped pairs. If the path is the
        same as --output-dups, output unmapped reads together with dups. By default,
        unmapped pairs are dropped.
      synonyms:
      - --output-unmapped
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: output file for duplicate statistics.  If file exists, it will
        be open in the append mode. If the path ends with .gz or .lz4, the output
        is pbgzip-/lz4c-compressed. By default, statistics are not printed.
      synonyms:
      - --output-stats
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'Pairs with both sides mapped within this distance (bp) from each
        other are considered duplicates.  [default: 3]'
      synonyms:
      - --max-mismatch
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: '[max|sum]              define the mismatch as either the max or
        the sum of the mismatches ofthe genomic locations of the both sides of the
        two compared molecules  [default: max]'
      synonyms:
      - --method
      args: !EmptyFlagArg {}
      optional: true
    - !Flag
      description: Separator (\t, \v, etc. characters are supported, pass them in
        quotes)
      synonyms:
      - --sep
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: The first character of comment lines
      synonyms:
      - --comment-char
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: '[dups|dedup|both|none] Which of the outputs should receive header
        and comment lines'
      synonyms:
      - --send-header-to
      args: !EmptyFlagArg {}
      optional: true
    - !Flag
      description: Chrom 1 column; default 1
      synonyms:
      - --c1
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: Chrom 2 column; default 3
      synonyms:
      - --c2
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: Position 1 column; default 2
      synonyms:
      - --p1
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: Position 2 column; default 4
      synonyms:
      - --p2
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: Strand 1 column; default 5
      synonyms:
      - --s1
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: Strand 2 column; default 6
      synonyms:
      - --s2
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: Placeholder for a chromosome on an unmapped side; default !
      synonyms:
      - --unmapped-chrom
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: If specified, duplicate pairs are marked as DD in "pair_type" and
        as a duplicate in the sam entries.
      synonyms:
      - --mark-dups
      args: !EmptyFlagArg {}
      optional: true
    - !Flag
      description: 'Extra columns that also must match for two pairs to be marked
        as duplicates. Can be either provided as 0-based column indices or as column
        names (requires the "#columns" header field). The option can be provided multiple
        times if multiple column pairs must match. Example: --extra-col-pair "phase1"
        "phase2"'
      synonyms:
      - --extra-col-pair
      args: !SimpleFlagArg
        name: TEXT...
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed input decompressing
        command.  [default: 3]'
      synonyms:
      - --nproc-in
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed output compressing
        command.  [default: 8]'
      synonyms:
      - --nproc-out
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'A command to decompress the input file. If provided, fully overrides
        the auto-guessed command. Does not work with stdin. Must read input from stdin
        and print output into stdout. EXAMPLE: pbgzip -dc -n 3'
      synonyms:
      - --cmd-in
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'A command to compress the output file. If provided, fully overrides
        the auto-guessed command. Does not work with stdout. Must read input from
        stdin and print output into stdout. EXAMPLE: pbgzip -c -n 8'
      synonyms:
      - --cmd-out
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    command:
    - pairtools
    - dedup
    parent: *id001
    subcommands: []
    help_flag: !Flag
      description: Show this message and exit.
      synonyms:
      - -h
      - --help
      args: !EmptyFlagArg {}
      optional: true
    usage_flag:
    version_flag:
    help_text: "Usage: pairtools dedup [OPTIONS] [PAIRS_PATH]\n\n  Find and remove\
      \ PCR/optical duplicates.\n\n  Find PCR duplicates in an upper-triangular flipped\
      \ sorted pairs/pairsam\n  file. Allow for a +/-N bp mismatch at each side of\
      \ duplicated molecules.\n\n  PAIRS_PATH : input triu-flipped sorted .pairs or\
      \ .pairsam file.  If the\n  path ends with .gz/.lz4, the input is decompressed\
      \ by pbgzip/lz4c.  By\n  default, the input is read from stdin.\n\nOptions:\n\
      \  -o, --output TEXT               output file for pairs after duplicate\n \
      \                                 removal. If the path ends with .gz or .lz4,\n\
      \                                  the output is pbgzip-/lz4c-compressed. By\n\
      \                                  default, the output is printed into stdout.\n\
      \n  --output-dups TEXT              output file for duplicated pairs.  If the\n\
      \                                  path ends with .gz or .lz4, the output is\n\
      \                                  pbgzip-/lz4c-compressed. If the path is the\n\
      \                                  same as in --output or -, output duplicates\n\
      \                                  together  with deduped pairs. By default,\n\
      \                                  duplicates are dropped.\n\n  --output-unmapped\
      \ TEXT          output file for unmapped pairs. If the path\n              \
      \                    ends with .gz or .lz4, the output is\n                \
      \                  pbgzip-/lz4c-compressed. If the path is the\n           \
      \                       same as in --output or -, output unmapped\n        \
      \                          pairs together with deduped pairs. If the\n     \
      \                             path is the same as --output-dups, output\n  \
      \                                unmapped reads together with dups. By\n   \
      \                               default, unmapped pairs are dropped.\n\n  --output-stats\
      \ TEXT             output file for duplicate statistics.  If\n             \
      \                     file exists, it will be open in the append\n         \
      \                         mode. If the path ends with .gz or .lz4, the\n   \
      \                               output is pbgzip-/lz4c-compressed. By\n    \
      \                              default, statistics are not printed.\n\n  --max-mismatch\
      \ INTEGER          Pairs with both sides mapped within this\n              \
      \                    distance (bp) from each other are considered\n        \
      \                          duplicates.  [default: 3]\n\n  --method [max|sum]\
      \              define the mismatch as either the max or the\n              \
      \                    sum of the mismatches ofthe genomic\n                 \
      \                 locations of the both sides of the two\n                 \
      \                 compared molecules  [default: max]\n\n  --sep TEXT       \
      \               Separator (\\t, \\v, etc. characters are\n                 \
      \                 supported, pass them in quotes)\n\n  --comment-char TEXT \
      \            The first character of comment lines\n  --send-header-to [dups|dedup|both|none]\n\
      \                                  Which of the outputs should receive header\n\
      \                                  and comment lines\n\n  --c1 INTEGER     \
      \               Chrom 1 column; default 1\n  --c2 INTEGER                  \
      \  Chrom 2 column; default 3\n  --p1 INTEGER                    Position 1 column;\
      \ default 2\n  --p2 INTEGER                    Position 2 column; default 4\n\
      \  --s1 INTEGER                    Strand 1 column; default 5\n  --s2 INTEGER\
      \                    Strand 2 column; default 6\n  --unmapped-chrom TEXT   \
      \        Placeholder for a chromosome on an unmapped\n                     \
      \             side; default !\n\n  --mark-dups                     If specified,\
      \ duplicate pairs are marked as\n                                  DD in \"\
      pair_type\" and as a duplicate in the\n                                  sam\
      \ entries.\n\n  --extra-col-pair TEXT...        Extra columns that also must\
      \ match for two\n                                  pairs to be marked as duplicates.\
      \ Can be\n                                  either provided as 0-based column\
      \ indices or\n                                  as column names (requires the\
      \ \"#columns\"\n                                  header field). The option\
      \ can be provided\n                                  multiple times if multiple\
      \ column pairs must\n                                  match. Example: --extra-col-pair\
      \ \"phase1\"\n                                  \"phase2\"\n\n  --nproc-in INTEGER\
      \              Number of processes used by the auto-guessed\n              \
      \                    input decompressing command.  [default: 3]\n\n  --nproc-out\
      \ INTEGER             Number of processes used by the auto-guessed\n       \
      \                           output compressing command.  [default: 8]\n\n  --cmd-in\
      \ TEXT                   A command to decompress the input file. If\n      \
      \                            provided, fully overrides the auto-guessed\n  \
      \                                command. Does not work with stdin. Must read\n\
      \                                  input from stdin and print output into\n\
      \                                  stdout. EXAMPLE: pbgzip -dc -n 3\n\n  --cmd-out\
      \ TEXT                  A command to compress the output file. If\n        \
      \                          provided, fully overrides the auto-guessed\n    \
      \                              command. Does not work with stdout. Must\n  \
      \                                read input from stdin and print output into\n\
      \                                  stdout. EXAMPLE: pbgzip -c -n 8\n\n  -h,\
      \ --help                      Show this message and exit.\n"
    generated_using: &id002
    - --help
  - !Command
    positional:
    - !Positional
      description: ''
      position: 0
      name: PAIRS_PATH
      optional: true
    named:
    - !Flag
      description: output file for pairs from low coverage regions. If the path ends
        with .gz or .lz4, the output is pbgzip-/lz4c-compressed. By default, the output
        is printed into stdout.
      synonyms:
      - -o
      - --output
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: output file for pairs from high coverage regions. If the path ends
        with .gz or .lz4, the output is pbgzip-/lz4c-compressed. If the path is the
        same as in --output or -, output duplicates together  with deduped pairs.
        By default, duplicates are dropped.
      synonyms:
      - --output-highcov
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: output file for unmapped pairs. If the path ends with .gz or .lz4,
        the output is pbgzip-/lz4c-compressed. If the path is the same as in --output
        or -, output unmapped pairs together with deduped pairs. If the path is the
        same as --output-highcov, output unmapped reads together. By default, unmapped
        pairs are dropped.
      synonyms:
      - --output-unmapped
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: output file for statistics of multiple interactors.  If file exists,
        it will be open in the append mode. If the path ends with .gz or .lz4, the
        output is pbgzip-/lz4c-compressed. By default, statistics are not printed.
      synonyms:
      - --output-stats
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: The maximum allowed coverage per region.
      synonyms:
      - --max-cov
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: The resolution for calculating coverage. For each pair, the local
        coverage around each end is calculated as (1 + the number of neighbouring
        pairs within +/- max_dist bp)
      synonyms:
      - --max-dist
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: '[max|sum]              calculate the number of neighbouring pairs
        as either the sum or the max of the number of neighbours on the two sides  [default:
        max]'
      synonyms:
      - --method
      args: !EmptyFlagArg {}
      optional: true
    - !Flag
      description: Separator (\t, \v, etc. characters are supported, pass them in
        quotes)
      synonyms:
      - --sep
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: The first character of comment lines
      synonyms:
      - --comment-char
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: '[lowcov|highcov|both|none] Which of the outputs should receive
        header and comment lines'
      synonyms:
      - --send-header-to
      args: !EmptyFlagArg {}
      optional: true
    - !Flag
      description: Chrom 1 column; default 1
      synonyms:
      - --c1
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: Chrom 2 column; default 3
      synonyms:
      - --c2
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: Position 1 column; default 2
      synonyms:
      - --p1
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: Position 2 column; default 4
      synonyms:
      - --p2
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: Strand 1 column; default 5
      synonyms:
      - --s1
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: Strand 2 column; default 6
      synonyms:
      - --s2
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: Placeholder for a chromosome on an unmapped side; default !
      synonyms:
      - --unmapped-chrom
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: If specified, duplicate pairs are marked as FF in "pair_type" and
        as a duplicate in the sam entries.
      synonyms:
      - --mark-multi
      args: !EmptyFlagArg {}
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed input decompressing
        command.  [default: 3]'
      synonyms:
      - --nproc-in
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed output compressing
        command.  [default: 8]'
      synonyms:
      - --nproc-out
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'A command to decompress the input file. If provided, fully overrides
        the auto-guessed command. Does not work with stdin. Must read input from stdin
        and print output into stdout. EXAMPLE: pbgzip -dc -n 3'
      synonyms:
      - --cmd-in
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'A command to compress the output file. If provided, fully overrides
        the auto-guessed command. Does not work with stdout. Must read input from
        stdin and print output into stdout. EXAMPLE: pbgzip -c -n 8'
      synonyms:
      - --cmd-out
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    command:
    - pairtools
    - filterbycov
    parent: *id001
    subcommands: []
    help_flag: !Flag
      description: Show this message and exit.
      synonyms:
      - -h
      - --help
      args: !EmptyFlagArg {}
      optional: true
    usage_flag:
    version_flag:
    help_text: "Usage: pairtools filterbycov [OPTIONS] [PAIRS_PATH]\n\n  Remove pairs\
      \ from regions of high coverage.\n\n  Find and remove pairs with >(MAX_COV-1)\
      \ neighbouring pairs within a +/-\n  MAX_DIST bp window around either side.\
      \ Useful for single-cell  Hi-C\n  experiments, where coverage is naturally limited\
      \ by the chromosome  copy\n  number.\n\n  PAIRS_PATH : input triu-flipped sorted\
      \ .pairs or .pairsam file.  If the\n  path ends with .gz/.lz4, the input is\
      \ decompressed by pbgzip/lz4c.  By\n  default, the input is read from stdin.\n\
      \nOptions:\n  -o, --output TEXT               output file for pairs from low\
      \ coverage\n                                  regions. If the path ends with\
      \ .gz or .lz4,\n                                  the output is pbgzip-/lz4c-compressed.\
      \ By\n                                  default, the output is printed into\
      \ stdout.\n\n  --output-highcov TEXT           output file for pairs from high\
      \ coverage\n                                  regions. If the path ends with\
      \ .gz or .lz4,\n                                  the output is pbgzip-/lz4c-compressed.\
      \ If\n                                  the path is the same as in --output\
      \ or -,\n                                  output duplicates together  with\
      \ deduped\n                                  pairs. By default, duplicates are\
      \ dropped.\n\n  --output-unmapped TEXT          output file for unmapped pairs.\
      \ If the path\n                                  ends with .gz or .lz4, the\
      \ output is\n                                  pbgzip-/lz4c-compressed. If the\
      \ path is the\n                                  same as in --output or -, output\
      \ unmapped\n                                  pairs together with deduped pairs.\
      \ If the\n                                  path is the same as --output-highcov,\
      \ output\n                                  unmapped reads together. By default,\n\
      \                                  unmapped pairs are dropped.\n\n  --output-stats\
      \ TEXT             output file for statistics of multiple\n                \
      \                  interactors.  If file exists, it will be\n              \
      \                    open in the append mode. If the path ends\n           \
      \                       with .gz or .lz4, the output is\n                  \
      \                pbgzip-/lz4c-compressed. By default,\n                    \
      \              statistics are not printed.\n\n  --max-cov INTEGER          \
      \     The maximum allowed coverage per region.\n  --max-dist INTEGER       \
      \       The resolution for calculating coverage. For\n                     \
      \             each pair, the local coverage around each\n                  \
      \                end is calculated as (1 + the number of\n                 \
      \                 neighbouring pairs within +/- max_dist bp)\n\n  --method [max|sum]\
      \              calculate the number of neighbouring pairs\n                \
      \                  as either the sum or the max of the number\n            \
      \                      of neighbours on the two sides  [default:\n         \
      \                         max]\n\n  --sep TEXT                      Separator\
      \ (\\t, \\v, etc. characters are\n                                  supported,\
      \ pass them in quotes)\n\n  --comment-char TEXT             The first character\
      \ of comment lines\n  --send-header-to [lowcov|highcov|both|none]\n        \
      \                          Which of the outputs should receive header\n    \
      \                              and comment lines\n\n  --c1 INTEGER         \
      \           Chrom 1 column; default 1\n  --c2 INTEGER                    Chrom\
      \ 2 column; default 3\n  --p1 INTEGER                    Position 1 column;\
      \ default 2\n  --p2 INTEGER                    Position 2 column; default 4\n\
      \  --s1 INTEGER                    Strand 1 column; default 5\n  --s2 INTEGER\
      \                    Strand 2 column; default 6\n  --unmapped-chrom TEXT   \
      \        Placeholder for a chromosome on an unmapped\n                     \
      \             side; default !\n\n  --mark-multi                    If specified,\
      \ duplicate pairs are marked as\n                                  FF in \"\
      pair_type\" and as a duplicate in the\n                                  sam\
      \ entries.\n\n  --nproc-in INTEGER              Number of processes used by\
      \ the auto-guessed\n                                  input decompressing command.\
      \  [default: 3]\n\n  --nproc-out INTEGER             Number of processes used\
      \ by the auto-guessed\n                                  output compressing\
      \ command.  [default: 8]\n\n  --cmd-in TEXT                   A command to decompress\
      \ the input file. If\n                                  provided, fully overrides\
      \ the auto-guessed\n                                  command. Does not work\
      \ with stdin. Must read\n                                  input from stdin\
      \ and print output into\n                                  stdout. EXAMPLE:\
      \ pbgzip -dc -n 3\n\n  --cmd-out TEXT                  A command to compress\
      \ the output file. If\n                                  provided, fully overrides\
      \ the auto-guessed\n                                  command. Does not work\
      \ with stdout. Must\n                                  read input from stdin\
      \ and print output into\n                                  stdout. EXAMPLE:\
      \ pbgzip -c -n 8\n\n  -h, --help                      Show this message and\
      \ exit.\n"
    generated_using: *id002
  - !Command
    positional:
    - !Positional
      description: ''
      position: 0
      name: PAIRS_PATH
      optional: true
    named:
    - !Flag
      description: 'Chromosome order used to flip interchromosomal mates: path to
        a chromosomes file (e.g. UCSC chrom.sizes or similar) whose first column lists
        scaffold names. Any scaffolds not listed will be ordered lexicographically
        following the names provided.  [required]'
      synonyms:
      - -c
      - --chroms-path
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: output file. If the path ends with .gz or .lz4, the output is pbgzip-/lz4c-compressed.
        By default, the output is printed into stdout.
      synonyms:
      - -o
      - --output
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed input decompressing
        command.  [default: 3]'
      synonyms:
      - --nproc-in
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed output compressing
        command.  [default: 8]'
      synonyms:
      - --nproc-out
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'A command to decompress the input file. If provided, fully overrides
        the auto-guessed command. Does not work with stdin. Must read input from stdin
        and print output into stdout. EXAMPLE: pbgzip -dc -n 3'
      synonyms:
      - --cmd-in
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'A command to compress the output file. If provided, fully overrides
        the auto-guessed command. Does not work with stdout. Must read input from
        stdin and print output into stdout. EXAMPLE: pbgzip -c -n 8'
      synonyms:
      - --cmd-out
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    command:
    - pairtools
    - flip
    parent: *id001
    subcommands: []
    help_flag: !Flag
      description: Show this message and exit.
      synonyms:
      - -h
      - --help
      args: !EmptyFlagArg {}
      optional: true
    usage_flag:
    version_flag:
    help_text: "Usage: pairtools flip [OPTIONS] [PAIRS_PATH]\n\n  Flip pairs to get\
      \ an upper-triangular matrix.\n\n  Change the order of side1 and side2 in pairs,\
      \ such that  (order(chrom1) <\n  order(chrom2)  or (order(chrom1) == order(chrom2))\
      \ and (pos1 <=pos2))\n  Equivalent to reflecting the lower triangle of a Hi-C\
      \ matrix onto its\n  upper triangle, resulting in an upper triangular matrix.\
      \ The order of\n  chromosomes must be provided via a .chromsizes file.\n\n \
      \ PAIRS_PATH : input .pairs/.pairsam file. If the path ends with .gz or\n  .lz4,\
      \ the input is decompressed by pbgzip/lz4c. By default, the input is\n  read\
      \ from stdin.\n\nOptions:\n  -c, --chroms-path TEXT  Chromosome order used to\
      \ flip interchromosomal\n                          mates: path to a chromosomes\
      \ file (e.g. UCSC\n                          chrom.sizes or similar) whose first\
      \ column lists\n                          scaffold names. Any scaffolds not\
      \ listed will be\n                          ordered lexicographically following\
      \ the names\n                          provided.  [required]\n\n  -o, --output\
      \ TEXT       output file. If the path ends with .gz or .lz4, the\n         \
      \                 output is pbgzip-/lz4c-compressed. By default, the\n     \
      \                     output is printed into stdout.\n\n  --nproc-in INTEGER\
      \      Number of processes used by the auto-guessed input\n                \
      \          decompressing command.  [default: 3]\n\n  --nproc-out INTEGER   \
      \  Number of processes used by the auto-guessed output\n                   \
      \       compressing command.  [default: 8]\n\n  --cmd-in TEXT           A command\
      \ to decompress the input file. If provided,\n                          fully\
      \ overrides the auto-guessed command. Does not\n                          work\
      \ with stdin. Must read input from stdin and\n                          print\
      \ output into stdout. EXAMPLE: pbgzip -dc -n 3\n\n  --cmd-out TEXT         \
      \ A command to compress the output file. If provided,\n                    \
      \      fully overrides the auto-guessed command. Does not\n                \
      \          work with stdout. Must read input from stdin and\n              \
      \            print output into stdout. EXAMPLE: pbgzip -c -n 8\n\n  -h, --help\
      \              Show this message and exit.\n"
    generated_using: *id002
  - !Command
    positional:
    - !Positional
      description: ''
      position: 0
      name: PAIRSAM_PATH
      optional: true
    named:
    - !Flag
      description: output .pairsam file. If the path ends with .gz or .lz4, the output
        is pbgzip-/lz4c-compressed. By default, the output is printed into stdout.
      synonyms:
      - -o
      - --output
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed input decompressing
        command.  [default: 3]'
      synonyms:
      - --nproc-in
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed output compressing
        command.  [default: 8]'
      synonyms:
      - --nproc-out
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'A command to decompress the input file. If provided, fully overrides
        the auto-guessed command. Does not work with stdin. Must read input from stdin
        and print output into stdout. EXAMPLE: pbgzip -dc -n 3'
      synonyms:
      - --cmd-in
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'A command to compress the output file. If provided, fully overrides
        the auto-guessed command. Does not work with stdout. Must read input from
        stdin and print output into stdout. EXAMPLE: pbgzip -c -n 8'
      synonyms:
      - --cmd-out
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    command:
    - pairtools
    - markasdup
    parent: *id001
    subcommands: []
    help_flag: !Flag
      description: Show this message and exit.
      synonyms:
      - -h
      - --help
      args: !EmptyFlagArg {}
      optional: true
    usage_flag:
    version_flag:
    help_text: "Usage: pairtools markasdup [OPTIONS] [PAIRSAM_PATH]\n\n  Tag pairs\
      \ as duplicates.\n\n  Change the type of all pairs inside a .pairs/.pairsam\
      \ file to DD. If sam\n  entries are present, change the pair type in the Yt\
      \ SAM tag to 'Yt:Z:DD'.\n\n  PAIRSAM_PATH : input .pairs/.pairsam file. If the\
      \ path ends with .gz, the\n  input is gzip-decompressed. By default, the input\
      \ is read from stdin.\n\nOptions:\n  -o, --output TEXT    output .pairsam file.\
      \ If the path ends with .gz or\n                       .lz4, the output is pbgzip-/lz4c-compressed.\
      \ By\n                       default, the output is printed into stdout.\n\n\
      \  --nproc-in INTEGER   Number of processes used by the auto-guessed input\n\
      \                       decompressing command.  [default: 3]\n\n  --nproc-out\
      \ INTEGER  Number of processes used by the auto-guessed output\n           \
      \            compressing command.  [default: 8]\n\n  --cmd-in TEXT        A\
      \ command to decompress the input file. If provided,\n                     \
      \  fully overrides the auto-guessed command. Does not work\n               \
      \        with stdin. Must read input from stdin and print output\n         \
      \              into stdout. EXAMPLE: pbgzip -dc -n 3\n\n  --cmd-out TEXT   \
      \    A command to compress the output file. If provided,\n                 \
      \      fully overrides the auto-guessed command. Does not work\n           \
      \            with stdout. Must read input from stdin and print\n           \
      \            output into stdout. EXAMPLE: pbgzip -c -n 8\n\n  -h, --help   \
      \        Show this message and exit.\n"
    generated_using: *id002
  - *id003
  - !Command
    positional:
    - !Positional
      description: ''
      position: 0
      name: SAM_PATH
      optional: true
    named:
    - !Flag
      description: 'Chromosome order used to flip interchromosomal mates: path to
        a chromosomes file (e.g. UCSC chrom.sizes or similar) whose first column lists
        scaffold names. Any scaffolds not listed will be ordered lexicographically
        following the names provided.  [required]'
      synonyms:
      - -c
      - --chroms-path
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: output file.  If the path ends with .gz or .lz4, the output is
        pbgzip-/lz4-compressed.By default, the output is printed into stdout.
      synonyms:
      - -o
      - --output
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: Name of genome assembly (e.g. hg19, mm10) to store in the pairs
        header.
      synonyms:
      - --assembly
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'The minimal MAPQ score to consider a read as uniquely mapped  [default:
        1]'
      synonyms:
      - --min-mapq
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'The maximal size of a Hi-C molecule; used to rescue single ligationsfrom
        molecules with three alignments.  [default: 2000]'
      synonyms:
      - --max-molecule-size
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: If specified, do not add read ids to the output
      synonyms:
      - --drop-readid
      args: !EmptyFlagArg {}
      optional: true
    - !Flag
      description: If specified, remove sequences and PHREDs from the sam fields
      synonyms:
      - --drop-seq
      args: !EmptyFlagArg {}
      optional: true
    - !Flag
      description: If specified, do not add sams to the output
      synonyms:
      - --drop-sam
      args: !EmptyFlagArg {}
      optional: true
    - !Flag
      description: 'Report extra columns describing alignments Possible values (can
        take multiple values as a comma-separated list): a SAM tag (any pair of uppercase
        letters) or mapq, pos5, pos3, cigar, read_len, matched_bp, algn_ref_span,
        algn_read_span, dist_to_5, dist_to_3, seq.'
      synonyms:
      - --add-columns
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: output file for all parsed alignments, including walks. Useful
        for debugging and rnalysis of walks. If file exists, it will be open in the
        append mode. If the path ends with .gz or .lz4, the output is pbgzip-/lz4-compressed.
        By default, not used.
      synonyms:
      - --output-parsed-alignments
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: output file for various statistics of pairs file.  By default,
        statistics is not generated.
      synonyms:
      - --output-stats
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: "[5|3]    specifies whether the 5' or 3' end of the alignment is\
        \ reported as the position of the Hi-C read."
      synonyms:
      - --report-alignment-end
      args: !EmptyFlagArg {}
      optional: true
    - !Flag
      description: 'read segments that are not covered by any alignment and longer
        than the specified value are treated as "null" alignments. These null alignments
        convert otherwise linear alignments into walks, and affect how they get reported
        as a Hi-C pair (see --walks-policy).  [default: 20]'
      synonyms:
      - --max-inter-align-gap
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: "[mask|all|5any|5unique|3any|3unique] the policy for reporting\
        \ unrescuable walks (reads containing more than one alignment on one or both\
        \ sides, that can not be explained by a single ligation between two mappable\
        \ DNA fragments). \"mask\" - mask walks (chrom=\"!\", pos=0, strand=\"-\"\
        );  \"all\" - report all pairs of consecutive alignments [NOT IMPLEMENTED];\
        \  \"5any\" - report the 5'-most alignment on each side; \"5unique\" - report\
        \ the 5'-most unique alignment on each side, if present; \"3any\" - report\
        \ the 3'-most alignment on each side; \"3unique\" - report the 3'-most unique\
        \ alignment on each side, if present.  [default: mask]"
      synonyms:
      - --walks-policy
      args: !EmptyFlagArg {}
      optional: true
    - !Flag
      description: If specified, do not flip pairs in genomic order and instead preserve
        the order in which they were sequenced.
      synonyms:
      - --no-flip
      args: !EmptyFlagArg {}
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed input decompressing
        command.  [default: 3]'
      synonyms:
      - --nproc-in
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed output compressing
        command.  [default: 8]'
      synonyms:
      - --nproc-out
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'A command to decompress the input file. If provided, fully overrides
        the auto-guessed command. Does not work with stdin. Must read input from stdin
        and print output into stdout. EXAMPLE: pbgzip -dc -n 3'
      synonyms:
      - --cmd-in
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'A command to compress the output file. If provided, fully overrides
        the auto-guessed command. Does not work with stdout. Must read input from
        stdin and print output into stdout. EXAMPLE: pbgzip -c -n 8'
      synonyms:
      - --cmd-out
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    command:
    - pairtools
    - parse
    parent: *id001
    subcommands: []
    help_flag: !Flag
      description: Show this message and exit.
      synonyms:
      - -h
      - --help
      args: !EmptyFlagArg {}
      optional: true
    usage_flag:
    version_flag:
    help_text: "Usage: pairtools parse [OPTIONS] [SAM_PATH]\n\n  Find ligation junctions\
      \ in .sam, make .pairs.\n\n  SAM_PATH : an input .sam/.bam file with paired-end\
      \ sequence alignments of\n  Hi-C molecules. If the path ends with .bam, the\
      \ input is decompressed from\n  bam with samtools. By default, the input is\
      \ read from stdin.\n\nOptions:\n  -c, --chroms-path TEXT          Chromosome\
      \ order used to flip\n                                  interchromosomal mates:\
      \ path to a\n                                  chromosomes file (e.g. UCSC chrom.sizes\
      \ or\n                                  similar) whose first column lists scaffold\n\
      \                                  names. Any scaffolds not listed will be\n\
      \                                  ordered lexicographically following the\n\
      \                                  names provided.  [required]\n\n  -o, --output\
      \ TEXT               output file.  If the path ends with .gz or\n          \
      \                        .lz4, the output is\n                             \
      \     pbgzip-/lz4-compressed.By default, the\n                             \
      \     output is printed into stdout.\n\n  --assembly TEXT                 Name\
      \ of genome assembly (e.g. hg19, mm10) to\n                                \
      \  store in the pairs header.\n\n  --min-mapq INTEGER              The minimal\
      \ MAPQ score to consider a read as\n                                  uniquely\
      \ mapped  [default: 1]\n\n  --max-molecule-size INTEGER     The maximal size\
      \ of a Hi-C molecule; used to\n                                  rescue single\
      \ ligationsfrom molecules with\n                                  three alignments.\
      \  [default: 2000]\n\n  --drop-readid                   If specified, do not\
      \ add read ids to the\n                                  output\n\n  --drop-seq\
      \                      If specified, remove sequences and PHREDs\n         \
      \                         from the sam fields\n\n  --drop-sam              \
      \        If specified, do not add sams to the output\n  --add-columns TEXT \
      \             Report extra columns describing alignments\n                 \
      \                 Possible values (can take multiple values as\n           \
      \                       a comma-separated list): a SAM tag (any pair\n     \
      \                             of uppercase letters) or mapq, pos5, pos3,\n \
      \                                 cigar, read_len, matched_bp, algn_ref_span,\n\
      \                                  algn_read_span, dist_to_5, dist_to_3, seq.\n\
      \n  --output-parsed-alignments TEXT\n                                  output\
      \ file for all parsed alignments,\n                                  including\
      \ walks. Useful for debugging and\n                                  rnalysis\
      \ of walks. If file exists, it will\n                                  be open\
      \ in the append mode. If the path ends\n                                  with\
      \ .gz or .lz4, the output is\n                                  pbgzip-/lz4-compressed.\
      \ By default, not\n                                  used.\n\n  --output-stats\
      \ TEXT             output file for various statistics of pairs\n           \
      \                       file.  By default, statistics is not\n             \
      \                     generated.\n\n  --report-alignment-end [5|3]    specifies\
      \ whether the 5' or 3' end of the\n                                  alignment\
      \ is reported as the position of the\n                                  Hi-C\
      \ read.\n\n  --max-inter-align-gap INTEGER   read segments that are not covered\
      \ by any\n                                  alignment and longer than the specified\n\
      \                                  value are treated as \"null\" alignments.\n\
      \                                  These null alignments convert otherwise\n\
      \                                  linear alignments into walks, and affect\
      \ how\n                                  they get reported as a Hi-C pair (see\n\
      \                                  --walks-policy).  [default: 20]\n\n  --walks-policy\
      \ [mask|all|5any|5unique|3any|3unique]\n                                  the\
      \ policy for reporting unrescuable walks\n                                 \
      \ (reads containing more than one alignment on\n                           \
      \       one or both sides, that can not be explained\n                     \
      \             by a single ligation between two mappable\n                  \
      \                DNA fragments). \"mask\" - mask walks\n                   \
      \               (chrom=\"!\", pos=0, strand=\"-\");  \"all\" -\n           \
      \                       report all pairs of consecutive alignments\n       \
      \                           [NOT IMPLEMENTED];  \"5any\" - report the\n    \
      \                              5'-most alignment on each side; \"5unique\" -\n\
      \                                  report the 5'-most unique alignment on each\n\
      \                                  side, if present; \"3any\" - report the\n\
      \                                  3'-most alignment on each side; \"3unique\"\
      \ -\n                                  report the 3'-most unique alignment on\
      \ each\n                                  side, if present.  [default: mask]\n\
      \n  --no-flip                       If specified, do not flip pairs in genomic\n\
      \                                  order and instead preserve the order in\n\
      \                                  which they were sequenced.\n\n  --nproc-in\
      \ INTEGER              Number of processes used by the auto-guessed\n      \
      \                            input decompressing command.  [default: 3]\n\n\
      \  --nproc-out INTEGER             Number of processes used by the auto-guessed\n\
      \                                  output compressing command.  [default: 8]\n\
      \n  --cmd-in TEXT                   A command to decompress the input file.\
      \ If\n                                  provided, fully overrides the auto-guessed\n\
      \                                  command. Does not work with stdin. Must read\n\
      \                                  input from stdin and print output into\n\
      \                                  stdout. EXAMPLE: pbgzip -dc -n 3\n\n  --cmd-out\
      \ TEXT                  A command to compress the output file. If\n        \
      \                          provided, fully overrides the auto-guessed\n    \
      \                              command. Does not work with stdout. Must\n  \
      \                                read input from stdin and print output into\n\
      \                                  stdout. EXAMPLE: pbgzip -c -n 8\n\n  -h,\
      \ --help                      Show this message and exit.\n"
    generated_using: *id002
  - !Command
    positional:
    - !Positional
      description: ''
      position: 0
      name: PAIRS_PATH
      optional: true
    named:
    - !Flag
      description: output file. If the path ends with .gz or .lz4, the output is pbgzip-/lz4c-compressed.
        By default, the output is printed into stdout.
      synonyms:
      - -o
      - --output
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: phase suffixes.
      synonyms:
      - --phase-suffixes
      args: !SimpleFlagArg
        name: TEXT...
      optional: true
    - !Flag
      description: drop all columns besides the standard ones and phase1/2
      synonyms:
      - --clean-output
      args: !EmptyFlagArg {}
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed input decompressing
        command.  [default: 3]'
      synonyms:
      - --nproc-in
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed output compressing
        command.  [default: 8]'
      synonyms:
      - --nproc-out
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'A command to decompress the input file. If provided, fully overrides
        the auto-guessed command. Does not work with stdin. Must read input from stdin
        and print output into stdout. EXAMPLE: pbgzip -dc -n 3'
      synonyms:
      - --cmd-in
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'A command to compress the output file. If provided, fully overrides
        the auto-guessed command. Does not work with stdout. Must read input from
        stdin and print output into stdout. EXAMPLE: pbgzip -c -n 8'
      synonyms:
      - --cmd-out
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    command:
    - pairtools
    - phase
    parent: *id001
    subcommands: []
    help_flag: !Flag
      description: Show this message and exit.
      synonyms:
      - -h
      - --help
      args: !EmptyFlagArg {}
      optional: true
    usage_flag:
    version_flag:
    help_text: "Usage: pairtools phase [OPTIONS] [PAIRS_PATH]\n\n  Phase pairs mapped\
      \ to a diploid genome.\n\n  PAIRS_PATH : input .pairs/.pairsam file. If the\
      \ path ends with .gz or\n  .lz4, the input is decompressed by pbgzip/lz4c. By\
      \ default, the input is\n  read from stdin.\n\nOptions:\n  -o, --output TEXT\
      \         output file. If the path ends with .gz or .lz4,\n                \
      \            the output is pbgzip-/lz4c-compressed. By default,\n          \
      \                  the output is printed into stdout.\n\n  --phase-suffixes\
      \ TEXT...  phase suffixes.\n  --clean-output            drop all columns besides\
      \ the standard ones and\n                            phase1/2\n\n  --nproc-in\
      \ INTEGER        Number of processes used by the auto-guessed input\n      \
      \                      decompressing command.  [default: 3]\n\n  --nproc-out\
      \ INTEGER       Number of processes used by the auto-guessed\n             \
      \               output compressing command.  [default: 8]\n\n  --cmd-in TEXT\
      \             A command to decompress the input file. If\n                 \
      \           provided, fully overrides the auto-guessed\n                   \
      \         command. Does not work with stdin. Must read input\n             \
      \               from stdin and print output into stdout. EXAMPLE:\n        \
      \                    pbgzip -dc -n 3\n\n  --cmd-out TEXT            A command\
      \ to compress the output file. If\n                            provided, fully\
      \ overrides the auto-guessed\n                            command. Does not\
      \ work with stdout. Must read\n                            input from stdin\
      \ and print output into stdout.\n                            EXAMPLE: pbgzip\
      \ -c -n 8\n\n  -h, --help                Show this message and exit.\n"
    generated_using: *id002
  - !Command
    positional:
    - !Positional
      description: ''
      position: 0
      name: PAIRS_PATH
      optional: true
    named:
    - !Flag
      description: a tab-separated BED file with the positions of restriction fragments
        (chrom, start, end). Can be generated using cooler digest.  [required]
      synonyms:
      - -f
      - --frags
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: output .pairs/.pairsam file. If the path ends with .gz/.lz4, the
        output is compressed by pbgzip/lz4c. By default, the output is printed into
        stdout.
      synonyms:
      - -o
      - --output
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed input decompressing
        command.  [default: 3]'
      synonyms:
      - --nproc-in
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed output compressing
        command.  [default: 8]'
      synonyms:
      - --nproc-out
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'A command to decompress the input file. If provided, fully overrides
        the auto-guessed command. Does not work with stdin. Must read input from stdin
        and print output into stdout. EXAMPLE: pbgzip -dc -n 3'
      synonyms:
      - --cmd-in
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'A command to compress the output file. If provided, fully overrides
        the auto-guessed command. Does not work with stdout. Must read input from
        stdin and print output into stdout. EXAMPLE: pbgzip -c -n 8'
      synonyms:
      - --cmd-out
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    command:
    - pairtools
    - restrict
    parent: *id001
    subcommands: []
    help_flag: !Flag
      description: Show this message and exit.
      synonyms:
      - -h
      - --help
      args: !EmptyFlagArg {}
      optional: true
    usage_flag:
    version_flag:
    help_text: "Usage: pairtools restrict [OPTIONS] [PAIRS_PATH]\n\n  Assign restriction\
      \ fragments to pairs.\n\n  Identify the restriction fragments that got ligated\
      \ into a Hi-C molecule.\n\n  PAIRS_PATH : input .pairs/.pairsam file. If the\
      \ path ends with .gz/.lz4,\n  the  input is decompressed by pbgzip/lz4c. By\
      \ default, the input is read\n  from stdin.\n\nOptions:\n  -f, --frags TEXT\
      \     a tab-separated BED file with the positions of\n                     \
      \  restriction fragments (chrom, start, end). Can be\n                     \
      \  generated using cooler digest.  [required]\n\n  -o, --output TEXT    output\
      \ .pairs/.pairsam file. If the path ends with\n                       .gz/.lz4,\
      \ the output is compressed by pbgzip/lz4c. By\n                       default,\
      \ the output is printed into stdout.\n\n  --nproc-in INTEGER   Number of processes\
      \ used by the auto-guessed input\n                       decompressing command.\
      \  [default: 3]\n\n  --nproc-out INTEGER  Number of processes used by the auto-guessed\
      \ output\n                       compressing command.  [default: 8]\n\n  --cmd-in\
      \ TEXT        A command to decompress the input file. If provided,\n       \
      \                fully overrides the auto-guessed command. Does not work\n \
      \                      with stdin. Must read input from stdin and print output\n\
      \                       into stdout. EXAMPLE: pbgzip -dc -n 3\n\n  --cmd-out\
      \ TEXT       A command to compress the output file. If provided,\n         \
      \              fully overrides the auto-guessed command. Does not work\n   \
      \                    with stdout. Must read input from stdin and print\n   \
      \                    output into stdout. EXAMPLE: pbgzip -c -n 8\n\n  -h, --help\
      \           Show this message and exit.\n"
    generated_using: *id002
  - !Command
    positional:
    - !Positional
      description: ''
      position: 0
      name: FRACTION
      optional: false
    - !Positional
      description: ''
      position: 1
      name: PAIRS_PATH
      optional: true
    named:
    - !Flag
      description: output file. If the path ends with .gz or .lz4, the output is pbgzip-/lz4c-compressed.
        By default, the output is printed into stdout.
      synonyms:
      - -o
      - --output
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: the seed of the random number generator.
      synonyms:
      - -s
      - --seed
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed input decompressing
        command.  [default: 3]'
      synonyms:
      - --nproc-in
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed output compressing
        command.  [default: 8]'
      synonyms:
      - --nproc-out
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'A command to decompress the input file. If provided, fully overrides
        the auto-guessed command. Does not work with stdin. Must read input from stdin
        and print output into stdout. EXAMPLE: pbgzip -dc -n 3'
      synonyms:
      - --cmd-in
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'A command to compress the output file. If provided, fully overrides
        the auto-guessed command. Does not work with stdout. Must read input from
        stdin and print output into stdout. EXAMPLE: pbgzip -c -n 8'
      synonyms:
      - --cmd-out
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    command:
    - pairtools
    - sample
    parent: *id001
    subcommands: []
    help_flag: !Flag
      description: Show this message and exit.
      synonyms:
      - -h
      - --help
      args: !EmptyFlagArg {}
      optional: true
    usage_flag:
    version_flag:
    help_text: "Usage: pairtools sample [OPTIONS] FRACTION [PAIRS_PATH]\n\n  Select\
      \ a random subset of pairs in a pairs file.\n\n  FRACTION: the fraction of the\
      \ randomly selected pairs subset\n\n  PAIRS_PATH : input .pairs/.pairsam file.\
      \ If the path ends with .gz or\n  .lz4, the input is decompressed by pbgzip/lz4c.\
      \ By default, the input is\n  read from stdin.\n\nOptions:\n  -o, --output TEXT\
      \    output file. If the path ends with .gz or .lz4, the\n                 \
      \      output is pbgzip-/lz4c-compressed. By default, the\n                \
      \       output is printed into stdout.\n\n  -s, --seed INTEGER   the seed of\
      \ the random number generator.\n  --nproc-in INTEGER   Number of processes used\
      \ by the auto-guessed input\n                       decompressing command. \
      \ [default: 3]\n\n  --nproc-out INTEGER  Number of processes used by the auto-guessed\
      \ output\n                       compressing command.  [default: 8]\n\n  --cmd-in\
      \ TEXT        A command to decompress the input file. If provided,\n       \
      \                fully overrides the auto-guessed command. Does not work\n \
      \                      with stdin. Must read input from stdin and print output\n\
      \                       into stdout. EXAMPLE: pbgzip -dc -n 3\n\n  --cmd-out\
      \ TEXT       A command to compress the output file. If provided,\n         \
      \              fully overrides the auto-guessed command. Does not work\n   \
      \                    with stdout. Must read input from stdin and print\n   \
      \                    output into stdout. EXAMPLE: pbgzip -c -n 8\n\n  -h, --help\
      \           Show this message and exit.\n"
    generated_using: *id002
  - !Command
    positional:
    - !Positional
      description: ''
      position: 0
      name: CONDITION
      optional: false
    - !Positional
      description: ''
      position: 1
      name: PAIRS_PATH
      optional: true
    named:
    - !Flag
      description: output file. If the path ends with .gz or .lz4, the output is pbgzip-/lz4c-compressed.
        By default, the output is printed into stdout.
      synonyms:
      - -o
      - --output
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: output file for pairs of other types.  If the path ends with .gz
        or .lz4, the output is pbgzip-/lz4c-compressed. By default, such pairs are
        dropped.
      synonyms:
      - --output-rest
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: '[selected|rest|both|none] Which of the outputs should receive
        header and comment lines  [default: both]'
      synonyms:
      - --send-comments-to
      args: !EmptyFlagArg {}
      optional: true
    - !Flag
      description: 'A path to a chromosomes file (tab-separated, 1st column contains
        chromosome names) containing a chromosome subset of interest. If provided,
        additionally filter pairs with both sides originating from the provided subset
        of chromosomes. This operation modifies the #chromosomes: and #chromsize:
        header fields accordingly.'
      synonyms:
      - --chrom-subset
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: An auxiliary code to execute before filtering. Use to define functions
        that can be evaluated in the CONDITION statement
      synonyms:
      - --startup-code
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: '...  Cast a given column to a given type. By default, only pos
        and mapq are cast to int, other columns are kept as str. Provide as -t <column_name>
        <type>, e.g. -t read_len1 int. Multiple entries are allowed.'
      synonyms:
      - -t
      - --type-cast
      args: !SimpleFlagArg
        name: TEXT TEXT
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed input decompressing
        command.  [default: 3]'
      synonyms:
      - --nproc-in
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed output compressing
        command.  [default: 8]'
      synonyms:
      - --nproc-out
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'A command to decompress the input file. If provided, fully overrides
        the auto-guessed command. Does not work with stdin. Must read input from stdin
        and print output into stdout. EXAMPLE: pbgzip -dc -n 3'
      synonyms:
      - --cmd-in
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'A command to compress the output file. If provided, fully overrides
        the auto-guessed command. Does not work with stdout. Must read input from
        stdin and print output into stdout. EXAMPLE: pbgzip -c -n 8'
      synonyms:
      - --cmd-out
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    command:
    - pairtools
    - select
    parent: *id001
    subcommands: []
    help_flag: !Flag
      description: Show this message and exit.
      synonyms:
      - -h
      - --help
      args: !EmptyFlagArg {}
      optional: true
    usage_flag:
    version_flag:
    help_text: "Usage: pairtools select [OPTIONS] CONDITION [PAIRS_PATH]\n\n  Select\
      \ pairs according to some condition.\n\n  CONDITION : A Python expression; if\
      \ it returns True, select the read pair.\n  Any column declared in the #columns\
      \ line of the pairs header can be\n  accessed by its name. If the header lacks\
      \ the #columns line, the columns\n  are assumed to follow the .pairs/.pairsam\
      \ standard (readID, chrom1,\n  chrom2,  pos1, pos2, strand1, strand2, pair_type).\
      \ Finally, CONDITION has\n  access to  COLS list which contains the string values\
      \ of columns. In Bash,\n  quote  CONDITION with single quotes, and use double\
      \ quotes for string\n  variables inside CONDITION.\n\n  PAIRS_PATH : input .pairs/.pairsam\
      \ file. If the path ends with .gz or\n  .lz4, the input is decompressed by pbgzip/lz4c.\
      \ By default, the input is\n  read from stdin.\n\n  The following functions\
      \ can be used in CONDITION besides the standard\n  Python functions:\n\n  -\
      \ csv_match(x, csv) - True if variable x is contained in a list of comma-\n\
      \  separated values, e.g. csv_match(chrom1, 'chr1,chr2')\n\n  - wildcard_match(x,\
      \ wildcard) - True if variable x matches a wildcard,\n  e.g. wildcard_match(pair_type,\
      \ 'C*')\n\n  - regex_match(x, regex) - True if variable x matches a Python-flavor\n\
      \  regex, e.g. regex_match(chrom1, 'chr\\d')\n\n  Examples:\n  pairtools select\
      \ '(pair_type==\"UU\") or (pair_type==\"UR\") or (pair_type==\"RU\")'\n  pairtools\
      \ select 'chrom1==chrom2'\n  pairtools select 'COLS[1]==COLS[3]'\n  pairtools\
      \ select '(chrom1==chrom2) and (abs(pos1 - pos2) < 1e6)'\n  pairtools select\
      \ '(chrom1==\"!\") and (chrom2!=\"!\")'\n  pairtools select 'regex_match(chrom1,\
      \ \"chr\\d+\") and regex_match(chrom2, \"chr\\d+\")'\n\n  pairtools select 'True'\
      \ --chr-subset mm9.reduced.chromsizes\n\nOptions:\n  -o, --output TEXT     \
      \          output file. If the path ends with .gz or\n                     \
      \             .lz4, the output is pbgzip-/lz4c-compressed.\n               \
      \                   By default, the output is printed into\n               \
      \                   stdout.\n\n  --output-rest TEXT              output file\
      \ for pairs of other types.  If\n                                  the path\
      \ ends with .gz or .lz4, the output\n                                  is pbgzip-/lz4c-compressed.\
      \ By default, such\n                                  pairs are dropped.\n\n\
      \  --send-comments-to [selected|rest|both|none]\n                          \
      \        Which of the outputs should receive header\n                      \
      \            and comment lines  [default: both]\n\n  --chrom-subset TEXT   \
      \          A path to a chromosomes file (tab-separated,\n                  \
      \                1st column contains chromosome names)\n                   \
      \               containing a chromosome subset of interest.\n              \
      \                    If provided, additionally filter pairs with\n         \
      \                         both sides originating from the provided\n       \
      \                           subset of chromosomes. This operation\n        \
      \                          modifies the #chromosomes: and #chromsize:\n    \
      \                              header fields accordingly.\n\n  --startup-code\
      \ TEXT             An auxiliary code to execute before\n                   \
      \               filtering. Use to define functions that can\n              \
      \                    be evaluated in the CONDITION statement\n\n  -t, --type-cast\
      \ <TEXT TEXT>...  Cast a given column to a given type. By\n                \
      \                  default, only pos and mapq are cast to int,\n           \
      \                       other columns are kept as str. Provide as -t\n     \
      \                             <column_name> <type>, e.g. -t read_len1 int.\n\
      \                                  Multiple entries are allowed.\n\n  --nproc-in\
      \ INTEGER              Number of processes used by the auto-guessed\n      \
      \                            input decompressing command.  [default: 3]\n\n\
      \  --nproc-out INTEGER             Number of processes used by the auto-guessed\n\
      \                                  output compressing command.  [default: 8]\n\
      \n  --cmd-in TEXT                   A command to decompress the input file.\
      \ If\n                                  provided, fully overrides the auto-guessed\n\
      \                                  command. Does not work with stdin. Must read\n\
      \                                  input from stdin and print output into\n\
      \                                  stdout. EXAMPLE: pbgzip -dc -n 3\n\n  --cmd-out\
      \ TEXT                  A command to compress the output file. If\n        \
      \                          provided, fully overrides the auto-guessed\n    \
      \                              command. Does not work with stdout. Must\n  \
      \                                read input from stdin and print output into\n\
      \                                  stdout. EXAMPLE: pbgzip -c -n 8\n\n  -h,\
      \ --help                      Show this message and exit.\n"
    generated_using: *id002
  - !Command
    positional:
    - !Positional
      description: ''
      position: 0
      name: PAIRS_PATH
      optional: true
    named:
    - !Flag
      description: output pairs file. If the path ends with .gz or .lz4, the output
        is compressed by pbgzip or lz4, correspondingly. By default, the output is
        printed into stdout.
      synonyms:
      - -o
      - --output
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'Number of processes to split the sorting work between.  [default:
        8]'
      synonyms:
      - --nproc
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: Custom temporary folder for sorting intermediates.
      synonyms:
      - --tmpdir
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'The amount of memory used by default.  [default: 2G]'
      synonyms:
      - --memory
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'A binary to compress temporary sorted chunks. Must decompress
        input when the flag -d is provided. Suggested alternatives: gzip, lzop, lz4c,
        snzip. If "auto", then use lz4c if available, and gzip otherwise.  [default:
        auto]'
      synonyms:
      - --compress-program
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed input decompressing
        command.  [default: 3]'
      synonyms:
      - --nproc-in
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed output compressing
        command.  [default: 8]'
      synonyms:
      - --nproc-out
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'A command to decompress the input file. If provided, fully overrides
        the auto-guessed command. Does not work with stdin. Must read input from stdin
        and print output into stdout. EXAMPLE: pbgzip -dc -n 3'
      synonyms:
      - --cmd-in
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'A command to compress the output file. If provided, fully overrides
        the auto-guessed command. Does not work with stdout. Must read input from
        stdin and print output into stdout. EXAMPLE: pbgzip -c -n 8'
      synonyms:
      - --cmd-out
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    command:
    - pairtools
    - sort
    parent: *id001
    subcommands: []
    help_flag: !Flag
      description: Show this message and exit.
      synonyms:
      - -h
      - --help
      args: !EmptyFlagArg {}
      optional: true
    usage_flag:
    version_flag:
    help_text: "Usage: pairtools sort [OPTIONS] [PAIRS_PATH]\n\n  Sort a .pairs/.pairsam\
      \ file.\n\n  Sort pairs in the lexicographic order along chrom1 and chrom2,\
      \ in the\n  numeric order along pos1 and pos2 and in the lexicographic order\
      \ along\n  pair_type.\n\n  PAIRS_PATH : input .pairs/.pairsam file. If the path\
      \ ends with .gz or\n  .lz4, the  input is decompressed by pbgzip or lz4c, correspondingly.\
      \ By\n  default, the  input is read as text from stdin.\n\nOptions:\n  -o, --output\
      \ TEXT        output pairs file. If the path ends with .gz or\n            \
      \               .lz4, the output is compressed by pbgzip or lz4,\n         \
      \                  correspondingly. By default, the output is printed\n    \
      \                       into stdout.\n\n  --nproc INTEGER          Number of\
      \ processes to split the sorting work\n                           between. \
      \ [default: 8]\n\n  --tmpdir TEXT            Custom temporary folder for sorting\
      \ intermediates.\n  --memory TEXT            The amount of memory used by default.\
      \  [default:\n                           2G]\n\n  --compress-program TEXT  A\
      \ binary to compress temporary sorted chunks. Must\n                       \
      \    decompress input when the flag -d is provided.\n                      \
      \     Suggested alternatives: gzip, lzop, lz4c, snzip. If\n                \
      \           \"auto\", then use lz4c if available, and gzip\n               \
      \            otherwise.  [default: auto]\n\n  --nproc-in INTEGER       Number\
      \ of processes used by the auto-guessed input\n                           decompressing\
      \ command.  [default: 3]\n\n  --nproc-out INTEGER      Number of processes used\
      \ by the auto-guessed output\n                           compressing command.\
      \  [default: 8]\n\n  --cmd-in TEXT            A command to decompress the input\
      \ file. If\n                           provided, fully overrides the auto-guessed\
      \ command.\n                           Does not work with stdin. Must read input\
      \ from\n                           stdin and print output into stdout. EXAMPLE:\
      \ pbgzip\n                           -dc -n 3\n\n  --cmd-out TEXT          \
      \ A command to compress the output file. If provided,\n                    \
      \       fully overrides the auto-guessed command. Does not\n               \
      \            work with stdout. Must read input from stdin and\n            \
      \               print output into stdout. EXAMPLE: pbgzip -c -n 8\n\n  -h, --help\
      \               Show this message and exit.\n"
    generated_using: *id002
  - !Command
    positional:
    - !Positional
      description: stdin.
      position: 0
      name: from
      optional: false
    named:
    - !Flag
      description: output pairs file. If the path ends with .gz or .lz4, the output
        is pbgzip-/lz4c-compressed. If -, pairs are printed to stdout. If not specified,
        pairs are dropped.
      synonyms:
      - --output-pairs
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: output sam file. If the path ends with .bam, the output is compressed
        into a bam file. If -, sam entries are printed to stdout. If not specified,
        sam entries are dropped.
      synonyms:
      - --output-sam
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed input decompressing
        command.  [default: 3]'
      synonyms:
      - --nproc-in
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed output compressing
        command.  [default: 8]'
      synonyms:
      - --nproc-out
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'A command to decompress the input file. If provided, fully overrides
        the auto-guessed command. Does not work with stdin. Must read input from stdin
        and print output into stdout. EXAMPLE: pbgzip -dc -n 3'
      synonyms:
      - --cmd-in
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'A command to compress the output file. If provided, fully overrides
        the auto-guessed command. Does not work with stdout. Must read input from
        stdin and print output into stdout. EXAMPLE: pbgzip -c -n 8'
      synonyms:
      - --cmd-out
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    command:
    - pairtools
    - split
    parent: *id001
    subcommands: []
    help_flag: !Flag
      description: Show this message and exit.
      synonyms:
      - -h
      - --help
      args: !EmptyFlagArg {}
      optional: true
    usage_flag:
    version_flag:
    help_text: "Usage: pairtools split [OPTIONS] [PAIRSAM_PATH]\n\n  Split a .pairsam\
      \ file into .pairs and .sam.\n\n  Restore a .sam file from sam1 and sam2 fields\
      \ of a .pairsam file. Create\n  a .pairs file without sam1/sam2 fields.\n\n\
      \  PAIRSAM_PATH : input .pairsam file. If the path ends with .gz or .lz4, the\n\
      \  input is decompressed by pbgzip or lz4c. By default, the input is read\n\
      \  from  stdin.\n\nOptions:\n  --output-pairs TEXT  output pairs file. If the\
      \ path ends with .gz or .lz4,\n                       the output is pbgzip-/lz4c-compressed.\
      \ If -, pairs are\n                       printed to stdout. If not specified,\
      \ pairs are dropped.\n\n  --output-sam TEXT    output sam file. If the path\
      \ ends with .bam, the output\n                       is compressed into a bam\
      \ file. If -, sam entries are\n                       printed to stdout. If\
      \ not specified, sam entries are\n                       dropped.\n\n  --nproc-in\
      \ INTEGER   Number of processes used by the auto-guessed input\n           \
      \            decompressing command.  [default: 3]\n\n  --nproc-out INTEGER \
      \ Number of processes used by the auto-guessed output\n                    \
      \   compressing command.  [default: 8]\n\n  --cmd-in TEXT        A command to\
      \ decompress the input file. If provided,\n                       fully overrides\
      \ the auto-guessed command. Does not work\n                       with stdin.\
      \ Must read input from stdin and print output\n                       into stdout.\
      \ EXAMPLE: pbgzip -dc -n 3\n\n  --cmd-out TEXT       A command to compress the\
      \ output file. If provided,\n                       fully overrides the auto-guessed\
      \ command. Does not work\n                       with stdout. Must read input\
      \ from stdin and print\n                       output into stdout. EXAMPLE:\
      \ pbgzip -c -n 8\n\n  -h, --help           Show this message and exit.\n"
    generated_using: *id002
  - !Command
    positional:
    - !Positional
      description: ''
      position: 0
      name: INPUT_PATH
      optional: true
    named:
    - !Flag
      description: output stats tsv file.
      synonyms:
      - -o
      - --output
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: If specified, merge multiple input stats files instead of calculating
        statistics of a .pairs/.pairsam file. Merging is performed via summation of
        all overlapping statistics. Non-overlapping statistics are appended to the
        end of the file.
      synonyms:
      - --merge
      args: !EmptyFlagArg {}
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed input decompressing
        command.  [default: 3]'
      synonyms:
      - --nproc-in
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'Number of processes used by the auto-guessed output compressing
        command.  [default: 8]'
      synonyms:
      - --nproc-out
      args: !SimpleFlagArg
        name: INTEGER
      optional: true
    - !Flag
      description: 'A command to decompress the input file. If provided, fully overrides
        the auto-guessed command. Does not work with stdin. Must read input from stdin
        and print output into stdout. EXAMPLE: pbgzip -dc -n 3'
      synonyms:
      - --cmd-in
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    - !Flag
      description: 'A command to compress the output file. If provided, fully overrides
        the auto-guessed command. Does not work with stdout. Must read input from
        stdin and print output into stdout. EXAMPLE: pbgzip -c -n 8'
      synonyms:
      - --cmd-out
      args: !SimpleFlagArg
        name: TEXT
      optional: true
    command:
    - pairtools
    - stats
    parent: *id001
    subcommands: []
    help_flag: !Flag
      description: Show this message and exit.
      synonyms:
      - -h
      - --help
      args: !EmptyFlagArg {}
      optional: true
    usage_flag:
    version_flag:
    help_text: "Usage: pairtools stats [OPTIONS] [INPUT_PATH]...\n\n  Calculate pairs\
      \ statistics.\n\n  INPUT_PATH : by default, a .pairs/.pairsam file to calculate\
      \ statistics.\n  If not provided, the input is read from stdin. If --merge is\
      \ specified,\n  then INPUT_PATH is interpreted as an arbitrary number  of stats\
      \ files to\n  merge.\n\n  The files with paths ending with .gz/.lz4 are decompressed\
      \ by pbgzip/lz4c.\n\nOptions:\n  -o, --output TEXT    output stats tsv file.\n\
      \  --merge              If specified, merge multiple input stats files instead\n\
      \                       of calculating statistics of a .pairs/.pairsam file.\n\
      \                       Merging is performed via summation of all overlapping\n\
      \                       statistics. Non-overlapping statistics are appended\
      \ to\n                       the end of the file.\n\n  --nproc-in INTEGER  \
      \ Number of processes used by the auto-guessed input\n                     \
      \  decompressing command.  [default: 3]\n\n  --nproc-out INTEGER  Number of\
      \ processes used by the auto-guessed output\n                       compressing\
      \ command.  [default: 8]\n\n  --cmd-in TEXT        A command to decompress the\
      \ input file. If provided,\n                       fully overrides the auto-guessed\
      \ command. Does not work\n                       with stdin. Must read input\
      \ from stdin and print output\n                       into stdout. EXAMPLE:\
      \ pbgzip -dc -n 3\n\n  --cmd-out TEXT       A command to compress the output\
      \ file. If provided,\n                       fully overrides the auto-guessed\
      \ command. Does not work\n                       with stdout. Must read input\
      \ from stdin and print\n                       output into stdout. EXAMPLE:\
      \ pbgzip -c -n 8\n\n  -h, --help           Show this message and exit.\n"
    generated_using: *id002
  help_flag: !Flag
    description: Show this message and exit.
    synonyms:
    - -h
    - --help
    args: !EmptyFlagArg {}
    optional: true
  usage_flag:
  version_flag: !Flag
    description: Show the version and exit.
    synonyms:
    - --version
    args: !EmptyFlagArg {}
    optional: true
  help_text: "Usage: pairtools [OPTIONS] COMMAND [ARGS]...\n\n  Flexible tools for\
    \ Hi-C data processing.\n\n  All pairtools have a few common options, which should\
    \ be typed _before_\n  the command name.\n\nOptions:\n  --post-mortem        \
    \  Post mortem debugging\n  --output-profile TEXT  Profile performance with Python\
    \ cProfile and dump the\n                         statistics into a binary file\n\
    \n  --version              Show the version and exit.\n  -h, --help          \
    \   Show this message and exit.\n\nCommands:\n  dedup        Find and remove PCR/optical\
    \ duplicates.\n  filterbycov  Remove pairs from regions of high coverage.\n  flip\
    \         Flip pairs to get an upper-triangular matrix.\n  markasdup    Tag pairs\
    \ as duplicates.\n  merge        Merge sorted .pairs/.pairsam files.\n  parse\
    \        Find ligation junctions in .sam, make .pairs.\n  phase        Phase pairs\
    \ mapped to a diploid genome.\n  restrict     Assign restriction fragments to\
    \ pairs.\n  sample       Select a random subset of pairs in a pairs file.\n  select\
    \       Select pairs according to some condition.\n  sort         Sort a .pairs/.pairsam\
    \ file.\n  split        Split a .pairsam file into .pairs and .sam.\n  stats \
    \       Calculate pairs statistics.\n"
  generated_using: *id002
subcommands: []
help_flag: !Flag
  description: Show this message and exit.
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "Usage: pairtools merge [OPTIONS] [PAIRS_PATH]...\n\n  Merge sorted .pairs/.pairsam\
  \ files.\n\n  Merge triu-flipped sorted pairs/pairsam files. If present, the @SQ\
  \ records\n  of the SAM header must be identical; the sorting order of  these lines\
  \ is\n  taken from the first file in the list.  The ID fields of the @PG records\n\
  \  of the SAM header are modified with a numeric suffix to produce unique\n  records.\
  \ The other unique SAM and non-SAM header lines are copied into the\n  output header.\n\
  \n  PAIRS_PATH : upper-triangular flipped sorted .pairs/.pairsam files to\n  merge\
  \ or a group/groups of .pairs/.pairsam files specified by a wildcard.\n  For paths\
  \ ending in .gz/.lz4, the files are decompressed by pbgzip/lz4c.\n\nOptions:\n \
  \ -o, --output TEXT        output file. If the path ends with .gz/.lz4, the\n  \
  \                         output is compressed by pbgzip/lz4c. By default,\n   \
  \                        the output is printed into stdout.\n\n  --max-nmerge INTEGER\
  \     The maximal number of inputs merged at once. For\n                       \
  \    more, store merged intermediates in temporary\n                           files.\
  \  [default: 8]\n\n  --tmpdir TEXT            Custom temporary folder for merged\
  \ intermediates.\n  --memory TEXT            The amount of memory used by default.\
  \  [default:\n                           2G]\n\n  --compress-program TEXT  A binary\
  \ to compress temporary merged chunks. Must\n                           decompress\
  \ input when the flag -d is provided.\n                           Suggested alternatives:\
  \ lz4c, gzip, lzop, snzip.\n                           NOTE: fails silently if the\
  \ command syntax is\n                           wrong.   [default: ]\n\n  --nproc\
  \ INTEGER          Number of threads for merging.  [default: 8]\n  --nproc-in INTEGER\
  \       Number of processes used by the auto-guessed input\n                   \
  \        decompressing command.  [default: 1]\n\n  --nproc-out INTEGER      Number\
  \ of processes used by the auto-guessed output\n                           compressing\
  \ command.  [default: 8]\n\n  --cmd-in TEXT            A command to decompress the\
  \ input. If provided,\n                           fully overrides the auto-guessed\
  \ command. Does not\n                           work with stdin. Must read input\
  \ from stdin and\n                           print output into stdout. EXAMPLE:\
  \ pbgzip -dc -n 3\n\n  --cmd-out TEXT           A command to compress the output.\
  \ If provided,\n                           fully overrides the auto-guessed command.\
  \ Does not\n                           work with stdout. Must read input from stdin\
  \ and\n                           print output into stdout. EXAMPLE: pbgzip -c -n\
  \ 8\n\n  -h, --help               Show this message and exit.\n"
generated_using: *id002
