!Command
positional:
- !Positional
  description: ''
  position: 0
  name: OPTION
  optional: true
named:
- !Flag
  description: suppress column 1 (lines unique to FILE1)
  synonyms:
  - '-1'
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: suppress column 2 (lines unique to FILE2)
  synonyms:
  - '-2'
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: suppress column 3 (lines that appear in both files)
  synonyms:
  - '-3'
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: check that the input is correctly sorted, even if all input lines are
    pairable
  synonyms:
  - --check-order
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: do not check that the input is correctly sorted
  synonyms:
  - --nocheck-order
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: separate columns with STR
  synonyms:
  - --output-delimiter
  args: !SimpleFlagArg
    name: STR
  optional: true
- !Flag
  description: line delimiter is NUL, not newline
  synonyms:
  - -z
  - --zero-terminated
  args: !EmptyFlagArg {}
  optional: true
command:
- comm
parent:
subcommands: []
help_flag: !Flag
  description: this help and exit
  synonyms:
  - --help
  args: !SimpleFlagArg
    name: display
  optional: true
usage_flag:
version_flag: !Flag
  description: version information and exit
  synonyms:
  - --version
  args: !SimpleFlagArg
    name: output
  optional: true
help_text: "Usage: comm [OPTION]... FILE1 FILE2\nCompare sorted files FILE1 and FILE2\
  \ line by line.\n\nWhen FILE1 or FILE2 (not both) is -, read standard input.\n\n\
  With no options, produce three-column output. Column one contains\nlines unique\
  \ to FILE1, column two contains lines unique to FILE2\nand column three contains\
  \ lines common to both files.\n\n  -1              suppress column 1 (lines unique\
  \ to FILE1)\n  -2              suppress column 2 (lines unique to FILE2)\n  -3 \
  \             suppress column 3 (lines that appear in both files)\n\n  --check-order\
  \     check that the input is correctly sorted, even\n                      if all\
  \ input lines are pairable\n  --nocheck-order   do not check that the input is correctly\
  \ sorted\n  --output-delimiter=STR  separate columns with STR\n  -z, --zero-terminated\
  \    line delimiter is NUL, not newline\n      --help display this help and exit\n\
  \      --version output version information and exit\n\nNote, comparisons honor\
  \ the rules specified by 'LC_COLLATE'.\n\nExamples:\n  comm -12 file1 file2  Print\
  \ only lines present in both file1 and file2.\n  comm -3 file1 file2  Print lines\
  \ in file1 not in file2, and vice versa.\n\nGNU coreutils online help: <http://www.gnu.org/software/coreutils/>\n\
  Full documentation at: <http://www.gnu.org/software/coreutils/comm>\nor available\
  \ locally via: info '(coreutils) comm invocation'\n"
generated_using:
- --help
