!Command
positional: []
named:
- !Flag
  description: the separator before instead of after
  synonyms:
  - -b
  - --before
  args: !SimpleFlagArg
    name: attach
  optional: true
- !Flag
  description: the separator as a regular expression
  synonyms:
  - -r
  - --regex
  args: !SimpleFlagArg
    name: interpret
  optional: true
- !Flag
  description: use STRING as the separator instead of newline
  synonyms:
  - -s
  - --separator
  args: !SimpleFlagArg
    name: STRING
  optional: true
command:
- tac
subcommands: []
help_flag: !Flag
  description: this help and exit
  synonyms:
  - --help
  args: !SimpleFlagArg
    name: display
  optional: true
usage_flag:
version_flag: !Flag
  description: version information and exit
  synonyms:
  - --version
  args: !SimpleFlagArg
    name: output
  optional: true
