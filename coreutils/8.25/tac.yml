!Command
positional:
- !Positional
  description: ''
  position: 0
  name: OPTION
  optional: true
named:
- !Flag
  description: the separator before instead of after
  synonyms:
  - -b
  - --before
  args: !SimpleFlagArg
    name: attach
  optional: true
- !Flag
  description: the separator as a regular expression
  synonyms:
  - -r
  - --regex
  args: !SimpleFlagArg
    name: interpret
  optional: true
- !Flag
  description: use STRING as the separator instead of newline
  synonyms:
  - -s
  - --separator
  args: !SimpleFlagArg
    name: STRING
  optional: true
command:
- tac
parent:
subcommands: []
help_flag: !Flag
  description: this help and exit
  synonyms:
  - --help
  args: !SimpleFlagArg
    name: display
  optional: true
usage_flag:
version_flag: !Flag
  description: version information and exit
  synonyms:
  - --version
  args: !SimpleFlagArg
    name: output
  optional: true
help_text: "Usage: tac [OPTION]... [FILE]...\nWrite each FILE to standard output,\
  \ last line first.\n\nWith no FILE, or when FILE is -, read standard input.\n\n\
  Mandatory arguments to long options are mandatory for short options too.\n  -b,\
  \ --before attach the separator before instead of after\n  -r, --regex interpret\
  \ the separator as a regular expression\n  -s, --separator=STRING use STRING as\
  \ the separator instead of newline\n      --help display this help and exit\n  \
  \    --version output version information and exit\n\nGNU coreutils online help:\
  \ <http://www.gnu.org/software/coreutils/>\nFull documentation at: <http://www.gnu.org/software/coreutils/tac>\n\
  or available locally via: info '(coreutils) tac invocation'\n"
generated_using:
- --help
