!Command
positional:
- !Positional
  description: ''
  position: 0
  name: OPTION
  optional: true
named:
- !Flag
  description: reuse characters from LIST instead of TABs
  synonyms:
  - -d
  - --delimiters
  args: !SimpleFlagArg
    name: LIST
  optional: true
- !Flag
  description: one file at a time instead of in parallel
  synonyms:
  - -s
  - --serial
  args: !SimpleFlagArg
    name: paste
  optional: true
- !Flag
  description: line delimiter is NUL, not newline
  synonyms:
  - -z
  - --zero-terminated
  args: !EmptyFlagArg {}
  optional: true
command:
- paste
parent:
subcommands: []
help_flag: !Flag
  description: this help and exit
  synonyms:
  - --help
  args: !SimpleFlagArg
    name: display
  optional: true
usage_flag:
version_flag: !Flag
  description: version information and exit
  synonyms:
  - --version
  args: !SimpleFlagArg
    name: output
  optional: true
help_text: "Usage: paste [OPTION]... [FILE]...\nWrite lines consisting of the sequentially\
  \ corresponding lines from\neach FILE, separated by TABs, to standard output.\n\n\
  With no FILE, or when FILE is -, read standard input.\n\nMandatory arguments to\
  \ long options are mandatory for short options too.\n  -d, --delimiters=LIST reuse\
  \ characters from LIST instead of TABs\n  -s, --serial paste one file at a time\
  \ instead of in parallel\n  -z, --zero-terminated    line delimiter is NUL, not\
  \ newline\n      --help display this help and exit\n      --version output version\
  \ information and exit\n\nGNU coreutils online help: <http://www.gnu.org/software/coreutils/>\n\
  Full documentation at: <http://www.gnu.org/software/coreutils/paste>\nor available\
  \ locally via: info '(coreutils) paste invocation'\n"
generated_using:
- --help
