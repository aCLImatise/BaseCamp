!Command
positional: []
named:
- !Flag
  description: =[-]NUM       print the first NUM bytes of each file; with the leading
    '-', print all but the last NUM bytes of each file
  synonyms:
  - -c
  - --bytes
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: =[-]NUM       print the first NUM lines instead of the first 10; with
    the leading '-', print all but the last NUM lines of each file
  synonyms:
  - -n
  - --lines
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: print headers giving file names
  synonyms:
  - -q
  - --quiet
  - --silent
  args: !SimpleFlagArg
    name: never
  optional: true
- !Flag
  description: print headers giving file names
  synonyms:
  - -v
  - --verbose
  args: !SimpleFlagArg
    name: always
  optional: true
- !Flag
  description: line delimiter is NUL, not newline
  synonyms:
  - -z
  - --zero-terminated
  args: !EmptyFlagArg {}
  optional: true
command:
- head
subcommands: []
help_flag: !Flag
  description: this help and exit
  synonyms:
  - --help
  args: !SimpleFlagArg
    name: display
  optional: true
usage_flag:
version_flag: !Flag
  description: version information and exit
  synonyms:
  - --version
  args: !SimpleFlagArg
    name: output
  optional: true
