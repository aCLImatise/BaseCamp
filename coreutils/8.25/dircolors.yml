!Command
positional:
- !Positional
  description: ''
  position: 0
  name: OPTION
  optional: true
named:
- !Flag
  description: Bourne shell code to set LS_COLORS
  synonyms:
  - -b
  - --sh
  - --bourne-shell
  args: !SimpleFlagArg
    name: output
  optional: true
- !Flag
  description: C shell code to set LS_COLORS
  synonyms:
  - -c
  - --csh
  - --c-shell
  args: !SimpleFlagArg
    name: output
  optional: true
- !Flag
  description: defaults
  synonyms:
  - -p
  - --print-database
  args: !SimpleFlagArg
    name: output
  optional: true
command:
- dircolors
parent:
subcommands: []
help_flag: !Flag
  description: this help and exit
  synonyms:
  - --help
  args: !SimpleFlagArg
    name: display
  optional: true
usage_flag:
version_flag: !Flag
  description: version information and exit
  synonyms:
  - --version
  args: !SimpleFlagArg
    name: output
  optional: true
help_text: "Usage: dircolors [OPTION]... [FILE]\nOutput commands to set the LS_COLORS\
  \ environment variable.\n\nDetermine format of output:\n  -b, --sh, --bourne-shell\
  \ output Bourne shell code to set LS_COLORS\n  -c, --csh, --c-shell output C shell\
  \ code to set LS_COLORS\n  -p, --print-database output defaults\n      --help display\
  \ this help and exit\n      --version output version information and exit\n\nIf\
  \ FILE is specified, read it to determine which colors to use for which\nfile types\
  \ and extensions.  Otherwise, a precompiled database is used.\nFor details on the\
  \ format of these files, run 'dircolors --print-database'.\n\nGNU coreutils online\
  \ help: <http://www.gnu.org/software/coreutils/>\nFull documentation at: <http://www.gnu.org/software/coreutils/dircolors>\n\
  or available locally via: info '(coreutils) dircolors invocation'\n"
generated_using:
- --help
