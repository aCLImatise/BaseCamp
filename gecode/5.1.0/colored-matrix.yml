!Command
positional: []
named: []
command:
- colored-matrix
parent:
subcommands: []
help_flag:
usage_flag:
version_flag:
help_text: "Gecode configuration information:\n - Version: 5.1.0\n - Variable types:\
  \ BoolVar IntVar SetVar FloatVar \n - Trigonometric and transcendental float constraints:\
  \ disabled\n - Thread support: enabled (8 processing units)\n - Gist support: disabled\n\
  \nOptions for Colored matrix:\n\t-help, --help, -?\n\t\tprint this help message\n\
  \t-model (corner, rows, both, columns, matrix) default: corner\n\t\tmodel variants\n\
  \t\t  corner: Use direct corners model with not-all-equal constraints.\n\t\t  rows:\
  \ Use model on pairs of rows (same_or_0 and distinct_except_0 constraints).\n\t\t\
  \  both: Use both rows and corners model\n\t\t  columns: Use model on pairs of columns\
  \ (same_or_0 and distinct_except_0 constraints).\n\t\t  matrix: Use both rows and\
  \ columns model\n\t-symmetry (none, matrix, values, both) default: matrix\n\t\t\
  symmetry variants\n\t\t  none: Don't use symmetry breaking.\n\t\t  matrix: Order\
  \ matrix rows and columns\n\t\t  values: Order values\n\t\t  both: Order both rows/columns\
  \ and values\n\t-ipl (def,val,bnd,dom,speed,memory,basic,advanced)\n\t\tdefault:\
  \ def\n\t\tinteger propagation level (comma-separated list)\n\t-decay (double) default:\
  \ 1\n\t\tdecay factor\n\t-seed (unsigned int) default: 1\n\t\trandom number generator\
  \ seed\n\t-step (double) default: 0\n\t\tstep distance for float optimization\n\t\
  -search (dfs, bab) default: dfs\n\t\tsearch engine variants\n\t\t  dfs: Find a solution.\n\
  \t\t  bab: Find an optimal solution.\n\t-solutions (unsigned int) default: 1\n\t\
  \tnumber of solutions (0 = all)\n\t-threads (double) default: 1\n\t\tnumber of threads\
  \ (0 = #processing units)\n\t-c-d (unsigned int) default: 8\n\t\trecomputation commit\
  \ distance\n\t-a-d (unsigned int) default: 2\n\t\trecomputation adaptation distance\n\
  \t-d-l (unsigned int) default: 5\n\t\tdiscrepancy limit for LDS\n\t-node (unsigned\
  \ int) default: 0\n\t\tnode cutoff (0 = none, solution mode)\n\t-fail (unsigned\
  \ int) default: 0\n\t\tfailure cutoff (0 = none, solution mode)\n\t-time (unsigned\
  \ int) default: 0\n\t\ttime (in ms) cutoff (0 = none, solution mode)\n\t-interrupt\
  \ (optional: false, 0, true, 1) default: true\n\t\twhether to catch Ctrl-C (true)\
  \ or not (false)\n\t-assets (unsigned int) default: 0\n\t\t#portfolio assets (#engines)\n\
  \t-slice (unsigned int) default: 250\n\t\tportfolio slice (in #failures)\n\t-restart\
  \ (none, constant, linear, luby, geometric) default: none\n\t\trestart sequence\
  \ type\n\t-restart-base (double) default: 1.5\n\t\tbase for geometric restart sequence\n\
  \t-restart-scale (unsigned int) default: 250\n\t\tscale factor for restart sequence\n\
  \t-nogoods (optional: false, 0, true, 1) default: false\n\t\twhether to use no-goods\
  \ from restarts\n\t-nogoods-limit (unsigned int) default: 128\n\t\tdepth limit for\
  \ no-good extraction\n\t-relax (double) default: 0\n\t\tprobability for relaxing\
  \ variable\n\t-mode (solution, time, stat, gist) default: solution\n\t\thow to execute\
  \ script\n\t-iterations (unsigned int) default: 1\n\t\titerations per sample (time\
  \ mode)\n\t-samples (unsigned int) default: 1\n\t\thow many samples (time mode)\n\
  \t-print-last (optional: false, 0, true, 1) default: false\n\t\twhether to only\
  \ print the last solution (solution mode)\n\t-file-sol (string) default: stdout\n\
  \t\twhere to print solutions (supports stdout, stdlog, stderr)\n\t-file-stat (string)\
  \ default: stdout\n\t\twhere to print statistics (supports stdout, stdlog, stderr)\n\
  \t-trace (init,prune,fix,fail,done,propagate,commit,none,all,variable,general) default:\
  \ none\n\t\ttrace flags (comma-separated list)\n\t-height (unsigned int) default:\
  \ 8\n\t\tHeight of matrix\n\t-width (unsigned int) default: 8\n\t\tWidth of matrix\n\
  \t-size (unsigned int) default: 0\n\t\tIf different from 0, used as both width and\
  \ height\n\t-colors (unsigned int) default: 4\n\t\tMaximum number of colors\n\t\
  -not-all-equal (nq, naive, reified, nvalues, count, dfa) default: nq\n\t\tHow to\
  \ implement the not all equals constraint (used in corners model)\n\t\t  nq: Use\
  \ nq constraint.\n\t\t  naive: Use naive reified decomposition.\n\t\t  reified:\
  \ Use reified decomposition.\n\t\t  nvalues: Use nvalues.\n\t\t  count: Use count.\n\
  \t\t  dfa: Use dfa.\n\t-same-or-0 (reified, tuple-set, dfa) default: dfa\n\t\tHow\
  \ to implement the same or 0 constraint (used in the decomposed no monochrome rectangle\
  \ constraint)\n\t\t  reified: Use reified decomposition.\n\t\t  tuple-set: Use tuple\
  \ set representation.\n\t\t  dfa: Use dfa representation.\n\t-distinct-except-0\
  \ (reified, dfa, count) default: dfa\n\t\tHow to implement the distinct except 0\
  \ constraint (used in the decomposed no monochrome rectangle constraint)\n\t\t \
  \ reified: Use reified decomposition.\n\t\t  dfa: Use dfa decomposition.\n\t\t \
  \ count: Use global cardinality.\n\t-no-monochrome-rectangle (decompositions, dfa)\
  \ default: dfa\n\t\tHow to implement no monochrome rectangle (used in the rows model)\n\
  \t\t  decompositions: Use decompositions into same_or_0 and distinct_except_0.\n\
  \t\t  dfa: Use DFA as direct implementation.\n"
generated_using:
- --help
