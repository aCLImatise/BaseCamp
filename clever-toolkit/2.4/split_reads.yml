!Command
command:
- split-reads
positional: []
named:
- !Flag
  description: '[ --split_length ] arg (=35) Length of prefix/suffix to be extracted.'
  synonyms:
  - -l
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "Process single end reads (instead of paired\nend).\n"
  synonyms:
  - --single-end
  args: !EmptyFlagArg {}
  optional: true
parent:
subcommands: []
usage: []
help_flag:
usage_flag:
version_flag:
help_text: "split-reads version: v2.4\nUsage: split-reads [options] <input.1.fastq(.gz)>\
  \ <input.2.fastq(.gz)>\n       split-reads [options] --single-end <input.fastq(.gz)>\n\
  \nReads input in FASTQ format and writes prefix and suffix\nof each read to stdout.\n\
  \nAllowed options:\n  -l [ --split_length ] arg (=35) Length of prefix/suffix to\
  \ be extracted.\n  --single-end                    Process single end reads (instead\
  \ of paired \n                                  end).\n\n"
generated_using:
- --help
