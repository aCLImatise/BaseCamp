!Command
positional: []
named: []
command:
- blastclust
parent:
subcommands: []
help_flag:
usage_flag:
version_flag:
help_text: "\nblastclust 2.2.26   arguments:\n\n  -i  FASTA input file (program will\
  \ format the database and remove files in the end) [File In]\n    default = stdin\n\
  \  -a  Number of CPU's to use [Integer]\n    default = 1\n  -o  Output file for\
  \ list of clusters [File Out]\n    default = stdout\n  -L  Length coverage threshold\
  \ [Real]\n    default = 0.9\n  -S  Score coverage threshold (bit score / length\
  \ if < 3.0, percentage of identities otherwise) [Real]\n    default = 1.75\n  -b\
  \  Require coverage on both neighbours? [T/F]\n    default = TRUE\n  -s  File to\
  \ save all neighbours [File Out]  Optional\n  -r  File to restore neighbors for\
  \ reclustering [File In]  Optional\n  -d  Input as a database [File In]  Optional\n\
  \  -v  Print progress messages (verbose mode) [File Out]\n    default = stdout\n\
  \  -C  Complete unfinished clustering [T/F]\n    default = FALSE\n  -l  Restrict\
  \ reclustering to id list [File In]  Optional\n  -p  Is input proteins? [T/F]\n\
  \    default = TRUE\n  -e  Enable id parsing in database formatting? [T/F]\n   \
  \ default = TRUE\n  -c  Configuration file with advanced options [File In]  Optional\n\
  \  -W  Word size to use (0 for default: proteins 3, nucleotides 32) [Integer]\n\
  \    default = 0\n\n"
generated_using:
- --help
