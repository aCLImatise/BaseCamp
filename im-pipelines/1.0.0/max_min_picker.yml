!Command
positional: []
named:
- !Flag
  description: similarity threshold (1.0 means identical)
  synonyms:
  - -t
  - --threshold
  args: !SimpleFlagArg
    name: THRESHOLD
  optional: true
- !Flag
  description: descriptor or fingerprint type (default rdkit)
  synonyms:
  - -d
  - --descriptor
  args: !ChoiceFlagArg
    choices: !!set
      maccs:
      morgan2:
      morgan3:
  optional: true
- !Flag
  description: Quiet mode
  synonyms:
  - -q
  - --quiet
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: maximum number to pick for diverse subset selection
  synonyms:
  - -n
  - --num
  args: !SimpleFlagArg
    name: NUM
  optional: true
- !Flag
  description: optional file containing any seed molecules that have already been
    picked
  synonyms:
  - -s
  - --seed-molecules
  args: !SimpleFlagArg
    name: SEED_MOLECULES
  optional: true
- !Flag
  description: Approach to find biggest fragment if more than one (hac = biggest by
    heavy atom count, mw = biggest by mol weight)
  synonyms:
  - --fragment-method
  args: !ChoiceFlagArg
    choices: !!set
      hac:
      mw:
  optional: true
- !Flag
  description: Output the biggest fragment rather than the original molecule
  synonyms:
  - --output-fragment
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: Input file, if not defined the STDIN is used
  synonyms:
  - -i
  - --input
  args: !SimpleFlagArg
    name: INPUT
  optional: true
- !Flag
  description: Input format. When using STDIN this must be specified.
  synonyms:
  - -if
  - --informat
  args: !ChoiceFlagArg
    choices: !!set
      sdf:
      json:
  optional: true
- !Flag
  description: Base name for output file (no extension). If not defined then SDTOUT
    is used for the structures and output is used as base name of the other files.
  synonyms:
  - -o
  - --output
  args: !SimpleFlagArg
    name: OUTPUT
  optional: true
- !Flag
  description: Output format. Defaults to 'sdf'.
  synonyms:
  - -of
  - --outformat
  args: !ChoiceFlagArg
    choices: !!set
      sdf:
      json:
  optional: true
- !Flag
  description: Write metadata and metrics files
  synonyms:
  - --meta
  args: !EmptyFlagArg {}
  optional: true
command:
- max_min_picker
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "usage: max_min_picker [-h] [-t THRESHOLD] [-d {maccs,morgan2,morgan3}]\
  \ [-q]\n                      [-n NUM] [-s SEED_MOLECULES]\n                   \
  \   [--fragment-method {hac,mw}] [--output-fragment]\n                      [-i\
  \ INPUT] [-if {sdf,json}] [-o OUTPUT] [-of {sdf,json}]\n                      [--meta]\n\
  \nRDKit Butina Cluster\n\noptional arguments:\n  -h, --help            show this\
  \ help message and exit\n  -t THRESHOLD, --threshold THRESHOLD\n               \
  \         similarity threshold (1.0 means identical)\n  -d {maccs,morgan2,morgan3},\
  \ --descriptor {maccs,morgan2,morgan3}\n                        descriptor or fingerprint\
  \ type (default rdkit)\n  -q, --quiet           Quiet mode\n  -n NUM, --num NUM\
  \     maximum number to pick for diverse subset selection\n  -s SEED_MOLECULES,\
  \ --seed-molecules SEED_MOLECULES\n                        optional file containing\
  \ any seed molecules that have\n                        already been picked\n  --fragment-method\
  \ {hac,mw}\n                        Approach to find biggest fragment if more than\
  \ one\n                        (hac = biggest by heavy atom count, mw = biggest\
  \ by\n                        mol weight)\n  --output-fragment     Output the biggest\
  \ fragment rather than the original\n                        molecule\n  -i INPUT,\
  \ --input INPUT\n                        Input file, if not defined the STDIN is\
  \ used\n  -if {sdf,json}, --informat {sdf,json}\n                        Input format.\
  \ When using STDIN this must be specified.\n  -o OUTPUT, --output OUTPUT\n     \
  \                   Base name for output file (no extension). If not\n         \
  \               defined then SDTOUT is used for the structures and\n           \
  \             output is used as base name of the other files.\n  -of {sdf,json},\
  \ --outformat {sdf,json}\n                        Output format. Defaults to 'sdf'.\n\
  \  --meta                Write metadata and metrics files\n"
generated_using:
- --help
