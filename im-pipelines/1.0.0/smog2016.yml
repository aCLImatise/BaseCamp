!Command
positional: []
named:
- !Flag
  description: Input file, if not defined the STDIN is used
  synonyms:
  - -i
  - --input
  args: !SimpleFlagArg
    name: INPUT
  optional: true
- !Flag
  description: Input format. When using STDIN this must be specified.
  synonyms:
  - -if
  - --informat
  args: !ChoiceFlagArg
    choices: !!set
      sdf:
      json:
  optional: true
- !Flag
  description: Base name for output file (no extension). If not defined then SDTOUT
    is used for the structures and output is used as base name of the other files.
  synonyms:
  - -o
  - --output
  args: !SimpleFlagArg
    name: OUTPUT
  optional: true
- !Flag
  description: Output format. Defaults to 'sdf'.
  synonyms:
  - -of
  - --outformat
  args: !ChoiceFlagArg
    choices: !!set
      sdf:
      json:
  optional: true
- !Flag
  description: Write metadata and metrics files
  synonyms:
  - --meta
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: Do not compress the output (STDOUT is never compressed
  synonyms:
  - --no-gzip
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: PDB file for scoring
  synonyms:
  - -pdb
  - --pdb_file
  args: !SimpleFlagArg
    name: PDB_FILE
  optional: true
- !Flag
  description: The maximum score to allow
  synonyms:
  - -t
  - --threshold
  args: !SimpleFlagArg
    name: THRESHOLD
  optional: true
- !Flag
  description: Number of threads to used. Default is the number of cores
  synonyms:
  - --threads
  args: !SimpleFlagArg
    name: THREADS
  optional: true
- !Flag
  description: Thin output mode
  synonyms:
  - --thin
  args: !EmptyFlagArg {}
  optional: true
command:
- smog2016
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "usage: smog2016 [-h] [-i INPUT] [-if {sdf,json}] [-o OUTPUT] [-of {sdf,json}]\n\
  \                [--meta] [--no-gzip] [-pdb PDB_FILE] [-t THRESHOLD]\n         \
  \       [--threads THREADS] [--thin]\n\nSMoG2016 - Docking calculation.\n\noptional\
  \ arguments:\n  -h, --help            show this help message and exit\n  -i INPUT,\
  \ --input INPUT\n                        Input file, if not defined the STDIN is\
  \ used\n  -if {sdf,json}, --informat {sdf,json}\n                        Input format.\
  \ When using STDIN this must be specified.\n  -o OUTPUT, --output OUTPUT\n     \
  \                   Base name for output file (no extension). If not\n         \
  \               defined then SDTOUT is used for the structures and\n           \
  \             output is used as base name of the other files.\n  -of {sdf,json},\
  \ --outformat {sdf,json}\n                        Output format. Defaults to 'sdf'.\n\
  \  --meta                Write metadata and metrics files\n  --no-gzip         \
  \    Do not compress the output (STDOUT is never compressed\n  -pdb PDB_FILE, --pdb_file\
  \ PDB_FILE\n                        PDB file for scoring\n  -t THRESHOLD, --threshold\
  \ THRESHOLD\n                        The maximum score to allow\n  --threads THREADS\
  \     Number of threads to used. Default is the number of\n                    \
  \    cores\n  --thin                Thin output mode\n"
generated_using:
- --help
