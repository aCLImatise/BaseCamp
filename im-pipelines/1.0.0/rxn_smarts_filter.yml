!Command
positional: []
named:
- !Flag
  description: Input file, if not defined the STDIN is used
  synonyms:
  - -i
  - --input
  args: !SimpleFlagArg
    name: INPUT
  optional: true
- !Flag
  description: Input format. When using STDIN this must be specified.
  synonyms:
  - -if
  - --informat
  args: !ChoiceFlagArg
    choices: !!set
      ? sdf
      ? json
  optional: true
- !Flag
  description: Base name for output file (no extension). If not defined then SDTOUT
    is used for the structures and output is used as base name of the other files.
  synonyms:
  - -o
  - --output
  args: !SimpleFlagArg
    name: OUTPUT
  optional: true
- !Flag
  description: Output format. Defaults to 'sdf'.
  synonyms:
  - -of
  - --outformat
  args: !ChoiceFlagArg
    choices: !!set
      ? sdf
      ? json
  optional: true
- !Flag
  description: Write metadata and metrics files
  synonyms:
  - --meta
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: Quiet mode
  synonyms:
  - -q
  - --quiet
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: Output one file for each reaction
  synonyms:
  - -m
  - --multi
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: Thin output mode
  synonyms:
  - --thin
  args: !EmptyFlagArg {}
  optional: true
command:
- rxn_smarts_filter
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "usage: rxn_smarts_filter [-h] [-i INPUT] [-if {sdf,json}] [-o OUTPUT]\n\
  \                         [-of {sdf,json}] [--meta] [-q] [-m] [--thin]\n\nRDKit\
  \ rxn smarts filter\n\noptional arguments:\n  -h, --help            show this help\
  \ message and exit\n  -i INPUT, --input INPUT\n                        Input file,\
  \ if not defined the STDIN is used\n  -if {sdf,json}, --informat {sdf,json}\n  \
  \                      Input format. When using STDIN this must be specified.\n\
  \  -o OUTPUT, --output OUTPUT\n                        Base name for output file\
  \ (no extension). If not\n                        defined then SDTOUT is used for\
  \ the structures and\n                        output is used as base name of the\
  \ other files.\n  -of {sdf,json}, --outformat {sdf,json}\n                     \
  \   Output format. Defaults to 'sdf'.\n  --meta                Write metadata and\
  \ metrics files\n  -q, --quiet           Quiet mode\n  -m, --multi           Output\
  \ one file for each reaction\n  --thin                Thin output mode\n"
generated_using:
- --help
