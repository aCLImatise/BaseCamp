!Command
positional:
- !Positional
  description: Directory containing bowtie output files
  position: 0
  name: bowtie_outdir
  optional: false
- !Positional
  description: A file containing the order of chromosome names to consider (one chromosome
    name per line)
  position: 1
  name: chrsize_path
  optional: false
named:
- !Flag
  description: If not using a cluster for submitting jobs, specify the job_id by integer
    ranging from 1 to total number of chromosomes in chrsize_path
  synonyms:
  - -job_id
  args: !SimpleFlagArg
    name: JOB_ID
  optional: true
- !Flag
  description: HPC variable name for job ID (1-based index)
  synonyms:
  - -var_id
  args: !SimpleFlagArg
    name: VAR_ID
  optional: true
command:
- unify_bowtie.py
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
