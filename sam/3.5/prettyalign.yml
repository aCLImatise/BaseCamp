!Command
positional:
- !Positional
  description: ''
  position: 0
  name: filename
  optional: false
named:
- !Flag
  description: Output in a FASTA-like format (off).
  synonyms:
  - -f
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: IG format (off), turn off column index.
  synonyms:
  - -I
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: Toggle output column indices (on).
  synonyms:
  - -c
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: Toggle sequence index (number) on each line (off).
  synonyms:
  - -n
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: Toggle sequence ID to each line (on).
  synonyms:
  - -i
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: (max-inserts)  Max number of insertions printed (2000000). -m0 Do not
    print insertions. -m-4 Print insert lengths with 4 digits.
  synonyms:
  - -m
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: (linelength)   Characters per line (70).
  synonyms:
  - -l
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: (space char)   Char to show inserts (`.').
  synonyms:
  - -s
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: (max length)   Maximum length of input lines (30000).
  synonyms:
  - -L
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: Output in a BLAST-style format (off).
  synonyms:
  - -b
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: (sequence id)  Reference sequence for -b switch (first sequence)
  synonyms:
  - -R
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: (n1,n2,...,nN) How to number the first residue in each sequence when
    using BLAST-style output (1,1,1,...,1)
  synonyms:
  - -N
  args: !EmptyFlagArg {}
  optional: true
command:
- prettyalign
parent:
subcommands: []
help_flag:
usage_flag:
version_flag:
help_text: "SAM: prettyalign v3.5 (July 15, 2005) compiled 12/08/06_16:15:56\n\nUsage:\
  \ prettyalign filename [options] > align.pretty\n\n       Input in the format produced\
  \ by align2model.\n       The pretty alignment is sent to stdout.\n\nOptions:  \
  \               Meaning  (current value)\n       -f                Output in a FASTA-like\
  \ format (off).\n       -I                IG format (off), turn off column index.\n\
  \       -c                Toggle output column indices (on).\n       -n        \
  \        Toggle sequence index (number) on each line (off).\n       -i         \
  \       Toggle sequence ID to each line (on).\n       -m (max-inserts)  Max number\
  \ of insertions printed (2000000).\n                         -m0 Do not print insertions.\n\
  \                         -m-4 Print insert lengths with 4 digits.\n       -l (linelength)\
  \   Characters per line (70).\n       -s (space char)   Char to show inserts (`.').\n\
  \       -L (max length)   Maximum length of input lines (30000).\n       -b    \
  \            Output in a BLAST-style format (off).\n       -R (sequence id)  Reference\
  \ sequence for -b switch (first sequence)\n       -N (n1,n2,...,nN) How to number\
  \ the first residue in each sequence\n                         when using BLAST-style\
  \ output (1,1,1,...,1)\n"
generated_using:
- --help
