!Command
positional:
- !Positional
  description: ''
  position: 0
  name: Required Parameters ...
  optional: false
named:
- !Flag
  description: <organism name, ok to use 'null'>
  synonyms:
  - -org
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: (Use gene_id instead of transcript_id to identify the transcripts from
    GTF files)
  synonyms:
  - -gid
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: (Use transcript_id instead of gene_id to identify the transcripts from
    GTF files, default)
  synonyms:
  - -tid
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: (gff3 annotations from ensembl usually have repeat definitions too)
  synonyms:
  - -ensemblRepeats
  args: !SimpleFlagArg
    name: GFF3 file
  optional: true
command:
- loadGenome.pl
parent:
subcommands: []
help_flag:
usage_flag:
version_flag:
help_text: "\tCurrent Settings:\n\t\tGenome name = \n\t\tFASTA file = \n\t\tGTF file\
  \ =  \n\t\tOrganism = \n\t\tVersion = custom\n\n\tGenome will be stored in directory:\n\
  \t\t/tmp/tmpyjsfxisi/share/homer-4.11-1/.//data/genomes//\n!! Options -name, -fasta,\
  \ -gtf, and -org are REQUIRED !!\n\n\tProgram will prepare a custom genome for use\
  \ with HOMER\n\n\tUsage: loadGenome.pl <Required Parameters ...> [options]\n\n\t\
  NOTE: If your genome is available at UCSC, consider using the update scripts\n\t\
  \tlocated in the /tmp/tmpyjsfxisi/share/homer-4.11-1/.//update directory\n\n\tRequired\
  \ Parameters:\n\t\t-name <genome name> (i.e. hg19, tair10, etc.)\n\t\t-fasta <genome\
  \ fasta file> (Single genome sequence, preferrabley soft masked, unzipped)\n\t\t\
  -gtf <gene annotation file> (Transcript annotation in gtf format, -gff/-gff3 to\
  \ use them)\n\t\t\t(Always best to use a gtf file whenever possible, gffs can be\
  \ organized differently...)\n\t\t-org <organism name, ok to use 'null'>\n\n\tOther\
  \ options:\n\t\t-force (Overwrite any existing genome with the same name)\n\t\t\
  -promoters <promoter set name> (Create promoter set with genome and gtf files)\n\
  \t\t\t-id <idtype> (options: gene, refseq, unigene, ensembl, or custom, default:\
  \ custom)\n\t\t-version <version id> (Assign version, versions starting with 'v'\
  \ are managed\n\t\t\tby the configureHomer.pl script and my be overwritten, default:\
  \ custom)\n\t\t-gid (Use gene_id instead of transcript_id to identify the transcripts\
  \ from GTF files)\n\t\t-tid (Use transcript_id instead of gene_id to identify the\
  \ transcripts from GTF files, default)\n\t\t-ensemblRepeats <GFF3 file> (gff3 annotations\
  \ from ensembl usually have repeat definitions too)\n\n"
generated_using:
- --help
