!Command
positional: []
named: []
command:
- pyfasta
parent:
subcommands: []
help_flag:
usage_flag:
version_flag:
help_text: "--help not a valid action\n\n    available actions:\n        `extract`:\
  \ extract sequences from a fasta file\n        `info`: show info about the fasta\
  \ file and exit.\n        `split`: split a large fasta file into separate files\n\
  \                 and/or into K-mers.\n        `flatten`: flatten a fasta file inplace\
  \ so that later\n                   command-line (and programmattic) access via\n\
  \                   pyfasta will use the inplace flattened version\n           \
  \        rather than creating another .flat copy of the\n                   sequence.\n\
  \n    to view the help for a particular action, use:\n        pyfasta [action] --help\n\
  \    e.g.:\n        pyfasta extract --help\n    \n"
generated_using:
- --help
