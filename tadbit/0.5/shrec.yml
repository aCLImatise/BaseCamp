!Command
positional: []
named:
- !Flag
  description: matrix file.
  synonyms:
  - -i
  - --infile
  args: !SimpleFlagArg
    name: FNAM
  optional: true
- !Flag
  description: generate cmm and xyz files
  synonyms:
  - --cmm
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: generate distance matrix file
  synonyms:
  - --mtx
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: display matplotlib 3D plot
  synonyms:
  - --show
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: input is already a distance matrix
  synonyms:
  - --dist
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: print process info
  synonyms:
  - --verbose
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: Replace zeros by sum of the matrix divided by the square of the number
    of rows (recommended for binary matrices). By default it uses half the minimum
    value
  synonyms:
  - --strict
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: '[0] Average distance (nm) beetween two particles; by default no scalling
    is applied'
  synonyms:
  - --scale
  args: !SimpleFlagArg
    name: SCALE
  optional: true
- !Flag
  description: '[numpy] implementation to search shortest path using Floyd-Warshall
    can be one of "numpy", "cython" or "cythonP" (parallel version for very large
    matrices)'
  synonyms:
  - --fw
  args: !SimpleFlagArg
    name: FW
  optional: true
command:
- shrec.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "usage: shrec.py -i PATH [options]\n\noptional arguments:\n  -h, --help\
  \            show this help message and exit\n  -i FNAM, --infile FNAM\n       \
  \                 matrix file.\n  --cmm                 generate cmm and xyz files\n\
  \  --mtx                 generate distance matrix file\n  --show               \
  \ display matplotlib 3D plot\n  --dist                input is already a distance\
  \ matrix\n  --verbose             print process info\n  --strict              Replace\
  \ zeros by sum of the matrix divided by the\n                        square of the\
  \ number of rows (recommended for binary\n                        matrices). By\
  \ default it uses half the minimum value\n  --scale SCALE         [0] Average distance\
  \ (nm) beetween two particles; by\n                        default no scalling is\
  \ applied\n  --fw FW               [numpy] implementation to search shortest path\
  \ using\n                        Floyd-Warshall can be one of \"numpy\", \"cython\"\
  \ or\n                        \"cythonP\" (parallel version for very large matrices)\n"
generated_using:
- --help
