!Command
command:
- HomologyByXCorr
positional: []
named:
- !Flag
  description: '<string> : query fasta sequence'
  synonyms:
  - -q
  args: !EmptyFlagArg {}
  optional: true
parent:
subcommands: []
usage: []
help_flag:
usage_flag:
version_flag:
help_text: "\nInvalid command-line arg: --help\n\nHomologyByXCorr: Compares two sequences\
  \ via cross-correlation\n\n\nAvailable arguments:\n\n-q<string> : query fasta sequence\n\
  \0-t<string> : target fasta sequence\n\0-cq<int> : query sequence id (def=-1)\n\0\
  -ct<int> : target sequence id (def=-1)\n\0-l<int> : minimum alignment length (def=0)\n\
  \0-t_chunk<int> : target chunk size (def=4096)\n\0-q_chunk<int> : query chunk size\
  \ (def=4096)\n\0-o<int> : output file (binary)\n\0-nblocks<int> : total number of\
  \ blocks to process (when parallel) (def=0)\n\0-block<int> : block # to process\
  \ (when parallel) (def=0)\n\0-nblocks_query<int> : total number of QUERY blocks\
  \ to process (when parallel) (def=0)\n\0-block_query<int> : QUERY block # to process\
  \ (when parallel) (def=0)\n\0-same_only<bool> : only align sequences that have the\
  \ same name. (def=0)\n\0-min_prob<double> : minimum probability to keep match (def=0.9999)\n\
  \0-guide<string> : XCorr input file (binary) to guide alignments (def=)\n\0-proteins<bool>\
  \ : translate seqeunces to proteins when aligning. (def=0)\n\0-cutoff<double> :\
  \ signal selection cutoff (def=1.8)\n\0-chain<bool> : chain matches before writing\
  \ them out (def=0)\n\0-select<string> : selection file (def=)\n\0-pairs<string>\
  \ : valid block pairs to process (def=)\n\0-line<double> : pick one block and compare\
  \ to the query (def=-1)\n\0\n"
generated_using:
- --help
