!Command
positional: []
named: []
command:
- xpath
parent:
subcommands: []
help_flag:
usage_flag:
version_flag:
help_text: "Unknown option ignore: --helpUsage:\n/tmp/tmplye26cr2/bin/xpath [options]\
  \ -e query [-e query...] [filename...]\n\nIf no filenames are given, supply XML\
  \ on STDIN. You must provide at\nleast one query. Each supplementary query is done\
  \ in order, the\nprevious query giving the context of the next one.\n\nOptions:\n\
  \n-q quiet, only output the resulting PATH.\n-s suffix, use suffix instead of linefeed.\n\
  -p postfix, use prefix instead of nothing.\n-n Don't use an external DTD.\n"
generated_using:
- --help
