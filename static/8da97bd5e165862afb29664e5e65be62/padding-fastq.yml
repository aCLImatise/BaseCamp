!Command
positional: []
named:
- !Flag
  description: The input file (in FASTQ format) containing the short reads to be processed.
  synonyms:
  - -i
  - --input
  args: !SimpleFlagArg
    name: INPUT_FILENAME
  optional: true
- !Flag
  description: The output FASTQ file containing the short reads where the reads, shorted
    than '--size', are padding using ' --nucleotide'.
  synonyms:
  - -o
  - --output
  args: !SimpleFlagArg
    name: OUTPUT_FILENAME
  optional: true
- !Flag
  description: The character to be use for padding. Default is 'N'.
  synonyms:
  - -n
  - --nucleotide
  args: !SimpleFlagArg
    name: NUCLEOTIDE
  optional: true
- !Flag
  description: If if this is larger than zero then all the short reads strictly shorter
    than this threshold will be padded. Reads longer than the threshold will be trimmed
    from 3 end. Default is '0'.
  synonyms:
  - -s
  - --size
  args: !SimpleFlagArg
    name: SIZE
  optional: true
command:
- padding-fastq.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: show program's version number and exit
  synonyms:
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "Usage: padding-fastq.py [options]\n\nIt takes a FASTQ file and padds its\
  \ reads which are shorter than a given size\nwith a given nucleotide.\n\nOptions:\n\
  \  --version             show program's version number and exit\n  -h, --help  \
  \          show this help message and exit\n  -i INPUT_FILENAME, --input=INPUT_FILENAME\n\
  \                        The input file (in FASTQ format) containing the short\n\
  \                        reads to be processed.\n  -o OUTPUT_FILENAME, --output=OUTPUT_FILENAME\n\
  \                        The output FASTQ file containing the short reads where\n\
  \                        the reads, shorted than '--size', are padding using '\n\
  \                        --nucleotide'.\n  -n NUCLEOTIDE, --nucleotide=NUCLEOTIDE\n\
  \                        The character to be use for padding. Default is 'N'.\n\
  \  -s SIZE, --size=SIZE  If if this is larger than zero then all the short\n   \
  \                     reads strictly shorter than this threshold will be\n     \
  \                   padded. Reads longer than the threshold will be\n          \
  \              trimmed from 3 end. Default is '0'.\n"
generated_using:
- --help
