!Command
command:
- parsec
- tools
- upload_file
positional:
- !Positional
  optional: false
  position: 0
  name: auto
  description: --space_to_tab    whether to convert spaces to tabs. Default is ``False``.
named:
- !Flag
  optional: true
  synonyms:
  - --dbkey
  description: (optional) genome dbkey
  args: !SimpleFlagArg
    name: TEXT
- !Flag
  optional: true
  synonyms:
  - --file_name
  description: (optional) name of the new history dataset
  args: !SimpleFlagArg
    name: TEXT
- !Flag
  optional: true
  synonyms:
  - --file_type
  description: (optional) Galaxy datatype for the new dataset, default is
  args: !SimpleFlagArg
    name: TEXT
- !Flag
  optional: true
  synonyms:
  - --to_posix_lines
  description: "if ``True`` (the default), convert universal line endings\nto POSIX\
    \ line endings. Set to ``False`` when uploading a\ngzip, bz2 or zip archive containing\
    \ a binary file"
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: Show this message and exit.
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "Usage: parsec tools upload_file [OPTIONS] PATH HISTORY_ID\n\n  Upload\
  \ the file specified by ``path`` to the history specified by\n  ``history_id``.\n\
  \n  Output:\n\n      Information about the created upload job\n\nOptions:\n  --dbkey\
  \ TEXT      (optional) genome dbkey\n  --file_name TEXT  (optional) name of the\
  \ new history dataset\n  --file_type TEXT  (optional) Galaxy datatype for the new\
  \ dataset, default is\n                    auto\n\n  --space_to_tab    whether to\
  \ convert spaces to tabs. Default is ``False``.\n                    Applicable\
  \ only if to_posix_lines is ``True``\n\n  --to_posix_lines  if ``True`` (the default),\
  \ convert universal line endings\n                    to POSIX line endings. Set\
  \ to ``False`` when uploading a\n                    gzip, bz2 or zip archive containing\
  \ a binary file\n\n  -h, --help        Show this message and exit.\n"
generated_using:
- --help
docker_image: quay.io/biocontainers/galaxy-parsec:1.15.0--pyh5e36f6f_0
