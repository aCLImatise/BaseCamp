!Command
command:
- assembly.py
- modify_contig
positional:
- !Positional
  optional: false
  position: 0
  name: rsealfon.
  description: 'positional arguments:'
- !Positional
  optional: false
  position: 1
  name: input
  description: "input alignment of reference and contig (should\ncontain exactly 2\
    \ sequences)"
- !Positional
  optional: false
  position: 2
  name: output
  description: Destination file for modified contigs
- !Positional
  optional: false
  position: 3
  name: ref
  description: reference sequence name (exact match required)
named:
- !Flag
  optional: true
  synonyms:
  - -n
  - --name
  description: 'fasta header output name (default: existing header)'
  args: !SimpleFlagArg
    name: NAME
- !Flag
  optional: true
  synonyms:
  - -cn
  - --call-reference-ns
  description: "should the reference sequence be called if there is an\nN in the contig\
    \ and a more specific base in the\nreference (default: False)"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -t
  - --trim-ends
  description: "should ends of contig.fasta be trimmed to length of\nreference (default:\
    \ False)"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -r5
  - --replace-5ends
  description: "should the 5'-end of contig.fasta be replaced by\nreference (default:\
    \ False)"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -r3
  - --replace-3ends
  description: "should the 3'-end of contig.fasta be replaced by\nreference (default:\
    \ False)"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -l
  - --replace-length
  description: "length of ends to be replaced (if replace-ends is yes)\n(default:\
    \ 10)"
  args: !SimpleFlagArg
    name: REPLACE_LENGTH
- !Flag
  optional: true
  synonyms:
  - -f
  - --format
  description: 'Format for input alignment (default: fasta)'
  args: !SimpleFlagArg
    name: FORMAT
- !Flag
  optional: true
  synonyms:
  - -r
  - --replace-end-gaps
  description: "Replace gaps at the beginning and end of the sequence\nwith reference\
    \ sequence (default: False)"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -rn
  - --remove-end-ns
  description: "Remove leading and trailing N's in the contig\n(default: False)"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -ca
  - --call-reference-ambiguous
  description: "should the reference sequence be called if the contig\nseq is ambiguous\
    \ and the reference sequence is more\ninformative & consistant with the ambiguous\
    \ base (ie\nY->C) (default: False)"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --tmp_dir
  description: 'Base directory for temp files. [default: /tmp]'
  args: !SimpleFlagArg
    name: TMP_DIR
- !Flag
  optional: true
  synonyms:
  - --tmp_dirKeep
  description: "Keep the tmp_dir if an exception occurs while running.\nDefault is\
    \ to delete all temp files at the end, even\nif there's a failure."
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --loglevel
  description: 'Verboseness of output. [default: DEBUG]'
  args: !ChoiceFlagArg
    choices: !!set
      INFO:
      DEBUG:
      CRITICAL:
      WARNING:
      ERROR:
      EXCEPTION:
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: show this help message and exit
  args: !EmptyFlagArg {}
usage_flag:
version_flag: !Flag
  optional: true
  synonyms:
  - --version
  - -V
  description: show program's version number and exit
  args: !EmptyFlagArg {}
help_text: "usage: assembly.py subcommand modify_contig [-h] [-n NAME] [-cn] [-t]\
  \ [-r5]\n                                            [-r3] [-l REPLACE_LENGTH]\n\
  \                                            [-f FORMAT] [-r] [-rn] [-ca]\n    \
  \                                        [--tmp_dir TMP_DIR]\n                 \
  \                           [--tmp_dirKeep]\n                                  \
  \          [--loglevel {DEBUG,INFO,WARNING,ERROR,CRITICAL,EXCEPTION}]\n        \
  \                                    [--version]\n                             \
  \               input output ref\n\nModifies an input contig. Depending on the options\
  \ selected, can replace N\ncalls with reference calls, replace ambiguous calls with\
  \ reference calls, trim\nto the length of the reference, replace contig ends with\
  \ reference calls, and\ntrim leading and trailing Ns. Author: rsealfon.\n\npositional\
  \ arguments:\n  input                 input alignment of reference and contig (should\n\
  \                        contain exactly 2 sequences)\n  output                Destination\
  \ file for modified contigs\n  ref                   reference sequence name (exact\
  \ match required)\n\noptional arguments:\n  -h, --help            show this help\
  \ message and exit\n  -n NAME, --name NAME  fasta header output name (default: existing\
  \ header)\n  -cn, --call-reference-ns\n                        should the reference\
  \ sequence be called if there is an\n                        N in the contig and\
  \ a more specific base in the\n                        reference (default: False)\n\
  \  -t, --trim-ends       should ends of contig.fasta be trimmed to length of\n \
  \                       reference (default: False)\n  -r5, --replace-5ends  should\
  \ the 5'-end of contig.fasta be replaced by\n                        reference (default:\
  \ False)\n  -r3, --replace-3ends  should the 3'-end of contig.fasta be replaced\
  \ by\n                        reference (default: False)\n  -l REPLACE_LENGTH, --replace-length\
  \ REPLACE_LENGTH\n                        length of ends to be replaced (if replace-ends\
  \ is yes)\n                        (default: 10)\n  -f FORMAT, --format FORMAT\n\
  \                        Format for input alignment (default: fasta)\n  -r, --replace-end-gaps\n\
  \                        Replace gaps at the beginning and end of the sequence\n\
  \                        with reference sequence (default: False)\n  -rn, --remove-end-ns\
  \  Remove leading and trailing N's in the contig\n                        (default:\
  \ False)\n  -ca, --call-reference-ambiguous\n                        should the\
  \ reference sequence be called if the contig\n                        seq is ambiguous\
  \ and the reference sequence is more\n                        informative & consistant\
  \ with the ambiguous base (ie\n                        Y->C) (default: False)\n\
  \  --tmp_dir TMP_DIR     Base directory for temp files. [default: /tmp]\n  --tmp_dirKeep\
  \         Keep the tmp_dir if an exception occurs while running.\n             \
  \           Default is to delete all temp files at the end, even\n             \
  \           if there's a failure.\n  --loglevel {DEBUG,INFO,WARNING,ERROR,CRITICAL,EXCEPTION}\n\
  \                        Verboseness of output. [default: DEBUG]\n  --version, -V\
  \         show program's version number and exit\n"
generated_using:
- --help
docker_image:
