!Command
command:
- meme-rename
positional:
- !Positional
  optional: false
  position: 0
  name: Options
  description: "<dir>+          full path of MEME Suite program output directory\n\
    [-d <dest>]     destination directory; default: working directory\n[-h]      \
    \      print this message\nCopy HTML files from MEME Suite programs to files with\
    \ distinct names to\nmake it easier to share them:\n<dest>/<program>.<last_dir>[.<arg_order_num>].html\n\
    where <last_dir> is the last directory on the path, and <program> is the\nname\
    \ of the MEME Suite program. If the <program>.<last_dir> combination is\nnot unique\
    \ then a number based on the argument order is added.\n"
named: []
parent:
subcommands: []
usage: []
help_flag:
usage_flag:
version_flag:
help_text: "Usage:\n    meme-rename [options] <dir>+\n\n      Options\n        <dir>+\
  \          full path of MEME Suite program output directory\n        [-d <dest>]\
  \     destination directory; default: working directory \n        [-h]         \
  \   print this message\n\n        Copy HTML files from MEME Suite programs to files\
  \ with distinct names to \n        make it easier to share them:\n             \
  \   <dest>/<program>.<last_dir>[.<arg_order_num>].html\n        where <last_dir>\
  \ is the last directory on the path, and <program> is the\n        name of the MEME\
  \ Suite program. If the <program>.<last_dir> combination is\n        not unique\
  \ then a number based on the argument order is added.\n\n"
generated_using:
- --help
docker_image:
