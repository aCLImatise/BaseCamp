!Command
positional: []
named:
- !Flag
  description: The first FASTQ input file containing the short reads which are interleaved.
  synonyms:
  - -i
  - --input
  args: !SimpleFlagArg
    name: INPUT_FILENAME
  optional: true
- !Flag
  description: The output FASTQ file where the forward short reads from the input
    FASTQ.
  synonyms:
  - -f
  - --forward
  args: !SimpleFlagArg
    name: OUTPUT_FORWARD_FILENAME
  optional: true
- !Flag
  description: The output FASTQ file where the reverse short reads from the input
    FASTQ.
  synonyms:
  - -r
  - --reverse
  args: !SimpleFlagArg
    name: OUTPUT_REVERSE_FILENAME
  optional: true
command:
- unshuffle.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: show program's version number and exit
  synonyms:
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "Usage: unshuffle.py [options]\n\nIt un-interleaves reads from an input\
  \ FASTQ files where the reads are\ninterleaved into two output FASTQ file.\n\nOptions:\n\
  \  --version             show program's version number and exit\n  -h, --help  \
  \          show this help message and exit\n  -i INPUT_FILENAME, --input=INPUT_FILENAME\n\
  \                        The first FASTQ input file containing the short reads\n\
  \                        which are interleaved.\n  -f OUTPUT_FORWARD_FILENAME, --forward=OUTPUT_FORWARD_FILENAME\n\
  \                        The output FASTQ file where the forward short reads\n \
  \                       from the input FASTQ.\n  -r OUTPUT_REVERSE_FILENAME, --reverse=OUTPUT_REVERSE_FILENAME\n\
  \                        The output FASTQ file where the reverse short reads\n \
  \                       from the input FASTQ.\n"
generated_using:
- --help
