!Command
command:
- assembly.py
- trim_rmdup_subsamp
positional:
- !Positional
  optional: false
  position: 0
  name: inBam
  description: Input reads, unaligned BAM format.
- !Positional
  optional: false
  position: 1
  name: clipDb
  description: Trimmomatic clip DB.
- !Positional
  optional: false
  position: 2
  name: outBam
  description: "Output reads, unaligned BAM format (currently, read\ngroups and other\
    \ header information are destroyed in\nthis process)."
named:
- !Flag
  optional: true
  synonyms:
  - --n_reads
  description: "Subsample reads to no more than this many individual\nreads. Note\
    \ that paired reads are given priority, and\nunpaired reads are included to reach\
    \ the count if\nthere are too few paired reads to reach n_reads.\n(default 100000)"
  args: !SimpleFlagArg
    name: N_READS
- !Flag
  optional: true
  synonyms:
  - --loglevel
  description: 'Verboseness of output. [default: DEBUG]'
  args: !ChoiceFlagArg
    choices: !!set
      INFO:
      DEBUG:
      CRITICAL:
      WARNING:
      ERROR:
      EXCEPTION:
- !Flag
  optional: true
  synonyms:
  - --tmp_dir
  description: 'Base directory for temp files. [default: /tmp]'
  args: !SimpleFlagArg
    name: TMP_DIR
- !Flag
  optional: true
  synonyms:
  - --tmp_dirKeep
  description: "Keep the tmp_dir if an exception occurs while running.\nDefault is\
    \ to delete all temp files at the end, even\nif there's a failure.\n"
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: show this help message and exit
  args: !EmptyFlagArg {}
usage_flag:
version_flag: !Flag
  optional: true
  synonyms:
  - --version
  - -V
  description: show program's version number and exit
  args: !EmptyFlagArg {}
help_text: "usage: assembly.py subcommand trim_rmdup_subsamp [-h] [--n_reads N_READS]\n\
  \                                                 [--loglevel {DEBUG,INFO,WARNING,ERROR,CRITICAL,EXCEPTION}]\n\
  \                                                 [--version]\n                \
  \                                 [--tmp_dir TMP_DIR]\n                        \
  \                         [--tmp_dirKeep]\n                                    \
  \             inBam clipDb outBam\n\nTake reads through Trimmomatic, Prinseq, and\
  \ subsampling. This should probably\nmove over to read_utils or taxon_filter.\n\n\
  positional arguments:\n  inBam                 Input reads, unaligned BAM format.\n\
  \  clipDb                Trimmomatic clip DB.\n  outBam                Output reads,\
  \ unaligned BAM format (currently, read\n                        groups and other\
  \ header information are destroyed in\n                        this process).\n\n\
  optional arguments:\n  -h, --help            show this help message and exit\n \
  \ --n_reads N_READS     Subsample reads to no more than this many individual\n \
  \                       reads. Note that paired reads are given priority, and\n\
  \                        unpaired reads are included to reach the count if\n   \
  \                     there are too few paired reads to reach n_reads.\n       \
  \                 (default 100000)\n  --loglevel {DEBUG,INFO,WARNING,ERROR,CRITICAL,EXCEPTION}\n\
  \                        Verboseness of output. [default: DEBUG]\n  --version, -V\
  \         show program's version number and exit\n  --tmp_dir TMP_DIR     Base directory\
  \ for temp files. [default: /tmp]\n  --tmp_dirKeep         Keep the tmp_dir if an\
  \ exception occurs while running.\n                        Default is to delete\
  \ all temp files at the end, even\n                        if there's a failure.\n"
generated_using:
- --help
docker_image:
