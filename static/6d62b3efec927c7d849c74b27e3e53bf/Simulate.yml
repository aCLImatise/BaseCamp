!Command
command:
- Simulate
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - -d
  - -distribution
  description: "what kind of distribution to use for generating read\ncounts? Currently\
    \ supports Poisson, NegativeBinomial,\nZeroTruncatedPoisson,\nZeroTruncatedNegativeBinomial,\
    \ PoissonRegression,\nNegativeBinomialRegression,\nZeroTruncatedPoissonRegression,\n\
    ZeroTruncatedNegativeBinomialRegression"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -n
  - -numPoints
  description: number of locations with at least a single read mapping.
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -v
  - -VERBOSE
  description: output additional messages about run to stderr if set
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -s
  - -seed
  description: "seed (positive integer) for random numbers (defaults to\nsystem time)"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -r
  - -countFilename
  description: filename to write counts to
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -c
  - -covFilename
  description: "filename(s) to write covariates to (if you have more than\n1, wrap\
    \ in quotes)"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -f
  - -format
  description: "format for the counts file. Options are BED_BINNED\n(default), BED_UNBINNED"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -about
  description: print about message
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag:
usage_flag:
version_flag:
help_text: "Usage: Simulate [OPTIONS]\n\nOptions:\n  -d, -distribution   what kind\
  \ of distribution to use for generating read \n                      counts? Currently\
  \ supports Poisson, NegativeBinomial, \n                      ZeroTruncatedPoisson,\
  \ \n                      ZeroTruncatedNegativeBinomial, PoissonRegression, \n \
  \                     NegativeBinomialRegression, \n                      ZeroTruncatedPoissonRegression,\
  \ \n                      ZeroTruncatedNegativeBinomialRegression \n  -n, -numPoints\
  \      number of locations with at least a single read mapping. \n  -v, -VERBOSE\
  \        output additional messages about run to stderr if set \n  -s, -seed   \
  \        seed (positive integer) for random numbers (defaults to \n            \
  \          system time) \n  -r, -countFilename  filename to write counts to \n \
  \ -c, -covFilename    filename(s) to write covariates to (if you have more than\
  \ \n                      1, wrap in quotes) \n  -f, -format         format for\
  \ the counts file. Options are BED_BINNED \n                      (default), BED_UNBINNED\
  \ \n\nHelp options:\n  -?, -help           print this help message \n      -about\
  \          print about message \n\n"
generated_using: []
docker_image:
