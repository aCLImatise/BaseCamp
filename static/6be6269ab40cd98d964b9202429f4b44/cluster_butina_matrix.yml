!Command
positional: []
named:
- !Flag
  description: Input file, if not defined the STDIN is used
  synonyms:
  - -i
  - --input
  args: !SimpleFlagArg
    name: INPUT
  optional: true
- !Flag
  description: Input format. When using STDIN this must be specified.
  synonyms:
  - -if
  - --informat
  args: !ChoiceFlagArg
    choices: !!set
      sdf:
      json:
  optional: true
- !Flag
  description: Base name for output file (no extension). If not defined then SDTOUT
    is used for the structures and output is used as base name of the other files.
  synonyms:
  - -o
  - --output
  args: !SimpleFlagArg
    name: OUTPUT
  optional: true
- !Flag
  description: Output format. Defaults to 'tsv'.
  synonyms:
  - -of
  - --outformat
  args: !ChoiceFlagArg
    choices: !!set
      json:
      tsv:
  optional: true
- !Flag
  description: Write metadata and metrics files
  synonyms:
  - --meta
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: Similarity clustering threshold (1.0 means identical)
  synonyms:
  - -t
  - --threshold
  args: !SimpleFlagArg
    name: THRESHOLD
  optional: true
- !Flag
  description: Threshold for outputting values (1.0 means identical)
  synonyms:
  - -mt
  - --matrixThreshold
  args: !SimpleFlagArg
    name: MATRIXTHRESHOLD
  optional: true
- !Flag
  description: descriptor or fingerprint type (default rdkit)
  synonyms:
  - -d
  - --descriptor
  args: !ChoiceFlagArg
    choices: !!set
      maccs:
      rdkit:
      morgan2:
      morgan3:
  optional: true
- !Flag
  description: similarity metric (default tanimoto)
  synonyms:
  - -m
  - --metric
  args: !ChoiceFlagArg
    choices: !!set
      braunblanquet:
      russel:
      rogotgoldberg:
      mcconnaughey:
      sokal:
      kulczynski:
      asymmetric:
      tanimoto:
      cosine:
      dice:
  optional: true
- !Flag
  description: Quiet mode
  synonyms:
  - -q
  - --quiet
  args: !EmptyFlagArg {}
  optional: true
command:
- cluster_butina_matrix
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "usage: cluster_butina_matrix [-h] [-i INPUT] [-if {sdf,json}] [-o OUTPUT]\n\
  \                             [-of {tsv,json}] [--meta] [-t THRESHOLD]\n       \
  \                      [-mt MATRIXTHRESHOLD]\n                             [-d {maccs,morgan2,morgan3,rdkit}]\n\
  \                             [-m {asymmetric,braunblanquet,cosine,dice,kulczynski,mcconnaughey,rogotgoldberg,russel,sokal,tanimoto}]\n\
  \                             [-q]\n\nRDKit Butina Cluster Matrix\n\noptional arguments:\n\
  \  -h, --help            show this help message and exit\n  -i INPUT, --input INPUT\n\
  \                        Input file, if not defined the STDIN is used\n  -if {sdf,json},\
  \ --informat {sdf,json}\n                        Input format. When using STDIN\
  \ this must be specified.\n  -o OUTPUT, --output OUTPUT\n                      \
  \  Base name for output file (no extension). If not\n                        defined\
  \ then SDTOUT is used for the structures and\n                        output is\
  \ used as base name of the other files.\n  -of {tsv,json}, --outformat {tsv,json}\n\
  \                        Output format. Defaults to 'tsv'.\n  --meta           \
  \     Write metadata and metrics files\n  -t THRESHOLD, --threshold THRESHOLD\n\
  \                        Similarity clustering threshold (1.0 means identical)\n\
  \  -mt MATRIXTHRESHOLD, --matrixThreshold MATRIXTHRESHOLD\n                    \
  \    Threshold for outputting values (1.0 means identical)\n  -d {maccs,morgan2,morgan3,rdkit},\
  \ --descriptor {maccs,morgan2,morgan3,rdkit}\n                        descriptor\
  \ or fingerprint type (default rdkit)\n  -m {asymmetric,braunblanquet,cosine,dice,kulczynski,mcconnaughey,rogotgoldberg,russel,sokal,tanimoto},\
  \ --metric {asymmetric,braunblanquet,cosine,dice,kulczynski,mcconnaughey,rogotgoldberg,russel,sokal,tanimoto}\n\
  \                        similarity metric (default tanimoto)\n  -q, --quiet   \
  \        Quiet mode\n"
generated_using:
- --help
