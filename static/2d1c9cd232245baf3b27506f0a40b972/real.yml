!Command
positional: []
named: []
command:
- real
parent:
subcommands: []
help_flag:
usage_flag:
version_flag:
help_text: "This is real version 0.0.31\nreal is distributed under version 3.0 of\
  \ the GNU GENERAL PUBLIC LICENSE.\nMachine supports popcnt instruction.\nIgnoring\
  \ argument --help\nOptions:\n-t <textfilename>\n-p <patternfilename>\n-o <outputfilename>\n\
  -s <maximum number of errors in seed, default=2>\n-e <total maximum number of errors,\
  \ default=5>\n-l <length of seed, default=32>\n-u <search for unique match, default=1>\n\
  -f <fraction of physical memory to use, default=0.75>\n-q <use quality scores, default=1>\n\
  -Q <offset for quality scores, default=autodetect>\n-R <rewrite pattern file, default=1>\n\
  -T <number of matching threads, default=8>\n-similarity <sequence similarity, default=0.995>\n\
  -trans <transitions fraction of mutations, default=0.71>\n-gc <composition bias,\
  \ default=0.41>\n-gcmut_bias <mutability bias of G&C, default=2>\n-filter_level\
  \ <filtering level for equal hits 0-4, default=2>\nMandatory argument -t (text file\
  \ name) is not given.\n"
generated_using:
- --help
