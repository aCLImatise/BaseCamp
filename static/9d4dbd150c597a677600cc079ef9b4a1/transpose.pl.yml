!Command
command:
- transpose.pl
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - -d
  description: ": set the delimiter for the columns to DELIM (default is\ntab).\n"
  args: !SimpleFlagArg
    name: DELIM
parent:
subcommands: []
usage: []
help_flag:
usage_flag:
version_flag:
help_text: "syntax: EXE_BASE_DIR/lib/transpose.pl [OPTIONS] [FILE | < FILE]\n\nTransposes\
  \ a table -- flips the rows and columns so that what\nwere columns in the original\
  \ table become the rows and what\nwere rows in the original table become the columns.\
  \  The original\nfile is assumed to have rows delimited by newlines and columns\n\
  delimited by tabs (the column delimiter can actually be set with\nthe -d flag; see\
  \ below).\n\nFILE: a file containing a table with each row containing a tuple\n\
  \  and each field in the tuple seperated by a delimiter.  By\n  default, the delimiter\
  \ is assumed to be tab.\n\nOPTIONS are:\n-d DELIM: set the delimiter for the columns\
  \ to DELIM (default is\n          tab).\n\n\n\n"
generated_using:
- --help
docker_image:
