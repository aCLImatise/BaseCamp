!Command
command:
- nextstrain
- shell
positional:
- !Positional
  optional: false
  position: 0
  name: directory
  description: ''
named:
- !Flag
  optional: true
  synonyms:
  - --help-all
  description: Show a full help message of all options and exit
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - --help
  - -h
  description: Show a brief help message of common options and exit
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "usage: nextstrain shell [options] <directory> [...]\n       nextstrain\
  \ shell --help\n\nStart a new shell inside the Nextstrain containerized build environment\
  \ to\nrun ad-hoc commands and perform debugging.\n\nThe shell runs inside a container,\
  \ which requires Docker.  Run `nextstrain\ncheck-setup` to check if Docker is installed\
  \ and works.\n\npositional arguments:\n  <directory>       Path to pathogen build\
  \ directory\n  ...               Additional arguments to pass to the executed program\n\
  \noptional arguments:\n  --help, -h        Show a brief help message of common options\
  \ and exit\n  --help-all        Show a full help message of all options and exit\n\
  \nRun again with --help-all instead to see more options.\n"
generated_using:
- --help
docker_image: quay.io/biocontainers/nextstrain-cli:3.0.3--py_0
