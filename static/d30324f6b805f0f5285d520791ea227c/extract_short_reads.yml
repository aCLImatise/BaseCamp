!Command
positional: []
named:
- !Flag
  description: The input file in FASTQ format (Solexa). Can be given as gzipped file
    too.
  synonyms:
  - --input
  args: !SimpleFlagArg
    name: INPUT_FILENAME
  optional: true
- !Flag
  description: A text file containing on each line a name of short read which should
    be extracted from the input FASTQ file.
  synonyms:
  - --list
  args: !SimpleFlagArg
    name: INPUT_LIST_FILENAME
  optional: true
- !Flag
  description: The output FASTQ file.
  synonyms:
  - --output
  args: !SimpleFlagArg
    name: OUTPUT_FILENAME
  optional: true
- !Flag
  description: If specified then only the mate reads from the input list '--list'
    are extracted. Default is False.
  synonyms:
  - --mate
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: The size of the buffer used for keeping the list of reads ids (given
    by --list). Default is 2000000000.
  synonyms:
  - --buffer-size
  args: !SimpleFlagArg
    name: BUCKET
  optional: true
command:
- extract_short_reads.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: show program's version number and exit
  synonyms:
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "Usage: extract_short_reads.py [options]\n\nGiven a list of short read\
  \ names it extracts them from a FASTQ file.\n\nOptions:\n  --version           \
  \  show program's version number and exit\n  -h, --help            show this help\
  \ message and exit\n  --input=INPUT_FILENAME\n                        The input\
  \ file in FASTQ format (Solexa). Can be given\n                        as gzipped\
  \ file too.\n  --list=INPUT_LIST_FILENAME\n                        A text file containing\
  \ on each line a name of short\n                        read which should be extracted\
  \ from the input FASTQ\n                        file.\n  --output=OUTPUT_FILENAME\n\
  \                        The output FASTQ file.\n  --mate                If specified\
  \ then only the mate reads from the input\n                        list '--list'\
  \ are extracted. Default is False.\n  --buffer-size=BUCKET  The size of the buffer\
  \ used for keeping the list of\n                        reads ids (given by --list).\
  \ Default is 2000000000.\n"
generated_using:
- --help
