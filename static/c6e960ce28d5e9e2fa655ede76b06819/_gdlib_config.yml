!Command
command:
- _gdlib-config
positional:
- !Positional
  optional: false
  position: 0
  name: gdlib-config
  description: ''
named:
- !Flag
  optional: true
  synonyms:
  - --libdir
  description: '# directory where GD library is installed'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --includedir
  description: '# directory where GD library headers are installed'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --majorversion
  description: '# GD library major version number'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --minorversion
  description: '# GD library minor version number'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --revision
  description: '# GD library revision version number'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --ldflags
  description: '# options required for linking against GD library'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --libs
  description: '# libs required for linking against GD library'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --cflags
  description: '# options required for compiling GD library apps'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --includes
  description: '# same as --cflags'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --features
  description: "# lists features compiled into gd, separated by spaces.\n# Currently\
    \ (as of 2.2.5) the optional features\n# are GD_PNG, GD_JPEG, GD_XPM, GD_FREETYPE,\
    \ and\n# GD_FONTCONFIG. When these features are reported by\n# --features, it\
    \ is safe to include calls to the\n# related functions in your code."
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --all
  description: '# print a summary of all GD library configure options'
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag:
usage_flag:
version_flag: !Flag
  optional: true
  synonyms:
  - --version
  description: '# complete GD library version string'
  args: !EmptyFlagArg {}
help_text: "gdlib-config: warning: this script is deprecated; please use the pkg-config\
  \ file instead.\nPrint information on GD library's version, configuration, and use.\n\
  Usage: gdlib-config [options]\nOptions:\n\t--libdir          # directory where GD\
  \ library is installed\n\t--includedir      # directory where GD library headers\
  \ are installed\n\t--version         # complete GD library version string\n\t--majorversion\
  \    # GD library major version number\n\t--minorversion    # GD library minor version\
  \ number\n\t--revision        # GD library revision version number\n\t--ldflags\
  \         # options required for linking against GD library\n\t--libs          \
  \  # libs required for linking against GD library\n\t--cflags          # options\
  \ required for compiling GD library apps\n\t--includes        # same as --cflags\n\
  \t--features        # lists features compiled into gd, separated by spaces.\n  \
  \                        # Currently (as of 2.2.5) the optional features\n     \
  \                     # are GD_PNG, GD_JPEG, GD_XPM, GD_FREETYPE, and \n       \
  \                   # GD_FONTCONFIG. When these features are reported by\n     \
  \                     # --features, it is safe to include calls to the\n       \
  \                   # related functions in your code.\n\t--all             # print\
  \ a summary of all GD library configure options\n"
generated_using:
- --help
docker_image:
