!Command
command:
- readlink
positional: []
named:
- !Flag
  description: "canonicalize by following every symlink in\nevery component of the\
    \ given name recursively;\nall but the last component must exist"
  synonyms:
  - -f
  - --canonicalize
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "canonicalize by following every symlink in\nevery component of the\
    \ given name recursively,\nall components must exist"
  synonyms:
  - -e
  - --canonicalize-existing
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "canonicalize by following every symlink in\nevery component of the\
    \ given name recursively,\nwithout requirements on components existence"
  synonyms:
  - -m
  - --canonicalize-missing
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "do not output the trailing delimiter\n-q, --quiet\n-s, --silent  \
    \                suppress most error messages (on by default)\n-v, --verbose \
    \                report error messages\n-z, --zero                    end each\
    \ output line with NUL, not newline\n--help     display this help and exit\n--version\
    \  output version information and exit"
  synonyms:
  - -n
  - --no-newline
  args: !EmptyFlagArg {}
  optional: true
parent:
subcommands: []
usage: []
help_flag:
usage_flag:
version_flag:
help_text: "Usage: readlink [OPTION]... FILE...\nPrint value of a symbolic link or\
  \ canonical file name\n\n  -f, --canonicalize            canonicalize by following\
  \ every symlink in\n                                every component of the given\
  \ name recursively;\n                                all but the last component\
  \ must exist\n  -e, --canonicalize-existing   canonicalize by following every symlink\
  \ in\n                                every component of the given name recursively,\n\
  \                                all components must exist\n  -m, --canonicalize-missing\
  \    canonicalize by following every symlink in\n                              \
  \  every component of the given name recursively,\n                            \
  \    without requirements on components existence\n  -n, --no-newline          \
  \    do not output the trailing delimiter\n  -q, --quiet\n  -s, --silent       \
  \           suppress most error messages (on by default)\n  -v, --verbose      \
  \           report error messages\n  -z, --zero                    end each output\
  \ line with NUL, not newline\n      --help     display this help and exit\n    \
  \  --version  output version information and exit\n\nGNU coreutils online help:\
  \ <https://www.gnu.org/software/coreutils/>\nReport any translation bugs to <https://translationproject.org/team/>\n\
  Full documentation <https://www.gnu.org/software/coreutils/readlink>\nor available\
  \ locally via: info '(coreutils) readlink invocation'\n"
generated_using:
- --help
