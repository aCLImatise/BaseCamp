!Command
positional:
- !Positional
  description: -> prompt for databases inside directory
  position: 0
  name: directory
  optional: false
- !Positional
  description: -> also prompt for DB, but more specific (e.g. "prot*.arb")
  position: 1
  name: filemask
  optional: false
named:
- !Flag
  description: => execute macro 'macroname' after startup
  synonyms:
  - --execute
  args: !SimpleFlagArg
    name: macroname
  optional: true
command:
- arb_ntree
parent:
subcommands: []
help_flag:
usage_flag:
version_flag:
help_text: "\narb_ntree version arb-6.0.6.rev15220\n(C) 1993-2019 Lehrstuhl fuer Mikrobiologie\
  \ - TU Muenchen\nhttp://www.arb-home.de/\n(version build by: root@x86_64-conda_cos6-linux-gnu)\n\
  \nPossible usage:\n  arb_ntree               => start ARB (intro)\n  arb_ntree DB\
  \            => start ARB with DB\n  arb_ntree DB1 DB2       => merge from DB1 into\
  \ DB2\n  arb_ntree --import FILE => import FILE into new database (FILE may be a\
  \ filemask)\n\nAdditional arguments possible with command lines above:\n  --execute\
  \ macroname     => execute macro 'macroname' after startup\n\nEach DB argument may\
  \ be one of the following:\n  database.arb            -> use existing or new database\n\
  \  \":\"                     -> connect to database of a running instance of ARB\n\
  \  directory               -> prompt for databases inside directory\n  filemask\
  \                -> also prompt for DB, but more specific (e.g. \"prot*.arb\")\n\
  \n"
generated_using:
- --help
