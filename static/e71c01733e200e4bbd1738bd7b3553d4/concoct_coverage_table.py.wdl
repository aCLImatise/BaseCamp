version 1.0

task ConcoctCoverageTablepy {
  input {
    File? sample_names
    String bed_file
    String bam_files
  }
  command <<<
    concoct_coverage_table_py \
      ~{bed_file} \
      ~{bam_files} \
      ~{if defined(sample_names) then ("--samplenames " +  '"' + sample_names + '"') else ""}
  >>>
  runtime {
    docker: "None"
  }
  parameter_meta {
    sample_names: "File with sample names, one line each. Should be same\\nnr of bamfiles. Default sample names used are the file\\nnames of the bamfiles, excluding the file extension.\\n"
    bed_file: "Contigs BEDFile with four columns representing:\\n'Contig ID, Start Position, End Position and SubContig\\nID' respectively. The Subcontig ID must contain the\\npattern 'concoct_part_[0-9]*' while the contigs which\\nare not cutup cannot contain this pattern. This file\\ncan be generated by the cut_up_fasta.py script."
    bam_files: "BAM files with mappings to the original contigs."
  }
  output {
    File out_stdout = stdout()
  }
}