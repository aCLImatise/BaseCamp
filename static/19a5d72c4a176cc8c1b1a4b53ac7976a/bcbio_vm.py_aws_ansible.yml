!Command
command:
- bcbio_vm.py
- aws
- ansible
positional:
- !Positional
  optional: false
  position: 0
  name: zone
  description: "AWS availability zone to create resources in (ie. us-\neast-1a)"
named:
- !Flag
  optional: true
  synonyms:
  - --keypair
  description: 'Create new keypair for access (default: False)'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -n
  - --network
  description: "network to use for the VPC, in CIDR notation\n(a.b.c.d/e) (default:\
    \ 10.0.0.0/16)"
  args: !SimpleFlagArg
    name: NETWORK
- !Flag
  optional: true
  synonyms:
  - -c
  - --cluster
  description: "base name for created VPC resources (default: bcbio)\n"
  args: !SimpleFlagArg
    name: CLUSTER
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: show this help message and exit
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "usage: bcbio_vm.py aws ansible [-h] [--keypair] [-n NETWORK] [-c CLUSTER]\
  \ zone\n\npositional arguments:\n  zone                  AWS availability zone to\
  \ create resources in (ie. us-\n                        east-1a)\n\noptional arguments:\n\
  \  -h, --help            show this help message and exit\n  --keypair          \
  \   Create new keypair for access (default: False)\n  -n NETWORK, --network NETWORK\n\
  \                        network to use for the VPC, in CIDR notation\n        \
  \                (a.b.c.d/e) (default: 10.0.0.0/16)\n  -c CLUSTER, --cluster CLUSTER\n\
  \                        base name for created VPC resources (default: bcbio)\n"
generated_using:
- --help
docker_image:
