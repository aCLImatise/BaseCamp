!Command
positional:
- !Positional
  description: '4262'
  position: 0
  name: chr18_gl000207_random
  optional: false
named:
- !Flag
  description: Increase the BED/GFF/VCF entry -b base pairs in each direction. - (Integer)
    or (Float, e.g. 0.1) if used with -pct.
  synonyms:
  - -b
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: The number of base pairs to subtract from the start coordinate. - (Integer)
    or (Float, e.g. 0.1) if used with -pct.
  synonyms:
  - -l
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: The number of base pairs to add to the end coordinate. - (Integer)
    or (Float, e.g. 0.1) if used with -pct.
  synonyms:
  - -r
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: Define -l and -r based on strand. E.g. if used, -l 500 for a negative-stranded
    feature,  it will add 500 bp downstream.  Default = false.
  synonyms:
  - -s
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: Define -l and -r as a fraction of the feature's length. E.g. if used
    on a 1000bp feature, -l 0.50,  will add 500 bp "upstream".  Default = false.
  synonyms:
  - -pct
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: the header from the input file prior to results.
  synonyms:
  - -header
  args: !SimpleFlagArg
    name: Print
  optional: true
command:
- slopBed
subcommands: []
help_flag:
usage_flag:
version_flag:
