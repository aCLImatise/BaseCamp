!Command
command:
- bamcleanheader.py
positional:
- !Positional
  optional: false
  position: 0
  name: input
  description: SAM/BAM file to inject header lines into. If '-' or absent then defaults
    to stdin.
named:
- !Flag
  optional: true
  synonyms:
  - -S
  - --is_sam
  description: input is SAM
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: show this help message and exit
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "usage: bamcleanheader.py [-h] [-S] [input [input ...]]\n\nbamcleanheader.py\n\
  author: Author (email@site.com)\nversion: $Revision: 0.0.1 $\ndescription: remove\
  \ illegal and malformed fields from a BAM file's header\n\npositional arguments:\n\
  \  input         SAM/BAM file to inject header lines into. If '-' or absent then\
  \ defaults to stdin.\n\noptional arguments:\n  -h, --help    show this help message\
  \ and exit\n  -S, --is_sam  input is SAM\n"
generated_using:
- --help
docker_image: quay.io/biocontainers/bamkit:16.07.26--py_0
