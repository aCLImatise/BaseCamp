from datetime import datetime
from typing import List, Optional, Dict, Any

from janis_core import *
from janis_core.types.common_data_types import String, Boolean, Int, File

Align_Families_Py_V0_1_0 = CommandToolBuilder(tool="align_families.py", base_command=["align-families.py"], inputs=[ToolInput(tag="in_aligner", input_type=String(optional=True), prefix="--aligner", doc=InputDocumentation(doc="The multiple sequence aligner to use. Default: kalign")), ToolInput(tag="in_no_check_ids", input_type=Boolean(optional=True), prefix="--no-check-ids", doc=InputDocumentation(doc="Don't check to make sure read pairs have identical ids. By default, if this encounters a pair of reads in families.tsv with ids that aren't identical (minus an ending /1 or /2), it will throw an error.")), ToolInput(tag="in_processes", input_type=Int(optional=True), prefix="--processes", doc=InputDocumentation(doc="Number of worker subprocesses to use. If 0, no subprocesses will be started and everything will be done inside one process. Give 'auto' to use as many processes as there are CPU cores. Default: 0.")), ToolInput(tag="in_queue_size", input_type=Int(optional=True), prefix="--queue-size", doc=InputDocumentation(doc="How long to go accumulating responses from worker subprocesses before dealing with all of them. Default: 8 * the number of worker --processes.")), ToolInput(tag="in_phone_home", input_type=Boolean(optional=True), prefix="--phone-home", doc=InputDocumentation(doc="Report helpful usage data to the developer, to better understand the use cases and performance of the tool. The only data which will be recorded is the name and version of the tool, the size of the input data, the time and memory taken to process it, and the IP address of the machine running it. Also, if the script fails, it will report the name of the exception thrown and the line of code it occurred in. No filenames are sent, and the only parameters reported are --aligner, --processes, and --queue-size, which are necessary to evaluate performance. All the reporting and recording code is available at https://github.com/NickSto/ET.")), ToolInput(tag="in_galaxy", input_type=Boolean(optional=True), prefix="--galaxy", doc=InputDocumentation(doc="Tell the script it's running on Galaxy. Currently this only affects data reported when phoning home.")), ToolInput(tag="in_test", input_type=Boolean(optional=True), prefix="--test", doc=InputDocumentation(doc="If reporting usage data, mark this as a test run.")), ToolInput(tag="in_log_file", input_type=File(optional=True), prefix="--log-file", doc=InputDocumentation(doc="Print log messages to this file instead of to stderr. NOTE: Will overwrite the file.")), ToolInput(tag="in_read_families_dot_tsv", input_type=String(), position=0, doc=InputDocumentation(doc="The input reads, sorted into families. One line per read pair, 8 tab-delimited columns:\n1. canonical barcode\n2. barcode order ('ab' for alpha+beta, 'ba' for beta-alpha)\n3. read 1 name\n4. read 1 sequence\n5. read 1 quality scores\n6. read 2 name\n7. read 2 sequence\n8. read 2 quality scores"))], outputs=[], container="quay.io/biocontainers/dunovo:2.16--py39h38f01e4_0", version="v0.1.0")


if __name__ == "__main__":
    # or "cwl"
    Align_Families_Py_V0_1_0().translate("wdl")

