!Command
positional: []
named:
- !Flag
  description: The first FASTQ input file containing the short reads to be interleaved.
  synonyms:
  - -f
  - --input_1
  args: !SimpleFlagArg
    name: INPUT_1_FILENAME
  optional: true
- !Flag
  description: The second FASTQ input file containing the short reads to be interleaved.
  synonyms:
  - -r
  - --input_2
  args: !SimpleFlagArg
    name: INPUT_2_FILENAME
  optional: true
- !Flag
  description: The output FASTQ file where the short reads from the two input FASTQ
    files are interleaved.
  synonyms:
  - -o
  - --output
  args: !SimpleFlagArg
    name: OUTPUT_FILENAME
  optional: true
command:
- shuffle.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: show program's version number and exit
  synonyms:
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "Usage: shuffle.py [options]\n\nIt shuffles the reads into one single file\
  \ (read-A/1 from file reads1.fq,\nread-A/2 from file reads2.fq, read-B/1 from file\
  \ reads1.fq ...).\n\nOptions:\n  --version             show program's version number\
  \ and exit\n  -h, --help            show this help message and exit\n  -f INPUT_1_FILENAME,\
  \ --input_1=INPUT_1_FILENAME\n                        The first FASTQ input file\
  \ containing the short reads\n                        to be interleaved.\n  -r INPUT_2_FILENAME,\
  \ --input_2=INPUT_2_FILENAME\n                        The second FASTQ input file\
  \ containing the short reads\n                        to be interleaved.\n  -o OUTPUT_FILENAME,\
  \ --output=OUTPUT_FILENAME\n                        The output FASTQ file where\
  \ the short reads from the\n                        two input FASTQ files are interleaved.\n"
generated_using:
- --help
