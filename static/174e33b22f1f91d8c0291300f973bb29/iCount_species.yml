!Command
command:
- iCount
- species
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - -r
  - --release
  description: 'Release number. Only ENSEMBL releases 59-88 are available (default:
    88)'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -S
  - --stdout_log
  description: Threshold value (0-50) for logging to stdout. If 0, logging to stdout
    if turned OFF.
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -F
  - --file_log
  description: Threshold value (0-50) for logging to file. If 0, logging to file if
    turned OFF.
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -P
  - --file_logpath
  description: Path to log file.
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -M
  - --results_file
  description: File into which to store Metrics.
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: show this help message and exit
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "usage: iCount species [-h] [-r] [-S] [-F] [-P] [-M]\n\nGet list of available\
  \ species for given ENSEMBL release.\n\noptional arguments:\n  -h, --help      \
  \      show this help message and exit\n  -r , --release        Release number.\
  \ Only ENSEMBL releases 59-88 are available (default: 88)\n  -S , --stdout_log \
  \    Threshold value (0-50) for logging to stdout. If 0, logging to stdout if turned\
  \ OFF.\n  -F , --file_log       Threshold value (0-50) for logging to file. If 0,\
  \ logging to file if turned OFF.\n  -P , --file_logpath   Path to log file.\n  -M\
  \ , --results_file   File into which to store Metrics.\n"
generated_using:
- --help
docker_image:
