!Command
command:
- fastaxsort
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - -T
  - --tax-id-mode
  description: NCBI Taxonomic data in sequence records are IDs, not names.
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -i
  - --indentifier
  description: "Taxa are sorted using sequence identifiers (default uses whole\ndescriptions)"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -f
  - --field
  description: "Sort sequence records by values at a specific field in sequence\n\
    descriptions or identifiers. With this option, the description\nor identifier\
    \ is split into fields using strings of white space\nas field delimiters (the\
    \ default Perl delimiter for splitting\nlines of data into fields, which are invalid\
    \ characters in\nsequence identfiers).\nThis option takes a mandatory integer\
    \ option argument giving the\nindex for which field to sort by. One-based indexing\
    \ is used, so\nthe first field after the sequence identifier has index 1. As\n\
    standard in Perl, negative indices count backwards from the last\nfield; field\
    \ \"-1\" is the last field, \"-2\" is the second-to-last\netc. Sequence records\
    \ for which the specified field does not\nexist will sort on a null key."
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -S
  - --split-on-regex
  description: "Use regex <regex> to split descriptions/identifiers for the -f\noption\
    \ instead of the perl default (which splits on one or more\nwhitespace characters).\
    \ Special characters must be quoted to\nprotect them from the shell."
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -a
  - --annotate
  description: "Add FAST taxonomic addresses in dot-hex notation to sequence\nrecord\
    \ descriptions"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -j
  description: "[string] --join=[strong]\nUse [string] to append FAST taxonomic addresses\
    \ to sequence\nrecord descriptions, instead of default \" \". Use \"\\t\" to\n\
    indicate a tab-character."
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --index
  description: "of printing the sorted sequence records, print a key\nthat maps fastaxsort\
    \ taxonomically generated taxonomic addresses\nin dot-hexadecimal notation to\
    \ NCBI taxonomic names or IDs."
  args: !SimpleFlagArg
    name: Instead
- !Flag
  optional: true
  synonyms:
  - --man
  description: Print the manual page and exit.
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -l
  - --log
  description: "Creates, or appends to, a generic FAST logfile in the current\nworking\
    \ directory. The logfile records date/time of execution,\nfull command with options\
    \ and arguments, and an optional\ncomment."
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -L
  description: "[string], --logname=[string]\nUse [string] as the name of the logfile.\
    \ Default is\n\"FAST.log.txt\"."
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -C
  description: "[string], --comment=[string]\nInclude comment [string] in logfile.\
    \ No comment is saved by\ndefault."
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --format
  description: "=[format]\nUse alternative format for input. See man page for \"fasconvert\"\
    \nfor allowed formats. This is for convenience; the FAST tools are\ndesigned to\
    \ exchange data in Fasta format, and \"fasta\" is the\ndefault format for this\
    \ tool."
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -m
  description: "[dna|rna|protein], --moltype=[dna|rna|protein]\nSpecify the type of\
    \ sequence on input (should not be needed in\nmost cases, but sometimes Bioperl\
    \ cannot guess and complains\nwhen processing data)."
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -q
  - --fastq
  description: "Use fastq format as input and output.\n"
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: Print a brief help message and exit.
  args: !EmptyFlagArg {}
usage_flag:
version_flag: !Flag
  optional: true
  synonyms:
  - --version
  description: Print version information and exit.
  args: !EmptyFlagArg {}
help_text: "Usage:\n    fastaxsort [OPTION]... [NODES-FILE] [NAMES-FILE] [MULTIFASTA-FILE]...\n\
  \n    fastaxsort --tax-id-mode [OPTION]... [NODES-FILE] [MULTIFASTA-FILE]...\n\n\
  Options:\n    -T --tax-id-mode\n            NCBI Taxonomic data in sequence records\
  \ are IDs, not names.\n\n    -i --indentifier\n            Taxa are sorted using\
  \ sequence identifiers (default uses whole\n            descriptions)\n\n    -f\
  \ --field\n            Sort sequence records by values at a specific field in sequence\n\
  \            descriptions or identifiers. With this option, the description\n  \
  \          or identifier is split into fields using strings of white space\n   \
  \         as field delimiters (the default Perl delimiter for splitting\n      \
  \      lines of data into fields, which are invalid characters in\n            sequence\
  \ identfiers).\n\n            This option takes a mandatory integer option argument\
  \ giving the\n            index for which field to sort by. One-based indexing is\
  \ used, so\n            the first field after the sequence identifier has index\
  \ 1. As\n            standard in Perl, negative indices count backwards from the\
  \ last\n            field; field \"-1\" is the last field, \"-2\" is the second-to-last\n\
  \            etc. Sequence records for which the specified field does not\n    \
  \        exist will sort on a null key.\n\n    -S --split-on-regex\n           \
  \ Use regex <regex> to split descriptions/identifiers for the -f\n            option\
  \ instead of the perl default (which splits on one or more\n            whitespace\
  \ characters). Special characters must be quoted to\n            protect them from\
  \ the shell.\n\n    -a --annotate\n            Add FAST taxonomic addresses in dot-hex\
  \ notation to sequence\n            record descriptions\n\n    -j [string] --join=[strong]\n\
  \            Use [string] to append FAST taxonomic addresses to sequence\n     \
  \       record descriptions, instead of default \" \". Use \"\\t\" to\n        \
  \    indicate a tab-character.\n\n    --index Instead of printing the sorted sequence\
  \ records, print a key\n            that maps fastaxsort taxonomically generated\
  \ taxonomic addresses\n            in dot-hexadecimal notation to NCBI taxonomic\
  \ names or IDs.\n\n    -h, --help\n            Print a brief help message and exit.\n\
  \n    --man   Print the manual page and exit.\n\n    --version\n            Print\
  \ version information and exit.\n\n    -l, --log\n            Creates, or appends\
  \ to, a generic FAST logfile in the current\n            working directory. The\
  \ logfile records date/time of execution,\n            full command with options\
  \ and arguments, and an optional\n            comment.\n\n    -L [string], --logname=[string]\n\
  \            Use [string] as the name of the logfile. Default is\n            \"\
  FAST.log.txt\".\n\n    -C [string], --comment=[string]\n            Include comment\
  \ [string] in logfile. No comment is saved by\n            default.\n\n    --format=[format]\n\
  \            Use alternative format for input. See man page for \"fasconvert\"\n\
  \            for allowed formats. This is for convenience; the FAST tools are\n\
  \            designed to exchange data in Fasta format, and \"fasta\" is the\n \
  \           default format for this tool.\n\n    -m [dna|rna|protein], --moltype=[dna|rna|protein]\n\
  \            Specify the type of sequence on input (should not be needed in\n  \
  \          most cases, but sometimes Bioperl cannot guess and complains\n      \
  \      when processing data).\n\n    -q --fastq\n            Use fastq format as\
  \ input and output.\n\n"
generated_using:
- --help
docker_image: quay.io/biocontainers/perl-fast:1.06--pl526_0
