!Command
command:
- axt_to_maf.py
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - --silent
  description: ': prevents stats report'
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag:
usage_flag:
version_flag:
help_text: "missing secondary species\n\nApplication to convert AXT file to MAF file.\
  \ Reads an AXT file from standard\ninput and writes a MAF file to standard out;\
  \  some statistics are written to\nstandard error.\n\naxt_to_maf primary:lengths_file\
  \ secondary:lengths_file < axt_file > maf_file\n  --silent: prevents stats report\n\
  \n  Lengths files provide the length of each chromosome (maf format needs this\n\
  \  information but axt file does not contain it).  The format is a series of\n \
  \ lines of the form:\n\n    <chromosome name> <length>\n\n  The chromosome field\
  \ in each axt block must match some <chromosome name> in\n  the lengths file.\n\n"
generated_using:
- --help
docker_image:
