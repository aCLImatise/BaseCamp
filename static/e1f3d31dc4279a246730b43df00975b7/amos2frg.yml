!Command
command:
- amos2frg
positional:
- !Positional
  optional: false
  position: 0
  name: extension
  description: otherwise -i and -o are those specified in the command line
named: []
parent:
subcommands: []
usage: []
help_flag:
usage_flag:
version_flag:
help_text: "\n.USAGE.\n  amos2frg [-i infile] [-o outfile] [-a accession]\n\n.DESCRIPTION.\n\
  \  This program converts from an AMOS message file to a frg file (Celera\n  Assembler\
  \ message file).\n\n.OPTIONS.\n  if -i and -o are not provided reads from STDIN\
  \ and writes to STDOUT\n  if -i is provided but -o is not, outfile is same as infile\
  \ except for the\n  extension\n  otherwise -i and -o are those specified in the\
  \ command line\n  if -i is provided the filename must end in .afg\n  if -a is specified\
  \ starts numbering output at specified accession, else\n  the accessions from the\
  \ input file are used in the output\n\n.KEYWORDS.\n  converter, amos, frg\n\n"
generated_using:
- --help
docker_image:
