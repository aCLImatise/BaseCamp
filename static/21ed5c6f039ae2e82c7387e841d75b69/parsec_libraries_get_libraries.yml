!Command
command:
- parsec
- libraries
- get_libraries
positional:
- !Positional
  optional: false
  position: 0
  name: both.
  description: "Output:\nlist of dicts each containing basic information about a library"
named:
- !Flag
  optional: true
  synonyms:
  - --library_id
  description: filter for library by library id
  args: !SimpleFlagArg
    name: TEXT
- !Flag
  optional: true
  synonyms:
  - --name
  description: "If ``name`` is set and multiple names match the given\nname, all the\
    \ libraries matching the argument will be\nreturned"
  args: !SimpleFlagArg
    name: TEXT
- !Flag
  optional: true
  synonyms:
  - --deleted
  description: "If ``False`` (the default), return only non-deleted\nlibraries. If\
    \ ``True``, return only deleted libraries. If\n``None``, return both deleted and\
    \ non-deleted libraries."
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: Show this message and exit.
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "Usage: parsec libraries get_libraries [OPTIONS]\n\n  Get all the libraries\
  \ or filter for specific one(s) via the provided name\n  or ID. Provide only one\
  \ argument: ``name`` or ``library_id``, but not\n  both.\n\n  Output:\n\n      list\
  \ of dicts each containing basic information about a library\n\nOptions:\n  --library_id\
  \ TEXT  filter for library by library id\n  --name TEXT        If ``name`` is set\
  \ and multiple names match the given\n                     name, all the libraries\
  \ matching the argument will be\n                     returned\n\n  --deleted  \
  \        If ``False`` (the default), return only non-deleted\n                 \
  \    libraries. If ``True``, return only deleted libraries. If\n               \
  \      ``None``, return both deleted and non-deleted libraries.\n\n  -h, --help\
  \         Show this message and exit.\n"
generated_using:
- --help
docker_image: quay.io/biocontainers/galaxy-parsec:1.15.0--pyh5e36f6f_0
