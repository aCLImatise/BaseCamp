!Command
positional: []
named:
- !Flag
  description: The input file in FASTA format.
  synonyms:
  - -i
  - --input
  args: !SimpleFlagArg
    name: INPUT_FILENAME
  optional: true
- !Flag
  description: Any splitted file should not have the size (in bytes) over this threshold
    if that is possible. If this is set to 1 then one sequence will be written in
    each output FASTA file. Default is 2147482648.
  synonyms:
  - -t
  - --threshold
  args: !SimpleFlagArg
    name: THRESHOLD
  optional: true
- !Flag
  description: This is another way to split the input FASTA file by giving the number
    of sequences per output FASTA file. If this option is used it will over ride the
    '-- threshold'.
  synonyms:
  - -f
  - --seq_per_fasta
  args: !SimpleFlagArg
    name: SEQ_PER_FASTA
  optional: true
- !Flag
  description: A file containing the number of nucleotides which are in the input
    FASTA file. If it is not given then the file size will be used.
  synonyms:
  - -s
  - --size
  args: !SimpleFlagArg
    name: SIZE_INPUT_FASTA_FILENAME
  optional: true
- !Flag
  description: A file containing the number of sequences which are in the input FASTA
    file. If it is not given then the file size divided by 50 will be used. It is
    used/needed by '--seq_per_fasta'
  synonyms:
  - -q
  - --seqs
  args: !SimpleFlagArg
    name: SEQS_INPUT_FASTA_FILENAME
  optional: true
- !Flag
  description: The output file which contains the paths to the splitted FASTA files.
  synonyms:
  - -o
  - --output
  args: !SimpleFlagArg
    name: OUTPUT_FILENAME
  optional: true
- !Flag
  description: The output file which contains the paths to the maximum lengths of
    the splitted FASTA files.
  synonyms:
  - -x
  - --output-max-lens
  args: !SimpleFlagArg
    name: OUTPUT_MAX_LENS_FILENAME
  optional: true
command:
- split-fasta.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: show program's version number and exit
  synonyms:
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "Usage: split-fasta.py [options]\n\nIt splits a FASTA file into several\
  \ equally sized FASTA files such that all\nare below a given size threshold.\n\n\
  Options:\n  --version             show program's version number and exit\n  -h,\
  \ --help            show this help message and exit\n  -i INPUT_FILENAME, --input=INPUT_FILENAME\n\
  \                        The input file in FASTA format.\n  -t THRESHOLD, --threshold=THRESHOLD\n\
  \                        Any splitted file should not have the size (in bytes)\n\
  \                        over this threshold if that is possible. If this is\n \
  \                       set to 1 then one sequence will be written in each\n   \
  \                     output FASTA file. Default is 2147482648.\n  -f SEQ_PER_FASTA,\
  \ --seq_per_fasta=SEQ_PER_FASTA\n                        This is another way to\
  \ split the input FASTA file by\n                        giving the number of sequences\
  \ per output FASTA file.\n                        If this option is used it will\
  \ over ride the '--\n                        threshold'.\n  -s SIZE_INPUT_FASTA_FILENAME,\
  \ --size=SIZE_INPUT_FASTA_FILENAME\n                        A file containing the\
  \ number of nucleotides which are\n                        in the input FASTA file.\
  \ If it is not given then the\n                        file size will be used.\n\
  \  -q SEQS_INPUT_FASTA_FILENAME, --seqs=SEQS_INPUT_FASTA_FILENAME\n            \
  \            A file containing the number of sequences which are in\n          \
  \              the input FASTA file. If it is not given then the file\n        \
  \                size divided by 50 will be used. It is used/needed by\n       \
  \                 '--seq_per_fasta'\n  -o OUTPUT_FILENAME, --output=OUTPUT_FILENAME\n\
  \                        The output file which contains the paths to the\n     \
  \                   splitted FASTA files.\n  -x OUTPUT_MAX_LENS_FILENAME, --output-max-lens=OUTPUT_MAX_LENS_FILENAME\n\
  \                        The output file which contains the paths to the\n     \
  \                   maximum lengths of the splitted FASTA files.\n"
generated_using:
- --help
