!Command
command:
- blobtools
- bamfilter
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - -b
  - --bam
  description: BAM file (sorted by name)
  args: !SimpleFlagArg
    name: FILE
- !Flag
  optional: true
  synonyms:
  - -i
  - --include
  description: "List of contigs whose reads are included\n- writes FASTAs of pairs\
    \ where at least\none read maps sequences in list\n(InUn.fq, InIn.fq, ExIn.fq)"
  args: !SimpleFlagArg
    name: FILE
- !Flag
  optional: true
  synonyms:
  - -e
  - --exclude
  description: "List of contigs whose reads are excluded (outputs reads that do not\
    \ map to sequences in list)\n- writes FASTAs of pairs where at least\none read\
    \ does not maps to sequences in list\n(InUn.fq, InIn.fq, ExIn.fq)"
  args: !SimpleFlagArg
    name: FILE
- !Flag
  optional: true
  synonyms:
  - -U
  - --exclude_unmapped
  description: Include pairs where both reads are unmapped
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -n
  - --noninterleaved
  description: Use if fw and rev reads should be in separate files
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -f
  - --read_format
  description: 'FASTQ = fq, FASTA = fa [default: fa]'
  args: !SimpleFlagArg
    name: FORMAT
- !Flag
  optional: true
  synonyms:
  - -o
  - --out
  description: Output prefix
  args: !SimpleFlagArg
    name: PREFIX
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: show this
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "usage: blobtools bamfilter  -b FILE [-i FILE] [-e FILE] [-U] [-n] [-o\
  \ PREFIX] [-f FORMAT]\n                                [-h|--help]\n\n    Options:\n\
  \        -h --help                   show this\n        -b, --bam FILE         \
  \     BAM file (sorted by name)\n        -i, --include FILE          List of contigs\
  \ whose reads are included\n                                    - writes FASTAs\
  \ of pairs where at least\n                                        one read maps\
  \ sequences in list\n                                        (InUn.fq, InIn.fq,\
  \ ExIn.fq)\n        -e, --exclude FILE          List of contigs whose reads are\
  \ excluded (outputs reads that do not map to sequences in list)\n              \
  \                      - writes FASTAs of pairs where at least\n               \
  \                         one read does not maps to sequences in list\n        \
  \                                (InUn.fq, InIn.fq, ExIn.fq)\n        -U, --exclude_unmapped\
  \      Include pairs where both reads are unmapped\n        -n, --noninterleaved\
  \        Use if fw and rev reads should be in separate files  \n        -f, --read_format\
  \ FORMAT    FASTQ = fq, FASTA = fa [default: fa]      \n        -o, --out PREFIX\
  \            Output prefix\n"
generated_using:
- --help
docker_image:
