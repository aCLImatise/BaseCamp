!Command
command:
- pairtools
- stats
positional:
- !Positional
  optional: false
  position: 0
  name: merge.
  description: The files with paths ending with .gz/.lz4 are decompressed by pbgzip/lz4c.
named:
- !Flag
  optional: true
  synonyms:
  - -o
  - --output
  description: output stats tsv file.
  args: !SimpleFlagArg
    name: TEXT
- !Flag
  optional: true
  synonyms:
  - --merge
  description: "If specified, merge multiple input stats files instead\nof calculating\
    \ statistics of a .pairs/.pairsam file.\nMerging is performed via summation of\
    \ all overlapping\nstatistics. Non-overlapping statistics are appended to\nthe\
    \ end of the file."
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --nproc-in
  description: "Number of processes used by the auto-guessed input\ndecompressing\
    \ command.  [default: 3]"
  args: !SimpleFlagArg
    name: INTEGER
- !Flag
  optional: true
  synonyms:
  - --nproc-out
  description: "Number of processes used by the auto-guessed output\ncompressing command.\
    \  [default: 8]"
  args: !SimpleFlagArg
    name: INTEGER
- !Flag
  optional: true
  synonyms:
  - --cmd-in
  description: "A command to decompress the input file. If provided,\nfully overrides\
    \ the auto-guessed command. Does not work\nwith stdin. Must read input from stdin\
    \ and print output\ninto stdout. EXAMPLE: pbgzip -dc -n 3"
  args: !SimpleFlagArg
    name: TEXT
- !Flag
  optional: true
  synonyms:
  - --cmd-out
  description: "A command to compress the output file. If provided,\nfully overrides\
    \ the auto-guessed command. Does not work\nwith stdout. Must read input from stdin\
    \ and print\noutput into stdout. EXAMPLE: pbgzip -c -n 8"
  args: !SimpleFlagArg
    name: TEXT
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: Show this message and exit.
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "Usage: pairtools stats [OPTIONS] [INPUT_PATH]...\n\n  Calculate pairs\
  \ statistics.\n\n  INPUT_PATH : by default, a .pairs/.pairsam file to calculate\
  \ statistics.\n  If not provided, the input is read from stdin. If --merge is specified,\n\
  \  then INPUT_PATH is interpreted as an arbitrary number  of stats files to\n  merge.\n\
  \n  The files with paths ending with .gz/.lz4 are decompressed by pbgzip/lz4c.\n\
  \nOptions:\n  -o, --output TEXT    output stats tsv file.\n  --merge           \
  \   If specified, merge multiple input stats files instead\n                   \
  \    of calculating statistics of a .pairs/.pairsam file.\n                    \
  \   Merging is performed via summation of all overlapping\n                    \
  \   statistics. Non-overlapping statistics are appended to\n                   \
  \    the end of the file.\n  --nproc-in INTEGER   Number of processes used by the\
  \ auto-guessed input\n                       decompressing command.  [default: 3]\n\
  \  --nproc-out INTEGER  Number of processes used by the auto-guessed output\n  \
  \                     compressing command.  [default: 8]\n  --cmd-in TEXT      \
  \  A command to decompress the input file. If provided,\n                      \
  \ fully overrides the auto-guessed command. Does not work\n                    \
  \   with stdin. Must read input from stdin and print output\n                  \
  \     into stdout. EXAMPLE: pbgzip -dc -n 3\n  --cmd-out TEXT       A command to\
  \ compress the output file. If provided,\n                       fully overrides\
  \ the auto-guessed command. Does not work\n                       with stdout. Must\
  \ read input from stdin and print\n                       output into stdout. EXAMPLE:\
  \ pbgzip -c -n 8\n  -h, --help           Show this message and exit.\n"
generated_using:
- --help
docker_image:
