!Command
command:
- GapFiller
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - --k
  description: 'length of the word used to hash (default: 12)'
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - --block-length
  description: 'length of perfect match (default: 15)'
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - --output-prefix
  description: 'output files prefix (default: "GapFiller_output")'
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - --gz
  description: compress output with gzip
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --bz2
  description: compress output with bzip2
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --seed1
  description: "seed1 fasta file (can be compressed with gzip or bzip2,\nor a pipe)"
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - --seed2
  description: "seed2 fasta file (can be compressed with gzip or bzip2,\nor a pipe)"
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - --seed-sam
  description: "seed sam file sorted by ID, with header (sam or bam\nformat; can be\
    \ repeated multiple times)"
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - --query
  description: "query fasta file: use different reads for extension\ninstead of seeds\
    \ (can be compressed with gzip or bzip2,\nor a pipe)"
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - --query-sam
  description: "query sam file: use different reads for extension\ninstead of sam\
    \ seeds (sam or bam format; can be\nrepeated multiple times)"
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - --seed-ins
  description: seed reads insert size
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - --seed-var
  description: seed reads insert variation
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - --store-layout
  description: 'store contigs layout (default: false)'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --overlap
  description: 'minimum suffix-prefix overlap (default: 30)'
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - --mismatch-rate
  description: 'maximum number of mismatches every 100 bp (default: 5)'
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - --extThreshold
  description: 'number of reads needed to extend a contig (default: 2)'
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - --limit
  description: limits the number of extended reads (useful for tests)
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - --no-read-cycle
  description: "allow reads to be used multiple times within the same\ncontig (default:\
    \ false)"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --mate-pairs
  description: 'default: paired-ends'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --verbose
  description: print a lot of information! Use with --limit option
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - --help
  description: produce help message
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "GapFiller version 2.1.2\n\nAllowed options\n  --help                produce\
  \ help message\n  --k arg               length of the word used to hash (default:\
  \ 12)\n  --block-length arg    length of perfect match (default: 15)\n  --output-prefix\
  \ arg   output files prefix (default: \"GapFiller_output\")\n  --gz            \
  \      compress output with gzip\n  --bz2                 compress output with bzip2\n\
  \  --seed1 arg           seed1 fasta file (can be compressed with gzip or bzip2,\n\
  \                        or a pipe)\n  --seed2 arg           seed2 fasta file (can\
  \ be compressed with gzip or bzip2,\n                        or a pipe)\n  --seed-sam\
  \ arg        seed sam file sorted by ID, with header (sam or bam \n            \
  \            format; can be repeated multiple times)\n  --query arg           query\
  \ fasta file: use different reads for extension \n                        instead\
  \ of seeds (can be compressed with gzip or bzip2,\n                        or a\
  \ pipe)\n  --query-sam arg       query sam file: use different reads for extension\
  \ \n                        instead of sam seeds (sam or bam format; can be \n \
  \                       repeated multiple times)\n  --seed-ins arg        seed reads\
  \ insert size\n  --seed-var arg        seed reads insert variation\n  --store-layout\
  \        store contigs layout (default: false)\n                        \n  --overlap\
  \ arg         minimum suffix-prefix overlap (default: 30)\n  --mismatch-rate arg\
  \   maximum number of mismatches every 100 bp (default: 5)\n  --extThreshold arg\
  \    number of reads needed to extend a contig (default: 2)\n  --limit arg     \
  \      limits the number of extended reads (useful for tests)\n  --no-read-cycle\
  \       allow reads to be used multiple times within the same \n               \
  \         contig (default: false)\n  --mate-pairs          default: paired-ends\n\
  \  --verbose             print a lot of information! Use with --limit option\n\n"
generated_using:
- --help
docker_image: quay.io/biocontainers/gapfiller:2.1.2--h176a8bc_1
