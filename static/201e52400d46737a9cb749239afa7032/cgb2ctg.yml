!Command
command:
- cgb2ctg
positional:
- !Positional
  optional: false
  position: 0
  name: extension
  description: "otherwise -i and -o are those specified in the command line\nif -i\
    \ is provided the filename must end in .cgb\n"
named: []
parent:
subcommands: []
usage: []
help_flag:
usage_flag:
version_flag:
help_text: "\n    cgb2ctg [-i infile] [-o outfile] -f frgfile\n\n    if -i and -o\
  \ are not provided reads from STDIN and writes to STDOUT\n    if -i is provided\
  \ but -o is not, outfile is same as infile except for the\n  extension\n    otherwise\
  \ -i and -o are those specified in the command line\n    if -i is provided the filename\
  \ must end in .cgb\n"
generated_using:
- --help
docker_image:
