!Command
positional: []
named:
- !Flag
  description: (GEM archive)
  synonyms:
  - --index
  - -I
  args: !SimpleFlagArg
    name: index_file.gem
  optional: true
- !Flag
  description: (default=stdin)
  synonyms:
  - --input
  - -i
  args: !SimpleFlagArg
    name: input_file
  optional: true
- !Flag
  description: (default=stdout)
  synonyms:
  - --output
  - -o
  args: !SimpleFlagArg
    name: output_prefix
  optional: true
command:
- gem-retriever
parent:
subcommands: []
help_flag:
usage_flag:
version_flag:
help_text: "USAGE: ./gem-retriever [ARGS]...\n    [I/O]\n      --index|-I <index_file.gem>\
  \ (GEM archive)\n      --input|-i <input_file> (default=stdin)\n      --output|-o\
  \ <output_prefix> (default=stdout)\n    [Miscellaneous]\n      --help|-h  (print\
  \ usage)\n      --version \n"
generated_using:
- --help
