version 1.0

task PhyluceAssemblyGetFastasFromMatchCounts {
  input {
    String? contigs
    String? locus_db
    String? match_count_output
    String? incomplete_matrix
    String? path_output_fasta
    String? verbosity
    String? log_path
    String? extend_locus_db
    String? extend_locus_contigs
  }
  command <<<
    phyluce_assembly_get_fastas_from_match_counts \
      ~{if defined(contigs) then ("--contigs " +  '"' + contigs + '"') else ""} \
      ~{if defined(locus_db) then ("--locus-db " +  '"' + locus_db + '"') else ""} \
      ~{if defined(match_count_output) then ("--match-count-output " +  '"' + match_count_output + '"') else ""} \
      ~{if defined(incomplete_matrix) then ("--incomplete-matrix " +  '"' + incomplete_matrix + '"') else ""} \
      ~{if defined(path_output_fasta) then ("--output " +  '"' + path_output_fasta + '"') else ""} \
      ~{if defined(verbosity) then ("--verbosity " +  '"' + verbosity + '"') else ""} \
      ~{if defined(log_path) then ("--log-path " +  '"' + log_path + '"') else ""} \
      ~{if defined(extend_locus_db) then ("--extend-locus-db " +  '"' + extend_locus_db + '"') else ""} \
      ~{if defined(extend_locus_contigs) then ("--extend-locus-contigs " +  '"' + extend_locus_contigs + '"') else ""}
  >>>
  parameter_meta {
    contigs: "The directory containing the assembled contigs in which you searched for UCE loci. (default: None)"
    locus_db: "The SQL database file holding probe matches to targeted loci (usually \"lastz/probe.matches.sqlite\"). (default: None)"
    match_count_output: "The output file containing taxa and loci in complete/incomplete matrices generated by get_match_counts.py. (default: None)"
    incomplete_matrix: "The path to the outfile for incomplete-matrix records. Required when processing an incomplete data matrix. (default: False)"
    path_output_fasta: "The path to the output FASTA file you want to create. (default: None)"
    verbosity: "The logging level to use. (default: INFO)"
    log_path: "The path to a directory to hold logs. (default: None)"
    extend_locus_db: "An SQLlite database file holding probe matches to other targeted loci. (default: None)"
    extend_locus_contigs: "A directory holding the assembled contigs (from genomes or another study) referenced by --extend- locus-db. (default: None)"
  }
}