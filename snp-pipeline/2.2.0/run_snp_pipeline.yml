!Command
positional:
- !Positional
  description: Relative or absolute path to the reference fasta file
  position: 0
  name: referenceFile
  optional: false
named:
- !Flag
  description: 'Force processing even when result files already exist and are newer
    than inputs (default: False)'
  synonyms:
  - -f
  - --force
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: 'Create a mirror copy of the reference directory and all the sample
    directories.  Use this option to avoid polluting the reference directory and sample
    directories with the intermediate files generated by the snp pipeline.  A "reference"
    subdirectory and a "samples" subdirectory are created under the output directory
    (see the -o option).  One directory per sample is created under the "samples"
    directory. Three suboptions allow a choice of how the reference and samples are
    mirrored. -m soft : creates soft links to the fasta and fastq files instead of
    copying -m hard : creates hard links to the fasta and fastq files instead of copying
    -m copy : copies the fasta and fastq files (default: None)'
  synonyms:
  - -m
  - --mirror
  args: !SimpleFlagArg
    name: MODE
  optional: true
- !Flag
  description: 'Relative or absolute path to a configuration file for overriding defaults
    and defining extra parameters for the tools and scripts within the pipeline. Note:
    A default parameter configuration file named "snppipeline.conf" is used whenever
    the pipeline is run without the -c option.  The configuration file used for each
    run is copied into the log directory, capturing the parameters used during the
    run. (default: None)'
  synonyms:
  - -c
  - --conf
  args: !SimpleFlagArg
    name: FILE
  optional: true
- !Flag
  description: '|slurm|torque, --queue_mgr grid|slurm|torque Job queue manager for
    remote parallel job execution in an HPC environment. Currently "grid", "slurm",
    and "torque" are supported. If not specified, the pipeline will execute locally.
    (default: None)'
  synonyms:
  - -Q
  args: !SimpleFlagArg
    name: grid
  optional: true
- !Flag
  description: 'Output directory for the result files. Additional subdirectories are
    automatically created under the output directory for logs files and the mirrored
    samples and reference files (see the -m option). The output directory will be
    created if it does not already exist. If not specified, the output files are written
    to the current working directory. If you re- run the pipeline on previously processed
    samples, and specify a different output directory, the pipeline will not rebuild
    everything unless you either force a rebuild (see the -f option) or you request
    mirrored inputs (see the -m option). (default: .)'
  synonyms:
  - -o
  - --out_dir
  args: !SimpleFlagArg
    name: DIR
  optional: true
- !Flag
  description: 'Relative or absolute path to the parent directory of all the sample
    directories.  The -s option should be used when all the sample directories are
    in subdirectories immediately below a parent directory. Note: You must specify
    either the -s or -S option, but not both. Note: The specified directory should
    contain only a collection of sample directories, nothing else. Note: Unless you
    request mirrored inputs, see the -m option, additional files will be written to
    each of the sample directories during the execution of the SNP Pipeline (default:
    None)'
  synonyms:
  - -s
  - --samples_dir
  args: !SimpleFlagArg
    name: DIR
  optional: true
- !Flag
  description: 'Relative or absolute path to a file listing all of the sample directories.  The
    -S option should be used when the samples are not under a common parent directory.
    Note: If you are not mirroring the samples (see the -m option), you can improve
    parallel processing performance by sorting the the list of directories descending
    by size, largest first. The -m option automatically generates a sorted directory
    list. Note: You must specify either the -s or -S option, but not both. Note: Unless
    you request mirrored inputs, see the -m option, additional files will be written
    to each of the sample directories during the execution of the SNP Pipeline (default:
    None)'
  synonyms:
  - -S
  - --samples_file
  args: !SimpleFlagArg
    name: FILE
  optional: true
- !Flag
  description: 'Purge the intermediate output files (the entire "samples" directory)
    when the pipeline completes successfully. (default: False)'
  synonyms:
  - --purge
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: 'Verbose message level (0=no info, 5=lots) (default: 1)'
  synonyms:
  - -v
  - --verbose
  args: !SimpleFlagArg
    name: 0..5
  optional: true
command:
- run_snp_pipeline.sh
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: show program's version number and exit
  synonyms:
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "usage: cfsan_snp_pipeline run [-h] [-f] [-m MODE] [-c FILE]\n        \
  \                      [-Q grid|slurm|torque] [-o DIR]\n                       \
  \       (-s DIR | -S FILE) [--purge] [-v 0..5]\n                              [--version]\n\
  \                              referenceFile\n\nRun the SNP Pipeline on a specified\
  \ data set.\n\npositional arguments:\n  referenceFile         Relative or absolute\
  \ path to the reference fasta file\n\noptional arguments:\n  -h, --help        \
  \    show this help message and exit\n  -f, --force           Force processing even\
  \ when result files already exist\n                        and are newer than inputs\
  \ (default: False)\n  -m MODE, --mirror MODE\n                        Create a mirror\
  \ copy of the reference directory and\n                        all the sample directories.\
  \  Use this option to avoid\n                        polluting the reference directory\
  \ and sample\n                        directories with the intermediate files generated\
  \ by\n                        the snp pipeline.  A \"reference\" subdirectory and\
  \ a\n                        \"samples\" subdirectory are created under the output\n\
  \                        directory (see the -o option).  One directory per\n   \
  \                     sample is created under the \"samples\" directory.\n     \
  \                   Three suboptions allow a choice of how the reference\n     \
  \                   and samples are mirrored.\n                          -m soft\
  \ : creates soft links to the fasta and fastq\n                                \
  \    files instead of copying\n                          -m hard : creates hard\
  \ links to the fasta and fastq\n                                    files instead\
  \ of copying\n                          -m copy : copies the fasta and fastq files\n\
  \                        (default: None)\n  -c FILE, --conf FILE  Relative or absolute\
  \ path to a configuration file for\n                        overriding defaults\
  \ and defining extra parameters for\n                        the tools and scripts\
  \ within the pipeline.\n                        Note: A default parameter configuration\
  \ file named\n                              \"snppipeline.conf\" is used whenever\
  \ the pipeline\n                              is run without the -c option.  The\
  \ configuration\n                              file used for each run is copied\
  \ into the log\n                              directory, capturing the parameters\
  \ used during\n                              the run. (default: None)\n  -Q grid|slurm|torque,\
  \ --queue_mgr grid|slurm|torque\n                        Job queue manager for remote\
  \ parallel job execution in\n                        an HPC environment. Currently\
  \ \"grid\", \"slurm\", and\n                        \"torque\" are supported. If\
  \ not specified, the pipeline\n                        will execute locally. (default:\
  \ None)\n  -o DIR, --out_dir DIR\n                        Output directory for the\
  \ result files. Additional\n                        subdirectories are automatically\
  \ created under the\n                        output directory for logs files and\
  \ the mirrored\n                        samples and reference files (see the -m\
  \ option). The\n                        output directory will be created if it does\
  \ not\n                        already exist. If not specified, the output files\
  \ are\n                        written to the current working directory. If you\
  \ re-\n                        run the pipeline on previously processed samples,\
  \ and\n                        specify a different output directory, the pipeline\n\
  \                        will not rebuild everything unless you either force a\n\
  \                        rebuild (see the -f option) or you request mirrored\n \
  \                       inputs (see the -m option). (default: .)\n  -s DIR, --samples_dir\
  \ DIR\n                        Relative or absolute path to the parent directory\
  \ of\n                        all the sample directories.  The -s option should\
  \ be\n                        used when all the sample directories are in\n    \
  \                    subdirectories immediately below a parent directory.\n    \
  \                    Note: You must specify either the -s or -S option, but\n  \
  \                            not both.\n                        Note: The specified\
  \ directory should contain only a\n                              collection of sample\
  \ directories, nothing else.\n                        Note: Unless you request mirrored\
  \ inputs, see the\n                              -m option, additional files will\
  \ be written to\n                              each of the sample directories during\
  \ the\n                              execution of the SNP Pipeline (default: None)\n\
  \  -S FILE, --samples_file FILE\n                        Relative or absolute path\
  \ to a file listing all of the\n                        sample directories.  The\
  \ -S option should be used when\n                        the samples are not under\
  \ a common parent directory.\n                        Note: If you are not mirroring\
  \ the samples (see the\n                              -m option), you can improve\
  \ parallel processing\n                              performance by sorting the\
  \ the list of\n                              directories descending by size, largest\
  \ first.\n                              The -m option automatically generates a\
  \ sorted\n                              directory list.\n                      \
  \  Note: You must specify either the -s or -S option, but\n                    \
  \          not both.\n                        Note: Unless you request mirrored\
  \ inputs, see the\n                              -m option, additional files will\
  \ be written to\n                              each of the sample directories during\
  \ the\n                              execution of the SNP Pipeline (default: None)\n\
  \  --purge               Purge the intermediate output files (the entire\n     \
  \                   \"samples\" directory) when the pipeline completes\n       \
  \                 successfully. (default: False)\n  -v 0..5, --verbose 0..5\n  \
  \                      Verbose message level (0=no info, 5=lots) (default: 1)\n\
  \  --version             show program's version number and exit\n"
generated_using:
- --help
