!Command
positional:
- !Positional
  description: ''
  position: 0
  name: file
  optional: false
- !Positional
  description: ''
  position: 1
  name: classifier
  optional: false
named: []
command:
- tadaridaC.r
parent:
subcommands: []
help_flag:
usage_flag:
version_flag:
help_text: "7: (function () \n   traceback(2))()\n6: stop(msg, call. = FALSE, domain\
  \ = NA)\n5: value[[3L]](cond)\n4: tryCatchOne(expr, names, parentenv, handlers[[1L]])\n\
  3: tryCatchList(expr, classes, parentenv, handlers)\n2: tryCatch({\n       attr(package,\
  \ \"LibPath\") <- which.lib.loc\n       ns <- loadNamespace(package, lib.loc)\n\
  \       env <- attachNamespace(ns, pos = pos, deps)\n   }, error = function(e) {\n\
  \       P <- if (!is.null(cc <- conditionCall(e))) \n           paste(\" in\", deparse(cc)[1L])\n\
  \       else \"\"\n       msg <- gettextf(\"package or namespace load failed for\
  \ %s%s:\\n %s\", \n           sQuote(package), P, conditionMessage(e))\n       if\
  \ (logical.return) \n           message(paste(\"Error:\", msg), domain = NA)\n \
  \      else stop(msg, call. = FALSE, domain = NA)\n   })\n1: library(data.table)\n\
  [1] \"usage: Rscript TadaridaC.r <file> <classifier>\"\n"
generated_using:
- -h
