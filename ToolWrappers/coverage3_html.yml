!Command
command:
- coverage3
- html
positional:
- !Positional
  description: ''
  position: 0
  name: modules
  optional: true
named:
- !Flag
  description: Write the output files to DIR.
  synonyms:
  - -d
  - --directory
  args: !SimpleFlagArg
    name: DIR
  optional: true
- !Flag
  description: "Exit with a status of 2 if the total coverage is less\nthan MIN."
  synonyms:
  - --fail-under
  args: !SimpleFlagArg
    name: MIN
  optional: true
- !Flag
  description: Ignore errors while reading source files.
  synonyms:
  - -i
  - --ignore-errors
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: ",PAT2,...\nInclude only files whose paths match one of these\npatterns.\
    \ Accepts shell-style wildcards, which must be\nquoted."
  synonyms:
  - --include
  args: !SimpleFlagArg
    name: PAT1
  optional: true
- !Flag
  description: ",PAT2,...  Omit files whose paths match one of these patterns.\nAccepts\
    \ shell-style wildcards, which must be quoted."
  synonyms:
  - --omit
  args: !SimpleFlagArg
    name: PAT1
  optional: true
- !Flag
  description: A text string to use as the title on the HTML.
  synonyms:
  - --title
  args: !SimpleFlagArg
    name: TITLE
  optional: true
- !Flag
  description: Skip files with 100% coverage.
  synonyms:
  - --skip-covered
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: Debug options, separated by commas
  synonyms:
  - --debug
  args: !SimpleFlagArg
    name: OPTS
  optional: true
- !Flag
  description: Specify configuration file.  Defaults to '.coveragerc'
  synonyms:
  - --rcfile
  args: !SimpleFlagArg
    name: RCFILE
  optional: true
parent:
subcommands: []
usage: []
help_flag: !Flag
  description: Get help on this command.
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "Usage: coverage3 html [options] [modules]\n\nCreate an HTML report of\
  \ the coverage of the files.  Each file gets its own\npage, with the source decorated\
  \ to show executed, excluded, and missed lines.\n\nOptions:\n  -d DIR, --directory=DIR\n\
  \                        Write the output files to DIR.\n  --fail-under=MIN    \
  \  Exit with a status of 2 if the total coverage is less\n                     \
  \   than MIN.\n  -i, --ignore-errors   Ignore errors while reading source files.\n\
  \  --include=PAT1,PAT2,...\n                        Include only files whose paths\
  \ match one of these\n                        patterns. Accepts shell-style wildcards,\
  \ which must be\n                        quoted.\n  --omit=PAT1,PAT2,...  Omit files\
  \ whose paths match one of these patterns.\n                        Accepts shell-style\
  \ wildcards, which must be quoted.\n  --title=TITLE         A text string to use\
  \ as the title on the HTML.\n  --skip-covered        Skip files with 100% coverage.\n\
  \  --debug=OPTS          Debug options, separated by commas\n  -h, --help      \
  \      Get help on this command.\n  --rcfile=RCFILE       Specify configuration\
  \ file.  Defaults to '.coveragerc'\n"
generated_using:
- --help
