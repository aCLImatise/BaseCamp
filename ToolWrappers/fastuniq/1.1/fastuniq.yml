!Command
command:
- fastuniq
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - -i
  description: ": The input file list of paired FSATQ sequence files [FILE IN]\nMaximum\
    \ 1000 pairs"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -t
  description: ": Output sequence format [q/f/p]\nq : FASTQ format into TWO output\
    \ files\nf : FASTA format into TWO output files\np : FASTA format into ONE output\
    \ file\ndefault = q"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -o
  description: ': The first output file [FILE OUT]'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -p
  description: ": The second output file [FILE OUT]\nOptional. ONLY required when\
    \ output sequence format(-t) is specify as\n[q] or [f]."
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -c
  description: ": Types of sequence descriptions for output [0/1]\n0 : The raw descriptions\n\
    1 : New serial numbers assigned by FastUniq\ndefault = 0"
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag:
usage_flag:
version_flag:
help_text: "fastuniq: invalid option -- '-'\n-i : The input file list of paired FSATQ\
  \ sequence files [FILE IN]\n        Maximum 1000 pairs\n\n     This parameter is\
  \ used to specify a list of paired sequence files in\n     FASTQ format as input,\
  \ in which two adjacent files with reads in the\n     same order belong to a pair.\n\
  \n-t : Output sequence format [q/f/p]\n        q : FASTQ format into TWO output\
  \ files\n        f : FASTA format into TWO output files\n        p : FASTA format\
  \ into ONE output file\n        default = q\n\n     This parameter is used to specify\
  \ sequence format in output file(s).\n     FastUniq could output read pairs into\
  \ two files in either FASTQ [q]\n     or FASTA [f] format, in which reads in the\
  \ same order belonging to a\n     pair. FastUniq could also output read pairs into\
  \ a single file in\n     FASTA format [p], in which adjacent reads belonging to\
  \ a pair.\n\n-o : The first output file [FILE OUT]\n\n-p : The second output file\
  \ [FILE OUT]\n     Optional. ONLY required when output sequence format(-t) is specify\
  \ as\n     [q] or [f].\n\n-c : Types of sequence descriptions for output [0/1]\n\
  \        0 : The raw descriptions\n        1 : New serial numbers assigned by FastUniq\n\
  \        default = 0\n\nfastuniq: invalid option -- 'h'\n-i : The input file list\
  \ of paired FSATQ sequence files [FILE IN]\n        Maximum 1000 pairs\n\n     This\
  \ parameter is used to specify a list of paired sequence files in\n     FASTQ format\
  \ as input, in which two adjacent files with reads in the\n     same order belong\
  \ to a pair.\n\n-t : Output sequence format [q/f/p]\n        q : FASTQ format into\
  \ TWO output files\n        f : FASTA format into TWO output files\n        p :\
  \ FASTA format into ONE output file\n        default = q\n\n     This parameter\
  \ is used to specify sequence format in output file(s).\n     FastUniq could output\
  \ read pairs into two files in either FASTQ [q]\n     or FASTA [f] format, in which\
  \ reads in the same order belonging to a\n     pair. FastUniq could also output\
  \ read pairs into a single file in\n     FASTA format [p], in which adjacent reads\
  \ belonging to a pair.\n\n-o : The first output file [FILE OUT]\n\n-p : The second\
  \ output file [FILE OUT]\n     Optional. ONLY required when output sequence format(-t)\
  \ is specify as\n     [q] or [f].\n\n-c : Types of sequence descriptions for output\
  \ [0/1]\n        0 : The raw descriptions\n        1 : New serial numbers assigned\
  \ by FastUniq\n        default = 0\n\nfastuniq: invalid option -- 'e'\n-i : The\
  \ input file list of paired FSATQ sequence files [FILE IN]\n        Maximum 1000\
  \ pairs\n\n     This parameter is used to specify a list of paired sequence files\
  \ in\n     FASTQ format as input, in which two adjacent files with reads in the\n\
  \     same order belong to a pair.\n\n-t : Output sequence format [q/f/p]\n    \
  \    q : FASTQ format into TWO output files\n        f : FASTA format into TWO output\
  \ files\n        p : FASTA format into ONE output file\n        default = q\n\n\
  \     This parameter is used to specify sequence format in output file(s).\n   \
  \  FastUniq could output read pairs into two files in either FASTQ [q]\n     or\
  \ FASTA [f] format, in which reads in the same order belonging to a\n     pair.\
  \ FastUniq could also output read pairs into a single file in\n     FASTA format\
  \ [p], in which adjacent reads belonging to a pair.\n\n-o : The first output file\
  \ [FILE OUT]\n\n-p : The second output file [FILE OUT]\n     Optional. ONLY required\
  \ when output sequence format(-t) is specify as\n     [q] or [f].\n\n-c : Types\
  \ of sequence descriptions for output [0/1]\n        0 : The raw descriptions\n\
  \        1 : New serial numbers assigned by FastUniq\n        default = 0\n\nfastuniq:\
  \ invalid option -- 'l'\n-i : The input file list of paired FSATQ sequence files\
  \ [FILE IN]\n        Maximum 1000 pairs\n\n     This parameter is used to specify\
  \ a list of paired sequence files in\n     FASTQ format as input, in which two adjacent\
  \ files with reads in the\n     same order belong to a pair.\n\n-t : Output sequence\
  \ format [q/f/p]\n        q : FASTQ format into TWO output files\n        f : FASTA\
  \ format into TWO output files\n        p : FASTA format into ONE output file\n\
  \        default = q\n\n     This parameter is used to specify sequence format in\
  \ output file(s).\n     FastUniq could output read pairs into two files in either\
  \ FASTQ [q]\n     or FASTA [f] format, in which reads in the same order belonging\
  \ to a\n     pair. FastUniq could also output read pairs into a single file in\n\
  \     FASTA format [p], in which adjacent reads belonging to a pair.\n\n-o : The\
  \ first output file [FILE OUT]\n\n-p : The second output file [FILE OUT]\n     Optional.\
  \ ONLY required when output sequence format(-t) is specify as\n     [q] or [f].\n\
  \n-c : Types of sequence descriptions for output [0/1]\n        0 : The raw descriptions\n\
  \        1 : New serial numbers assigned by FastUniq\n        default = 0\n\nfastuniq:\
  \ option requires an argument -- 'p'\n-i : The input file list of paired FSATQ sequence\
  \ files [FILE IN]\n        Maximum 1000 pairs\n\n     This parameter is used to\
  \ specify a list of paired sequence files in\n     FASTQ format as input, in which\
  \ two adjacent files with reads in the\n     same order belong to a pair.\n\n-t\
  \ : Output sequence format [q/f/p]\n        q : FASTQ format into TWO output files\n\
  \        f : FASTA format into TWO output files\n        p : FASTA format into ONE\
  \ output file\n        default = q\n\n     This parameter is used to specify sequence\
  \ format in output file(s).\n     FastUniq could output read pairs into two files\
  \ in either FASTQ [q]\n     or FASTA [f] format, in which reads in the same order\
  \ belonging to a\n     pair. FastUniq could also output read pairs into a single\
  \ file in\n     FASTA format [p], in which adjacent reads belonging to a pair.\n\
  \n-o : The first output file [FILE OUT]\n\n-p : The second output file [FILE OUT]\n\
  \     Optional. ONLY required when output sequence format(-t) is specify as\n  \
  \   [q] or [f].\n\n-c : Types of sequence descriptions for output [0/1]\n      \
  \  0 : The raw descriptions\n        1 : New serial numbers assigned by FastUniq\n\
  \        default = 0\n\nError in input the name of FASTQ file list!\n"
generated_using:
- --help
docker_image:
