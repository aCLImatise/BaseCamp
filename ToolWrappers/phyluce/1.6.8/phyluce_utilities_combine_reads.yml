!Command
command:
- phyluce_utilities_combine_reads
positional: []
named:
- !Flag
  description: The configuration file for reads we are combining
  synonyms:
  - --config
  args: !SimpleFlagArg
    name: CONFIG
  optional: true
- !Flag
  description: The configuration file for reads we are combining
  synonyms:
  - --output
  args: !SimpleFlagArg
    name: OUTPUT
  optional: true
- !Flag
  description: "A subdirectory, below the level of the group,\ncontaining the reads\n"
  synonyms:
  - --subfolder
  args: !SimpleFlagArg
    name: SUBFOLDER
  optional: true
parent:
subcommands: []
usage: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "usage: phyluce_utilities_combine_reads [-h] --config CONFIG --output OUTPUT\n\
  \                                       [--subfolder SUBFOLDER]\n\nCombine groups\
  \ of reads based on an input file in config format\n\noptional arguments:\n  -h,\
  \ --help            show this help message and exit\n  --config CONFIG       The\
  \ configuration file for reads we are combining\n  --output OUTPUT       The configuration\
  \ file for reads we are combining\n  --subfolder SUBFOLDER\n                   \
  \     A subdirectory, below the level of the group,\n                        containing\
  \ the reads\n"
generated_using:
- --help
