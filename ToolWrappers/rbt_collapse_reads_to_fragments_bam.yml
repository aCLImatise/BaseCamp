!Command
command:
- rbt
- collapse-reads-to-fragments
- bam
positional:
- !Positional
  description: ''
  position: 0
  name: FLAGS
  optional: true
- !Positional
  description: ''
  position: 1
  name: bam
  optional: false
named:
- !Flag
  description: "Add list of reads that were merged for each consensus read. Note that\
    \ this can yield\nvery long FASTQ name lines which cannot be handled by some tools."
  synonyms:
  - --verbose-read-names
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "Maximum hamming distance between the sequences of any pair of reads\
    \ in the same\ncluster. [default: 2]  [possible values: 1, 2, 3, 4, 5, 6, 7, 8]"
  synonyms:
  - -D
  - --max-seq-dist
  args: !SimpleFlagArg
    name: max-seq-dist
  optional: true
parent:
subcommands: []
usage: []
help_flag: !Flag
  description: Prints help information
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag: !Flag
  description: Prints version information
  synonyms:
  - -V
  - --version
  args: !EmptyFlagArg {}
  optional: true
help_text: "rbt-collapse-reads-to-fragments-bam \nFelix Mölder <felix.moelder@uni-due.de>\n\
  Tool to merge sets of PCR duplicate reads from a BAM file into one maximum likelihood\
  \ fragment sequence each with\naccordingly improved base quality scores.\n\nTakes\
  \ a BAM file and returns a BAM file in which all PCR duplicates have been merged\
  \ into a consensus read.\nDuplicates must be marked by Picard Tools using the TAG_DUPLICATE_SET_MEMBERS\
  \ option.\n\nAssumptions:\n  - Reads are of equal length\n  - Reads are marked by\
  \ Picard Tools\n\n\n\nUSAGE:\n    rbt collapse-reads-to-fragments bam [FLAGS] [OPTIONS]\
  \ <bam> <consensus-bam>\n\nFLAGS:\n    -h, --help                  Prints help information\n\
  \    -V, --version               Prints version information\n        --verbose-read-names\
  \    Add list of reads that were merged for each consensus read. Note that this\
  \ can yield\n                                very long FASTQ name lines which cannot\
  \ be handled by some tools.\n\nOPTIONS:\n    -D, --max-seq-dist <max-seq-dist> \
  \   Maximum hamming distance between the sequences of any pair of reads in the same\n\
  \                                         cluster. [default: 2]  [possible values:\
  \ 1, 2, 3, 4, 5, 6, 7, 8]\n\nARGS:\n    <bam>              Input BAM file with marked\
  \ duplicates\n    <consensus-bam>    Output BAM file with unaligned consensus reads\n"
generated_using:
- --help
