!Command
command:
- vcfnormalizesvs
positional:
- !Positional
  description: ''
  position: 0
  name: var.vcf
  optional: false
named:
- !Flag
  description: / --reference <ref.fa>   FASTA-format reference genome from which to
    pull SV sequences.
  synonyms:
  - -r
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: / --insertions <ins.fa>   FASTA-format insertion sequences, with IDs
    matching the ALT allele tags in the vcf
  synonyms:
  - -i
  args: !EmptyFlagArg {}
  optional: true
parent:
subcommands: []
usage: []
help_flag:
usage_flag:
version_flag:
help_text: "vcfnormalizesvs: Convert a VCF with representative alleles to one with\
  \ canonical, sequence-based ref/alt alleles.\nusage: vcfnormalizesvs [OPTIONS] var.vcf\n\
  Options: \n   -r / --reference <ref.fa>   FASTA-format reference genome from which\
  \ to pull SV sequences.\n   -i / --insertions <ins.fa>   FASTA-format insertion\
  \ sequences, with IDs matching the ALT allele tags in the vcf\n\n"
generated_using:
- --help
