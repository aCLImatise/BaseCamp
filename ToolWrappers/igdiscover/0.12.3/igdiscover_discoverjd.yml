!Command
command:
- igdiscover
- discoverjd
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - --database
  description: FASTA file with reference gene sequences
  args: !SimpleFlagArg
    name: FASTA
- !Flag
  optional: true
  synonyms:
  - --merge
  description: "Merge overlapping genes. Default: Enabled for D,\ndisabled for J and\
    \ V."
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --no-merge
  description: Do not merge overlapping genes
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --gene
  description: 'Which gene category to discover. Default: J'
  args: !ChoiceFlagArg
    choices: !!set
      D:
      J:
      V:
- !Flag
  optional: true
  synonyms:
  - --j-coverage
  description: "Require that the sequence covers at least PERCENT of\nthe J gene.\
    \ Default: 90 when --gene=J; 0 otherwise"
  args: !SimpleFlagArg
    name: PERCENT
- !Flag
  optional: true
  synonyms:
  - --allele-ratio
  description: "Required allele ratio. Works only for genes named\n\"NAME*ALLELE\"\
    . Default: 0.2"
  args: !SimpleFlagArg
    name: RATIO
- !Flag
  optional: true
  synonyms:
  - --cross-mapping-ratio
  description: "Ratio for detection of cross-mapping artifacts.\nDefault: 0.1"
  args: !SimpleFlagArg
    name: RATIO
- !Flag
  optional: true
  synonyms:
  - --min-count
  description: "Omit candidates with fewer than N exact occurrences in\nthe input\
    \ table. Default: 1 for J; 10 for D; 100 for V"
  args: !SimpleFlagArg
    name: N
- !Flag
  optional: true
  synonyms:
  - --no-perfect-matches
  description: "Do not filter out sequences for which the V assignment\n(or J for\
    \ --gene=V) has at least one error"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --d-core-length
  description: "Use only D core regions that have at least length L\n(only applies\
    \ when --gene=D). Default: 6"
  args: !SimpleFlagArg
    name: L
- !Flag
  optional: true
  synonyms:
  - --d-core
  description: "D core region location (only applies when --gene=D).\nDefault: slice(2,\
    \ -2, None)"
  args: !SimpleFlagArg
    name: D_CORE
- !Flag
  optional: true
  synonyms:
  - --fasta
  description: Write discovered sequences to FASTA file
  args: !SimpleFlagArg
    name: FASTA
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: show this help message and exit
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "usage: igdiscover discoverjd [-h] [--database FASTA] [--merge] [--no-merge]\n\
  \                             [--gene {V,D,J}] [--j-coverage PERCENT]\n        \
  \                     [--allele-ratio RATIO]\n                             [--cross-mapping-ratio\
  \ RATIO] [--min-count N]\n                             [--no-perfect-matches] [--d-core-length\
  \ L]\n                             [--d-core D_CORE] [--fasta FASTA]\n         \
  \                    table\n\nDiscover D and J genes\n\nThe most frequent D/J sequences\
  \ are considered candidates.\n\nFor J genes, candidate sequences are merged if they\
  \ overlap each other.\n\nThe result table is written to standard output. Use --fasta\
  \ to also\ngenerate FASTA output.\n\npositional arguments:\n  table            \
  \     Table with parsed and filtered IgBLAST results\n\noptional arguments:\n  -h,\
  \ --help            show this help message and exit\n  --database FASTA      FASTA\
  \ file with reference gene sequences\n  --merge               Merge overlapping\
  \ genes. Default: Enabled for D,\n                        disabled for J and V.\n\
  \  --no-merge            Do not merge overlapping genes\n  --gene {V,D,J}      \
  \  Which gene category to discover. Default: J\n  --j-coverage PERCENT  Require\
  \ that the sequence covers at least PERCENT of\n                        the J gene.\
  \ Default: 90 when --gene=J; 0 otherwise\n  --allele-ratio RATIO  Required allele\
  \ ratio. Works only for genes named\n                        \"NAME*ALLELE\". Default:\
  \ 0.2\n  --cross-mapping-ratio RATIO\n                        Ratio for detection\
  \ of cross-mapping artifacts.\n                        Default: 0.1\n  --min-count\
  \ N         Omit candidates with fewer than N exact occurrences in\n           \
  \             the input table. Default: 1 for J; 10 for D; 100 for V\n  --no-perfect-matches\
  \  Do not filter out sequences for which the V assignment\n                    \
  \    (or J for --gene=V) has at least one error\n  --d-core-length L     Use only\
  \ D core regions that have at least length L\n                        (only applies\
  \ when --gene=D). Default: 6\n  --d-core D_CORE       D core region location (only\
  \ applies when --gene=D).\n                        Default: slice(2, -2, None)\n\
  \  --fasta FASTA         Write discovered sequences to FASTA file\n"
generated_using:
- --help
docker_image:
