!Command
command:
- whatshap
- stats
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - --gtf
  description: Write phased blocks to GTF file.
  args: !SimpleFlagArg
    name: GTF
- !Flag
  optional: true
  synonyms:
  - --sample
  description: "Name of the sample to process. If not given, use first\nsample found\
    \ in VCF."
  args: !SimpleFlagArg
    name: SAMPLE
- !Flag
  optional: true
  synonyms:
  - --chr-lengths
  description: "File with chromosome lengths (one line per chromosome,\ntab separated\
    \ '<chr> <length>') needed to compute N50\nvalues."
  args: !SimpleFlagArg
    name: CHR_LENGTHS
- !Flag
  optional: true
  synonyms:
  - --tsv
  description: Filename to write statistics to (tab-separated).
  args: !SimpleFlagArg
    name: TSV
- !Flag
  optional: true
  synonyms:
  - --only-snvs
  description: Only process SNVs and ignore all other variants.
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --block-list
  description: "Filename to write list of all blocks to (one block per\nline)."
  args: !SimpleFlagArg
    name: BLOCK_LIST
- !Flag
  optional: true
  synonyms:
  - --chromosome
  description: "Name of chromosome to process. If not given, all\nchromosomes in the\
    \ input VCF are considered.\n"
  args: !SimpleFlagArg
    name: CHROMOSOME
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: show this help message and exit
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "usage: whatshap stats [-h] [--gtf GTF] [--sample SAMPLE]\n           \
  \           [--chr-lengths CHR_LENGTHS] [--tsv TSV] [--only-snvs]\n            \
  \          [--block-list BLOCK_LIST] [--chromosome CHROMOSOME]\n               \
  \       VCF\n\nPrint phasing statistics of a single VCF file\n\npositional arguments:\n\
  \  VCF                   Phased VCF file\n\noptional arguments:\n  -h, --help  \
  \          show this help message and exit\n  --gtf GTF             Write phased\
  \ blocks to GTF file.\n  --sample SAMPLE       Name of the sample to process. If\
  \ not given, use first\n                        sample found in VCF.\n  --chr-lengths\
  \ CHR_LENGTHS\n                        File with chromosome lengths (one line per\
  \ chromosome,\n                        tab separated '<chr> <length>') needed to\
  \ compute N50\n                        values.\n  --tsv TSV             Filename\
  \ to write statistics to (tab-separated).\n  --only-snvs           Only process\
  \ SNVs and ignore all other variants.\n  --block-list BLOCK_LIST\n             \
  \           Filename to write list of all blocks to (one block per\n           \
  \             line).\n  --chromosome CHROMOSOME\n                        Name of\
  \ chromosome to process. If not given, all\n                        chromosomes\
  \ in the input VCF are considered.\n"
generated_using:
- --help
docker_image:
