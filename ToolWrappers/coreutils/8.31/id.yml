!Command
command:
- id
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - -a
  description: ignore, for compatibility with other versions
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -Z
  - --context
  description: print only the security context of the process
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -g
  - --group
  description: print only the effective group ID
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -G
  - --groups
  description: print all group IDs
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -n
  - --name
  description: print a name instead of a number, for -ugG
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -r
  - --real
  description: print the real ID instead of the effective ID, with -ugG
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -u
  - --user
  description: print only the effective user ID
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -z
  - --zero
  description: "delimit entries with NUL characters, not whitespace;\nnot permitted\
    \ in default format"
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - --help
  description: display this help and exit
  args: !EmptyFlagArg {}
usage_flag:
version_flag: !Flag
  optional: true
  synonyms:
  - --version
  description: output version information and exit
  args: !EmptyFlagArg {}
help_text: "Usage: id [OPTION]... [USER]...\nPrint user and group information for\
  \ each specified USER,\nor (when USER omitted) for the current user.\n\n  -a   \
  \          ignore, for compatibility with other versions\n  -Z, --context  print\
  \ only the security context of the process\n  -g, --group    print only the effective\
  \ group ID\n  -G, --groups   print all group IDs\n  -n, --name     print a name\
  \ instead of a number, for -ugG\n  -r, --real     print the real ID instead of the\
  \ effective ID, with -ugG\n  -u, --user     print only the effective user ID\n \
  \ -z, --zero     delimit entries with NUL characters, not whitespace;\n        \
  \           not permitted in default format\n      --help     display this help\
  \ and exit\n      --version  output version information and exit\n\nWithout any\
  \ OPTION, print some useful set of identified information.\n\nGNU coreutils online\
  \ help: <https://www.gnu.org/software/coreutils/>\nReport any translation bugs to\
  \ <https://translationproject.org/team/>\nFull documentation <https://www.gnu.org/software/coreutils/id>\n\
  or available locally via: info '(coreutils) id invocation'\n"
generated_using:
- --help
docker_image:
