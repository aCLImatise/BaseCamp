!Command
command:
- amber_to_pdb
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - --config
  description: Configuration file
  args: !SimpleFlagArg
    name: CONFIG
- !Flag
  optional: true
  synonyms:
  - --input_top_path
  description: 'AMBER topology file. Accepted formats: top, parmtop, prmtop.'
  args: !SimpleFlagArg
    name: INPUT_TOP_PATH
- !Flag
  optional: true
  synonyms:
  - --input_crd_path
  description: 'AMBER coordinates file. Accepted formats: crd, mdcrd, inpcrd.'
  args: !SimpleFlagArg
    name: INPUT_CRD_PATH
- !Flag
  optional: true
  synonyms:
  - --output_pdb_path
  description: "Structure PDB file. Accepted formats: pdb.\n"
  args: !SimpleFlagArg
    name: OUTPUT_PDB_PATH
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: show this help message and exit
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "usage: amber_to_pdb [-h] [--config CONFIG] --input_top_path INPUT_TOP_PATH\
  \ --input_crd_path INPUT_CRD_PATH --output_pdb_path OUTPUT_PDB_PATH\n\nGenerates\
  \ a PDB structure from AMBER topology (parmtop) and coordinates (crd) files, using\
  \ the ambpdb tool from the AmberTools MD package.\n\noptional arguments:\n  -h,\
  \ --help            show this help message and exit\n  --config CONFIG       Configuration\
  \ file\n\nrequired arguments:\n  --input_top_path INPUT_TOP_PATH\n             \
  \           AMBER topology file. Accepted formats: top, parmtop, prmtop.\n  --input_crd_path\
  \ INPUT_CRD_PATH\n                        AMBER coordinates file. Accepted formats:\
  \ crd, mdcrd, inpcrd.\n  --output_pdb_path OUTPUT_PDB_PATH\n                   \
  \     Structure PDB file. Accepted formats: pdb.\n"
generated_using:
- --help
docker_image: quay.io/biocontainers/biobb_amber:3.5.0--py_0
