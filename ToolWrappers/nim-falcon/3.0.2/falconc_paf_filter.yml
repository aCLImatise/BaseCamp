!Command
command:
- falconc
- paf-filter
positional:
- !Positional
  optional: false
  position: 0
  name: paf-filter
  description: ''
named:
- !Flag
  optional: true
  synonyms:
  - --help-syntax
  description: 'advanced: prepend,plurals,..'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -f
  description: "=, --fai-fn=      string  REQUIRED  input fasta-index filename (as\
    \ in\nfoo.fasta.fai, but assume foo.fasta is\nnext to it; foo.fasta must then\
    \ exist,\nbut it can be empty since it is ignored)"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -i
  description: =, --in-paf-fn=   string  "-"       paf filename ('-' => stdin)
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -o
  description: "=, --out-paf-fn=  string  \"-\"       output paf filename, with only\
    \ the\ncontigs inside the fasta ('-' => stdout)\n"
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: print this cligen-erated help
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "Usage:\n  paf-filter [required&optional-params] \nDrop the lines of PAF\
  \ that involve contigs not in FASTA index.\nWrite the rest to out_paf_fn.  (\"-\"\
  \ means stdin/stdout for PAF.)\nhttps://bioconvert.readthedocs.io/en/master/formats.html#paf-pairwise-mapping-format\n\
  Options:\n  -h, --help                            print this cligen-erated help\n\
  \  --help-syntax                         advanced: prepend,plurals,..\n  -f=, --fai-fn=\
  \      string  REQUIRED  input fasta-index filename (as in\n                   \
  \                     foo.fasta.fai, but assume foo.fasta is\n                 \
  \                       next to it; foo.fasta must then exist,\n               \
  \                         but it can be empty since it is ignored)\n  -i=, --in-paf-fn=\
  \   string  \"-\"       paf filename ('-' => stdin)\n  -o=, --out-paf-fn=  string\
  \  \"-\"       output paf filename, with only the\n                            \
  \            contigs inside the fasta ('-' => stdout)\n"
generated_using:
- --help
docker_image: quay.io/biocontainers/nim-falcon:3.0.2--h1341992_0
