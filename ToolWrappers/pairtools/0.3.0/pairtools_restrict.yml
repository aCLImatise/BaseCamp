!Command
command:
- pairtools
- restrict
positional:
- !Positional
  optional: false
  position: 0
  name: the
  description: input is decompressed by pbgzip/lz4c. By default, the input is read
named:
- !Flag
  optional: true
  synonyms:
  - -f
  - --frags
  description: "a tab-separated BED file with the positions of\nrestriction fragments\
    \ (chrom, start, end). Can be\ngenerated using cooler digest.  [required]"
  args: !SimpleFlagArg
    name: TEXT
- !Flag
  optional: true
  synonyms:
  - -o
  - --output
  description: "output .pairs/.pairsam file. If the path ends with\n.gz/.lz4, the\
    \ output is compressed by pbgzip/lz4c. By\ndefault, the output is printed into\
    \ stdout."
  args: !SimpleFlagArg
    name: TEXT
- !Flag
  optional: true
  synonyms:
  - --nproc-in
  description: "Number of processes used by the auto-guessed input\ndecompressing\
    \ command.  [default: 3]"
  args: !SimpleFlagArg
    name: INTEGER
- !Flag
  optional: true
  synonyms:
  - --nproc-out
  description: "Number of processes used by the auto-guessed output\ncompressing command.\
    \  [default: 8]"
  args: !SimpleFlagArg
    name: INTEGER
- !Flag
  optional: true
  synonyms:
  - --cmd-in
  description: "A command to decompress the input file. If provided,\nfully overrides\
    \ the auto-guessed command. Does not work\nwith stdin. Must read input from stdin\
    \ and print output\ninto stdout. EXAMPLE: pbgzip -dc -n 3"
  args: !SimpleFlagArg
    name: TEXT
- !Flag
  optional: true
  synonyms:
  - --cmd-out
  description: "A command to compress the output file. If provided,\nfully overrides\
    \ the auto-guessed command. Does not work\nwith stdout. Must read input from stdin\
    \ and print\noutput into stdout. EXAMPLE: pbgzip -c -n 8"
  args: !SimpleFlagArg
    name: TEXT
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: Show this message and exit.
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "Usage: pairtools restrict [OPTIONS] [PAIRS_PATH]\n\n  Assign restriction\
  \ fragments to pairs.\n\n  Identify the restriction fragments that got ligated into\
  \ a Hi-C molecule.\n\n  PAIRS_PATH : input .pairs/.pairsam file. If the path ends\
  \ with .gz/.lz4,\n  the  input is decompressed by pbgzip/lz4c. By default, the input\
  \ is read\n  from stdin.\n\nOptions:\n  -f, --frags TEXT     a tab-separated BED\
  \ file with the positions of\n                       restriction fragments (chrom,\
  \ start, end). Can be\n                       generated using cooler digest.  [required]\n\
  \  -o, --output TEXT    output .pairs/.pairsam file. If the path ends with\n   \
  \                    .gz/.lz4, the output is compressed by pbgzip/lz4c. By\n   \
  \                    default, the output is printed into stdout.\n  --nproc-in INTEGER\
  \   Number of processes used by the auto-guessed input\n                       decompressing\
  \ command.  [default: 3]\n  --nproc-out INTEGER  Number of processes used by the\
  \ auto-guessed output\n                       compressing command.  [default: 8]\n\
  \  --cmd-in TEXT        A command to decompress the input file. If provided,\n \
  \                      fully overrides the auto-guessed command. Does not work\n\
  \                       with stdin. Must read input from stdin and print output\n\
  \                       into stdout. EXAMPLE: pbgzip -dc -n 3\n  --cmd-out TEXT\
  \       A command to compress the output file. If provided,\n                  \
  \     fully overrides the auto-guessed command. Does not work\n                \
  \       with stdout. Must read input from stdin and print\n                    \
  \   output into stdout. EXAMPLE: pbgzip -c -n 8\n  -h, --help           Show this\
  \ message and exit.\n"
generated_using:
- --help
docker_image:
