!Command
command:
- sga
- filterBAM
positional: []
named:
- !Flag
  description: display verbose output
  synonyms:
  - -v
  - --verbose
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: load an asqg file and filter pairs that are shorter than --max-distance
  synonyms:
  - -a
  - --asqg
  args: !SimpleFlagArg
    name: FILE
  optional: true
- !Flag
  description: "search the graph for a path completing the mate-pair fragment. If\
    \ the path is less than LEN\nthen the pair will be discarded."
  synonyms:
  - -d
  - --max-distance
  args: !SimpleFlagArg
    name: LEN
  optional: true
- !Flag
  description: 'filter out pairs where one read has an error rate higher than F (default:
    no filter)'
  synonyms:
  - -e
  - --error-rate
  args: !SimpleFlagArg
    name: F
  optional: true
- !Flag
  description: 'filter out pairs where one read has mapping quality less than F (default:
    10)'
  synonyms:
  - -q
  - --min-quality
  args: !SimpleFlagArg
    name: F
  optional: true
- !Flag
  description: write the filtered reads to FILE
  synonyms:
  - -o
  - --out-bam
  args: !SimpleFlagArg
    name: FILE
  optional: true
- !Flag
  description: load the FM-index with prefix STR
  synonyms:
  - -p
  - --prefix
  args: !SimpleFlagArg
    name: STR
  optional: true
- !Flag
  description: filter out pairs that contain a kmer that has been seen in the FM-index
    more than N times
  synonyms:
  - -x
  - --max-kmer-depth
  args: !SimpleFlagArg
    name: N
  optional: true
- !Flag
  description: filter out pairs aligning with FR orientation, which may be contiminates
    in a mate pair library
  synonyms:
  - -c
  - --mate-contamination
  args: !EmptyFlagArg {}
  optional: true
parent:
subcommands: []
usage: []
help_flag: !Flag
  description: display this help and exit
  synonyms:
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "Usage: sga filterBAM [OPTION] ... ASQGFILE BAMFILE\nDiscard mate-pair\
  \ alignments from a BAM file that are potentially erroneous\n\n      --help    \
  \                       display this help and exit\n      -v, --verbose        \
  \            display verbose output\n      -a, --asqg=FILE                  load\
  \ an asqg file and filter pairs that are shorter than --max-distance\n      -d,\
  \ --max-distance=LEN           search the graph for a path completing the mate-pair\
  \ fragment. If the path is less than LEN\n                                     \
  \  then the pair will be discarded.\n      -e, --error-rate=F               filter\
  \ out pairs where one read has an error rate higher than F (default: no filter)\n\
  \      -q, --min-quality=F              filter out pairs where one read has mapping\
  \ quality less than F (default: 10)\n      -o, --out-bam=FILE               write\
  \ the filtered reads to FILE\n      -p, --prefix=STR                 load the FM-index\
  \ with prefix STR\n      -x, --max-kmer-depth=N           filter out pairs that\
  \ contain a kmer that has been seen in the FM-index more than N times\n      -c,\
  \ --mate-contamination         filter out pairs aligning with FR orientation, which\
  \ may be contiminates in a mate pair library\n\nReport bugs to js18@sanger.ac.uk\n\
  \n"
generated_using:
- --help
