!Command
command:
- round_statistics.py
positional: []
named:
- !Flag
  description: ''
  synonyms:
  - -o
  args: !SimpleFlagArg
    name: output
  optional: true
- !Flag
  description: ''
  synonyms:
  - -i
  args: !SimpleFlagArg
    name: Initial_mapped.fq
  optional: true
- !Flag
  description: ''
  synonyms:
  - -d
  args: !SimpleFlagArg
    name: output_per_round_folder
  optional: true
- !Flag
  description: ''
  synonyms:
  - -f
  args: !SimpleFlagArg
    name: fasta_file
  optional: true
parent:
subcommands: []
usage: []
help_flag: !Flag
  description: "show this help message and exit\n-f FASTA              input fasta\
    \ file.\n-d OUTPUT_PER_ROUND_DIR\noutput per round directory.\n-i INITIAL_MAPPED\
    \     seed fastq.\n-o OUTPUT_BASE        output folder.\n-R ROUND            \
    \  rounds to check. default:automatic stop!\n-t THREADS            threads.\n\
    --which-bowtie2=WHICH_BOWTIE2\nAssign the path to Bowtie2 binary files if not\
    \ added\nto the path. Default: try\nGetOrganelleDep/linux/bowtie2 first, then\
    \ $PATH\n--random-seed=RANDOM_SEED\nseed for random generator for bowtie2. Default:\
    \ 12345\n--threshold=THRESHOLD\nsites with coverage above the threshold would\
    \ be\nmarked as covered. default: 0,10\n--continue\n--keep-temp\n--draw      \
    \          Draw density plot using matplotlib, which should be\ninstalled.\n--max-coverage-tick=MAX_COV_TICK\n\
    --debug=DEBUG         Debug mode.\n"
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "Usage: round_statistics.py -f fasta_file -d output_per_round_folder -i\
  \ Initial_mapped.fq -o output\n\nOptions:\n  -h, --help            show this help\
  \ message and exit\n  -f FASTA              input fasta file.\n  -d OUTPUT_PER_ROUND_DIR\n\
  \                        output per round directory.\n  -i INITIAL_MAPPED     seed\
  \ fastq.\n  -o OUTPUT_BASE        output folder.\n  -R ROUND              rounds\
  \ to check. default:automatic stop!\n  -t THREADS            threads.\n  --which-bowtie2=WHICH_BOWTIE2\n\
  \                        Assign the path to Bowtie2 binary files if not added\n\
  \                        to the path. Default: try\n                        GetOrganelleDep/linux/bowtie2\
  \ first, then $PATH\n  --random-seed=RANDOM_SEED\n                        seed for\
  \ random generator for bowtie2. Default: 12345\n  --threshold=THRESHOLD\n      \
  \                  sites with coverage above the threshold would be\n          \
  \              marked as covered. default: 0,10\n  --continue            \n  --keep-temp\
  \           \n  --draw                Draw density plot using matplotlib, which\
  \ should be\n                        installed.\n  --max-coverage-tick=MAX_COV_TICK\n\
  \  --debug=DEBUG         Debug mode.\n"
generated_using:
- --help
