!Command
command:
- parsec
- libraries
- get_folders
positional:
- !Positional
  optional: false
  position: 0
  name: LIBRARY_ID
  description: ''
named:
- !Flag
  optional: true
  synonyms:
  - --folder_id
  description: filter for folder by folder id
  args: !SimpleFlagArg
    name: TEXT
- !Flag
  optional: true
  synonyms:
  - --name
  description: "filter for folder by name. For ``name`` specify the full\npath of\
    \ the folder starting from the library's root\nfolder, e.g. ``/subfolder/subsubfolder``."
  args: !SimpleFlagArg
    name: TEXT
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: Show this message and exit.
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "Usage: parsec libraries get_folders [OPTIONS] LIBRARY_ID\n\n  Get all\
  \ the folders or filter specific one(s) via the provided ``name`` or\n  ``folder_id``\
  \ in data library with id ``library_id``. Provide only one\n  argument: ``name``\
  \ or ``folder_id``, but not both.\n\n  Output:\n\n      list of dicts each containing\
  \ basic information about a folder\n\nOptions:\n  --folder_id TEXT  filter for folder\
  \ by folder id\n  --name TEXT       filter for folder by name. For ``name`` specify\
  \ the full\n                    path of the folder starting from the library's root\n\
  \                    folder, e.g. ``/subfolder/subsubfolder``.\n\n  -h, --help \
  \       Show this message and exit.\n"
generated_using:
- --help
docker_image: quay.io/biocontainers/galaxy-parsec:1.15.0--pyh5e36f6f_0
