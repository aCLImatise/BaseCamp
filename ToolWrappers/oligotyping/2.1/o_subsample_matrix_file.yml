!Command
command:
- o-subsample-matrix-file
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - -c
  - --cols-to-remove
  description: "Columns to be removed from the matrix (one column id\nin each line)"
  args: !SimpleFlagArg
    name: FILE
- !Flag
  optional: true
  synonyms:
  - -r
  - --rows-to-remove
  description: "Rows to be removed from the matrix (one row id in each\nline)"
  args: !SimpleFlagArg
    name: FILE
- !Flag
  optional: true
  synonyms:
  - -C
  - --cols-to-keep
  description: "Columns to be kept in the subsampled matrix (one\ncolumn id in each\
    \ line)"
  args: !SimpleFlagArg
    name: FILE
- !Flag
  optional: true
  synonyms:
  - -R
  - --rows-to-keep
  description: "Rows to be kept in the subsampled matrix (one row id\nin each line)"
  args: !SimpleFlagArg
    name: FILE
- !Flag
  optional: true
  synonyms:
  - -o
  - --output-file
  description: "Output file name\n"
  args: !SimpleFlagArg
    name: OUTPUT_FILE
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: show this help message and exit
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "usage: o-subsample-matrix-file [-h] [-c FILE] [-r FILE] [-C FILE] [-R\
  \ FILE]\n                               [-o OUTPUT_FILE]\n                     \
  \          FILE\n\nRemoves COLS and ROWS from a matrix file\n\npositional arguments:\n\
  \  FILE                  TAB delimited matrix to be processed\n\noptional arguments:\n\
  \  -h, --help            show this help message and exit\n  -c FILE, --cols-to-remove\
  \ FILE\n                        Columns to be removed from the matrix (one column\
  \ id\n                        in each line)\n  -r FILE, --rows-to-remove FILE\n\
  \                        Rows to be removed from the matrix (one row id in each\n\
  \                        line)\n  -C FILE, --cols-to-keep FILE\n               \
  \         Columns to be kept in the subsampled matrix (one\n                   \
  \     column id in each line)\n  -R FILE, --rows-to-keep FILE\n                \
  \        Rows to be kept in the subsampled matrix (one row id\n                \
  \        in each line)\n  -o OUTPUT_FILE, --output-file OUTPUT_FILE\n          \
  \              Output file name\n"
generated_using:
- --help
docker_image:
