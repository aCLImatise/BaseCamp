!Command
command:
- SOAPdenovo-63mer
- contig
positional:
- !Positional
  optional: false
  position: 0
  name: contig
  description: contig -g InputGraph [-R] [-M mergeLevel -D EdgeCovCutoff] [-s readsInfoFile
    -m maxkmer -p n_cpu -r]
named:
- !Flag
  optional: true
  synonyms:
  - -g
  description: 'inputGraph: prefix of input graph file names'
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - -R
  description: (optional)    resolve repeats using information generated in pregraph
    step, works only if -R is set in pregraph step too, [NO]
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -M
  description: 'mergeLevel(min 0, max 3): the strength of merging similar sequences
    during contiging, [1]'
  args: !SimpleFlagArg
    name: int
- !Flag
  optional: true
  synonyms:
  - -D
  description: 'EdgeCovCutoff: edges shorter than (2*K+1) with coverage no larger
    than EdgeCovCutoff will be deleted, [1]'
  args: !SimpleFlagArg
    name: int
- !Flag
  optional: true
  synonyms:
  - -e
  description: "arcWeight: two edges, between which the arc's weight is larger than\
    \ arcWeight, will be linerized, [0]"
  args: !SimpleFlagArg
    name: int
- !Flag
  optional: true
  synonyms:
  - -m
  description: max k when using multi-kmer, and the parameters below are used along
    with multi-kmer, [NO]
  args: !SimpleFlagArg
    name: int
- !Flag
  optional: true
  synonyms:
  - -s
  description: readsInfoFile:The file contains information of solexa reads(It's necessary
    when using multi-kmer)
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - -p
  description: number of cpu, [8]
  args: !SimpleFlagArg
    name: int
- !Flag
  optional: true
  synonyms:
  - -E
  description: (optional)    merge clean bubble before iterate, works only if -M is
    set when using multi-kmer, [NO]
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag:
usage_flag:
version_flag:
help_text: "\nVersion 2.04: released on July 13th, 2012\nCompile May  6 2020\t21:29:18\n\
  \n********************\nContig\n********************\n\nParameters: contig \n\n\n\
  contig -g InputGraph [-R] [-M mergeLevel -D EdgeCovCutoff] [-s readsInfoFile -m\
  \ maxkmer -p n_cpu -r]\n  -g <string>      inputGraph: prefix of input graph file\
  \ names\n  -R (optional)    resolve repeats using information generated in pregraph\
  \ step, works only if -R is set in pregraph step too, [NO]\n  -M <int>         mergeLevel(min\
  \ 0, max 3): the strength of merging similar sequences during contiging, [1]\n \
  \ -D <int>         EdgeCovCutoff: edges shorter than (2*K+1) with coverage no larger\
  \ than EdgeCovCutoff will be deleted, [1]\n  -e <int>         arcWeight: two edges,\
  \ between which the arc's weight is larger than arcWeight, will be linerized, [0]\n\
  \  -m <int>         max k when using multi-kmer, and the parameters below are used\
  \ along with multi-kmer, [NO]\n      -s <string>      readsInfoFile:The file contains\
  \ information of solexa reads(It's necessary when using multi-kmer)\n      -p <int>\
  \         number of cpu, [8]\n      -E (optional)    merge clean bubble before iterate,\
  \ works only if -M is set when using multi-kmer, [NO]\n"
generated_using: []
docker_image:
