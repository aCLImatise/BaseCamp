!Command
command:
- ganon-classify
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - -r
  - --single-reads
  description: "File[s] with single-end reads .fq .fastq .fasta\n.fa (e.g. file1.fq[.gz],[file2.fq[.gz]\
    \ ...\nfileN.fq[.gz]])"
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - -p
  - --paired-reads
  description: "Pairs of files with paired-end reads .fq .fastq\n.fasta .fa (e.g.\n\
    file1.1.fq[.gz],file1.2.fq[.gz],[file2.1.fq[.gz],file2.2.fq[.gz] ...\nfileN.1.fq[.gz],fileN.2.fq[.gz]])"
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - -i
  - --ibf
  description: "ibf (Interleaved Bloom Filter) file[s] (e.g. -b\na.ibf,b.ibf OR -b\
    \ a.ibf -b b.ibf )"
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - -m
  - --map
  description: "map files[s]. Tab-separated file mapping target\ngroups (taxids, assemblies)\
    \ and bin identifiers\nwith the following fields: target <tab> bin id\n(e.g. -g\
    \ a.map,b.map OR -g a.map -g b.map)"
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - -x
  - --tax
  description: "tax (taxonomy) files[s]. Tab-separated file\nwith a complete tree\
    \ with the following fields:\nnode <tab> parent node <tab> rank <tab> name (e.g.\n\
    -g a.tax,b.tax OR -g a.tax -g b.tax)"
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - -c
  - --hierarchy-labels
  description: "Hierarchy labels for the database files\n(hierarchy follows the order\
    \ of the sorted labels)\n(e.g. 1_host,2_target,1_host,3). Default:\n'1_default'"
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - -k
  - --min-kmers
  description: "Minimum percentage of k-mers matching for a\nread to to be assigned\
    \ [muttualy exclusive\n--max-error]. One per filter. Default: 0.25"
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - -e
  - --max-error
  description: "Maximum number of errors/mismatches allowed\n[muttualy exclusive --min-kmers].\
    \ One per filter."
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - -u
  - --max-error-unique
  description: "Maximum number of errors/mismatches allowed for\nunique matches after\
    \ filtering. One per\nhiearchy label."
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - -l
  - --strata-filter
  description: "Additional errors allowed (relative to the best\nmatch) to filter\
    \ and select matches. -1 for no\nfiltering. One per hiearchy label. Default: 0"
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - -f
  - --offset
  description: "Offset for skipping k-mers while counting.\nFunction must be enabled\
    \ on compilation time with\n-DGANON_OFFSET=ON. Default: 1 = no offset"
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - -o
  - --output-prefix
  description: "Output prefix for output files (prefix.lca,\nprefix.rep, prefix.all,\
    \ prefix.unc). If multi-level\nhiearchy is provded, files are generated\naccordingly\
    \ (prefix.hiearchy.lca, ...). Omit for\noutput to STDOUT (only .lca will be printed)"
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - -a
  - --output-all
  description: "Output file with all matches (prefix.all) [it\ncan be very big]"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -n
  - --output-unclassified
  description: Output unclassified read ids (prefix.unc)
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -s
  - --output-single
  description: "Generate only one output (prefix.lca and\nprefix.rep) even with multiple\
    \ hierarchy levels"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -t
  - --threads
  description: Number of threads
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - --n-reads
  description: 'Number of reads for each batch. Default: 400'
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - --n-batches
  description: "Number of batches of n-reads to hold in memory.\nDefault: 1000"
  args: !SimpleFlagArg
    name: arg
- !Flag
  optional: true
  synonyms:
  - --verbose
  description: Verbose output mode
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --quiet
  description: "Quiet output mode (only outputs errors and\nwarnings to the STDERR)"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -v
  description: ''
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: Print help
  args: !EmptyFlagArg {}
usage_flag:
version_flag: !Flag
  optional: true
  synonyms:
  - -v
  - --version
  description: Show version
  args: !EmptyFlagArg {}
help_text: "Ganon classifier\nUsage:\n  ganon-classify [OPTION...]\n\n  -r, --single-reads\
  \ arg      File[s] with single-end reads .fq .fastq .fasta\n                   \
  \           .fa (e.g. file1.fq[.gz],[file2.fq[.gz] ...\n                       \
  \       fileN.fq[.gz]])\n  -p, --paired-reads arg      Pairs of files with paired-end\
  \ reads .fq .fastq\n                              .fasta .fa (e.g.\n           \
  \                   file1.1.fq[.gz],file1.2.fq[.gz],[file2.1.fq[.gz],file2.2.fq[.gz]\
  \ ...\n                              fileN.1.fq[.gz],fileN.2.fq[.gz]])\n  -i, --ibf\
  \ arg               ibf (Interleaved Bloom Filter) file[s] (e.g. -b\n          \
  \                    a.ibf,b.ibf OR -b a.ibf -b b.ibf )\n  -m, --map arg       \
  \        map files[s]. Tab-separated file mapping target\n                     \
  \         groups (taxids, assemblies) and bin identifiers\n                    \
  \          with the following fields: target <tab> bin id\n                    \
  \          (e.g. -g a.map,b.map OR -g a.map -g b.map)\n  -x, --tax arg         \
  \      tax (taxonomy) files[s]. Tab-separated file\n                           \
  \   with a complete tree with the following fields:\n                          \
  \    node <tab> parent node <tab> rank <tab> name (e.g.\n                      \
  \        -g a.tax,b.tax OR -g a.tax -g b.tax)\n  -c, --hierarchy-labels arg  Hierarchy\
  \ labels for the database files\n                              (hierarchy follows\
  \ the order of the sorted labels)\n                              (e.g. 1_host,2_target,1_host,3).\
  \ Default:\n                              '1_default'\n  -k, --min-kmers arg   \
  \      Minimum percentage of k-mers matching for a\n                           \
  \   read to to be assigned [muttualy exclusive\n                              --max-error].\
  \ One per filter. Default: 0.25\n  -e, --max-error arg         Maximum number of\
  \ errors/mismatches allowed\n                              [muttualy exclusive --min-kmers].\
  \ One per filter.\n  -u, --max-error-unique arg  Maximum number of errors/mismatches\
  \ allowed for\n                              unique matches after filtering. One\
  \ per\n                              hiearchy label.\n  -l, --strata-filter arg\
  \     Additional errors allowed (relative to the best\n                        \
  \      match) to filter and select matches. -1 for no\n                        \
  \      filtering. One per hiearchy label. Default: 0\n  -f, --offset arg       \
  \     Offset for skipping k-mers while counting.\n                             \
  \ Function must be enabled on compilation time with\n                          \
  \    -DGANON_OFFSET=ON. Default: 1 = no offset\n  -o, --output-prefix arg     Output\
  \ prefix for output files (prefix.lca,\n                              prefix.rep,\
  \ prefix.all, prefix.unc). If multi-level\n                              hiearchy\
  \ is provded, files are generated\n                              accordingly (prefix.hiearchy.lca,\
  \ ...). Omit for\n                              output to STDOUT (only .lca will\
  \ be printed)\n  -a, --output-all            Output file with all matches (prefix.all)\
  \ [it\n                              can be very big]\n  -n, --output-unclassified\
  \   Output unclassified read ids (prefix.unc)\n  -s, --output-single         Generate\
  \ only one output (prefix.lca and\n                              prefix.rep) even\
  \ with multiple hierarchy levels\n  -t, --threads arg           Number of threads\n\
  \      --n-reads arg           Number of reads for each batch. Default: 400\n  \
  \    --n-batches arg         Number of batches of n-reads to hold in memory.\n \
  \                             Default: 1000\n      --verbose               Verbose\
  \ output mode\n      --quiet                 Quiet output mode (only outputs errors\
  \ and\n                              warnings to the STDERR)\n  -h, --help     \
  \             Print help\n  -v, --version               Show version\n\n"
generated_using:
- --help
docker_image: quay.io/biocontainers/ganon:0.4.0--py38hb45e0f6_0
