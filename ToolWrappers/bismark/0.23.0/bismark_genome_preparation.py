from datetime import datetime
from typing import List, Optional, Dict, Any

from janis_core import *
from janis_core.types.common_data_types import Boolean, Int, String

Bismark_Genome_Preparation_V0_1_0 = CommandToolBuilder(tool="bismark_genome_preparation", base_command=["bismark_genome_preparation"], inputs=[ToolInput(tag="in_verbose", input_type=Boolean(optional=True), prefix="--verbose", doc=InputDocumentation(doc="Print verbose output for more details or debugging.")), ToolInput(tag="in_path_to_aligner", input_type=Boolean(optional=True), prefix="--path_to_aligner", doc=InputDocumentation(doc="</../> The full path to the Bowtie 2 or HISAT2 installation folder on your system\n(depending on which aligner/indexer you intend to use; please note that this\nis the folder and not any executable). Unless this path is specified, it is\nassumed that the aligner in question (Bowtie 2/HISAT2) is in the PATH.")), ToolInput(tag="in_bowtie_two", input_type=Boolean(optional=True), prefix="--bowtie2", doc=InputDocumentation(doc="This will create bisulfite indexes for use with Bowtie 2. Recommended for most bisulfite\nsequencing applications (Default: ON).")), ToolInput(tag="in_his_at_two", input_type=Boolean(optional=True), prefix="--hisat2", doc=InputDocumentation(doc="This will create bisulfite indexes for use with HISAT2. At the time of writing, this is\nstill unchartered territory, and only recommended for specialist applications such as\nRNA-methylation analyses or SLAM-seq type applications (see also: --slam). (Default: OFF).")), ToolInput(tag="in_parallel", input_type=Int(optional=True), prefix="--parallel", doc=InputDocumentation(doc="Use several threads for each indexing process to speed up the genome preparation step.\nRemember that the indexing is run twice in parallel already (for the top and bottom strand\nseparately), so e.g. '--parallel 4' will use 8 threads in total. Please also see --large-index\nfor parallel processing of VERY LARGE genomes (e.g. the axolotl)")), ToolInput(tag="in_single_fast_a", input_type=Boolean(optional=True), prefix="--single_fasta", doc=InputDocumentation(doc="Instruct the Bismark Indexer to write the converted genomes into\nsingle-entry FastA files instead of making one multi-FastA file (MFA)\nper chromosome. This might be useful if individual bisulfite converted\nchromosomes are needed (e.g. for debugging), however it can cause a\nproblem with indexing if the number of chromosomes is vast (this is likely\nto be in the range of several thousand files; the operating system can\nonly handle lists up to a certain length, and some newly assembled\ngenomes may contain 20000-500000 contigs of scaffold files which do exceed\nthis list length limit).")), ToolInput(tag="in_genomic_composition", input_type=Boolean(optional=True), prefix="--genomic_composition", doc=InputDocumentation(doc="Calculate and extract the genomic sequence composition for mono and di-nucleotides\nand write the genomic composition table 'genomic_nucleotide_frequencies.txt' to the\ngenome folder. This may be useful later on when using bam2nuc or the Bismark option\n--nucleotide_coverage.")), ToolInput(tag="in_slam", input_type=Boolean(optional=True), prefix="--slam", doc=InputDocumentation(doc="Instead of performing an in-silico bisulfite conversion, this mode transforms T to C (forward strand),\nor A to G (reverse strand). The folder structure and rest of the indexing process is currently\nexactly the same as for bisulfite sequences, but this might change at some point. This means\nthat a genome prepared in --slam mode is currently indistinguishable from a true Bisulfite Genome,\nso please make sure you name the genome folder appropriately to avoid confusion.")), ToolInput(tag="in_large_index", input_type=Boolean(optional=True), prefix="--large-index", doc=InputDocumentation(doc="Force generated index to be 'large', even if reference has fewer than 4 billion nucleotides. At the time\nof writing this is required for parallel processing of VERY LARGE genomes (e.g. the axolotl)")), ToolInput(tag="in_arguments", input_type=String(), position=0, doc=InputDocumentation(doc=""))], outputs=[], container="quay.io/biocontainers/bismark:0.23.0--0", version="v0.1.0")


if __name__ == "__main__":
    # or "cwl"
    Bismark_Genome_Preparation_V0_1_0().translate("wdl")

