!Command
command:
- selectFasta
positional: []
named:
- !Flag
  description: FILE  (fastq file to select reads from)
  synonyms:
  - -fastq
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: FILE  (list of reads, fastq or fasta)
  synonyms:
  - -list
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: VAL   (number of random reads to be selected from fastq file)
  synonyms:
  - -random
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: (convert fastq file to fasta)
  synonyms:
  - -fastq2fasta
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: FILE  (fasta file to select reads from)
  synonyms:
  - -fasta
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: (from fasta file select reads in -list, if not flag, reads not in list
    are selected)
  synonyms:
  - -fasta_sel
  args: !EmptyFlagArg {}
  optional: true
parent:
subcommands: []
usage: []
help_flag: !Flag
  description: (Help)
  synonyms:
  - -h
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "Select fastQ or fasta reads from list, -list or -random is required\n\n\
  selectFasta\n  -h                  (Help)\n  -fastq        FILE  (fastq file to\
  \ select reads from) \n  -list         FILE  (list of reads, fastq or fasta) \n\
  \  -random       VAL   (number of random reads to be selected from fastq file) \n\
  \  -fastq2fasta        (convert fastq file to fasta)\n  -fasta        FILE  (fasta\
  \ file to select reads from) \n  -fasta_sel          (from fasta file select reads\
  \ in -list, if not flag, reads not in list are selected)\n\n"
generated_using:
- --help
