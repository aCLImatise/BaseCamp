!Command
command:
- treeannotator
positional: []
named:
- !Flag
  description: option of 'keep' (default), 'median', 'mean' or 'ca'
  synonyms:
  - -heights
  args: !SimpleFlagArg
    name: an
  optional: true
- !Flag
  description: percentage of states to be considered as 'burn-in'
  synonyms:
  - -burnin
  args: !SimpleFlagArg
    name: the
  optional: true
- !Flag
  description: percentage of states to be considered as 'burn-in'
  synonyms:
  - -b
  args: !SimpleFlagArg
    name: the
  optional: true
- !Flag
  description: minimum posterior probability for a node to be annotated
  synonyms:
  - -limit
  args: !SimpleFlagArg
    name: the
  optional: true
- !Flag
  description: a user target tree to be annotated
  synonyms:
  - -target
  args: !SimpleFlagArg
    name: specifies
  optional: true
- !Flag
  description: integer traits to be treated as discrete traits.
  synonyms:
  - -forceDiscrete
  args: !SimpleFlagArg
    name: forces
  optional: true
- !Flag
  description: less memory, which is a bit slower.
  synonyms:
  - -lowMem
  args: !SimpleFlagArg
    name: use
  optional: true
- !Flag
  description: HPD interval to be used for the bivariate traits
  synonyms:
  - -hpd2D
  args: !SimpleFlagArg
    name: the
  optional: true
- !Flag
  description: calculation of HPD intervals for the bivariate traits
  synonyms:
  - -nohpd2D
  args: !SimpleFlagArg
    name: suppress
  optional: true
- !Flag
  description: the tree set as begin from a not being from a sampled ancestor analysis,
    even if there are zero branch lengths in the tree set
  synonyms:
  - -noSA
  args: !SimpleFlagArg
    name: interpret
  optional: true
parent:
subcommands: []
usage: []
help_flag: !Flag
  description: to print this message
  synonyms:
  - -help
  args: !SimpleFlagArg
    name: option
  optional: true
usage_flag:
version_flag:
help_text: "  Usage: treeannotator [-heights <keep|median|mean|ca>] [-burnin <i>]\
  \ [-b <i>] [-limit <r>] [-target <target_file_name>] [-help] [-forceDiscrete] [-lowMem]\
  \ [-hpd2D <r>] [-nohpd2D] [-noSA] <input-file-name> [<output-file-name>]\n    -heights\
  \ an option of 'keep' (default), 'median', 'mean' or 'ca'\n    -burnin the percentage\
  \ of states to be considered as 'burn-in'\n    -b the percentage of states to be\
  \ considered as 'burn-in'\n    -limit the minimum posterior probability for a node\
  \ to be annotated\n    -target specifies a user target tree to be annotated\n  \
  \  -help option to print this message\n    -forceDiscrete forces integer traits\
  \ to be treated as discrete traits.\n    -lowMem use less memory, which is a bit\
  \ slower.\n    -hpd2D the HPD interval to be used for the bivariate traits\n   \
  \ -nohpd2D suppress calculation of HPD intervals for the bivariate traits\n    -noSA\
  \ interpret the tree set as begin from a not being from a sampled ancestor analysis,\
  \ even if there are zero branch lengths in the tree set\n"
generated_using:
- --help
