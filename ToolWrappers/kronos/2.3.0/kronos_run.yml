!Command
command:
- kronos
- run
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - -b
  - --job_scheduler
  description: job scheduler used to manage jobs on the cluster
  args: !ChoiceFlagArg
    choices: !!set
      drmaa:
      sge:
- !Flag
  optional: true
  synonyms:
  - -c
  - --components_dir
  description: path to components_dir
  args: !SimpleFlagArg
    name: COMPONENTS_DIR
- !Flag
  optional: true
  synonyms:
  - -d
  - --drmaa_library_path
  description: path of drmaa library
  args: !SimpleFlagArg
    name: DRMAA_LIBRARY_PATH
- !Flag
  optional: true
  synonyms:
  - -e
  - --pipeline_name
  description: pipeline name
  args: !SimpleFlagArg
    name: PIPELINE_NAME
- !Flag
  optional: true
  synonyms:
  - -i
  - --input_samples
  description: path to the input samples file
  args: !SimpleFlagArg
    name: FILE
- !Flag
  optional: true
  synonyms:
  - -j
  - --num_jobs
  description: maximum number of simultaneous jobs per pipeline
  args: !SimpleFlagArg
    name: NUM_JOBS
- !Flag
  optional: true
  synonyms:
  - -k
  - --kronos_pipeline
  description: path to kronos-made pipeline script.
  args: !SimpleFlagArg
    name: FILE
- !Flag
  optional: true
  synonyms:
  - -n
  - --num_pipelines
  description: maximum number of simultaneous running pipelines
  args: !SimpleFlagArg
    name: NUM_PIPELINES
- !Flag
  optional: true
  synonyms:
  - -p
  - --python_installation
  description: path to python executable
  args: !SimpleFlagArg
    name: PYTHON_INSTALLATION
- !Flag
  optional: true
  synonyms:
  - -q
  - --qsub_options
  description: "native qsub specifications for the cluster in a single\nstring"
  args: !SimpleFlagArg
    name: QSUB_OPTIONS
- !Flag
  optional: true
  synonyms:
  - -r
  - --run_id
  description: pipeline run id
  args: !SimpleFlagArg
    name: RUN_ID
- !Flag
  optional: true
  synonyms:
  - -s
  - --setup_file
  description: path to the setup file
  args: !SimpleFlagArg
    name: FILE
- !Flag
  optional: true
  synonyms:
  - -w
  - --working_dir
  description: path to the working_dir
  args: !SimpleFlagArg
    name: WORKING_DIR
- !Flag
  optional: true
  synonyms:
  - -y
  - --config_file
  description: path to the config_file.yaml
  args: !SimpleFlagArg
    name: FILE
- !Flag
  optional: true
  synonyms:
  - --no_prefix
  description: "switch off the prefix that is added to all the output\nfiles.\n"
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: show this help message and exit
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "usage: kronos run [-h] [-b {sge,drmaa}] -c COMPONENTS_DIR\n          \
  \        [-d DRMAA_LIBRARY_PATH] [-e PIPELINE_NAME] [-i FILE]\n                \
  \  [-j NUM_JOBS] [-k FILE] [-n NUM_PIPELINES]\n                  [-p PYTHON_INSTALLATION]\
  \ [-q QSUB_OPTIONS] [-r RUN_ID]\n                  [-s FILE] [-w WORKING_DIR] [-y\
  \ FILE] [--no_prefix]\n\nrun kronos-made pipelines with optional initialization\n\
  \noptional arguments:\n  -h, --help            show this help message and exit\n\
  \  -b {sge,drmaa}, --job_scheduler {sge,drmaa}\n                        job scheduler\
  \ used to manage jobs on the cluster\n  -c COMPONENTS_DIR, --components_dir COMPONENTS_DIR\n\
  \                        path to components_dir\n  -d DRMAA_LIBRARY_PATH, --drmaa_library_path\
  \ DRMAA_LIBRARY_PATH\n                        path of drmaa library\n  -e PIPELINE_NAME,\
  \ --pipeline_name PIPELINE_NAME\n                        pipeline name\n  -i FILE,\
  \ --input_samples FILE\n                        path to the input samples file\n\
  \  -j NUM_JOBS, --num_jobs NUM_JOBS\n                        maximum number of simultaneous\
  \ jobs per pipeline\n  -k FILE, --kronos_pipeline FILE\n                       \
  \ path to kronos-made pipeline script.\n  -n NUM_PIPELINES, --num_pipelines NUM_PIPELINES\n\
  \                        maximum number of simultaneous running pipelines\n  -p\
  \ PYTHON_INSTALLATION, --python_installation PYTHON_INSTALLATION\n             \
  \           path to python executable\n  -q QSUB_OPTIONS, --qsub_options QSUB_OPTIONS\n\
  \                        native qsub specifications for the cluster in a single\n\
  \                        string\n  -r RUN_ID, --run_id RUN_ID\n                \
  \        pipeline run id\n  -s FILE, --setup_file FILE\n                       \
  \ path to the setup file\n  -w WORKING_DIR, --working_dir WORKING_DIR\n        \
  \                path to the working_dir\n  -y FILE, --config_file FILE\n      \
  \                  path to the config_file.yaml\n  --no_prefix           switch\
  \ off the prefix that is added to all the output\n                        files.\n"
generated_using:
- --help
docker_image:
