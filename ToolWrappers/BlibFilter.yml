!Command
command:
- BlibFilter
positional:
- !Positional
  description: '-n [ --min-peaks ] arg (=1)       Only include spectra with at least
    this '
  position: 0
  name: 250M.
  optional: false
named:
- !Flag
  description: '[ --memory-cache ] arg (=250)  SQLite memory cache size in Megs.  Default'
  synonyms:
  - -m
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "[ --min-score ] arg (=0)       Best spectrum must have at least this\n\
    average score to be included.  Default 0."
  synonyms:
  - -s
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: '[ --best-scoring ] arg (=0)    Description of option.  Default false.'
  synonyms:
  - -b
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "[ --parameter-file ] arg       File containing search parameters.\
    \  Command\nline values override file values."
  synonyms:
  - -p
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "[ --verbosity ] arg (=status)  Control the level of output to stderr.\n\
    (silent, error, status, warn, debug,\ndetail, all)  Default status."
  synonyms:
  - -v
  args: !EmptyFlagArg {}
  optional: true
parent:
subcommands: []
usage: []
help_flag: !Flag
  description: '[ --help ]                     Print help message.'
  synonyms:
  - -h
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "Usage: BlibFilter [options] <redundant-library> <filtered-library>\n\n\
  Options:\n  -m [ --memory-cache ] arg (=250)  SQLite memory cache size in Megs.\
  \  Default \n                                    250M.\n  -n [ --min-peaks ] arg\
  \ (=1)       Only include spectra with at least this \n                        \
  \            many peaks.  Default 1.\n  -s [ --min-score ] arg (=0)       Best spectrum\
  \ must have at least this \n                                    average score to\
  \ be included.  Default 0.\n  -b [ --best-scoring ] arg (=0)    Description of option.\
  \  Default false.\n  -p [ --parameter-file ] arg       File containing search parameters.\
  \  Command\n                                    line values override file values.\n\
  \  -v [ --verbosity ] arg (=status)  Control the level of output to stderr. \n \
  \                                   (silent, error, status, warn, debug, \n    \
  \                                detail, all)  Default status.\n  -h [ --help ]\
  \                     Print help message.\n\n"
generated_using:
- --help
