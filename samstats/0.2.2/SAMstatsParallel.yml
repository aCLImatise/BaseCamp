!Command
positional: []
named:
- !Flag
  description: Input SAM file. Use '-' if input is being piped from stdin. File must
    be sorted by read name.
  synonyms:
  - --sorted_sam_file
  args: !SimpleFlagArg
    name: SORTED_SAM_FILE
  optional: true
- !Flag
  description: Output file name to store alignment statistics. The statistics will
    be printed to stdout if no file is provided
  synonyms:
  - --outf
  args: !SimpleFlagArg
    name: OUTF
  optional: true
- !Flag
  description: Number of lines to read a time from sortedSamFile
  synonyms:
  - --chunk_size
  args: !SimpleFlagArg
    name: CHUNK_SIZE
  optional: true
- !Flag
  description: 'number of threads to use. Note: the default is'
  synonyms:
  - --threads
  args: !SimpleFlagArg
    name: THREADS
  optional: true
command:
- SAMstatsParallel
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "usage: SAMstatsParallel [-h] --sorted_sam_file SORTED_SAM_FILE [--outf\
  \ OUTF]\n                        [--chunk_size CHUNK_SIZE] [--threads THREADS]\n\
  \nCompute SAM file mapping statistics for a SAM file sorted by read name\n\noptional\
  \ arguments:\n  -h, --help            show this help message and exit\n  --sorted_sam_file\
  \ SORTED_SAM_FILE\n                        Input SAM file. Use '-' if input is being\
  \ piped from\n                        stdin. File must be sorted by read name.\n\
  \  --outf OUTF           Output file name to store alignment statistics. The\n \
  \                       statistics will be printed to stdout if no file is\n   \
  \                     provided\n  --chunk_size CHUNK_SIZE\n                    \
  \    Number of lines to read a time from sortedSamFile\n  --threads THREADS    \
  \ number of threads to use. Note: the default is\n"
generated_using:
- --help
