!Command
positional:
- !Positional
  description: RDP assignment details downloaded as text from the RDP Classifier web
    portal or output by the command line RDP Classifier or Multiclassifier. By default,
    separate datasets will be created for each input (see [-c]).
  position: 0
  name: rdp_details
  optional: false
- !Positional
  description: A name to show in the list of datasets in the Krona chart (if multiple
    input files are present and [-c] is not specified). By default, the basename of
    the file will be used. _________
  position: 1
  name: name
  optional: false
named: []
command:
- ktImportRDP
subcommands: []
help_flag:
usage_flag:
version_flag:
