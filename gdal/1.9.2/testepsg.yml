!Command
positional: []
named:
- !Flag
  description: ': transform a coordinate from source GCS/PCS to target GCS/PCS'
  synonyms:
  - -t
  args: !EmptyFlagArg {}
  optional: true
command:
- testepsg
parent:
subcommands: []
help_flag:
usage_flag:
version_flag:
help_text: "testepsg [-xml] [-t src_def trg_def x y z]* [def]*\n  -t: transform a\
  \ coordinate from source GCS/PCS to target GCS/PCS\n\ndef's  on their own are translated\
  \ to WKT & XML and printed.\ndef's may be of any user input format, a WKT def, an\n\
  EPSG:n definition or the name of a file containing WKT/XML.\n"
generated_using: []
