!Command
positional: []
named:
- !Flag
  description: ': by default ogrtindex checks that all layers inserted into the index
    have the same attribute schemas. If you specify this option, this test will be
    disabled. Be aware that resulting index may be incompatible with MapServer!'
  synonyms:
  - -accept_different_schemas
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: ''
  synonyms:
  - -lnum
  args: !SimpleFlagArg
    name: n
  optional: true
command:
- ogrtindex
parent:
subcommands: []
help_flag:
usage_flag:
version_flag:
help_text: "Usage: ogrtindex [-lnum n]... [-lname name]... [-f output_format]\n  \
  \               [-write_absolute_path] [-skip_different_projection]\n          \
  \       [-accept_different_schemas]\n                 output_dataset src_dataset...\n\
  \n  -lnum n: Add layer number 'n' from each source file\n           in the tile\
  \ index.\n  -lname name: Add the layer named 'name' from each source file\n    \
  \           in the tile index.\n  -f output_format: Select an output format name.\
  \  The default\n                    is to create a shapefile.\n  -tileindex field_name:\
  \ The name to use for the dataset name.\n                         Defaults to LOCATION.\n\
  \  -write_absolute_path: Filenames are written with absolute paths.\n  -skip_different_projection:\
  \ Only layers with same projection ref \n        as layers already inserted in the\
  \ tileindex will be inserted.\n  -accept_different_schemas: by default ogrtindex\
  \ checks that all layers inserted\n                             into the index have\
  \ the same attribute schemas. If you\n                             specify this\
  \ option, this test will be disabled. Be aware that\n                          \
  \   resulting index may be incompatible with MapServer!\n\nIf no -lnum or -lname\
  \ arguments are given it is assumed that\nall layers in source datasets should be\
  \ added to the tile index\nas independent records.\n"
generated_using:
- --help
