!Command
command:
- behst-download-data
positional: []
named:
- !Flag
  description: "Folder where to download the default data files\n(default $XDG_DATA_HOME/behst,\
    \ if set, or\n$HOME/.local/share/behst otherwise)"
  synonyms:
  - -d
  - --data
  args: !SimpleFlagArg
    name: REFDIR
  optional: true
- !Flag
  description: "If activated, download only the minimal set of files\nto test BEHST\
    \ functionality (default files needed for\nvalid genomic set enrichment)"
  synonyms:
  - --small
  args: !EmptyFlagArg {}
  optional: true
parent:
subcommands: []
usage: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "usage: behst-download-data [-h] [-d REFDIR] [--small]\n\noptional arguments:\n\
  \  -h, --help            show this help message and exit\n  -d REFDIR, --data REFDIR\n\
  \                        Folder where to download the default data files\n     \
  \                   (default $XDG_DATA_HOME/behst, if set, or\n                \
  \        $HOME/.local/share/behst otherwise)\n  --small               If activated,\
  \ download only the minimal set of files\n                        to test BEHST\
  \ functionality (default files needed for\n                        valid genomic\
  \ set enrichment)\n\nCitation: Chicco D, Bi HS, Reimand J, Hoffman MM. 2018. \"\
  BEHST: Genomic set\nenrichment analysis enhanced through integration of chromatin\
  \ long-range\ninteractions\". In preparation.\n"
generated_using:
- --help
