!Command
positional:
- !Positional
  description: BED/GTF/GFF/VCF filenames, e.g., in a directory of bed files, you can
    use *.bed
  position: 0
  name: beds
  optional: false
named:
- !Flag
  description: Instead of counts, report fraction overlapped
  synonyms:
  - --frac
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: Run randomizations (default 1000, specify otherwise with --iterations)
    on each pairwise comparison and compute the enrichment score as (actual intersection
    count + 1) / (median randomized + 1)
  synonyms:
  - --enrichment
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: Required argument if --enrichment is used. Needs to be a string assembly
    name like "dm3" or "hg19"
  synonyms:
  - --genome
  args: !SimpleFlagArg
    name: GENOME
  optional: true
- !Flag
  description: Number of randomizations to perform for enrichement scores
  synonyms:
  - --iterations
  args: !SimpleFlagArg
    name: ITERATIONS
  optional: true
- !Flag
  description: Number of CPUs to use for randomization
  synonyms:
  - --processes
  args: !SimpleFlagArg
    name: PROCESSES
  optional: true
- !Flag
  description: Ignore any input BED files and use test BED files
  synonyms:
  - --test
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: 'Be verbose: print which files are currently being intersected and
    timing info at the end.'
  synonyms:
  - -v
  - --verbose
  args: !EmptyFlagArg {}
  optional: true
command:
- intersection_matrix.py
parent:
subcommands: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "usage: \n\n    Send in a list of `N` bed files, and this script will create\
  \ an N by\n    N matrix of their intersections, or optionally, co-localization scores.\n\
  \n    Run the example with::\n\n        /tmp/tmphzr59vlh/bin/intersection_matrix.py\
  \ --test > matrix.txt\n\n    You can then plot a quick heatmap in R with::\n\n \
  \       > m = as.matrix(read.table(\"matrix.txt\"))\n        > heatmap(m)\n\npositional\
  \ arguments:\n  beds                  BED/GTF/GFF/VCF filenames, e.g., in a directory\
  \ of bed\n                        files, you can use *.bed\n\noptional arguments:\n\
  \  -h, --help            show this help message and exit\n  --frac             \
  \   Instead of counts, report fraction overlapped\n  --enrichment          Run randomizations\
  \ (default 1000, specify otherwise\n                        with --iterations) on\
  \ each pairwise comparison and\n                        compute the enrichment score\
  \ as (actual intersection\n                        count + 1) / (median randomized\
  \ + 1)\n  --genome GENOME       Required argument if --enrichment is used. Needs\
  \ to be\n                        a string assembly name like \"dm3\" or \"hg19\"\
  \n  --iterations ITERATIONS\n                        Number of randomizations to\
  \ perform for enrichement\n                        scores\n  --processes PROCESSES\n\
  \                        Number of CPUs to use for randomization\n  --test     \
  \           Ignore any input BED files and use test BED files\n  -v, --verbose \
  \        Be verbose: print which files are currently being\n                   \
  \     intersected and timing info at the end.\n"
generated_using:
- --help
