!Command
command:
- index_gff
positional: []
named:
- !Flag
  description: "Index the given GFF. Takes as arguments as GFF filename\nand an output\
    \ directory."
  synonyms:
  - --index
  args: !SimpleFlagArg
    name: INDEX_GFF
  optional: true
- !Flag
  description: "Use the compressed version of the GFF 'ID=' field rather\nthan the\
    \ ID itself when creating .miso output filenames.\n"
  synonyms:
  - --compress-id
  args: !EmptyFlagArg {}
  optional: true
parent:
subcommands: []
usage: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "Usage: index_gff [options]\n\nOptions:\n  -h, --help         show this\
  \ help message and exit\n  --index=INDEX_GFF  Index the given GFF. Takes as arguments\
  \ as GFF filename\n                     and an output directory.\n  --compress-id\
  \      Use the compressed version of the GFF 'ID=' field rather\n              \
  \       than the ID itself when creating .miso output filenames.\n"
generated_using:
- --help
