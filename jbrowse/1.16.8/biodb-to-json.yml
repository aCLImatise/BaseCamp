!Command
positional: []
named:
- !Flag
  description: 'Directory where output should go. Default: data/'
  synonyms:
  - --out
  args: !SimpleFlagArg
    name: output directory
  optional: true
- !Flag
  description: If passed, compress the output with gzip (requires some web server
    configuration to serve properly).
  synonyms:
  - --compress
  args: !EmptyFlagArg {}
  optional: true
command:
- biodb-to-json.pl
parent:
subcommands: []
help_flag:
usage_flag:
version_flag:
help_text: "\e[1mNAME\e[0m\n    biodb-to-json.pl - format JBrowse JSON as described\
  \ in a configuration\n    file\n\n\e[1mDESCRIPTION\e[0m\n    Reads a configuration\
  \ file, in a format currently documented in\n    docs/config.html, and formats JBrowse\
  \ JSON from the data sources defined\n    in it.\n\n\e[1mUSAGE\e[0m\n      bin/biodb-to-json.pl\
  \                               \\\n        --conf <conf file>                 \
  \              \\\n        [--ref <ref seq names> | --refid <ref seq ids>]  \\\n\
  \        [--track <track name>]                           \\\n        [--out <output\
  \ directory>]                       \\\n        [--compress]\n\n\n      # format\
  \ the example volvox track data\n      bin/biodb-to-json.pl --conf docs/tutorial/conf_files/volvox.json\n\
  \n  \e[1mOPTIONS\e[0m\n    --help | -? | -h\n        Display an extended help screen.\n\
  \n    --quiet | -q\n        Quiet. Don't print progress messages.\n\n    --conf\
  \ <conf file>\n        Required. Path to the configuration file to read. File must\
  \ be in JSON\n        format.\n\n    --ref <ref seq name> | --refid <ref seq id>\n\
  \        Optional. Single reference sequence name or id for which to process\n \
  \       data.\n\n        By default, processes all data.\n\n    --out <output directory>\n\
  \        Directory where output should go. Default: data/\n\n    --compress\n  \
  \      If passed, compress the output with gzip (requires some web server\n    \
  \    configuration to serve properly).\n\n"
generated_using:
- --help
