!Command
command:
- igdiscover
- upstream
positional: []
named:
- !Flag
  description: 'Allow PERCENT errors in V gene match. Default: 1'
  synonyms:
  - --max-V-errors
  - --max-error-percentage
  - -e
  args: !SimpleFlagArg
    name: PERCENT
  optional: true
- !Flag
  description: Allow PERCENT errors in FR1 region.
  synonyms:
  - --max-FR1-errors
  args: !SimpleFlagArg
    name: PERCENT
  optional: true
- !Flag
  description: Allow PERCENT errors in CDR1 region.
  synonyms:
  - --max-CDR1-errors
  args: !SimpleFlagArg
    name: PERCENT
  optional: true
- !Flag
  description: 'Require at least N sequences for consensus. Default: 1'
  synonyms:
  - --min-consensus-size
  args: !SimpleFlagArg
    name: N
  optional: true
- !Flag
  description: 'Threshold for consensus computation. Default: 75%'
  synonyms:
  - --consensus-threshold
  - -t
  args: !SimpleFlagArg
    name: PERCENT
  optional: true
- !Flag
  description: Discard consensus sequences with ambiguous bases
  synonyms:
  - --no-ambiguous
  - --no-N
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: "{UTR,leader,UTR+leader}\nWhich part of the sequence before the V gene\
    \ match to\nanalyze. Default: UTR+leader"
  synonyms:
  - --part
  args: !EmptyFlagArg {}
  optional: true
- !Flag
  description: Enable debugging output
  synonyms:
  - --debug
  args: !EmptyFlagArg {}
  optional: true
parent:
subcommands: []
usage: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "usage: igdiscover upstream [-h] [--max-V-errors PERCENT]\n           \
  \                [--max-FR1-errors PERCENT]\n                           [--max-CDR1-errors\
  \ PERCENT]\n                           [--min-consensus-size N]\n              \
  \             [--consensus-threshold PERCENT] [--no-ambiguous]\n               \
  \            [--part {UTR,leader,UTR+leader}] [--debug]\n                      \
  \     table\n\nCluster upstream sequences (UTR and leader) for each gene\n\nFor\
  \ each gene, look at the sequences assigned to it. Take the upstream\nsequences\
  \ and compute a consensus for them. Only those assigned sequences are\ntaken that\
  \ have a very low error rate for the V gene match.\n\nOutput a FASTA file that contains\
  \ one consensus sequence for each gene.\n\npositional arguments:\n  table      \
  \           Table with parsed IgBLAST results (assigned.tab.gz or\n            \
  \            filtered.tab.gz)\n\noptional arguments:\n  -h, --help            show\
  \ this help message and exit\n  --max-V-errors PERCENT, --max-error-percentage PERCENT,\
  \ -e PERCENT\n                        Allow PERCENT errors in V gene match. Default:\
  \ 1\n  --max-FR1-errors PERCENT\n                        Allow PERCENT errors in\
  \ FR1 region.\n  --max-CDR1-errors PERCENT\n                        Allow PERCENT\
  \ errors in CDR1 region.\n  --min-consensus-size N\n                        Require\
  \ at least N sequences for consensus. Default: 1\n  --consensus-threshold PERCENT,\
  \ -t PERCENT\n                        Threshold for consensus computation. Default:\
  \ 75%\n  --no-ambiguous, --no-N\n                        Discard consensus sequences\
  \ with ambiguous bases\n  --part {UTR,leader,UTR+leader}\n                     \
  \   Which part of the sequence before the V gene match to\n                    \
  \    analyze. Default: UTR+leader\n  --debug               Enable debugging output\n"
generated_using:
- --help
