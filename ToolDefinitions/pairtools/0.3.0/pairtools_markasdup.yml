!Command
command:
- pairtools
- markasdup
positional:
- !Positional
  description: ''
  position: 0
  name: PAIRSAM_PATH
  optional: true
named:
- !Flag
  description: "output .pairsam file. If the path ends with .gz or\n.lz4, the output\
    \ is pbgzip-/lz4c-compressed. By\ndefault, the output is printed into stdout."
  synonyms:
  - -o
  - --output
  args: !SimpleFlagArg
    name: TEXT
  optional: true
- !Flag
  description: "Number of processes used by the auto-guessed input\ndecompressing\
    \ command.  [default: 3]"
  synonyms:
  - --nproc-in
  args: !SimpleFlagArg
    name: INTEGER
  optional: true
- !Flag
  description: "Number of processes used by the auto-guessed output\ncompressing command.\
    \  [default: 8]"
  synonyms:
  - --nproc-out
  args: !SimpleFlagArg
    name: INTEGER
  optional: true
- !Flag
  description: "A command to decompress the input file. If provided,\nfully overrides\
    \ the auto-guessed command. Does not work\nwith stdin. Must read input from stdin\
    \ and print output\ninto stdout. EXAMPLE: pbgzip -dc -n 3"
  synonyms:
  - --cmd-in
  args: !SimpleFlagArg
    name: TEXT
  optional: true
- !Flag
  description: "A command to compress the output file. If provided,\nfully overrides\
    \ the auto-guessed command. Does not work\nwith stdout. Must read input from stdin\
    \ and print\noutput into stdout. EXAMPLE: pbgzip -c -n 8"
  synonyms:
  - --cmd-out
  args: !SimpleFlagArg
    name: TEXT
  optional: true
parent:
subcommands: []
usage: []
help_flag: !Flag
  description: Show this message and exit.
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "Usage: pairtools markasdup [OPTIONS] [PAIRSAM_PATH]\n\n  Tag pairs as\
  \ duplicates.\n\n  Change the type of all pairs inside a .pairs/.pairsam file to\
  \ DD. If sam\n  entries are present, change the pair type in the Yt SAM tag to 'Yt:Z:DD'.\n\
  \n  PAIRSAM_PATH : input .pairs/.pairsam file. If the path ends with .gz, the\n\
  \  input is gzip-decompressed. By default, the input is read from stdin.\n\nOptions:\n\
  \  -o, --output TEXT    output .pairsam file. If the path ends with .gz or\n   \
  \                    .lz4, the output is pbgzip-/lz4c-compressed. By\n         \
  \              default, the output is printed into stdout.\n  --nproc-in INTEGER\
  \   Number of processes used by the auto-guessed input\n                       decompressing\
  \ command.  [default: 3]\n  --nproc-out INTEGER  Number of processes used by the\
  \ auto-guessed output\n                       compressing command.  [default: 8]\n\
  \  --cmd-in TEXT        A command to decompress the input file. If provided,\n \
  \                      fully overrides the auto-guessed command. Does not work\n\
  \                       with stdin. Must read input from stdin and print output\n\
  \                       into stdout. EXAMPLE: pbgzip -dc -n 3\n  --cmd-out TEXT\
  \       A command to compress the output file. If provided,\n                  \
  \     fully overrides the auto-guessed command. Does not work\n                \
  \       with stdout. Must read input from stdin and print\n                    \
  \   output into stdout. EXAMPLE: pbgzip -c -n 8\n  -h, --help           Show this\
  \ message and exit.\n"
generated_using:
- --help
