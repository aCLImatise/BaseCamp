!Command
command:
- outrigger
- psi
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - -i
  - --index
  description: "Name of the folder where you saved the output from\n\"outrigger index\"\
    \ (default is\n./outrigger_output/index, which is relative to the\ndirectory where\
    \ you called this program, assuming you\nhave called \"outrigger psi\" in the\
    \ same folder as you\ncalled \"outrigger index\")"
  args: !SimpleFlagArg
    name: INDEX
- !Flag
  optional: true
  synonyms:
  - -o
  - --output
  description: "Name of the folder where you saved the output from\n\"outrigger index\"\
    \ (default is ./outrigger_output,\nwhich is relative to the directory where you\
    \ called\nthe program). Cannot specify both an --index and\n--output with \"psi\""
  args: !SimpleFlagArg
    name: OUTPUT
- !Flag
  optional: true
  synonyms:
  - -c
  - --junction-reads-csv
  description: "Name of the compiled splice junction file to calculate\npsi scores\
    \ on. Default is the '--output' folder's\njunctions/reads.csv file. Not required\
    \ if you specify\nSJ.out.tab files with '--sj-out-tab'"
  args: !SimpleFlagArg
    name: JUNCTION_READS_CSV
- !Flag
  optional: true
  synonyms:
  - -j
  description: "[SJ_OUT_TAB [SJ_OUT_TAB ...]], --sj-out-tab [SJ_OUT_TAB [SJ_OUT_TAB\
    \ ...]]\nSJ.out.tab files from STAR aligner output. Not\nrequired if you specify\
    \ a file with \"--compiled-\njunction-reads\""
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -b
  description: "[BAM [BAM ...]], --bam [BAM [BAM ...]]\nBam files to use to calculate\
    \ psi on"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -m
  - --min-reads
  description: "Minimum number of reads per junction for calculating\nPsi (default=10)"
  args: !SimpleFlagArg
    name: MIN_READS
- !Flag
  optional: true
  synonyms:
  - -e
  - --method
  description: "How to deal with multiple junctions on an event - take\nthe mean (default)\
    \ or the min? (the other option)"
  args: !SimpleFlagArg
    name: METHOD
- !Flag
  optional: true
  synonyms:
  - -u
  - --uneven-coverage-multiplier
  description: "If a junction one one side of an exon is bigger than\nthe other side\
    \ of the exon by this amount, (default is\n10, so 10x bigger), then do not use\
    \ this event"
  args: !SimpleFlagArg
    name: UNEVEN_COVERAGE_MULTIPLIER
- !Flag
  optional: true
  synonyms:
  - --ignore-multimapping
  description: "Applies to STAR SJ.out.tab files only. If this flag is\nused, then\
    \ do not include reads that mapped to\nmultiple locations in the genome, not uniquely\
    \ to a\nlocus, in the read count for a junction. If inputting\n\"bam\" files,\
    \ then this means that reads with a mapping\nquality (MAPQ) of less than 255 are\
    \ considered\n\"multimapped.\" This is the same thing as what the STAR\naligner\
    \ does. By default, this is off, and all reads\nare used."
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --reads-col
  description: "Name of column in --splice-junction-csv containing\nreads to use.\
    \ (default='reads')"
  args: !SimpleFlagArg
    name: READS_COL
- !Flag
  optional: true
  synonyms:
  - --sample-id-col
  description: "Name of column in --splice-junction-csv containing\nsample ids to\
    \ use. (default='sample_id')"
  args: !SimpleFlagArg
    name: SAMPLE_ID_COL
- !Flag
  optional: true
  synonyms:
  - --junction-id-col
  description: "Name of column in --splice-junction-csv containing the\nID of the\
    \ junction to use. Must match exactly with the\njunctions in the index.(default='junction_id')"
  args: !SimpleFlagArg
    name: JUNCTION_ID_COL
- !Flag
  optional: true
  synonyms:
  - --debug
  description: "If given, print debugging logging information to\nstandard out"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --n-jobs
  description: "Number of threads to use when parallelizing psi\ncalculation and file\
    \ reading. Default is -1, which\nmeans to use as many threads as are available."
  args: !SimpleFlagArg
    name: N_JOBS
- !Flag
  optional: true
  synonyms:
  - --low-memory
  description: "If set, then use a smaller memory footprint. By\ndefault, this is\
    \ off.\n"
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: show this help message and exit
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "usage: outrigger psi [-h] [-i INDEX] [-o OUTPUT]\n                   \
  \  [-c JUNCTION_READS_CSV | -j [SJ_OUT_TAB [SJ_OUT_TAB ...]]\n                 \
  \    | -b [BAM [BAM ...]]] [-m MIN_READS] [-e METHOD]\n                     [-u\
  \ UNEVEN_COVERAGE_MULTIPLIER] [--ignore-multimapping]\n                     [--reads-col\
  \ READS_COL] [--sample-id-col SAMPLE_ID_COL]\n                     [--junction-id-col\
  \ JUNCTION_ID_COL] [--debug]\n                     [--n-jobs N_JOBS] [--low-memory]\n\
  \noptional arguments:\n  -h, --help            show this help message and exit\n\
  \  -i INDEX, --index INDEX\n                        Name of the folder where you\
  \ saved the output from\n                        \"outrigger index\" (default is\n\
  \                        ./outrigger_output/index, which is relative to the\n  \
  \                      directory where you called this program, assuming you\n \
  \                       have called \"outrigger psi\" in the same folder as you\n\
  \                        called \"outrigger index\")\n  -o OUTPUT, --output OUTPUT\n\
  \                        Name of the folder where you saved the output from\n  \
  \                      \"outrigger index\" (default is ./outrigger_output,\n   \
  \                     which is relative to the directory where you called\n    \
  \                    the program). Cannot specify both an --index and\n        \
  \                --output with \"psi\"\n  -c JUNCTION_READS_CSV, --junction-reads-csv\
  \ JUNCTION_READS_CSV\n                        Name of the compiled splice junction\
  \ file to calculate\n                        psi scores on. Default is the '--output'\
  \ folder's\n                        junctions/reads.csv file. Not required if you\
  \ specify\n                        SJ.out.tab files with '--sj-out-tab'\n  -j [SJ_OUT_TAB\
  \ [SJ_OUT_TAB ...]], --sj-out-tab [SJ_OUT_TAB [SJ_OUT_TAB ...]]\n              \
  \          SJ.out.tab files from STAR aligner output. Not\n                    \
  \    required if you specify a file with \"--compiled-\n                       \
  \ junction-reads\"\n  -b [BAM [BAM ...]], --bam [BAM [BAM ...]]\n              \
  \          Bam files to use to calculate psi on\n  -m MIN_READS, --min-reads MIN_READS\n\
  \                        Minimum number of reads per junction for calculating\n\
  \                        Psi (default=10)\n  -e METHOD, --method METHOD\n      \
  \                  How to deal with multiple junctions on an event - take\n    \
  \                    the mean (default) or the min? (the other option)\n  -u UNEVEN_COVERAGE_MULTIPLIER,\
  \ --uneven-coverage-multiplier UNEVEN_COVERAGE_MULTIPLIER\n                    \
  \    If a junction one one side of an exon is bigger than\n                    \
  \    the other side of the exon by this amount, (default is\n                  \
  \      10, so 10x bigger), then do not use this event\n  --ignore-multimapping\n\
  \                        Applies to STAR SJ.out.tab files only. If this flag is\n\
  \                        used, then do not include reads that mapped to\n      \
  \                  multiple locations in the genome, not uniquely to a\n       \
  \                 locus, in the read count for a junction. If inputting\n      \
  \                  \"bam\" files, then this means that reads with a mapping\n  \
  \                      quality (MAPQ) of less than 255 are considered\n        \
  \                \"multimapped.\" This is the same thing as what the STAR\n    \
  \                    aligner does. By default, this is off, and all reads\n    \
  \                    are used.\n  --reads-col READS_COL\n                      \
  \  Name of column in --splice-junction-csv containing\n                        reads\
  \ to use. (default='reads')\n  --sample-id-col SAMPLE_ID_COL\n                 \
  \       Name of column in --splice-junction-csv containing\n                   \
  \     sample ids to use. (default='sample_id')\n  --junction-id-col JUNCTION_ID_COL\n\
  \                        Name of column in --splice-junction-csv containing the\n\
  \                        ID of the junction to use. Must match exactly with the\n\
  \                        junctions in the index.(default='junction_id')\n  --debug\
  \               If given, print debugging logging information to\n             \
  \           standard out\n  --n-jobs N_JOBS       Number of threads to use when\
  \ parallelizing psi\n                        calculation and file reading. Default\
  \ is -1, which\n                        means to use as many threads as are available.\n\
  \  --low-memory          If set, then use a smaller memory footprint. By\n     \
  \                   default, this is off.\n"
generated_using:
- --help
docker_image:
