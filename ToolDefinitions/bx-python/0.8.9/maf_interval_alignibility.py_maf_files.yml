!Command
command:
- maf_interval_alignibility.py
- maf_files
positional:
- !Positional
  optional: false
  position: 0
  name: tested.
  description: 'usage: /usr/local/bin/maf_interval_alignibility.py maf_files [options]
    < interval_file'
named:
- !Flag
  optional: true
  synonyms:
  - -s
  - --species
  description: ': Comma separated list of species to include'
  args: !SimpleFlagArg
    name: SPECIES
- !Flag
  optional: true
  synonyms:
  - -p
  - --prefix
  description: ': Prefix to add to each interval chrom (usually reference species)'
  args: !SimpleFlagArg
    name: PREFIX
- !Flag
  optional: true
  synonyms:
  - -C
  - --usecache
  description: ':   Use a cache that keeps blocks of the MAF files in memory (requires
    ~20MB per MAF)'
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag:
usage_flag:
version_flag:
help_text: "\nWARNING: bz2/bz2t support and file cache support are new and not as\
  \ well\n         tested.\n\nusage: /usr/local/bin/maf_interval_alignibility.py maf_files\
  \ [options] < interval_file\n    -s, --species=SPECIES: Comma separated list of\
  \ species to include\n    -p, --prefix=PREFIX: Prefix to add to each interval chrom\
  \ (usually reference species)\n   -C, --usecache:   Use a cache that keeps blocks\
  \ of the MAF files in memory (requires ~20MB per MAF)\n\n"
generated_using: []
docker_image:
