!Command
command:
- shustring
positional:
- !Positional
  optional: false
  position: 0
  name: general
  description: "[-q quiet - do not print shustrings; default: print shustrings]\n\
    [-n nucleotide sequence (DNA); default: arbitrary ASCII strings]\n[-r include\
    \ reverse complement (implies -n); default: only forward strand]\n[-u preserve\
    \ IUPAC nomenclature in nucleotide sequences; default: convert to ACGT]\n[-p print\
    \ information about program]\n[-h print this help message]\n"
named: []
parent:
subcommands: []
usage: []
help_flag:
usage_flag:
version_flag:
help_text: "# unknown argument: -\nshustring version 2.6, copyright (c) 2005-2008\
  \ Bernhard Haubold & Thomas Wiehe\n\tdistributed under the GNU General Public License\n\
  purpose: compute shortest unique substrings\nusage: shustring [options]\noptions\n\
  \  input/output\n\t[-i <FILE> input file; default: FILE=stdin]\n\t[-o <FILE> write\
  \ output to FILE; default: FILE=stdout]\n  suffix array mode\n\t[-l <PATTERN> -\
  \ print local shustrings for PATTERN; default: global]\n\t[-M <NUM> shustring length\
  \ <= NUM (with -l only); default: find all]\n\t[-m <NUM> shustring length >= NUM\
  \ (with -l only); default: find all]\n  hash mode\n\t[-a hash mode (requires -i,\
  \ implies -n); default: suffix array mode]\n\t[-f <NUM> fixed word length NUM (with\
  \ -a only); default: look for shortest]\n  general\n\t[-q quiet - do not print shustrings;\
  \ default: print shustrings]\n\t[-n nucleotide sequence (DNA); default: arbitrary\
  \ ASCII strings]\n\t[-r include reverse complement (implies -n); default: only forward\
  \ strand]\n\t[-u preserve IUPAC nomenclature in nucleotide sequences; default: convert\
  \ to ACGT]\n\t[-p print information about program]\n\t[-h print this help message]\n"
generated_using:
- --help
docker_image:
