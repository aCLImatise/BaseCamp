!Command
command:
- iCount
- mapstar
positional:
- !Positional
  optional: false
  position: 0
  name: reads
  description: Sequencing reads to map to genome
- !Positional
  optional: false
  position: 1
  name: genome_index
  description: Folder with genome index
- !Positional
  optional: false
  position: 2
  name: out_dir
  description: Output folder, where to store mapping results
named:
- !Flag
  optional: true
  synonyms:
  - -a
  - --annotation
  description: 'GTF annotation needed for mapping to splice-junctions (default: )'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --multimax
  description: 'Number of allowed multiple hits (default: 10)'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -mis
  - --mismatches
  description: 'Number of allowed mismatches (default: 2)'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --threads
  description: 'Number of threads that STAR can use for generating index (default:
    1)'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -S
  - --stdout_log
  description: Threshold value (0-50) for logging to stdout. If 0, logging to stdout
    if turned OFF.
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -F
  - --file_log
  description: Threshold value (0-50) for logging to file. If 0, logging to file if
    turned OFF.
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -P
  - --file_logpath
  description: Path to log file.
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -M
  - --results_file
  description: File into which to store Metrics.
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: show this help message and exit
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "usage: iCount mapstar [-h] [-a] [--multimax] [-mis] [--threads] [-S] [-F]\
  \ [-P]\n                      [-M]\n                      reads genome_index out_dir\n\
  \nMap reads to genome with STAR.\n\npositional arguments:\n  reads             \
  \    Sequencing reads to map to genome\n  genome_index          Folder with genome\
  \ index\n  out_dir               Output folder, where to store mapping results\n\
  \noptional arguments:\n  -h, --help            show this help message and exit\n\
  \  -a , --annotation     GTF annotation needed for mapping to splice-junctions (default:\
  \ )\n  --multimax            Number of allowed multiple hits (default: 10)\n  -mis\
  \ , --mismatches   Number of allowed mismatches (default: 2)\n  --threads      \
  \       Number of threads that STAR can use for generating index (default: 1)\n\
  \  -S , --stdout_log     Threshold value (0-50) for logging to stdout. If 0, logging\
  \ to stdout if turned OFF.\n  -F , --file_log       Threshold value (0-50) for logging\
  \ to file. If 0, logging to file if turned OFF.\n  -P , --file_logpath   Path to\
  \ log file.\n  -M , --results_file   File into which to store Metrics.\n"
generated_using:
- --help
docker_image:
