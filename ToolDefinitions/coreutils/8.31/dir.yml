!Command
command:
- dir
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - -a
  - --all
  description: do not ignore entries starting with .
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -A
  - --almost-all
  description: do not list implied . and ..
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --author
  description: with -l, print the author of each file
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -b
  - --escape
  description: print C-style escapes for nongraphic characters
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --block-size
  description: "with -l, scale sizes by SIZE when printing them;\ne.g., '--block-size=M';\
    \ see SIZE format below"
  args: !SimpleFlagArg
    name: SIZE
- !Flag
  optional: true
  synonyms:
  - -B
  - --ignore-backups
  description: do not list implied entries ending with ~
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -c
  description: "with -lt: sort by, and show, ctime (time of last\nmodification of\
    \ file status information);\nwith -l: show ctime and sort by name;\notherwise:\
    \ sort by ctime, newest first"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -C
  description: list entries by columns
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --color
  description: "[=WHEN]         colorize the output; WHEN can be 'always' (default\n\
    if omitted), 'auto', or 'never'; more info below"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -d
  - --directory
  description: list directories themselves, not their contents
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -D
  - --dired
  description: generate output designed for Emacs' dired mode
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -f
  description: do not sort, enable -aU, disable -ls --color
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -F
  - --classify
  description: append indicator (one of */=>@|) to entries
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --file-type
  description: likewise, except do not append '*'
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --format
  description: "across -x, commas -m, horizontal -x, long -l,\nsingle-column -1, verbose\
    \ -l, vertical -C"
  args: !SimpleFlagArg
    name: WORD
- !Flag
  optional: true
  synonyms:
  - --full-time
  description: like -l --time-style=full-iso
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -g
  description: like -l, but do not list owner
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --group-directories-first
  description: "group directories before files;\ncan be augmented with a --sort option,\
    \ but any\nuse of --sort=none (-U) disables grouping"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -G
  - --no-group
  description: in a long listing, don't print group names
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --si
  description: likewise, but use powers of 1000 not 1024
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -H
  - --dereference-command-line
  description: follow symbolic links listed on the command line
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --dereference-command-line-symlink-to-dir
  description: "follow each command line symbolic link\nthat points to a directory"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --hide
  description: "do not list implied entries matching shell PATTERN\n(overridden by\
    \ -a or -A)"
  args: !SimpleFlagArg
    name: PATTERN
- !Flag
  optional: true
  synonyms:
  - --hyperlink
  description: "[=WHEN]     hyperlink file names; WHEN can be 'always'\n(default if\
    \ omitted), 'auto', or 'never'"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -p
  - --indicator-style
  description: append / indicator to directories
  args: !SimpleFlagArg
    name: slash
- !Flag
  optional: true
  synonyms:
  - -i
  - --inode
  description: print the index number of each file
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -I
  - --ignore
  description: do not list implied entries matching shell PATTERN
  args: !SimpleFlagArg
    name: PATTERN
- !Flag
  optional: true
  synonyms:
  - -k
  - --kibibytes
  description: "default to 1024-byte blocks for disk usage;\nused only with -s and\
    \ per directory totals"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -l
  description: use a long listing format
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -L
  - --dereference
  description: "when showing file information for a symbolic\nlink, show information\
    \ for the file the link\nreferences rather than for the link itself"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -m
  description: fill width with a comma separated list of entries
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -n
  - --numeric-uid-gid
  description: like -l, but list numeric user and group IDs
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -N
  - --literal
  description: print entry names without quoting
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -o
  description: like -l, but do not list group information
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -q
  - --hide-control-chars
  description: print ? instead of nongraphic characters
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --show-control-chars
  description: "show nongraphic characters as-is (the default,\nunless program is\
    \ 'ls' and output is a terminal)"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -Q
  - --quote-name
  description: enclose entry names in double quotes
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --quoting-style
  description: "use quoting style WORD for entry names:\nliteral, locale, shell, shell-always,\n\
    shell-escape, shell-escape-always, c, escape\n(overrides QUOTING_STYLE environment\
    \ variable)"
  args: !SimpleFlagArg
    name: WORD
- !Flag
  optional: true
  synonyms:
  - -r
  - --reverse
  description: reverse order while sorting
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -R
  - --recursive
  description: list subdirectories recursively
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -s
  - --size
  description: print the allocated size of each file, in blocks
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -S
  description: sort by file size, largest first
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --sort
  description: "sort by WORD instead of name: none (-U), size (-S),\ntime (-t), version\
    \ (-v), extension (-X)"
  args: !SimpleFlagArg
    name: WORD
- !Flag
  optional: true
  synonyms:
  - --time
  description: "with -l, show time as WORD instead of default\nmodification time:\
    \ atime or access or use (-u);\nctime or status (-c); also use specified time\n\
    as sort key if --sort=time (newest first)"
  args: !SimpleFlagArg
    name: WORD
- !Flag
  optional: true
  synonyms:
  - --time-style
  description: time/date format with -l; see TIME_STYLE below
  args: !SimpleFlagArg
    name: TIME_STYLE
- !Flag
  optional: true
  synonyms:
  - -t
  description: sort by modification time, newest first
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -T
  - --tabsize
  description: assume tab stops at each COLS instead of 8
  args: !SimpleFlagArg
    name: COLS
- !Flag
  optional: true
  synonyms:
  - -u
  description: "with -lt: sort by, and show, access time;\nwith -l: show access time\
    \ and sort by name;\notherwise: sort by access time, newest first"
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -U
  description: do not sort; list entries in directory order
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -v
  description: natural sort of (version) numbers within text
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -w
  - --width
  description: set output width to COLS.  0 means no limit
  args: !SimpleFlagArg
    name: COLS
- !Flag
  optional: true
  synonyms:
  - -x
  description: list entries by lines instead of by columns
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -X
  description: sort alphabetically by entry extension
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -Z
  - --context
  description: print any security context of each file
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - '-1'
  description: list one file per line.  Avoid '\n' with -q or -b
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - --help
  description: display this help and exit
  args: !EmptyFlagArg {}
usage_flag:
version_flag: !Flag
  optional: true
  synonyms:
  - --version
  description: output version information and exit
  args: !EmptyFlagArg {}
help_text: "Usage: dir [OPTION]... [FILE]...\nList information about the FILEs (the\
  \ current directory by default).\nSort entries alphabetically if none of -cftuvSUX\
  \ nor --sort is specified.\n\nMandatory arguments to long options are mandatory\
  \ for short options too.\n  -a, --all                  do not ignore entries starting\
  \ with .\n  -A, --almost-all           do not list implied . and ..\n      --author\
  \               with -l, print the author of each file\n  -b, --escape         \
  \      print C-style escapes for nongraphic characters\n      --block-size=SIZE\
  \      with -l, scale sizes by SIZE when printing them;\n                      \
  \         e.g., '--block-size=M'; see SIZE format below\n  -B, --ignore-backups\
  \       do not list implied entries ending with ~\n  -c                        \
  \ with -lt: sort by, and show, ctime (time of last\n                           \
  \    modification of file status information);\n                               with\
  \ -l: show ctime and sort by name;\n                               otherwise: sort\
  \ by ctime, newest first\n  -C                         list entries by columns\n\
  \      --color[=WHEN]         colorize the output; WHEN can be 'always' (default\n\
  \                               if omitted), 'auto', or 'never'; more info below\n\
  \  -d, --directory            list directories themselves, not their contents\n\
  \  -D, --dired                generate output designed for Emacs' dired mode\n \
  \ -f                         do not sort, enable -aU, disable -ls --color\n  -F,\
  \ --classify             append indicator (one of */=>@|) to entries\n      --file-type\
  \            likewise, except do not append '*'\n      --format=WORD          across\
  \ -x, commas -m, horizontal -x, long -l,\n                               single-column\
  \ -1, verbose -l, vertical -C\n      --full-time            like -l --time-style=full-iso\n\
  \  -g                         like -l, but do not list owner\n      --group-directories-first\n\
  \                             group directories before files;\n                \
  \               can be augmented with a --sort option, but any\n               \
  \                use of --sort=none (-U) disables grouping\n  -G, --no-group   \
  \          in a long listing, don't print group names\n  -h, --human-readable  \
  \     with -l and -s, print sizes like 1K 234M 2G etc.\n      --si             \
  \      likewise, but use powers of 1000 not 1024\n  -H, --dereference-command-line\n\
  \                             follow symbolic links listed on the command line\n\
  \      --dereference-command-line-symlink-to-dir\n                             follow\
  \ each command line symbolic link\n                               that points to\
  \ a directory\n      --hide=PATTERN         do not list implied entries matching\
  \ shell PATTERN\n                               (overridden by -a or -A)\n     \
  \ --hyperlink[=WHEN]     hyperlink file names; WHEN can be 'always'\n          \
  \                     (default if omitted), 'auto', or 'never'\n      --indicator-style=WORD\
  \  append indicator with style WORD to entry names:\n                          \
  \     none (default), slash (-p),\n                               file-type (--file-type),\
  \ classify (-F)\n  -i, --inode                print the index number of each file\n\
  \  -I, --ignore=PATTERN       do not list implied entries matching shell PATTERN\n\
  \  -k, --kibibytes            default to 1024-byte blocks for disk usage;\n    \
  \                           used only with -s and per directory totals\n  -l   \
  \                      use a long listing format\n  -L, --dereference          when\
  \ showing file information for a symbolic\n                               link,\
  \ show information for the file the link\n                               references\
  \ rather than for the link itself\n  -m                         fill width with\
  \ a comma separated list of entries\n  -n, --numeric-uid-gid      like -l, but list\
  \ numeric user and group IDs\n  -N, --literal              print entry names without\
  \ quoting\n  -o                         like -l, but do not list group information\n\
  \  -p, --indicator-style=slash\n                             append / indicator\
  \ to directories\n  -q, --hide-control-chars   print ? instead of nongraphic characters\n\
  \      --show-control-chars   show nongraphic characters as-is (the default,\n \
  \                              unless program is 'ls' and output is a terminal)\n\
  \  -Q, --quote-name           enclose entry names in double quotes\n      --quoting-style=WORD\
  \   use quoting style WORD for entry names:\n                               literal,\
  \ locale, shell, shell-always,\n                               shell-escape, shell-escape-always,\
  \ c, escape\n                               (overrides QUOTING_STYLE environment\
  \ variable)\n  -r, --reverse              reverse order while sorting\n  -R, --recursive\
  \            list subdirectories recursively\n  -s, --size                 print\
  \ the allocated size of each file, in blocks\n  -S                         sort\
  \ by file size, largest first\n      --sort=WORD            sort by WORD instead\
  \ of name: none (-U), size (-S),\n                               time (-t), version\
  \ (-v), extension (-X)\n      --time=WORD            with -l, show time as WORD\
  \ instead of default\n                               modification time: atime or\
  \ access or use (-u);\n                               ctime or status (-c); also\
  \ use specified time\n                               as sort key if --sort=time\
  \ (newest first)\n      --time-style=TIME_STYLE  time/date format with -l; see TIME_STYLE\
  \ below\n  -t                         sort by modification time, newest first\n\
  \  -T, --tabsize=COLS         assume tab stops at each COLS instead of 8\n  -u \
  \                        with -lt: sort by, and show, access time;\n           \
  \                    with -l: show access time and sort by name;\n             \
  \                  otherwise: sort by access time, newest first\n  -U          \
  \               do not sort; list entries in directory order\n  -v             \
  \            natural sort of (version) numbers within text\n  -w, --width=COLS \
  \          set output width to COLS.  0 means no limit\n  -x                   \
  \      list entries by lines instead of by columns\n  -X                       \
  \  sort alphabetically by entry extension\n  -Z, --context              print any\
  \ security context of each file\n  -1                         list one file per\
  \ line.  Avoid '\\n' with -q or -b\n      --help     display this help and exit\n\
  \      --version  output version information and exit\n\nThe SIZE argument is an\
  \ integer and optional unit (example: 10K is 10*1024).\nUnits are K,M,G,T,P,E,Z,Y\
  \ (powers of 1024) or KB,MB,... (powers of 1000).\nBinary prefixes can be used,\
  \ too: KiB=K, MiB=M, and so on.\n\nThe TIME_STYLE argument can be full-iso, long-iso,\
  \ iso, locale, or +FORMAT.\nFORMAT is interpreted like in date(1).  If FORMAT is\
  \ FORMAT1<newline>FORMAT2,\nthen FORMAT1 applies to non-recent files and FORMAT2\
  \ to recent files.\nTIME_STYLE prefixed with 'posix-' takes effect only outside\
  \ the POSIX locale.\nAlso the TIME_STYLE environment variable sets the default style\
  \ to use.\n\nUsing color to distinguish file types is disabled both by default and\n\
  with --color=never.  With --color=auto, ls emits color codes only when\nstandard\
  \ output is connected to a terminal.  The LS_COLORS environment\nvariable can change\
  \ the settings.  Use the dircolors command to set it.\n\nExit status:\n 0  if OK,\n\
  \ 1  if minor problems (e.g., cannot access subdirectory),\n 2  if serious trouble\
  \ (e.g., cannot access command-line argument).\n\nGNU coreutils online help: <https://www.gnu.org/software/coreutils/>\n\
  Report any translation bugs to <https://translationproject.org/team/>\nFull documentation\
  \ <https://www.gnu.org/software/coreutils/dir>\nor available locally via: info '(coreutils)\
  \ dir invocation'\n"
generated_using:
- --help
docker_image:
