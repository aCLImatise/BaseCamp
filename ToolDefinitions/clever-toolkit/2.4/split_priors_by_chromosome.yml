!Command
command:
- split-priors-by-chromosome
positional:
- !Positional
  optional: false
  position: 0
  name: list.
  description: -s SAMPLEWISE   Read header of given BAM file and priors split by sample
named:
- !Flag
  optional: true
  synonyms:
  - -z
  description: GZIP output files
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -c
  description: Only extracts given chromosomes. Expects comma-separated
  args: !SimpleFlagArg
    name: CHROMOSOMES
- !Flag
  optional: true
  synonyms:
  - -g
  description: "Use gzip instead of python zip module. Can be faster, but\nwill spawn\
    \ one gzip process for every chromosome/sample,\nwhich can be a lot.\n"
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: show this help message and exit
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "Usage: split-priors-by-chromosome [options] <output-prefix>\n\nReads alignment\
  \ priors (e.g. as generated by bam-to-alignment-priors) from \nstdin and writes\
  \ them to a separate file for each chromosome named\n<output-prefix>.<chromosome>.aln-priors.\n\
  \nOptions:\n  -h, --help      show this help message and exit\n  -z            \
  \  GZIP output files\n  -c CHROMOSOMES  Only extracts given chromosomes. Expects\
  \ comma-separated\n                  list.\n  -s SAMPLEWISE   Read header of given\
  \ BAM file and priors split by sample\n                  (encoded in read groups).\n\
  \  -g              Use gzip instead of python zip module. Can be faster, but\n \
  \                 will spawn one gzip process for every chromosome/sample,\n   \
  \               which can be a lot.\n"
generated_using:
- --help
docker_image:
