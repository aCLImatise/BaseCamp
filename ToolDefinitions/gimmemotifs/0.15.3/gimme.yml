!Command
command:
- gimme
positional:
- !Positional
  optional: false
  position: 0
  name: subcommand
  description: ''
named: []
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: "show this help message and exit\ncommands:\nmotifs      identify enriched\
    \ motifs (known and/or de novo)\nscan        scan for known motifs\nmaelstrom\
    \   find differential motifs\nmatch       find motif matches in database\nlogo\
    \        create sequence logo(s)\ncluster     cluster similar motifs\nbackground\
    \  create a background file\nthreshold   calculate motif scan threshold\nlocation\
    \    motif location histograms\ndiff        compare motif frequency and enrichment\n\
    between fasta files\ntype `gimme <command> -h` for more details\n"
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "\e[93mtype `gimme --help -h` for more details\e[0m\n\nusage: gimme [-h]\
  \ <subcommand> [options]\n\n    GimmeMotifs v0.15.3\n    \n\npositional arguments:\n\
  \  {motifs,scan,maelstrom,match,logo,cluster,background,threshold,location,diff,prediction}\n\
  \noptional arguments:\n  -h, --help            show this help message and exit\n\
  \n    commands:\n        motifs      identify enriched motifs (known and/or de novo)\n\
  \        scan        scan for known motifs\n        maelstrom   find differential\
  \ motifs\n        match       find motif matches in database\n        logo     \
  \   create sequence logo(s)\n        cluster     cluster similar motifs\n      \
  \  background  create a background file\n        threshold   calculate motif scan\
  \ threshold\n        location    motif location histograms\n        diff       \
  \ compare motif frequency and enrichment\n                    between fasta files\n\
  \n    type `gimme <command> -h` for more details\n    \n"
generated_using:
- --help
docker_image: quay.io/biocontainers/gimmemotifs:0.15.3--py38hc37a69a_0
