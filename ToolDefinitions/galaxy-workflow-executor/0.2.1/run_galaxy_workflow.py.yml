!Command
command:
- run_galaxy_workflow.py
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - -C
  - --conf
  description: A yaml file describing the galaxy credentials
  args: !SimpleFlagArg
    name: CONF
- !Flag
  optional: true
  synonyms:
  - -G
  - --galaxy-instance
  description: Galaxy server instance name
  args: !SimpleFlagArg
    name: GALAXY_INSTANCE
- !Flag
  optional: true
  synonyms:
  - -i
  - --yaml-inputs-path
  description: Path to Yaml detailing inputs
  args: !SimpleFlagArg
    name: YAML_INPUTS_PATH
- !Flag
  optional: true
  synonyms:
  - -o
  - --output-dir
  description: Path to output directory
  args: !SimpleFlagArg
    name: OUTPUT_DIR
- !Flag
  optional: true
  synonyms:
  - -H
  - --history
  description: Name of the history to create
  args: !SimpleFlagArg
    name: HISTORY
- !Flag
  optional: true
  synonyms:
  - -W
  - --workflow
  description: Workflow to run
  args: !SimpleFlagArg
    name: WORKFLOW
- !Flag
  optional: true
  synonyms:
  - -P
  - --parameters
  description: parameters file, by default json
  args: !SimpleFlagArg
    name: PARAMETERS
- !Flag
  optional: true
  synonyms:
  - --parameters-yaml
  description: read parameters file as yaml instead of json
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --debug
  description: Print debug information
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -a
  - --allowed-errors
  description: Yaml file with allowed steps that can have errors.
  args: !SimpleFlagArg
    name: ALLOWED_ERRORS
- !Flag
  optional: true
  synonyms:
  - -s
  - --state-file
  description: Path to read and save the execution state file.
  args: !SimpleFlagArg
    name: STATE_FILE
- !Flag
  optional: true
  synonyms:
  - -k
  - --keep-histories
  description: Keeps histories created, they will be purged if not.
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -w
  - --keep-workflow
  description: Keeps workflow created, it will be purged if not.
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --publish
  description: Keep result history and make it public/accesible.
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --accessible
  description: "Keep result history and make it accessible via link\nonly.\n"
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: show this help message and exit
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "usage: run_galaxy_workflow.py [-h] -C CONF [-G GALAXY_INSTANCE] -i\n \
  \                             YAML_INPUTS_PATH [-o OUTPUT_DIR] -H HISTORY -W\n \
  \                             WORKFLOW -P PARAMETERS [--parameters-yaml]\n     \
  \                         [--debug] [-a ALLOWED_ERRORS] [-s STATE_FILE]\n      \
  \                        [-k] [-w] [--publish] [--accessible]\n\noptional arguments:\n\
  \  -h, --help            show this help message and exit\n  -C CONF, --conf CONF\
  \  A yaml file describing the galaxy credentials\n  -G GALAXY_INSTANCE, --galaxy-instance\
  \ GALAXY_INSTANCE\n                        Galaxy server instance name\n  -i YAML_INPUTS_PATH,\
  \ --yaml-inputs-path YAML_INPUTS_PATH\n                        Path to Yaml detailing\
  \ inputs\n  -o OUTPUT_DIR, --output-dir OUTPUT_DIR\n                        Path\
  \ to output directory\n  -H HISTORY, --history HISTORY\n                       \
  \ Name of the history to create\n  -W WORKFLOW, --workflow WORKFLOW\n          \
  \              Workflow to run\n  -P PARAMETERS, --parameters PARAMETERS\n     \
  \                   parameters file, by default json\n  --parameters-yaml     read\
  \ parameters file as yaml instead of json\n  --debug               Print debug information\n\
  \  -a ALLOWED_ERRORS, --allowed-errors ALLOWED_ERRORS\n                        Yaml\
  \ file with allowed steps that can have errors.\n  -s STATE_FILE, --state-file STATE_FILE\n\
  \                        Path to read and save the execution state file.\n  -k,\
  \ --keep-histories  Keeps histories created, they will be purged if not.\n  -w,\
  \ --keep-workflow   Keeps workflow created, it will be purged if not.\n  --publish\
  \             Keep result history and make it public/accesible.\n  --accessible\
  \          Keep result history and make it accessible via link\n               \
  \         only.\n"
generated_using:
- --help
docker_image:
