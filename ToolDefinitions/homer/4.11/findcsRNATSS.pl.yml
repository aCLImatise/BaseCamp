!Command
command: &id001
- findcsRNATSS.pl
positional: []
named: []
parent:
subcommands:
- !Command
  command: &id002
  - findcsRNATSS.pl
  - annotatePeaks.pl
  positional: []
  named:
  - !Flag
    description: (*see note below)
    synonyms:
    - -o
    - -i
    args: !SimpleFlagArg
      name: prefix
    optional: true
  - !Flag
    description: (*see note below)
    synonyms:
    - -rna
    args: !SimpleFlagArg
      name: RNAseq tag directory
    optional: true
  - !Flag
    description: (HOMER genome identifier or FASTA file)
    synonyms:
    - -genome
    args: !SimpleFlagArg
      name: genome
    optional: true
  - !Flag
    description: (gene annotation, by default will use genome annotation if available)
    synonyms:
    - -gtf
    args: !SimpleFlagArg
      name: gtf file
    optional: true
  - !Flag
    description: '<#> (minimum number of norm. tags per TSS cluster, default: 7)'
    synonyms:
    - -ntagThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (size of regions to check for significant TSS clusters, default:
      150)'
    synonyms:
    - -size
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '(include single exon genes for TP/FP calculations, default: only
      use multiexon genes)'
    synonyms:
    - -includeSingleExons
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: (Include a metagene plot of read distributions)
    synonyms:
    - -metagene
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: <#> (max CPUs to use)
    synonyms:
    - -cpu
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)'
    synonyms:
    - -maxInputLog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)'
    synonyms:
    - -maxRNALog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum CDF difference to consider for TP/FP calculation, default:
      0.15)'
    synonyms:
    - -minDistDiff
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (if annotation-based TP/FP threshold calculation failes, default:
      1)'
    synonyms:
    - -defaultLog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (number of normalizaed reads added to input/target read counts,
      default: 1.0)'
    synonyms:
    - -pseudoCount
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (local fold enrichment used for initial TSS identification,
      default: 2)'
    synonyms:
    - -L
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: (only use RNAseq for stability calculations, not for filtering putative
      TSS)
    synonyms:
    - -noFilterRNA
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
      -100,500)'
    synonyms:
    - -senseRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
      -500,100)'
    synonyms:
    - -antisenseRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA coverage for FP identification,default:
      -150,-10)'
    synonyms:
    - -rnaRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum RNAseq read density for stable transcript, def: 2)'
    synonyms:
    - -stableThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum divergent csRNAseq signal for bi/divergent TSS, def:
      2)'
    synonyms:
    - -biThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: "<#> (minimum distance from annotated TSS to assign 'distal' annotation,\
      \ def: 500)"
    synonyms:
    - -dist
    args: !EmptyFlagArg {}
    optional: true
  parent: &id012 !Command
    command: *id001
    positional: []
    named: []
    parent:
    subcommands:
    - !Command
      command: *id002
      positional: []
      named:
      - !Flag
        description: (*see note below)
        synonyms:
        - -o
        - -i
        args: !SimpleFlagArg
          name: prefix
        optional: true
      - !Flag
        description: (*see note below)
        synonyms:
        - -rna
        args: !SimpleFlagArg
          name: RNAseq tag directory
        optional: true
      - !Flag
        description: (HOMER genome identifier or FASTA file)
        synonyms:
        - -genome
        args: !SimpleFlagArg
          name: genome
        optional: true
      - !Flag
        description: (gene annotation, by default will use genome annotation if available)
        synonyms:
        - -gtf
        args: !SimpleFlagArg
          name: gtf file
        optional: true
      - !Flag
        description: '<#> (minimum number of norm. tags per TSS cluster, default:
          7)'
        synonyms:
        - -ntagThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (size of regions to check for significant TSS clusters,
          default: 150)'
        synonyms:
        - -size
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '(include single exon genes for TP/FP calculations, default:
          only use multiexon genes)'
        synonyms:
        - -includeSingleExons
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: (Include a metagene plot of read distributions)
        synonyms:
        - -metagene
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: <#> (max CPUs to use)
        synonyms:
        - -cpu
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
          30000000000)'
        synonyms:
        - -maxInputLog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
          30000000000)'
        synonyms:
        - -maxRNALog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum CDF difference to consider for TP/FP calculation,
          default: 0.15)'
        synonyms:
        - -minDistDiff
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (if annotation-based TP/FP threshold calculation failes,
          default: 1)'
        synonyms:
        - -defaultLog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (number of normalizaed reads added to input/target read
          counts, default: 1.0)'
        synonyms:
        - -pseudoCount
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (local fold enrichment used for initial TSS identification,
          default: 2)'
        synonyms:
        - -L
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: (only use RNAseq for stability calculations, not for filtering
          putative TSS)
        synonyms:
        - -noFilterRNA
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
          -100,500)'
        synonyms:
        - -senseRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
          -500,100)'
        synonyms:
        - -antisenseRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA coverage for FP identification,default:
          -150,-10)'
        synonyms:
        - -rnaRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum RNAseq read density for stable transcript, def:
          2)'
        synonyms:
        - -stableThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum divergent csRNAseq signal for bi/divergent TSS,
          def: 2)'
        synonyms:
        - -biThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: "<#> (minimum distance from annotated TSS to assign 'distal'\
          \ annotation, def: 500)"
        synonyms:
        - -dist
        args: !EmptyFlagArg {}
        optional: true
      parent: &id011 !Command
        command: *id001
        positional: []
        named: []
        parent:
        subcommands:
        - !Command
          command: *id002
          positional: []
          named:
          - !Flag
            description: (*see note below)
            synonyms:
            - -o
            - -i
            args: !SimpleFlagArg
              name: prefix
            optional: true
          - !Flag
            description: (*see note below)
            synonyms:
            - -rna
            args: !SimpleFlagArg
              name: RNAseq tag directory
            optional: true
          - !Flag
            description: (HOMER genome identifier or FASTA file)
            synonyms:
            - -genome
            args: !SimpleFlagArg
              name: genome
            optional: true
          - !Flag
            description: (gene annotation, by default will use genome annotation if
              available)
            synonyms:
            - -gtf
            args: !SimpleFlagArg
              name: gtf file
            optional: true
          - !Flag
            description: '<#> (minimum number of norm. tags per TSS cluster, default:
              7)'
            synonyms:
            - -ntagThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (size of regions to check for significant TSS clusters,
              default: 150)'
            synonyms:
            - -size
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '(include single exon genes for TP/FP calculations, default:
              only use multiexon genes)'
            synonyms:
            - -includeSingleExons
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: (Include a metagene plot of read distributions)
            synonyms:
            - -metagene
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: <#> (max CPUs to use)
            synonyms:
            - -cpu
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
              30000000000)'
            synonyms:
            - -maxInputLog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
              30000000000)'
            synonyms:
            - -maxRNALog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum CDF difference to consider for TP/FP calculation,
              default: 0.15)'
            synonyms:
            - -minDistDiff
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (if annotation-based TP/FP threshold calculation failes,
              default: 1)'
            synonyms:
            - -defaultLog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (number of normalizaed reads added to input/target read
              counts, default: 1.0)'
            synonyms:
            - -pseudoCount
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (local fold enrichment used for initial TSS identification,
              default: 2)'
            synonyms:
            - -L
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: (only use RNAseq for stability calculations, not for filtering
              putative TSS)
            synonyms:
            - -noFilterRNA
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
              -100,500)'
            synonyms:
            - -senseRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
              -500,100)'
            synonyms:
            - -antisenseRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA coverage for FP
              identification,default: -150,-10)'
            synonyms:
            - -rnaRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum RNAseq read density for stable transcript,
              def: 2)'
            synonyms:
            - -stableThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum divergent csRNAseq signal for bi/divergent
              TSS, def: 2)'
            synonyms:
            - -biThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: "<#> (minimum distance from annotated TSS to assign 'distal'\
              \ annotation, def: 500)"
            synonyms:
            - -dist
            args: !EmptyFlagArg {}
            optional: true
          parent: &id003 !Command
            command: *id001
            positional: []
            named:
            - !Flag
              description: "(file name for to output peaks, default: stdout)\n\"-o\
                \ auto\" will send output to \"<tag directory>/peaks.txt\", \".../regions.txt\"\
                ,\nor \".../transcripts.txt\" depending on the \"-style\" option"
              synonyms:
              - -o
              args: !SimpleFlagArg
                name: filename|auto
              optional: true
            - !Flag
              description: "(Specialized options for specific analysis strategies)\n\
                factor (transcription factor ChIP-Seq, uses -center, output: peaks.txt,\
                \  default)\nhistone (histone modification ChIP-Seq, region based,\
                \ uses -region -size 500 -L 0, regions.txt)\ngroseq (de novo transcript\
                \ identification from GroSeq data, transcripts.txt)\ntss (TSS identification\
                \ from 5' RNA sequencing, tss.txt)\ndnase (Hypersensitivity [crawford\
                \ style (nicking)], peaks.txt)\nsuper (Super Enhancers, superEnhancers.txt)\n\
                superhistone (Super Enhancers from H3K27ac data, superEnhancers.txt)\n\
                mC (Cytosine methylation (BS-seq/methylC-seq), regions.txt)\ndamid\
                \ (DamID enrichment from DpnI digestion, regions.txt)\nclip (CLIP-Seq\
                \ enrichment, strand specific, peaks.txt)"
              synonyms:
              - -style
              args: !SimpleFlagArg
                name: option
              optional: true
            - !Flag
              description: (Experiment to use as IgG/Input/Control)
              synonyms:
              - -i
              args: !SimpleFlagArg
                name: input tag directory
              optional: true
            - !Flag
              description: '<#> (Peak size, default: auto)'
              synonyms:
              - -size
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (minimum distance between peaks, default: peak size
                x2)'
              synonyms:
              - -minDist
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (Set effective mappable genome size, default: 2e9)'
              synonyms:
              - -gsize
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#|auto> (Approximate fragment length, default: auto)'
              synonyms:
              - -fragLength
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#|auto> (Approximate fragment length of input tags, default:
                auto)'
              synonyms:
              - -inputFragLength
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (Maximum tags per bp to count, 0 = no limit, default:
                auto)'
              synonyms:
              - -tbp
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (Maximum tags per bp to count in input, 0 = no limit,
                default: auto)'
              synonyms:
              - -inputtbp
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: (find peaks using tags on both strands or separate, default:both)
              synonyms:
              - -strand
              args: !SimpleFlagArg
                name: both|separate
              optional: true
            - !Flag
              description: '# (Tag count to normalize to, default 10000000)'
              synonyms:
              - -norm
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: (extends start/stop coordinates to cover full region considered
                "enriched")
              synonyms:
              - -region
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: "<#> (number of fractions peaks are divided in when extending\
                \ 'regions', def: 4)"
              synonyms:
              - -regionRes
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: (Centers peaks on maximum tag overlap and calculates focus
                ratios)
              synonyms:
              - -center
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: "(Centers peaks on most likely nucleosome free region [works\
                \ best with mnase data])\n(-center and -nfr can be performed later\
                \ with \"getPeakTags\""
              synonyms:
              - -nfr
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (fold enrichment over input tag count, default: 4.0)'
              synonyms:
              - -F
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (poisson p-value threshold relative to input tag count,
                default: 0.0001)'
              synonyms:
              - -P
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (fold enrichment over local tag count, default: 4.0)'
              synonyms:
              - -L
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (poisson p-value threshold relative to local tag count,
                default: 0.0001)'
              synonyms:
              - -LP
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (fold enrichment limit of expected unique tag positions,
                default: 2.0)'
              synonyms:
              - -C
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (region to check for local tag enrichment, default:
                10000)'
              synonyms:
              - -localSize
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (Size of region to search for control tags, default:
                2x peak size)'
              synonyms:
              - -inputSize
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: <#> (False discovery rate, default = 0.001)
              synonyms:
              - -fdr
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (Set poisson p-value cutoff, default: uses fdr)'
              synonyms:
              - -poisson
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (Set # of tags to define a peak, default: 25)'
              synonyms:
              - -tagThreshold
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (Set # of normalized tags to define a peak, by default
                uses 1e7 for norm)'
              synonyms:
              - -ntagThreshold
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (Absolute minimum tags per peak, default: expected
                tags per peak)'
              synonyms:
              - -minTagThreshold
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (Slope threshold to identify super vs. typical enh.,
                default: 1.00)'
              synonyms:
              - -superSlope
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (moving window/number of peaks to use to calculate
                slope, default: 10)'
              synonyms:
              - -superWindow
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '(Output typical enhancers to this file, default: not used)'
              synonyms:
              - -typical
              args: !SimpleFlagArg
                name: filename
              optional: true
            - !Flag
              description: (initial peaks to use for super enhancer merging/scoring)
              synonyms:
              - -inputPeaks
              args: !SimpleFlagArg
                name: filename
              optional: true
            - !Flag
              description: (regions to exclude from analysis, i.e. TSS regions for
                H3K27ac)
              synonyms:
              - -excludePeaks
              args: !SimpleFlagArg
                name: filename
              optional: true
            - !Flag
              description: '/ -methylC (find unmethylated/methylated regions, default:
                -unmethyC)'
              synonyms:
              - -unmethylC
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (methylation threshold of regions, default: avg methylation/2)'
              synonyms:
              - -mCthresh
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (Minimum number of cytosines per methylation peak,
                default: 6)'
              synonyms:
              - -minNumC
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (size of region for initiation detection/artifact
                size, default: 250)'
              synonyms:
              - -tssSize
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (size of regoin for transcript body detection, default:
                1000)'
              synonyms:
              - -minBodySize
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (fold enrichment for new initiation dectection, default:
                4.0)'
              synonyms:
              - -tssFold
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (fold enrichment for new transcript dectection, default:
                4.0)'
              synonyms:
              - -bodyFold
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (end transcript when levels are this much less than
                the start, default: 10.0)'
              synonyms:
              - -endFold
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '(method used for identifying new transcripts, default:
                fold)'
              synonyms:
              - -method
              args: !SimpleFlagArg
                name: fold|level
              optional: true
            - !Flag
              description: "(directory of binary files specifying uniquely mappable\
                \ locations)\nDownload from http://biowhat.ucsd.edu/homer/groseq/"
              synonyms:
              - -uniqmap
              args: !SimpleFlagArg
                name: directory
              optional: true
            - !Flag
              description: '<#> (confidence p-value: 1.00e-05)'
              synonyms:
              - -confPvalue
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (Minimum initial read depth for transcripts, default:
                auto)'
              synonyms:
              - -minReadDepth
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: '<#> (Pseudo tag count, default: 2.0)'
              synonyms:
              - -pseudoCount
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: (reverse strand of reads - for first-strand rna-seq/gro-seq)
              synonyms:
              - -rev
              args: !EmptyFlagArg {}
              optional: true
            - !Flag
              description: "(Output de novo transcripts in GTF format)\n\"-o auto\"\
                \ will produce <dir>/transcripts.txt and <dir>/transcripts.gtf"
              synonyms:
              - -gtf
              args: !SimpleFlagArg
                name: filename
              optional: true
            parent:
            subcommands:
            - !Command
              command: *id002
              positional: []
              named:
              - !Flag
                description: (*see note below)
                synonyms:
                - -o
                - -i
                args: !SimpleFlagArg
                  name: prefix
                optional: true
              - !Flag
                description: (*see note below)
                synonyms:
                - -rna
                args: !SimpleFlagArg
                  name: RNAseq tag directory
                optional: true
              - !Flag
                description: (HOMER genome identifier or FASTA file)
                synonyms:
                - -genome
                args: !SimpleFlagArg
                  name: genome
                optional: true
              - !Flag
                description: (gene annotation, by default will use genome annotation
                  if available)
                synonyms:
                - -gtf
                args: !SimpleFlagArg
                  name: gtf file
                optional: true
              - !Flag
                description: '<#> (minimum number of norm. tags per TSS cluster, default:
                  7)'
                synonyms:
                - -ntagThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (size of regions to check for significant TSS clusters,
                  default: 150)'
                synonyms:
                - -size
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '(include single exon genes for TP/FP calculations, default:
                  only use multiexon genes)'
                synonyms:
                - -includeSingleExons
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: (Include a metagene plot of read distributions)
                synonyms:
                - -metagene
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: <#> (max CPUs to use)
                synonyms:
                - -cpu
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (maximum log2 fold enrichment vs. input or RNA,
                  default: 30000000000)'
                synonyms:
                - -maxInputLog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (maximum log2 fold enrichment vs. input or RNA,
                  default: 30000000000)'
                synonyms:
                - -maxRNALog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum CDF difference to consider for TP/FP calculation,
                  default: 0.15)'
                synonyms:
                - -minDistDiff
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (if annotation-based TP/FP threshold calculation
                  failes, default: 1)'
                synonyms:
                - -defaultLog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (number of normalizaed reads added to input/target
                  read counts, default: 1.0)'
                synonyms:
                - -pseudoCount
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (local fold enrichment used for initial TSS identification,
                  default: 2)'
                synonyms:
                - -L
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: (only use RNAseq for stability calculations, not for
                  filtering putative TSS)
                synonyms:
                - -noFilterRNA
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA/input read
                  density,default: -100,500)'
                synonyms:
                - -senseRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA/input read
                  density,default: -500,100)'
                synonyms:
                - -antisenseRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA coverage for
                  FP identification,default: -150,-10)'
                synonyms:
                - -rnaRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum RNAseq read density for stable transcript,
                  def: 2)'
                synonyms:
                - -stableThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum divergent csRNAseq signal for bi/divergent
                  TSS, def: 2)'
                synonyms:
                - -biThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: "<#> (minimum distance from annotated TSS to assign 'distal'\
                  \ annotation, def: 500)"
                synonyms:
                - -dist
                args: !EmptyFlagArg {}
                optional: true
              parent: *id003
              subcommands: []
              usage: []
              help_flag:
              usage_flag:
              version_flag:
              help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\t\
                Options:\n\t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see\
                \ note below)\n\t\t-rna <RNAseq tag directory> (*see note below)\n\
                \t\t-genome <genome> (HOMER genome identifier or FASTA file)\n\t\t\
                -gtf <gtf file> (gene annotation, by default will use genome annotation\
                \ if available)\n\t\t\n\t\t-ntagThreshold <#> (minimum number of norm.\
                \ tags per TSS cluster, default: 7)\n\t\t-size <#> (size of regions\
                \ to check for significant TSS clusters, default: 150)\n\t\t-includeSingleExons\
                \ (include single exon genes for TP/FP calculations, default: only\
                \ use multiexon genes)\n\t\t-metagene (Include a metagene plot of\
                \ read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\tEnrichment\
                \ threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2 fold\
                \ enrichment vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold\
                \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\
                \t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP\
                \ calculation, default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based\
                \ TP/FP threshold calculation failes, default: 1)\n\t\t-pseudoCount\
                \ <#> (number of normalizaed reads added to input/target read counts,\
                \ default: 1.0)\n\t\t-ntagThreshold <#> (minimum number of norm. tags\
                \ per TSS cluster, default: 7)\n\t\t-L <#> (local fold enrichment\
                \ used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
                \ (only use RNAseq for stability calculations, not for filtering putative\
                \ TSS)\n\n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check\
                \ for significant TSS clusters, default: 150)\n\t\t-senseRegion <#>,<#>\
                \ (Size of region to calculate RNA/input read density,default: -100,500)\n\
                \t\t-antisenseRegion <#>,<#> (Size of region to calculate RNA/input\
                \ read density,default: -500,100)\n\t\t-rnaRegion <#>,<#> (Size of\
                \ region to calculate RNA coverage for FP identification,default:\
                \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum\
                \ RNAseq read density for stable transcript, def: 2)\n\t\t-biThreshold\
                \ <#> (minimum divergent csRNAseq signal for bi/divergent TSS, def:\
                \ 2)\n\t\t-dist <#> (minimum distance from annotated TSS to assign\
                \ 'distal' annotation, def: 500)\n\n\t* Tag Directories are assumed\
                \ to represent the 5' ends of RNA in sense orientation\n\n"
              generated_using: &id004
              - --help
            - !Command
              command: &id005
              - findcsRNATSS.pl
              - genome version
              positional: []
              named:
              - !Flag
                description: (*see note below)
                synonyms:
                - -o
                - -i
                args: !SimpleFlagArg
                  name: prefix
                optional: true
              - !Flag
                description: (*see note below)
                synonyms:
                - -rna
                args: !SimpleFlagArg
                  name: RNAseq tag directory
                optional: true
              - !Flag
                description: (HOMER genome identifier or FASTA file)
                synonyms:
                - -genome
                args: !SimpleFlagArg
                  name: genome
                optional: true
              - !Flag
                description: (gene annotation, by default will use genome annotation
                  if available)
                synonyms:
                - -gtf
                args: !SimpleFlagArg
                  name: gtf file
                optional: true
              - !Flag
                description: '<#> (minimum number of norm. tags per TSS cluster, default:
                  7)'
                synonyms:
                - -ntagThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (size of regions to check for significant TSS clusters,
                  default: 150)'
                synonyms:
                - -size
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '(include single exon genes for TP/FP calculations, default:
                  only use multiexon genes)'
                synonyms:
                - -includeSingleExons
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: (Include a metagene plot of read distributions)
                synonyms:
                - -metagene
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: <#> (max CPUs to use)
                synonyms:
                - -cpu
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (maximum log2 fold enrichment vs. input or RNA,
                  default: 30000000000)'
                synonyms:
                - -maxInputLog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (maximum log2 fold enrichment vs. input or RNA,
                  default: 30000000000)'
                synonyms:
                - -maxRNALog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum CDF difference to consider for TP/FP calculation,
                  default: 0.15)'
                synonyms:
                - -minDistDiff
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (if annotation-based TP/FP threshold calculation
                  failes, default: 1)'
                synonyms:
                - -defaultLog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (number of normalizaed reads added to input/target
                  read counts, default: 1.0)'
                synonyms:
                - -pseudoCount
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (local fold enrichment used for initial TSS identification,
                  default: 2)'
                synonyms:
                - -L
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: (only use RNAseq for stability calculations, not for
                  filtering putative TSS)
                synonyms:
                - -noFilterRNA
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA/input read
                  density,default: -100,500)'
                synonyms:
                - -senseRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA/input read
                  density,default: -500,100)'
                synonyms:
                - -antisenseRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA coverage for
                  FP identification,default: -150,-10)'
                synonyms:
                - -rnaRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum RNAseq read density for stable transcript,
                  def: 2)'
                synonyms:
                - -stableThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum divergent csRNAseq signal for bi/divergent
                  TSS, def: 2)'
                synonyms:
                - -biThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: "<#> (minimum distance from annotated TSS to assign 'distal'\
                  \ annotation, def: 500)"
                synonyms:
                - -dist
                args: !EmptyFlagArg {}
                optional: true
              parent: *id003
              subcommands: []
              usage: []
              help_flag:
              usage_flag:
              version_flag:
              help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\t\
                Options:\n\t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see\
                \ note below)\n\t\t-rna <RNAseq tag directory> (*see note below)\n\
                \t\t-genome <genome> (HOMER genome identifier or FASTA file)\n\t\t\
                -gtf <gtf file> (gene annotation, by default will use genome annotation\
                \ if available)\n\t\t\n\t\t-ntagThreshold <#> (minimum number of norm.\
                \ tags per TSS cluster, default: 7)\n\t\t-size <#> (size of regions\
                \ to check for significant TSS clusters, default: 150)\n\t\t-includeSingleExons\
                \ (include single exon genes for TP/FP calculations, default: only\
                \ use multiexon genes)\n\t\t-metagene (Include a metagene plot of\
                \ read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\tEnrichment\
                \ threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2 fold\
                \ enrichment vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold\
                \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\
                \t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP\
                \ calculation, default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based\
                \ TP/FP threshold calculation failes, default: 1)\n\t\t-pseudoCount\
                \ <#> (number of normalizaed reads added to input/target read counts,\
                \ default: 1.0)\n\t\t-ntagThreshold <#> (minimum number of norm. tags\
                \ per TSS cluster, default: 7)\n\t\t-L <#> (local fold enrichment\
                \ used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
                \ (only use RNAseq for stability calculations, not for filtering putative\
                \ TSS)\n\n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check\
                \ for significant TSS clusters, default: 150)\n\t\t-senseRegion <#>,<#>\
                \ (Size of region to calculate RNA/input read density,default: -100,500)\n\
                \t\t-antisenseRegion <#>,<#> (Size of region to calculate RNA/input\
                \ read density,default: -500,100)\n\t\t-rnaRegion <#>,<#> (Size of\
                \ region to calculate RNA coverage for FP identification,default:\
                \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum\
                \ RNAseq read density for stable transcript, def: 2)\n\t\t-biThreshold\
                \ <#> (minimum divergent csRNAseq signal for bi/divergent TSS, def:\
                \ 2)\n\t\t-dist <#> (minimum distance from annotated TSS to assign\
                \ 'distal' annotation, def: 500)\n\n\t* Tag Directories are assumed\
                \ to represent the 5' ends of RNA in sense orientation\n\n"
              generated_using: *id004
            - !Command
              command: &id006
              - findcsRNATSS.pl
              - options...
              positional: []
              named:
              - !Flag
                description: (*see note below)
                synonyms:
                - -o
                - -i
                args: !SimpleFlagArg
                  name: prefix
                optional: true
              - !Flag
                description: (*see note below)
                synonyms:
                - -rna
                args: !SimpleFlagArg
                  name: RNAseq tag directory
                optional: true
              - !Flag
                description: (HOMER genome identifier or FASTA file)
                synonyms:
                - -genome
                args: !SimpleFlagArg
                  name: genome
                optional: true
              - !Flag
                description: (gene annotation, by default will use genome annotation
                  if available)
                synonyms:
                - -gtf
                args: !SimpleFlagArg
                  name: gtf file
                optional: true
              - !Flag
                description: '<#> (minimum number of norm. tags per TSS cluster, default:
                  7)'
                synonyms:
                - -ntagThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (size of regions to check for significant TSS clusters,
                  default: 150)'
                synonyms:
                - -size
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '(include single exon genes for TP/FP calculations, default:
                  only use multiexon genes)'
                synonyms:
                - -includeSingleExons
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: (Include a metagene plot of read distributions)
                synonyms:
                - -metagene
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: <#> (max CPUs to use)
                synonyms:
                - -cpu
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (maximum log2 fold enrichment vs. input or RNA,
                  default: 30000000000)'
                synonyms:
                - -maxInputLog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (maximum log2 fold enrichment vs. input or RNA,
                  default: 30000000000)'
                synonyms:
                - -maxRNALog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum CDF difference to consider for TP/FP calculation,
                  default: 0.15)'
                synonyms:
                - -minDistDiff
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (if annotation-based TP/FP threshold calculation
                  failes, default: 1)'
                synonyms:
                - -defaultLog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (number of normalizaed reads added to input/target
                  read counts, default: 1.0)'
                synonyms:
                - -pseudoCount
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (local fold enrichment used for initial TSS identification,
                  default: 2)'
                synonyms:
                - -L
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: (only use RNAseq for stability calculations, not for
                  filtering putative TSS)
                synonyms:
                - -noFilterRNA
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA/input read
                  density,default: -100,500)'
                synonyms:
                - -senseRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA/input read
                  density,default: -500,100)'
                synonyms:
                - -antisenseRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA coverage for
                  FP identification,default: -150,-10)'
                synonyms:
                - -rnaRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum RNAseq read density for stable transcript,
                  def: 2)'
                synonyms:
                - -stableThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum divergent csRNAseq signal for bi/divergent
                  TSS, def: 2)'
                synonyms:
                - -biThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: "<#> (minimum distance from annotated TSS to assign 'distal'\
                  \ annotation, def: 500)"
                synonyms:
                - -dist
                args: !EmptyFlagArg {}
                optional: true
              parent: *id003
              subcommands: []
              usage: []
              help_flag:
              usage_flag:
              version_flag:
              help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\t\
                Options:\n\t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see\
                \ note below)\n\t\t-rna <RNAseq tag directory> (*see note below)\n\
                \t\t-genome <genome> (HOMER genome identifier or FASTA file)\n\t\t\
                -gtf <gtf file> (gene annotation, by default will use genome annotation\
                \ if available)\n\t\t\n\t\t-ntagThreshold <#> (minimum number of norm.\
                \ tags per TSS cluster, default: 7)\n\t\t-size <#> (size of regions\
                \ to check for significant TSS clusters, default: 150)\n\t\t-includeSingleExons\
                \ (include single exon genes for TP/FP calculations, default: only\
                \ use multiexon genes)\n\t\t-metagene (Include a metagene plot of\
                \ read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\tEnrichment\
                \ threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2 fold\
                \ enrichment vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold\
                \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\
                \t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP\
                \ calculation, default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based\
                \ TP/FP threshold calculation failes, default: 1)\n\t\t-pseudoCount\
                \ <#> (number of normalizaed reads added to input/target read counts,\
                \ default: 1.0)\n\t\t-ntagThreshold <#> (minimum number of norm. tags\
                \ per TSS cluster, default: 7)\n\t\t-L <#> (local fold enrichment\
                \ used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
                \ (only use RNAseq for stability calculations, not for filtering putative\
                \ TSS)\n\n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check\
                \ for significant TSS clusters, default: 150)\n\t\t-senseRegion <#>,<#>\
                \ (Size of region to calculate RNA/input read density,default: -100,500)\n\
                \t\t-antisenseRegion <#>,<#> (Size of region to calculate RNA/input\
                \ read density,default: -500,100)\n\t\t-rnaRegion <#>,<#> (Size of\
                \ region to calculate RNA coverage for FP identification,default:\
                \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum\
                \ RNAseq read density for stable transcript, def: 2)\n\t\t-biThreshold\
                \ <#> (minimum divergent csRNAseq signal for bi/divergent TSS, def:\
                \ 2)\n\t\t-dist <#> (minimum distance from annotated TSS to assign\
                \ 'distal' annotation, def: 500)\n\n\t* Tag Directories are assumed\
                \ to represent the 5' ends of RNA in sense orientation\n\n"
              generated_using: *id004
            - !Command
              command: &id007
              - findcsRNATSS.pl
              - findPeaks
              positional: []
              named:
              - !Flag
                description: (*see note below)
                synonyms:
                - -o
                - -i
                args: !SimpleFlagArg
                  name: prefix
                optional: true
              - !Flag
                description: (*see note below)
                synonyms:
                - -rna
                args: !SimpleFlagArg
                  name: RNAseq tag directory
                optional: true
              - !Flag
                description: (HOMER genome identifier or FASTA file)
                synonyms:
                - -genome
                args: !SimpleFlagArg
                  name: genome
                optional: true
              - !Flag
                description: (gene annotation, by default will use genome annotation
                  if available)
                synonyms:
                - -gtf
                args: !SimpleFlagArg
                  name: gtf file
                optional: true
              - !Flag
                description: '<#> (minimum number of norm. tags per TSS cluster, default:
                  7)'
                synonyms:
                - -ntagThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (size of regions to check for significant TSS clusters,
                  default: 150)'
                synonyms:
                - -size
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '(include single exon genes for TP/FP calculations, default:
                  only use multiexon genes)'
                synonyms:
                - -includeSingleExons
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: (Include a metagene plot of read distributions)
                synonyms:
                - -metagene
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: <#> (max CPUs to use)
                synonyms:
                - -cpu
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (maximum log2 fold enrichment vs. input or RNA,
                  default: 30000000000)'
                synonyms:
                - -maxInputLog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (maximum log2 fold enrichment vs. input or RNA,
                  default: 30000000000)'
                synonyms:
                - -maxRNALog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum CDF difference to consider for TP/FP calculation,
                  default: 0.15)'
                synonyms:
                - -minDistDiff
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (if annotation-based TP/FP threshold calculation
                  failes, default: 1)'
                synonyms:
                - -defaultLog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (number of normalizaed reads added to input/target
                  read counts, default: 1.0)'
                synonyms:
                - -pseudoCount
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (local fold enrichment used for initial TSS identification,
                  default: 2)'
                synonyms:
                - -L
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: (only use RNAseq for stability calculations, not for
                  filtering putative TSS)
                synonyms:
                - -noFilterRNA
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA/input read
                  density,default: -100,500)'
                synonyms:
                - -senseRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA/input read
                  density,default: -500,100)'
                synonyms:
                - -antisenseRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA coverage for
                  FP identification,default: -150,-10)'
                synonyms:
                - -rnaRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum RNAseq read density for stable transcript,
                  def: 2)'
                synonyms:
                - -stableThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum divergent csRNAseq signal for bi/divergent
                  TSS, def: 2)'
                synonyms:
                - -biThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: "<#> (minimum distance from annotated TSS to assign 'distal'\
                  \ annotation, def: 500)"
                synonyms:
                - -dist
                args: !EmptyFlagArg {}
                optional: true
              parent: *id003
              subcommands: []
              usage: []
              help_flag:
              usage_flag:
              version_flag:
              help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\t\
                Options:\n\t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see\
                \ note below)\n\t\t-rna <RNAseq tag directory> (*see note below)\n\
                \t\t-genome <genome> (HOMER genome identifier or FASTA file)\n\t\t\
                -gtf <gtf file> (gene annotation, by default will use genome annotation\
                \ if available)\n\t\t\n\t\t-ntagThreshold <#> (minimum number of norm.\
                \ tags per TSS cluster, default: 7)\n\t\t-size <#> (size of regions\
                \ to check for significant TSS clusters, default: 150)\n\t\t-includeSingleExons\
                \ (include single exon genes for TP/FP calculations, default: only\
                \ use multiexon genes)\n\t\t-metagene (Include a metagene plot of\
                \ read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\tEnrichment\
                \ threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2 fold\
                \ enrichment vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold\
                \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\
                \t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP\
                \ calculation, default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based\
                \ TP/FP threshold calculation failes, default: 1)\n\t\t-pseudoCount\
                \ <#> (number of normalizaed reads added to input/target read counts,\
                \ default: 1.0)\n\t\t-ntagThreshold <#> (minimum number of norm. tags\
                \ per TSS cluster, default: 7)\n\t\t-L <#> (local fold enrichment\
                \ used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
                \ (only use RNAseq for stability calculations, not for filtering putative\
                \ TSS)\n\n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check\
                \ for significant TSS clusters, default: 150)\n\t\t-senseRegion <#>,<#>\
                \ (Size of region to calculate RNA/input read density,default: -100,500)\n\
                \t\t-antisenseRegion <#>,<#> (Size of region to calculate RNA/input\
                \ read density,default: -500,100)\n\t\t-rnaRegion <#>,<#> (Size of\
                \ region to calculate RNA coverage for FP identification,default:\
                \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum\
                \ RNAseq read density for stable transcript, def: 2)\n\t\t-biThreshold\
                \ <#> (minimum divergent csRNAseq signal for bi/divergent TSS, def:\
                \ 2)\n\t\t-dist <#> (minimum distance from annotated TSS to assign\
                \ 'distal' annotation, def: 500)\n\n\t* Tag Directories are assumed\
                \ to represent the 5' ends of RNA in sense orientation\n\n"
              generated_using: *id004
            - !Command
              command: &id008
              - findcsRNATSS.pl
              - additional
              positional: []
              named:
              - !Flag
                description: (*see note below)
                synonyms:
                - -o
                - -i
                args: !SimpleFlagArg
                  name: prefix
                optional: true
              - !Flag
                description: (*see note below)
                synonyms:
                - -rna
                args: !SimpleFlagArg
                  name: RNAseq tag directory
                optional: true
              - !Flag
                description: (HOMER genome identifier or FASTA file)
                synonyms:
                - -genome
                args: !SimpleFlagArg
                  name: genome
                optional: true
              - !Flag
                description: (gene annotation, by default will use genome annotation
                  if available)
                synonyms:
                - -gtf
                args: !SimpleFlagArg
                  name: gtf file
                optional: true
              - !Flag
                description: '<#> (minimum number of norm. tags per TSS cluster, default:
                  7)'
                synonyms:
                - -ntagThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (size of regions to check for significant TSS clusters,
                  default: 150)'
                synonyms:
                - -size
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '(include single exon genes for TP/FP calculations, default:
                  only use multiexon genes)'
                synonyms:
                - -includeSingleExons
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: (Include a metagene plot of read distributions)
                synonyms:
                - -metagene
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: <#> (max CPUs to use)
                synonyms:
                - -cpu
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (maximum log2 fold enrichment vs. input or RNA,
                  default: 30000000000)'
                synonyms:
                - -maxInputLog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (maximum log2 fold enrichment vs. input or RNA,
                  default: 30000000000)'
                synonyms:
                - -maxRNALog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum CDF difference to consider for TP/FP calculation,
                  default: 0.15)'
                synonyms:
                - -minDistDiff
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (if annotation-based TP/FP threshold calculation
                  failes, default: 1)'
                synonyms:
                - -defaultLog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (number of normalizaed reads added to input/target
                  read counts, default: 1.0)'
                synonyms:
                - -pseudoCount
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (local fold enrichment used for initial TSS identification,
                  default: 2)'
                synonyms:
                - -L
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: (only use RNAseq for stability calculations, not for
                  filtering putative TSS)
                synonyms:
                - -noFilterRNA
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA/input read
                  density,default: -100,500)'
                synonyms:
                - -senseRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA/input read
                  density,default: -500,100)'
                synonyms:
                - -antisenseRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA coverage for
                  FP identification,default: -150,-10)'
                synonyms:
                - -rnaRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum RNAseq read density for stable transcript,
                  def: 2)'
                synonyms:
                - -stableThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum divergent csRNAseq signal for bi/divergent
                  TSS, def: 2)'
                synonyms:
                - -biThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: "<#> (minimum distance from annotated TSS to assign 'distal'\
                  \ annotation, def: 500)"
                synonyms:
                - -dist
                args: !EmptyFlagArg {}
                optional: true
              parent: *id003
              subcommands: []
              usage: []
              help_flag:
              usage_flag:
              version_flag:
              help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\t\
                Options:\n\t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see\
                \ note below)\n\t\t-rna <RNAseq tag directory> (*see note below)\n\
                \t\t-genome <genome> (HOMER genome identifier or FASTA file)\n\t\t\
                -gtf <gtf file> (gene annotation, by default will use genome annotation\
                \ if available)\n\t\t\n\t\t-ntagThreshold <#> (minimum number of norm.\
                \ tags per TSS cluster, default: 7)\n\t\t-size <#> (size of regions\
                \ to check for significant TSS clusters, default: 150)\n\t\t-includeSingleExons\
                \ (include single exon genes for TP/FP calculations, default: only\
                \ use multiexon genes)\n\t\t-metagene (Include a metagene plot of\
                \ read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\tEnrichment\
                \ threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2 fold\
                \ enrichment vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold\
                \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\
                \t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP\
                \ calculation, default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based\
                \ TP/FP threshold calculation failes, default: 1)\n\t\t-pseudoCount\
                \ <#> (number of normalizaed reads added to input/target read counts,\
                \ default: 1.0)\n\t\t-ntagThreshold <#> (minimum number of norm. tags\
                \ per TSS cluster, default: 7)\n\t\t-L <#> (local fold enrichment\
                \ used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
                \ (only use RNAseq for stability calculations, not for filtering putative\
                \ TSS)\n\n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check\
                \ for significant TSS clusters, default: 150)\n\t\t-senseRegion <#>,<#>\
                \ (Size of region to calculate RNA/input read density,default: -100,500)\n\
                \t\t-antisenseRegion <#>,<#> (Size of region to calculate RNA/input\
                \ read density,default: -500,100)\n\t\t-rnaRegion <#>,<#> (Size of\
                \ region to calculate RNA coverage for FP identification,default:\
                \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum\
                \ RNAseq read density for stable transcript, def: 2)\n\t\t-biThreshold\
                \ <#> (minimum divergent csRNAseq signal for bi/divergent TSS, def:\
                \ 2)\n\t\t-dist <#> (minimum distance from annotated TSS to assign\
                \ 'distal' annotation, def: 500)\n\n\t* Tag Directories are assumed\
                \ to represent the 5' ends of RNA in sense orientation\n\n"
              generated_using: *id004
            - !Command
              command: &id009
              - findcsRNATSS.pl
              - tag directory
              positional: []
              named:
              - !Flag
                description: (*see note below)
                synonyms:
                - -o
                - -i
                args: !SimpleFlagArg
                  name: prefix
                optional: true
              - !Flag
                description: (*see note below)
                synonyms:
                - -rna
                args: !SimpleFlagArg
                  name: RNAseq tag directory
                optional: true
              - !Flag
                description: (HOMER genome identifier or FASTA file)
                synonyms:
                - -genome
                args: !SimpleFlagArg
                  name: genome
                optional: true
              - !Flag
                description: (gene annotation, by default will use genome annotation
                  if available)
                synonyms:
                - -gtf
                args: !SimpleFlagArg
                  name: gtf file
                optional: true
              - !Flag
                description: '<#> (minimum number of norm. tags per TSS cluster, default:
                  7)'
                synonyms:
                - -ntagThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (size of regions to check for significant TSS clusters,
                  default: 150)'
                synonyms:
                - -size
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '(include single exon genes for TP/FP calculations, default:
                  only use multiexon genes)'
                synonyms:
                - -includeSingleExons
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: (Include a metagene plot of read distributions)
                synonyms:
                - -metagene
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: <#> (max CPUs to use)
                synonyms:
                - -cpu
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (maximum log2 fold enrichment vs. input or RNA,
                  default: 30000000000)'
                synonyms:
                - -maxInputLog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (maximum log2 fold enrichment vs. input or RNA,
                  default: 30000000000)'
                synonyms:
                - -maxRNALog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum CDF difference to consider for TP/FP calculation,
                  default: 0.15)'
                synonyms:
                - -minDistDiff
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (if annotation-based TP/FP threshold calculation
                  failes, default: 1)'
                synonyms:
                - -defaultLog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (number of normalizaed reads added to input/target
                  read counts, default: 1.0)'
                synonyms:
                - -pseudoCount
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (local fold enrichment used for initial TSS identification,
                  default: 2)'
                synonyms:
                - -L
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: (only use RNAseq for stability calculations, not for
                  filtering putative TSS)
                synonyms:
                - -noFilterRNA
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA/input read
                  density,default: -100,500)'
                synonyms:
                - -senseRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA/input read
                  density,default: -500,100)'
                synonyms:
                - -antisenseRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA coverage for
                  FP identification,default: -150,-10)'
                synonyms:
                - -rnaRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum RNAseq read density for stable transcript,
                  def: 2)'
                synonyms:
                - -stableThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum divergent csRNAseq signal for bi/divergent
                  TSS, def: 2)'
                synonyms:
                - -biThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: "<#> (minimum distance from annotated TSS to assign 'distal'\
                  \ annotation, def: 500)"
                synonyms:
                - -dist
                args: !EmptyFlagArg {}
                optional: true
              parent: *id003
              subcommands: []
              usage: []
              help_flag:
              usage_flag:
              version_flag:
              help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\t\
                Options:\n\t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see\
                \ note below)\n\t\t-rna <RNAseq tag directory> (*see note below)\n\
                \t\t-genome <genome> (HOMER genome identifier or FASTA file)\n\t\t\
                -gtf <gtf file> (gene annotation, by default will use genome annotation\
                \ if available)\n\t\t\n\t\t-ntagThreshold <#> (minimum number of norm.\
                \ tags per TSS cluster, default: 7)\n\t\t-size <#> (size of regions\
                \ to check for significant TSS clusters, default: 150)\n\t\t-includeSingleExons\
                \ (include single exon genes for TP/FP calculations, default: only\
                \ use multiexon genes)\n\t\t-metagene (Include a metagene plot of\
                \ read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\tEnrichment\
                \ threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2 fold\
                \ enrichment vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold\
                \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\
                \t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP\
                \ calculation, default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based\
                \ TP/FP threshold calculation failes, default: 1)\n\t\t-pseudoCount\
                \ <#> (number of normalizaed reads added to input/target read counts,\
                \ default: 1.0)\n\t\t-ntagThreshold <#> (minimum number of norm. tags\
                \ per TSS cluster, default: 7)\n\t\t-L <#> (local fold enrichment\
                \ used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
                \ (only use RNAseq for stability calculations, not for filtering putative\
                \ TSS)\n\n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check\
                \ for significant TSS clusters, default: 150)\n\t\t-senseRegion <#>,<#>\
                \ (Size of region to calculate RNA/input read density,default: -100,500)\n\
                \t\t-antisenseRegion <#>,<#> (Size of region to calculate RNA/input\
                \ read density,default: -500,100)\n\t\t-rnaRegion <#>,<#> (Size of\
                \ region to calculate RNA coverage for FP identification,default:\
                \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum\
                \ RNAseq read density for stable transcript, def: 2)\n\t\t-biThreshold\
                \ <#> (minimum divergent csRNAseq signal for bi/divergent TSS, def:\
                \ 2)\n\t\t-dist <#> (minimum distance from annotated TSS to assign\
                \ 'distal' annotation, def: 500)\n\n\t* Tag Directories are assumed\
                \ to represent the 5' ends of RNA in sense orientation\n\n"
              generated_using: *id004
            - !Command
              command: &id010
              - findcsRNATSS.pl
              - peak file | tss
              positional: []
              named:
              - !Flag
                description: (*see note below)
                synonyms:
                - -o
                - -i
                args: !SimpleFlagArg
                  name: prefix
                optional: true
              - !Flag
                description: (*see note below)
                synonyms:
                - -rna
                args: !SimpleFlagArg
                  name: RNAseq tag directory
                optional: true
              - !Flag
                description: (HOMER genome identifier or FASTA file)
                synonyms:
                - -genome
                args: !SimpleFlagArg
                  name: genome
                optional: true
              - !Flag
                description: (gene annotation, by default will use genome annotation
                  if available)
                synonyms:
                - -gtf
                args: !SimpleFlagArg
                  name: gtf file
                optional: true
              - !Flag
                description: '<#> (minimum number of norm. tags per TSS cluster, default:
                  7)'
                synonyms:
                - -ntagThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (size of regions to check for significant TSS clusters,
                  default: 150)'
                synonyms:
                - -size
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '(include single exon genes for TP/FP calculations, default:
                  only use multiexon genes)'
                synonyms:
                - -includeSingleExons
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: (Include a metagene plot of read distributions)
                synonyms:
                - -metagene
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: <#> (max CPUs to use)
                synonyms:
                - -cpu
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (maximum log2 fold enrichment vs. input or RNA,
                  default: 30000000000)'
                synonyms:
                - -maxInputLog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (maximum log2 fold enrichment vs. input or RNA,
                  default: 30000000000)'
                synonyms:
                - -maxRNALog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum CDF difference to consider for TP/FP calculation,
                  default: 0.15)'
                synonyms:
                - -minDistDiff
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (if annotation-based TP/FP threshold calculation
                  failes, default: 1)'
                synonyms:
                - -defaultLog2Fold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (number of normalizaed reads added to input/target
                  read counts, default: 1.0)'
                synonyms:
                - -pseudoCount
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (local fold enrichment used for initial TSS identification,
                  default: 2)'
                synonyms:
                - -L
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: (only use RNAseq for stability calculations, not for
                  filtering putative TSS)
                synonyms:
                - -noFilterRNA
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA/input read
                  density,default: -100,500)'
                synonyms:
                - -senseRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA/input read
                  density,default: -500,100)'
                synonyms:
                - -antisenseRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#>,<#> (Size of region to calculate RNA coverage for
                  FP identification,default: -150,-10)'
                synonyms:
                - -rnaRegion
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum RNAseq read density for stable transcript,
                  def: 2)'
                synonyms:
                - -stableThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: '<#> (minimum divergent csRNAseq signal for bi/divergent
                  TSS, def: 2)'
                synonyms:
                - -biThreshold
                args: !EmptyFlagArg {}
                optional: true
              - !Flag
                description: "<#> (minimum distance from annotated TSS to assign 'distal'\
                  \ annotation, def: 500)"
                synonyms:
                - -dist
                args: !EmptyFlagArg {}
                optional: true
              parent: *id003
              subcommands: []
              usage: []
              help_flag:
              usage_flag:
              version_flag:
              help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\t\
                Options:\n\t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see\
                \ note below)\n\t\t-rna <RNAseq tag directory> (*see note below)\n\
                \t\t-genome <genome> (HOMER genome identifier or FASTA file)\n\t\t\
                -gtf <gtf file> (gene annotation, by default will use genome annotation\
                \ if available)\n\t\t\n\t\t-ntagThreshold <#> (minimum number of norm.\
                \ tags per TSS cluster, default: 7)\n\t\t-size <#> (size of regions\
                \ to check for significant TSS clusters, default: 150)\n\t\t-includeSingleExons\
                \ (include single exon genes for TP/FP calculations, default: only\
                \ use multiexon genes)\n\t\t-metagene (Include a metagene plot of\
                \ read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\tEnrichment\
                \ threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2 fold\
                \ enrichment vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold\
                \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\
                \t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP\
                \ calculation, default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based\
                \ TP/FP threshold calculation failes, default: 1)\n\t\t-pseudoCount\
                \ <#> (number of normalizaed reads added to input/target read counts,\
                \ default: 1.0)\n\t\t-ntagThreshold <#> (minimum number of norm. tags\
                \ per TSS cluster, default: 7)\n\t\t-L <#> (local fold enrichment\
                \ used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
                \ (only use RNAseq for stability calculations, not for filtering putative\
                \ TSS)\n\n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check\
                \ for significant TSS clusters, default: 150)\n\t\t-senseRegion <#>,<#>\
                \ (Size of region to calculate RNA/input read density,default: -100,500)\n\
                \t\t-antisenseRegion <#>,<#> (Size of region to calculate RNA/input\
                \ read density,default: -500,100)\n\t\t-rnaRegion <#>,<#> (Size of\
                \ region to calculate RNA coverage for FP identification,default:\
                \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum\
                \ RNAseq read density for stable transcript, def: 2)\n\t\t-biThreshold\
                \ <#> (minimum divergent csRNAseq signal for bi/divergent TSS, def:\
                \ 2)\n\t\t-dist <#> (minimum distance from annotated TSS to assign\
                \ 'distal' annotation, def: 500)\n\n\t* Tag Directories are assumed\
                \ to represent the 5' ends of RNA in sense orientation\n\n"
              generated_using: *id004
            usage: []
            help_flag:
            usage_flag:
            version_flag:
            help_text: "!!! First argument needs to be a <tag directory>\n\n\tUsage:\
              \ findPeaks <tag directory> [options]\n\n\tFinds peaks in the provided\
              \ tag directory.  By default, peak list printed to stdout\n\n\tGeneral\
              \ analysis options:\n\t\t-o <filename|auto> (file name for to output\
              \ peaks, default: stdout)\n\t\t\t\"-o auto\" will send output to \"\
              <tag directory>/peaks.txt\", \".../regions.txt\",\n\t\t\tor \".../transcripts.txt\"\
              \ depending on the \"-style\" option\n\t\t-style <option> (Specialized\
              \ options for specific analysis strategies)\n\t\t\tfactor (transcription\
              \ factor ChIP-Seq, uses -center, output: peaks.txt,  default)\n\t\t\t\
              histone (histone modification ChIP-Seq, region based, uses -region -size\
              \ 500 -L 0, regions.txt)\n\t\t\tgroseq (de novo transcript identification\
              \ from GroSeq data, transcripts.txt)\n\t\t\ttss (TSS identification\
              \ from 5' RNA sequencing, tss.txt)\n\t\t\tdnase (Hypersensitivity [crawford\
              \ style (nicking)], peaks.txt)\n\t\t\tsuper (Super Enhancers, superEnhancers.txt)\n\
              \t\t\tsuperhistone (Super Enhancers from H3K27ac data, superEnhancers.txt)\n\
              \t\t\tmC (Cytosine methylation (BS-seq/methylC-seq), regions.txt)\n\t\
              \t\tdamid (DamID enrichment from DpnI digestion, regions.txt)\n\t\t\t\
              clip (CLIP-Seq enrichment, strand specific, peaks.txt)\n\n\tchipseq/histone\
              \ options:\n\t\t-i <input tag directory> (Experiment to use as IgG/Input/Control)\n\
              \t\t-size <#> (Peak size, default: auto)\n\t\t-minDist <#> (minimum\
              \ distance between peaks, default: peak size x2)\n\t\t-gsize <#> (Set\
              \ effective mappable genome size, default: 2e9)\n\t\t-fragLength <#|auto>\
              \ (Approximate fragment length, default: auto)\n\t\t-inputFragLength\
              \ <#|auto> (Approximate fragment length of input tags, default: auto)\n\
              \t\t-tbp <#> (Maximum tags per bp to count, 0 = no limit, default: auto)\n\
              \t\t-inputtbp <#> (Maximum tags per bp to count in input, 0 = no limit,\
              \ default: auto)\n\t\t-strand <both|separate> (find peaks using tags\
              \ on both strands or separate, default:both)\n\t\t-norm # (Tag count\
              \ to normalize to, default 10000000)\n\t\t-region (extends start/stop\
              \ coordinates to cover full region considered \"enriched\")\n\t\t\t\
              -regionRes <#> (number of fractions peaks are divided in when extending\
              \ 'regions', def: 4)\n\t\t-center (Centers peaks on maximum tag overlap\
              \ and calculates focus ratios)\n\t\t-nfr (Centers peaks on most likely\
              \ nucleosome free region [works best with mnase data])\n\t\t\t(-center\
              \ and -nfr can be performed later with \"getPeakTags\"\n\n\tPeak Filtering\
              \ options: (set -F/-L/-C to 0 to skip)\n\t\t-F <#> (fold enrichment\
              \ over input tag count, default: 4.0)\n\t\t  -P <#> (poisson p-value\
              \ threshold relative to input tag count, default: 0.0001)\n\t\t-L <#>\
              \ (fold enrichment over local tag count, default: 4.0)\n\t\t  -LP <#>\
              \ (poisson p-value threshold relative to local tag count, default: 0.0001)\n\
              \t\t-C <#> (fold enrichment limit of expected unique tag positions,\
              \ default: 2.0)\n\t\t-localSize <#> (region to check for local tag enrichment,\
              \ default: 10000)\n\t\t-inputSize <#> (Size of region to search for\
              \ control tags, default: 2x peak size)\n\t\t-fdr <#> (False discovery\
              \ rate, default = 0.001)\n\t\t-poisson <#> (Set poisson p-value cutoff,\
              \ default: uses fdr)\n\t\t-tagThreshold <#> (Set # of tags to define\
              \ a peak, default: 25)\n\t\t-ntagThreshold <#> (Set # of normalized\
              \ tags to define a peak, by default uses 1e7 for norm)\n\t\t-minTagThreshold\
              \ <#> (Absolute minimum tags per peak, default: expected tags per peak)\n\
              \n\tSuperEnhancer Options: (Need to specify \"-style super\"):\n\t\t\
              -superSlope <#> (Slope threshold to identify super vs. typical enh.,\
              \ default: 1.00)\n\t\t-superWindow <#> (moving window/number of peaks\
              \ to use to calculate slope, default: 10)\n\t\t-typical <filename> (Output\
              \ typical enhancers to this file, default: not used)\n\t\t-inputPeaks\
              \ <filename> (initial peaks to use for super enhancer merging/scoring)\n\
              \t\t-excludePeaks <filename> (regions to exclude from analysis, i.e.\
              \ TSS regions for H3K27ac)\n\n\tMethylC-Seq/BS-Seq options (Need to\
              \ specify \"-style mC\"):\n\t\t-unmethylC / -methylC (find unmethylated/methylated\
              \ regions, default: -unmethyC)\n\t\t-mCthresh <#> (methylation threshold\
              \ of regions, default: avg methylation/2)\n\t\t-minNumC <#> (Minimum\
              \ number of cytosines per methylation peak, default: 6)\n\n\tGroSeq\
              \ Options (Need to specify \"-style groseq\"):\n\t\t-tssSize <#> (size\
              \ of region for initiation detection/artifact size, default: 250)\n\t\
              \t-minBodySize <#> (size of regoin for transcript body detection, default:\
              \ 1000)\n\t\t-tssFold <#> (fold enrichment for new initiation dectection,\
              \ default: 4.0)\n\t\t-bodyFold <#> (fold enrichment for new transcript\
              \ dectection, default: 4.0)\n\t\t-endFold <#> (end transcript when levels\
              \ are this much less than the start, default: 10.0)\n\t\t-method <fold|level>\
              \ (method used for identifying new transcripts, default: fold)\n\t\t\
              -fragLength <#> (Approximate fragment length, default: 150)\n\t\t-uniqmap\
              \ <directory> (directory of binary files specifying uniquely mappable\
              \ locations)\n\t\t\tDownload from http://biowhat.ucsd.edu/homer/groseq/\n\
              \t\t-confPvalue <#> (confidence p-value: 1.00e-05)\n\t\t-minReadDepth\
              \ <#> (Minimum initial read depth for transcripts, default: auto)\n\t\
              \t-pseudoCount <#> (Pseudo tag count, default: 2.0)\n\t\t-rev (reverse\
              \ strand of reads - for first-strand rna-seq/gro-seq)\n\t\t-gtf <filename>\
              \ (Output de novo transcripts in GTF format)\n\t\t\t\"-o auto\" will\
              \ produce <dir>/transcripts.txt and <dir>/transcripts.gtf\n\t\t\n\n\t\
              No genome selected (\"none\") - limits what you can do\n\tPeak file\
              \ = 0.178757123489962.tss.tmp\n\tGenome = none\n\tOrganism = unknown\n\
              \tWill count tags on strand: +\n\tFragment Length set to 1\n\tWill use\
              \ up to 3 CPUs in parts that can use them\n\tPeak/BED file conversion\
              \ summary:\n\t\tBED/Header formatted lines: 0\n\t\tpeakfile formatted\
              \ lines: 0\n\t\tDuplicated Peak IDs: 0\n\n\tPeak File Statistics:\n\t\
              \tTotal Peaks: 0\n\t\tRedundant Peak IDs: 0\n\t\tPeaks lacking information:\
              \ 0 (need at least 5 columns per peak)\n\t\tPeaks with misformatted\
              \ coordinates: 0 (should be integer)\n\t\tPeaks with misformatted strand:\
              \ 0 (should be either +/- or 0/1)\n\n\tOnly 0 peaks found! If many more\
              \ expected, maybe the file format is for Macs only\n\tTry running the\
              \ command: changeNewLine.pl \"0.744680606135265.clean.pos\"\n\n\tReading\
              \ Positions...\n\t-----------------------\n\tFinding Closest TSS...\n\
              \tSkipping TSS assignment (can't find file for genome none)\n\t\tCan't\
              \ find promoterFile none/none.tss\n\tCould not find basic annotation\
              \ file (none/none.annotation)\n\tCould not find full/detailed annotation\
              \ file (none/none.annotation)\n\tCounting Tags in Peaks from each directory...\n\
              \tOrganism: unknown\n\tOutputing Annotation File...\n\tDone annotating\
              \ peaks file\n\n\n\tNo genome selected (\"none\") - limits what you\
              \ can do\n\tPeak file = 0.178757123489962.tss.tmp\n\tGenome = none\n\
              \tOrganism = unknown\n\tWill count tags on strand: +\n\tFragment Length\
              \ set to 1\n\tPeak Region set to 150\n\tTag Directories:\n--help not\
              \ recognized\n\n\n\tUsage: annotatePeaks.pl <peak file | tss> <genome\
              \ version>  [additional options...]\n\n\tAvailable Genomes (required\
              \ argument): (name,org,directory,default promoter set)\n\t\t\t-- or\
              \ --\n\t\tCustom: provide the path to genome FASTA files (directory\
              \ or single file)\n\t\tIf no genome is available, specify 'none'.\n\t\
              \tIf using FASTA file or none, may want to specify '-organism <...>'\n\
              \n\tUser defined annotation files (default is UCSC refGene annotation):\n\
              \t\tannotatePeaks.pl accepts GTF (gene transfer formatted) files to\
              \ annotate positions relative\n\t\tto custom annotations, such as those\
              \ from de novo transcript discovery or Gencode.\n\t\t-gtf <gtf format\
              \ file> (Use -gff and -gff3 if appropriate, but GTF is better)\n\t\t\
              -gid (by default the GTF file is processed by transcript_id, use this\
              \ option for gene_id)\n\t\t-ann <custom homer annotation file> (created\
              \ by assignGenomeAnnotation, see website)\n\n\tPeak vs. tss/tts/rna\
              \ mode (works with custom GTF file):\n\t\tIf the first argument is \"\
              tss\" (i.e. annotatePeaks.pl tss hg18 ...) then a TSS centric\n\t\t\
              analysis will be carried out.  Tag counts and motifs will be found relative\
              \ to the TSS.\n\t\t(no position file needed) [\"tts\" now works too\
              \ - e.g. 3' end of gene]\n\t\t[\"rna\" specifies gene bodies, will automaticall\
              \ set \"-size given\"]\n\t\tNOTE: The default TSS peak size is 4000\
              \ bp, i.e. +/- 2kb (change with -size option)\n\t\t-list <gene id list>\
              \ (subset of genes to perform analysis [unigene, gene id, accession,\n\
              \t\t\t probe, etc.], default = all promoters)\n\t\t-cTSS <promoter position\
              \ file i.e. peak file> (should be centered on TSS)\n\n\tPrimary Annotation\
              \ Options:\n\t\t-mask (Masked repeats, can also add 'r' to end of genome\
              \ name)\n\t\t-m <motif file 1> [motif file 2] ... (list of motifs to\
              \ find in peaks)\n\t\t\t-mscore (reports the highest log-odds score\
              \ within the peak)\n\t\t\t-nmotifs (reports the number of motifs per\
              \ peak)\n\t\t\t-mdist (reports distance to closest motif)\n\t\t\t-mfasta\
              \ <filename> (reports sites in a fasta file - for building new motifs)\n\
              \t\t\t-fm <motif file 1> [motif file 2] (list of motifs to filter from\
              \ above)\n\t\t\t-rmrevopp <#> (only count sites found within <#> on\
              \ both strands once, i.e. palindromic)\n\t\t\t-matrix <prefix> (outputs\
              \ a motif co-occurrence files:\n\t\t\t\tprefix.count.matrix.txt - number\
              \ of peaks with motif co-occurrence\n\t\t\t\tprefix.ratio.matrix.txt\
              \ - ratio of observed vs. expected  co-occurrence\n\t\t\t\tprefix.logPvalue.matrix.txt\
              \ - co-occurrence enrichment\n\t\t\t\tprefix.stats.txt - table of pair-wise\
              \ motif co-occurrence statistics\n\t\t\t\tadditional options:\n\t\t\t\
              \t-matrixMinDist <#> (minimum distance between motif pairs - to avoid\
              \ overlap, default: 4)\n\t\t\t\t-matrixMaxDist <#> (maximum distance\
              \ between motif pairs)\n\t\t\t-mbed <filename> (Output motif positions\
              \ to a BED file to load at UCSC (or -mpeak))\n\t\t\t-mlogic <filename>\
              \ (will output stats on common motif orientations)\n\t\t-d <tag directory\
              \ 1> [tag directory 2] ... (list of experiment directories to show\n\
              \t\t\ttag counts for) NOTE: -dfile <file> where file is a list of directories\
              \ in first column\n\t\t-bedGraph <bedGraph file 1> [bedGraph file 2]\
              \ ... (read coverage counts from bedGraph files)\n\t\t-wig <wiggle file\
              \ 1> [wiggle file 2] ... (read coverage counts from wiggle files)\n\t\
              \t-p <peak file> [peak file 2] ... (to find nearest peaks)\n\t\t\t-pdist\
              \ to report only distance (-pdist2 gives directional distance)\n\t\t\
              \t-pcount to report number of peaks within region\n\t\t-vcf <VCF file>\
              \ (annotate peaks with genetic variation infomation, one col per individual)\n\
              \t\t\t-editDistance (Computes the # bp changes relative to reference)\n\
              \t\t\t-individuals <name1> [name2] ... (restrict analysis to these individuals)\n\
              \t\t-gene <data file> ... (Adds additional data to result based on the\
              \ closest gene.\n\t\t\tThis is useful for adding gene expression data.\
              \  The file must have a header,\n\t\t\tand the first column must be\
              \ a GeneID, Accession number, etc.  If the peak\n\t\t\tcannot be mapped\
              \ to data in the file then the entry will be left empty.\n\t\t-go <output\
              \ directory> (perform GO analysis using genes near peaks)\n\t\t-genomeOntology\
              \ <output directory> (perform genomeOntology analysis on peaks)\n\t\t\
              \t-gsize <#> (Genome size for genomeOntology analysis, default: 2e9)\n\
              \n\tAnnotation vs. Histogram mode:\n\t\t-hist <bin size in bp> (i.e\
              \ 1, 2, 5, 10, 20, 50, 100 etc.)\n\t\tThe -hist option can be used to\
              \ generate histograms of position dependent features relative\n\t\t\
              to the center of peaks.  This is primarily meant to be used with -d\
              \ and -m options to map\n\t\tdistribution of motifs and ChIP-Seq tags.\
              \  For ChIP-Seq peaks for a Transcription factor\n\t\tyou might want\
              \ to use the -center option (below) to center peaks on the known motif\n\
              \t\t** If using \"-size given\", histogram will be scaled to each region\
              \ (i.e. 0-100%), with\n\t\tthe -hist parameter being the number of bins\
              \ to divide each region into.\n\t\t\tHistogram Mode specific Options:\n\
              \t\t\t-nuc (calculated mononucleotide frequencies at each position,\n\
              \t\t\t\tWill report by default if extracting sequence for other purposes\
              \ like motifs)\n\t\t\t-di (calculated dinucleotide frequencies at each\
              \ position)\n\t\t\t-histNorm <#> (normalize the total tag count for\
              \ each region to 1, where <#> is the\n\t\t\t\tminimum tag total per\
              \ region - use to avoid tag spikes from low coverage\n\t\t\t-ghist (outputs\
              \ profiles for each gene, for peak shape clustering)\n\t\t\t-rm <#>\
              \ (remove occurrences of same motif that occur within # bp)\n\n\tPeak\
              \ Centering: (other options are ignored)\n\t\t-center <motif file> (This\
              \ will re-center peaks on the specified motif, or remove peak\n\t\t\t\
              if there is no motif in the peak.  ONLY recentering will be performed,\
              \ and all other\n\t\t\toptions will be ignored.  This will output a\
              \ new peak file that can then be reanalyzed\n\t\t\tto reveal fine-grain\
              \ structure in peaks (It is advised to use -size < 200) with this\n\t\
              \t\tto keep peaks from moving too far (-mirror flips the position)\n\
              \t\t-multi (returns genomic positions of all sites instead of just the\
              \ closest to center)\n\n\tGenome comparisons (need genome & liftOver)\n\
              \t\t-cmpGenome <genome1> [genome2] (Genomes to compare for sequence/motifs)\n\
              \t\t-cmpLiftover <liftover1> [genome2] (Genomes to compare for sequence/motifs)\n\
              \n\tNormalization options:\n\t\t-fpkm (normalize read counts to million\
              \ reads or fragments per kilobase mapped)\n\t\t-raw (do not adjust the\
              \ tag counts based on total tags sequenced, -noadj works too)\n\t\t\
              -norm <#> (normalize tags to this tag count, default=1e7, 0=average\
              \ tag count in all directories)\n\t\t-normLength <#> (Fragment length\
              \ to normlize to for experiments with different lens, def: 100)\n\t\t\
              -log (output tag counts as log2(x+1+rand) values - for scatter plots)\n\
              \t\t-sqrt (output tag counts as sqrt(x+rand) values - for scatter plots)\n\
              \t\t-ratio (process tag values as ratios - i.e. chip-seq, or mCpG/CpG)\n\
              \n\tAdvanced normalization options: (-rlog and -vst require R and DESeq2\
              \ to be installed)\n\t\t-rlog (quantile/variance normalization on reported\
              \ genes using DESeq2 rlog funcition, needs R)\n\t\t-vst (quantile/variance\
              \ normalization on reported genes using DESeq2 vst function, needs R)\n\
              \n\tAdvanced Options:\n\t\t-len <#> / -fragLength <#> (Fragment length,\
              \ default=auto, might want to set to 1 for 5'RNA)\n\t\t-size <#> (Peak\
              \ size[from center of peak], default=inferred from peak file)\n\t\t\t\
              -size #,# (i.e. -size -10,50 count tags from -10 bp to +50 bp from center)\n\
              \t\t\t-size \"given\" (count tags etc. using the actual regions - for\
              \ variable length regions)\n\t\t-strand <+|-|both> (Count tags on specific\
              \ strands relative to peak, default: both)\n\t\t-pc <#> (maximum number\
              \ of tags to count per bp, default=0 [no maximum], -tbp <#> works too)\n\
              \t\t-CpG (Calculate CpG/GC content)\n\t\t-nfr (report nuclesome free\
              \ region scores instead of tag counts, also -nfrSize <#>)\n\t\t-norevopp\
              \ (do not search for motifs on the opposite strand [works with -center\
              \ too])\n\t\t-gwasCatalog <gwasCatalog file from UCSC> (list overlapping\
              \ GWAS risk SNPs)\n\t\t-pdist (only report distance to nearest peak\
              \ using -p, not peak name)\n\t\t-map <mapping file> (mapping between\
              \ peak IDs and promoter IDs, overrides closest assignment)\n\t\t-noann,\
              \ -nogene (skip genome annotation step, skip TSS annotation)\n\t\t-homer1/-homer2\
              \ (by default, the new version of homer [-homer2] is used for finding\
              \ motifs)\n\t\t-cpu <#> (Number of processors to use when possible -\
              \ only some parts utilize multiple cores)\n\t\t-noblanks (remove peaks/rows\
              \ with missing data)\n\n\tPeak/BED file conversion summary:\n\t\tBED/Header\
              \ formatted lines: 0\n\t\tpeakfile formatted lines: 0\n\t\tDuplicated\
              \ Peak IDs: 0\n\n\tPeak File Statistics:\n\t\tTotal Peaks: 0\n\t\tRedundant\
              \ Peak IDs: 0\n\t\tPeaks lacking information: 0 (need at least 5 columns\
              \ per peak)\n\t\tPeaks with misformatted coordinates: 0 (should be integer)\n\
              \t\tPeaks with misformatted strand: 0 (should be either +/- or 0/1)\n\
              \n\tOnly 0 peaks found! If many more expected, maybe the file format\
              \ is for Macs only\n\tTry running the command: changeNewLine.pl \"0.0945234839609874.2.tmp\"\
              \n\n\n\tNo genome selected (\"none\") - limits what you can do\n\tPeak\
              \ file = 0.178757123489962.tmp\n\tGenome = none\n\tOrganism = unknown\n\
              \tWill count tags on strand: +\n\tFragment Length set to 1\n\tTag Directories:\n\
              --help not recognized\n\n\n\tUsage: annotatePeaks.pl <peak file | tss>\
              \ <genome version>  [additional options...]\n\n\tAvailable Genomes (required\
              \ argument): (name,org,directory,default promoter set)\n\t\t\t-- or\
              \ --\n\t\tCustom: provide the path to genome FASTA files (directory\
              \ or single file)\n\t\tIf no genome is available, specify 'none'.\n\t\
              \tIf using FASTA file or none, may want to specify '-organism <...>'\n\
              \n\tUser defined annotation files (default is UCSC refGene annotation):\n\
              \t\tannotatePeaks.pl accepts GTF (gene transfer formatted) files to\
              \ annotate positions relative\n\t\tto custom annotations, such as those\
              \ from de novo transcript discovery or Gencode.\n\t\t-gtf <gtf format\
              \ file> (Use -gff and -gff3 if appropriate, but GTF is better)\n\t\t\
              -gid (by default the GTF file is processed by transcript_id, use this\
              \ option for gene_id)\n\t\t-ann <custom homer annotation file> (created\
              \ by assignGenomeAnnotation, see website)\n\n\tPeak vs. tss/tts/rna\
              \ mode (works with custom GTF file):\n\t\tIf the first argument is \"\
              tss\" (i.e. annotatePeaks.pl tss hg18 ...) then a TSS centric\n\t\t\
              analysis will be carried out.  Tag counts and motifs will be found relative\
              \ to the TSS.\n\t\t(no position file needed) [\"tts\" now works too\
              \ - e.g. 3' end of gene]\n\t\t[\"rna\" specifies gene bodies, will automaticall\
              \ set \"-size given\"]\n\t\tNOTE: The default TSS peak size is 4000\
              \ bp, i.e. +/- 2kb (change with -size option)\n\t\t-list <gene id list>\
              \ (subset of genes to perform analysis [unigene, gene id, accession,\n\
              \t\t\t probe, etc.], default = all promoters)\n\t\t-cTSS <promoter position\
              \ file i.e. peak file> (should be centered on TSS)\n\n\tPrimary Annotation\
              \ Options:\n\t\t-mask (Masked repeats, can also add 'r' to end of genome\
              \ name)\n\t\t-m <motif file 1> [motif file 2] ... (list of motifs to\
              \ find in peaks)\n\t\t\t-mscore (reports the highest log-odds score\
              \ within the peak)\n\t\t\t-nmotifs (reports the number of motifs per\
              \ peak)\n\t\t\t-mdist (reports distance to closest motif)\n\t\t\t-mfasta\
              \ <filename> (reports sites in a fasta file - for building new motifs)\n\
              \t\t\t-fm <motif file 1> [motif file 2] (list of motifs to filter from\
              \ above)\n\t\t\t-rmrevopp <#> (only count sites found within <#> on\
              \ both strands once, i.e. palindromic)\n\t\t\t-matrix <prefix> (outputs\
              \ a motif co-occurrence files:\n\t\t\t\tprefix.count.matrix.txt - number\
              \ of peaks with motif co-occurrence\n\t\t\t\tprefix.ratio.matrix.txt\
              \ - ratio of observed vs. expected  co-occurrence\n\t\t\t\tprefix.logPvalue.matrix.txt\
              \ - co-occurrence enrichment\n\t\t\t\tprefix.stats.txt - table of pair-wise\
              \ motif co-occurrence statistics\n\t\t\t\tadditional options:\n\t\t\t\
              \t-matrixMinDist <#> (minimum distance between motif pairs - to avoid\
              \ overlap, default: 4)\n\t\t\t\t-matrixMaxDist <#> (maximum distance\
              \ between motif pairs)\n\t\t\t-mbed <filename> (Output motif positions\
              \ to a BED file to load at UCSC (or -mpeak))\n\t\t\t-mlogic <filename>\
              \ (will output stats on common motif orientations)\n\t\t-d <tag directory\
              \ 1> [tag directory 2] ... (list of experiment directories to show\n\
              \t\t\ttag counts for) NOTE: -dfile <file> where file is a list of directories\
              \ in first column\n\t\t-bedGraph <bedGraph file 1> [bedGraph file 2]\
              \ ... (read coverage counts from bedGraph files)\n\t\t-wig <wiggle file\
              \ 1> [wiggle file 2] ... (read coverage counts from wiggle files)\n\t\
              \t-p <peak file> [peak file 2] ... (to find nearest peaks)\n\t\t\t-pdist\
              \ to report only distance (-pdist2 gives directional distance)\n\t\t\
              \t-pcount to report number of peaks within region\n\t\t-vcf <VCF file>\
              \ (annotate peaks with genetic variation infomation, one col per individual)\n\
              \t\t\t-editDistance (Computes the # bp changes relative to reference)\n\
              \t\t\t-individuals <name1> [name2] ... (restrict analysis to these individuals)\n\
              \t\t-gene <data file> ... (Adds additional data to result based on the\
              \ closest gene.\n\t\t\tThis is useful for adding gene expression data.\
              \  The file must have a header,\n\t\t\tand the first column must be\
              \ a GeneID, Accession number, etc.  If the peak\n\t\t\tcannot be mapped\
              \ to data in the file then the entry will be left empty.\n\t\t-go <output\
              \ directory> (perform GO analysis using genes near peaks)\n\t\t-genomeOntology\
              \ <output directory> (perform genomeOntology analysis on peaks)\n\t\t\
              \t-gsize <#> (Genome size for genomeOntology analysis, default: 2e9)\n\
              \n\tAnnotation vs. Histogram mode:\n\t\t-hist <bin size in bp> (i.e\
              \ 1, 2, 5, 10, 20, 50, 100 etc.)\n\t\tThe -hist option can be used to\
              \ generate histograms of position dependent features relative\n\t\t\
              to the center of peaks.  This is primarily meant to be used with -d\
              \ and -m options to map\n\t\tdistribution of motifs and ChIP-Seq tags.\
              \  For ChIP-Seq peaks for a Transcription factor\n\t\tyou might want\
              \ to use the -center option (below) to center peaks on the known motif\n\
              \t\t** If using \"-size given\", histogram will be scaled to each region\
              \ (i.e. 0-100%), with\n\t\tthe -hist parameter being the number of bins\
              \ to divide each region into.\n\t\t\tHistogram Mode specific Options:\n\
              \t\t\t-nuc (calculated mononucleotide frequencies at each position,\n\
              \t\t\t\tWill report by default if extracting sequence for other purposes\
              \ like motifs)\n\t\t\t-di (calculated dinucleotide frequencies at each\
              \ position)\n\t\t\t-histNorm <#> (normalize the total tag count for\
              \ each region to 1, where <#> is the\n\t\t\t\tminimum tag total per\
              \ region - use to avoid tag spikes from low coverage\n\t\t\t-ghist (outputs\
              \ profiles for each gene, for peak shape clustering)\n\t\t\t-rm <#>\
              \ (remove occurrences of same motif that occur within # bp)\n\n\tPeak\
              \ Centering: (other options are ignored)\n\t\t-center <motif file> (This\
              \ will re-center peaks on the specified motif, or remove peak\n\t\t\t\
              if there is no motif in the peak.  ONLY recentering will be performed,\
              \ and all other\n\t\t\toptions will be ignored.  This will output a\
              \ new peak file that can then be reanalyzed\n\t\t\tto reveal fine-grain\
              \ structure in peaks (It is advised to use -size < 200) with this\n\t\
              \t\tto keep peaks from moving too far (-mirror flips the position)\n\
              \t\t-multi (returns genomic positions of all sites instead of just the\
              \ closest to center)\n\n\tGenome comparisons (need genome & liftOver)\n\
              \t\t-cmpGenome <genome1> [genome2] (Genomes to compare for sequence/motifs)\n\
              \t\t-cmpLiftover <liftover1> [genome2] (Genomes to compare for sequence/motifs)\n\
              \n\tNormalization options:\n\t\t-fpkm (normalize read counts to million\
              \ reads or fragments per kilobase mapped)\n\t\t-raw (do not adjust the\
              \ tag counts based on total tags sequenced, -noadj works too)\n\t\t\
              -norm <#> (normalize tags to this tag count, default=1e7, 0=average\
              \ tag count in all directories)\n\t\t-normLength <#> (Fragment length\
              \ to normlize to for experiments with different lens, def: 100)\n\t\t\
              -log (output tag counts as log2(x+1+rand) values - for scatter plots)\n\
              \t\t-sqrt (output tag counts as sqrt(x+rand) values - for scatter plots)\n\
              \t\t-ratio (process tag values as ratios - i.e. chip-seq, or mCpG/CpG)\n\
              \n\tAdvanced normalization options: (-rlog and -vst require R and DESeq2\
              \ to be installed)\n\t\t-rlog (quantile/variance normalization on reported\
              \ genes using DESeq2 rlog funcition, needs R)\n\t\t-vst (quantile/variance\
              \ normalization on reported genes using DESeq2 vst function, needs R)\n\
              \n\tAdvanced Options:\n\t\t-len <#> / -fragLength <#> (Fragment length,\
              \ default=auto, might want to set to 1 for 5'RNA)\n\t\t-size <#> (Peak\
              \ size[from center of peak], default=inferred from peak file)\n\t\t\t\
              -size #,# (i.e. -size -10,50 count tags from -10 bp to +50 bp from center)\n\
              \t\t\t-size \"given\" (count tags etc. using the actual regions - for\
              \ variable length regions)\n\t\t-strand <+|-|both> (Count tags on specific\
              \ strands relative to peak, default: both)\n\t\t-pc <#> (maximum number\
              \ of tags to count per bp, default=0 [no maximum], -tbp <#> works too)\n\
              \t\t-CpG (Calculate CpG/GC content)\n\t\t-nfr (report nuclesome free\
              \ region scores instead of tag counts, also -nfrSize <#>)\n\t\t-norevopp\
              \ (do not search for motifs on the opposite strand [works with -center\
              \ too])\n\t\t-gwasCatalog <gwasCatalog file from UCSC> (list overlapping\
              \ GWAS risk SNPs)\n\t\t-pdist (only report distance to nearest peak\
              \ using -p, not peak name)\n\t\t-map <mapping file> (mapping between\
              \ peak IDs and promoter IDs, overrides closest assignment)\n\t\t-noann,\
              \ -nogene (skip genome annotation step, skip TSS annotation)\n\t\t-homer1/-homer2\
              \ (by default, the new version of homer [-homer2] is used for finding\
              \ motifs)\n\t\t-cpu <#> (Number of processors to use when possible -\
              \ only some parts utilize multiple cores)\n\t\t-noblanks (remove peaks/rows\
              \ with missing data)\n\nCould not open --help//tagInfo.txt\n"
            generated_using: *id004
          subcommands: []
          usage: []
          help_flag:
          usage_flag:
          version_flag:
          help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\
            \t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\
            \t\t-rna <RNAseq tag directory> (*see note below)\n\t\t-genome <genome>\
            \ (HOMER genome identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation,\
            \ by default will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold\
            \ <#> (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t\
            -size <#> (size of regions to check for significant TSS clusters, default:\
            \ 150)\n\t\t-includeSingleExons (include single exon genes for TP/FP calculations,\
            \ default: only use multiexon genes)\n\t\t-metagene (Include a metagene\
            \ plot of read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\t\
            Enrichment threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2\
            \ fold enrichment vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold\
            \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\
            \t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP calculation,\
            \ default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP\
            \ threshold calculation failes, default: 1)\n\t\t-pseudoCount <#> (number\
            \ of normalizaed reads added to input/target read counts, default: 1.0)\n\
            \t\t-ntagThreshold <#> (minimum number of norm. tags per TSS cluster,\
            \ default: 7)\n\t\t-L <#> (local fold enrichment used for initial TSS\
            \ identification, default: 2)\n\t\t-noFilterRNA (only use RNAseq for stability\
            \ calculations, not for filtering putative TSS)\n\n\t\tRegion sizes:\n\
            \t\t-size <#> (size of regions to check for significant TSS clusters,\
            \ default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to calculate\
            \ RNA/input read density,default: -100,500)\n\t\t-antisenseRegion <#>,<#>\
            \ (Size of region to calculate RNA/input read density,default: -500,100)\n\
            \t\t-rnaRegion <#>,<#> (Size of region to calculate RNA coverage for FP\
            \ identification,default: -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold\
            \ <#> (minimum RNAseq read density for stable transcript, def: 2)\n\t\t\
            -biThreshold <#> (minimum divergent csRNAseq signal for bi/divergent TSS,\
            \ def: 2)\n\t\t-dist <#> (minimum distance from annotated TSS to assign\
            \ 'distal' annotation, def: 500)\n\n\t* Tag Directories are assumed to\
            \ represent the 5' ends of RNA in sense orientation\n\n"
          generated_using: *id004
        - !Command
          command: *id005
          positional: []
          named:
          - !Flag
            description: (*see note below)
            synonyms:
            - -o
            - -i
            args: !SimpleFlagArg
              name: prefix
            optional: true
          - !Flag
            description: (*see note below)
            synonyms:
            - -rna
            args: !SimpleFlagArg
              name: RNAseq tag directory
            optional: true
          - !Flag
            description: (HOMER genome identifier or FASTA file)
            synonyms:
            - -genome
            args: !SimpleFlagArg
              name: genome
            optional: true
          - !Flag
            description: (gene annotation, by default will use genome annotation if
              available)
            synonyms:
            - -gtf
            args: !SimpleFlagArg
              name: gtf file
            optional: true
          - !Flag
            description: '<#> (minimum number of norm. tags per TSS cluster, default:
              7)'
            synonyms:
            - -ntagThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (size of regions to check for significant TSS clusters,
              default: 150)'
            synonyms:
            - -size
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '(include single exon genes for TP/FP calculations, default:
              only use multiexon genes)'
            synonyms:
            - -includeSingleExons
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: (Include a metagene plot of read distributions)
            synonyms:
            - -metagene
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: <#> (max CPUs to use)
            synonyms:
            - -cpu
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
              30000000000)'
            synonyms:
            - -maxInputLog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
              30000000000)'
            synonyms:
            - -maxRNALog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum CDF difference to consider for TP/FP calculation,
              default: 0.15)'
            synonyms:
            - -minDistDiff
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (if annotation-based TP/FP threshold calculation failes,
              default: 1)'
            synonyms:
            - -defaultLog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (number of normalizaed reads added to input/target read
              counts, default: 1.0)'
            synonyms:
            - -pseudoCount
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (local fold enrichment used for initial TSS identification,
              default: 2)'
            synonyms:
            - -L
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: (only use RNAseq for stability calculations, not for filtering
              putative TSS)
            synonyms:
            - -noFilterRNA
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
              -100,500)'
            synonyms:
            - -senseRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
              -500,100)'
            synonyms:
            - -antisenseRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA coverage for FP
              identification,default: -150,-10)'
            synonyms:
            - -rnaRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum RNAseq read density for stable transcript,
              def: 2)'
            synonyms:
            - -stableThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum divergent csRNAseq signal for bi/divergent
              TSS, def: 2)'
            synonyms:
            - -biThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: "<#> (minimum distance from annotated TSS to assign 'distal'\
              \ annotation, def: 500)"
            synonyms:
            - -dist
            args: !EmptyFlagArg {}
            optional: true
          parent: *id003
          subcommands: []
          usage: []
          help_flag:
          usage_flag:
          version_flag:
          help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\
            \t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\
            \t\t-rna <RNAseq tag directory> (*see note below)\n\t\t-genome <genome>\
            \ (HOMER genome identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation,\
            \ by default will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold\
            \ <#> (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t\
            -size <#> (size of regions to check for significant TSS clusters, default:\
            \ 150)\n\t\t-includeSingleExons (include single exon genes for TP/FP calculations,\
            \ default: only use multiexon genes)\n\t\t-metagene (Include a metagene\
            \ plot of read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\t\
            Enrichment threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2\
            \ fold enrichment vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold\
            \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\
            \t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP calculation,\
            \ default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP\
            \ threshold calculation failes, default: 1)\n\t\t-pseudoCount <#> (number\
            \ of normalizaed reads added to input/target read counts, default: 1.0)\n\
            \t\t-ntagThreshold <#> (minimum number of norm. tags per TSS cluster,\
            \ default: 7)\n\t\t-L <#> (local fold enrichment used for initial TSS\
            \ identification, default: 2)\n\t\t-noFilterRNA (only use RNAseq for stability\
            \ calculations, not for filtering putative TSS)\n\n\t\tRegion sizes:\n\
            \t\t-size <#> (size of regions to check for significant TSS clusters,\
            \ default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to calculate\
            \ RNA/input read density,default: -100,500)\n\t\t-antisenseRegion <#>,<#>\
            \ (Size of region to calculate RNA/input read density,default: -500,100)\n\
            \t\t-rnaRegion <#>,<#> (Size of region to calculate RNA coverage for FP\
            \ identification,default: -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold\
            \ <#> (minimum RNAseq read density for stable transcript, def: 2)\n\t\t\
            -biThreshold <#> (minimum divergent csRNAseq signal for bi/divergent TSS,\
            \ def: 2)\n\t\t-dist <#> (minimum distance from annotated TSS to assign\
            \ 'distal' annotation, def: 500)\n\n\t* Tag Directories are assumed to\
            \ represent the 5' ends of RNA in sense orientation\n\n"
          generated_using: *id004
        - !Command
          command: *id006
          positional: []
          named:
          - !Flag
            description: (*see note below)
            synonyms:
            - -o
            - -i
            args: !SimpleFlagArg
              name: prefix
            optional: true
          - !Flag
            description: (*see note below)
            synonyms:
            - -rna
            args: !SimpleFlagArg
              name: RNAseq tag directory
            optional: true
          - !Flag
            description: (HOMER genome identifier or FASTA file)
            synonyms:
            - -genome
            args: !SimpleFlagArg
              name: genome
            optional: true
          - !Flag
            description: (gene annotation, by default will use genome annotation if
              available)
            synonyms:
            - -gtf
            args: !SimpleFlagArg
              name: gtf file
            optional: true
          - !Flag
            description: '<#> (minimum number of norm. tags per TSS cluster, default:
              7)'
            synonyms:
            - -ntagThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (size of regions to check for significant TSS clusters,
              default: 150)'
            synonyms:
            - -size
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '(include single exon genes for TP/FP calculations, default:
              only use multiexon genes)'
            synonyms:
            - -includeSingleExons
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: (Include a metagene plot of read distributions)
            synonyms:
            - -metagene
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: <#> (max CPUs to use)
            synonyms:
            - -cpu
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
              30000000000)'
            synonyms:
            - -maxInputLog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
              30000000000)'
            synonyms:
            - -maxRNALog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum CDF difference to consider for TP/FP calculation,
              default: 0.15)'
            synonyms:
            - -minDistDiff
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (if annotation-based TP/FP threshold calculation failes,
              default: 1)'
            synonyms:
            - -defaultLog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (number of normalizaed reads added to input/target read
              counts, default: 1.0)'
            synonyms:
            - -pseudoCount
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (local fold enrichment used for initial TSS identification,
              default: 2)'
            synonyms:
            - -L
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: (only use RNAseq for stability calculations, not for filtering
              putative TSS)
            synonyms:
            - -noFilterRNA
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
              -100,500)'
            synonyms:
            - -senseRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
              -500,100)'
            synonyms:
            - -antisenseRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA coverage for FP
              identification,default: -150,-10)'
            synonyms:
            - -rnaRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum RNAseq read density for stable transcript,
              def: 2)'
            synonyms:
            - -stableThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum divergent csRNAseq signal for bi/divergent
              TSS, def: 2)'
            synonyms:
            - -biThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: "<#> (minimum distance from annotated TSS to assign 'distal'\
              \ annotation, def: 500)"
            synonyms:
            - -dist
            args: !EmptyFlagArg {}
            optional: true
          parent: *id003
          subcommands: []
          usage: []
          help_flag:
          usage_flag:
          version_flag:
          help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\
            \t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\
            \t\t-rna <RNAseq tag directory> (*see note below)\n\t\t-genome <genome>\
            \ (HOMER genome identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation,\
            \ by default will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold\
            \ <#> (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t\
            -size <#> (size of regions to check for significant TSS clusters, default:\
            \ 150)\n\t\t-includeSingleExons (include single exon genes for TP/FP calculations,\
            \ default: only use multiexon genes)\n\t\t-metagene (Include a metagene\
            \ plot of read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\t\
            Enrichment threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2\
            \ fold enrichment vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold\
            \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\
            \t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP calculation,\
            \ default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP\
            \ threshold calculation failes, default: 1)\n\t\t-pseudoCount <#> (number\
            \ of normalizaed reads added to input/target read counts, default: 1.0)\n\
            \t\t-ntagThreshold <#> (minimum number of norm. tags per TSS cluster,\
            \ default: 7)\n\t\t-L <#> (local fold enrichment used for initial TSS\
            \ identification, default: 2)\n\t\t-noFilterRNA (only use RNAseq for stability\
            \ calculations, not for filtering putative TSS)\n\n\t\tRegion sizes:\n\
            \t\t-size <#> (size of regions to check for significant TSS clusters,\
            \ default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to calculate\
            \ RNA/input read density,default: -100,500)\n\t\t-antisenseRegion <#>,<#>\
            \ (Size of region to calculate RNA/input read density,default: -500,100)\n\
            \t\t-rnaRegion <#>,<#> (Size of region to calculate RNA coverage for FP\
            \ identification,default: -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold\
            \ <#> (minimum RNAseq read density for stable transcript, def: 2)\n\t\t\
            -biThreshold <#> (minimum divergent csRNAseq signal for bi/divergent TSS,\
            \ def: 2)\n\t\t-dist <#> (minimum distance from annotated TSS to assign\
            \ 'distal' annotation, def: 500)\n\n\t* Tag Directories are assumed to\
            \ represent the 5' ends of RNA in sense orientation\n\n"
          generated_using: *id004
        - !Command
          command: *id007
          positional: []
          named:
          - !Flag
            description: (*see note below)
            synonyms:
            - -o
            - -i
            args: !SimpleFlagArg
              name: prefix
            optional: true
          - !Flag
            description: (*see note below)
            synonyms:
            - -rna
            args: !SimpleFlagArg
              name: RNAseq tag directory
            optional: true
          - !Flag
            description: (HOMER genome identifier or FASTA file)
            synonyms:
            - -genome
            args: !SimpleFlagArg
              name: genome
            optional: true
          - !Flag
            description: (gene annotation, by default will use genome annotation if
              available)
            synonyms:
            - -gtf
            args: !SimpleFlagArg
              name: gtf file
            optional: true
          - !Flag
            description: '<#> (minimum number of norm. tags per TSS cluster, default:
              7)'
            synonyms:
            - -ntagThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (size of regions to check for significant TSS clusters,
              default: 150)'
            synonyms:
            - -size
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '(include single exon genes for TP/FP calculations, default:
              only use multiexon genes)'
            synonyms:
            - -includeSingleExons
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: (Include a metagene plot of read distributions)
            synonyms:
            - -metagene
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: <#> (max CPUs to use)
            synonyms:
            - -cpu
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
              30000000000)'
            synonyms:
            - -maxInputLog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
              30000000000)'
            synonyms:
            - -maxRNALog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum CDF difference to consider for TP/FP calculation,
              default: 0.15)'
            synonyms:
            - -minDistDiff
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (if annotation-based TP/FP threshold calculation failes,
              default: 1)'
            synonyms:
            - -defaultLog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (number of normalizaed reads added to input/target read
              counts, default: 1.0)'
            synonyms:
            - -pseudoCount
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (local fold enrichment used for initial TSS identification,
              default: 2)'
            synonyms:
            - -L
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: (only use RNAseq for stability calculations, not for filtering
              putative TSS)
            synonyms:
            - -noFilterRNA
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
              -100,500)'
            synonyms:
            - -senseRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
              -500,100)'
            synonyms:
            - -antisenseRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA coverage for FP
              identification,default: -150,-10)'
            synonyms:
            - -rnaRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum RNAseq read density for stable transcript,
              def: 2)'
            synonyms:
            - -stableThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum divergent csRNAseq signal for bi/divergent
              TSS, def: 2)'
            synonyms:
            - -biThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: "<#> (minimum distance from annotated TSS to assign 'distal'\
              \ annotation, def: 500)"
            synonyms:
            - -dist
            args: !EmptyFlagArg {}
            optional: true
          parent: *id003
          subcommands: []
          usage: []
          help_flag:
          usage_flag:
          version_flag:
          help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\
            \t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\
            \t\t-rna <RNAseq tag directory> (*see note below)\n\t\t-genome <genome>\
            \ (HOMER genome identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation,\
            \ by default will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold\
            \ <#> (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t\
            -size <#> (size of regions to check for significant TSS clusters, default:\
            \ 150)\n\t\t-includeSingleExons (include single exon genes for TP/FP calculations,\
            \ default: only use multiexon genes)\n\t\t-metagene (Include a metagene\
            \ plot of read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\t\
            Enrichment threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2\
            \ fold enrichment vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold\
            \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\
            \t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP calculation,\
            \ default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP\
            \ threshold calculation failes, default: 1)\n\t\t-pseudoCount <#> (number\
            \ of normalizaed reads added to input/target read counts, default: 1.0)\n\
            \t\t-ntagThreshold <#> (minimum number of norm. tags per TSS cluster,\
            \ default: 7)\n\t\t-L <#> (local fold enrichment used for initial TSS\
            \ identification, default: 2)\n\t\t-noFilterRNA (only use RNAseq for stability\
            \ calculations, not for filtering putative TSS)\n\n\t\tRegion sizes:\n\
            \t\t-size <#> (size of regions to check for significant TSS clusters,\
            \ default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to calculate\
            \ RNA/input read density,default: -100,500)\n\t\t-antisenseRegion <#>,<#>\
            \ (Size of region to calculate RNA/input read density,default: -500,100)\n\
            \t\t-rnaRegion <#>,<#> (Size of region to calculate RNA coverage for FP\
            \ identification,default: -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold\
            \ <#> (minimum RNAseq read density for stable transcript, def: 2)\n\t\t\
            -biThreshold <#> (minimum divergent csRNAseq signal for bi/divergent TSS,\
            \ def: 2)\n\t\t-dist <#> (minimum distance from annotated TSS to assign\
            \ 'distal' annotation, def: 500)\n\n\t* Tag Directories are assumed to\
            \ represent the 5' ends of RNA in sense orientation\n\n"
          generated_using: *id004
        - !Command
          command: *id008
          positional: []
          named:
          - !Flag
            description: (*see note below)
            synonyms:
            - -o
            - -i
            args: !SimpleFlagArg
              name: prefix
            optional: true
          - !Flag
            description: (*see note below)
            synonyms:
            - -rna
            args: !SimpleFlagArg
              name: RNAseq tag directory
            optional: true
          - !Flag
            description: (HOMER genome identifier or FASTA file)
            synonyms:
            - -genome
            args: !SimpleFlagArg
              name: genome
            optional: true
          - !Flag
            description: (gene annotation, by default will use genome annotation if
              available)
            synonyms:
            - -gtf
            args: !SimpleFlagArg
              name: gtf file
            optional: true
          - !Flag
            description: '<#> (minimum number of norm. tags per TSS cluster, default:
              7)'
            synonyms:
            - -ntagThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (size of regions to check for significant TSS clusters,
              default: 150)'
            synonyms:
            - -size
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '(include single exon genes for TP/FP calculations, default:
              only use multiexon genes)'
            synonyms:
            - -includeSingleExons
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: (Include a metagene plot of read distributions)
            synonyms:
            - -metagene
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: <#> (max CPUs to use)
            synonyms:
            - -cpu
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
              30000000000)'
            synonyms:
            - -maxInputLog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
              30000000000)'
            synonyms:
            - -maxRNALog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum CDF difference to consider for TP/FP calculation,
              default: 0.15)'
            synonyms:
            - -minDistDiff
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (if annotation-based TP/FP threshold calculation failes,
              default: 1)'
            synonyms:
            - -defaultLog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (number of normalizaed reads added to input/target read
              counts, default: 1.0)'
            synonyms:
            - -pseudoCount
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (local fold enrichment used for initial TSS identification,
              default: 2)'
            synonyms:
            - -L
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: (only use RNAseq for stability calculations, not for filtering
              putative TSS)
            synonyms:
            - -noFilterRNA
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
              -100,500)'
            synonyms:
            - -senseRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
              -500,100)'
            synonyms:
            - -antisenseRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA coverage for FP
              identification,default: -150,-10)'
            synonyms:
            - -rnaRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum RNAseq read density for stable transcript,
              def: 2)'
            synonyms:
            - -stableThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum divergent csRNAseq signal for bi/divergent
              TSS, def: 2)'
            synonyms:
            - -biThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: "<#> (minimum distance from annotated TSS to assign 'distal'\
              \ annotation, def: 500)"
            synonyms:
            - -dist
            args: !EmptyFlagArg {}
            optional: true
          parent: *id003
          subcommands: []
          usage: []
          help_flag:
          usage_flag:
          version_flag:
          help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\
            \t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\
            \t\t-rna <RNAseq tag directory> (*see note below)\n\t\t-genome <genome>\
            \ (HOMER genome identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation,\
            \ by default will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold\
            \ <#> (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t\
            -size <#> (size of regions to check for significant TSS clusters, default:\
            \ 150)\n\t\t-includeSingleExons (include single exon genes for TP/FP calculations,\
            \ default: only use multiexon genes)\n\t\t-metagene (Include a metagene\
            \ plot of read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\t\
            Enrichment threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2\
            \ fold enrichment vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold\
            \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\
            \t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP calculation,\
            \ default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP\
            \ threshold calculation failes, default: 1)\n\t\t-pseudoCount <#> (number\
            \ of normalizaed reads added to input/target read counts, default: 1.0)\n\
            \t\t-ntagThreshold <#> (minimum number of norm. tags per TSS cluster,\
            \ default: 7)\n\t\t-L <#> (local fold enrichment used for initial TSS\
            \ identification, default: 2)\n\t\t-noFilterRNA (only use RNAseq for stability\
            \ calculations, not for filtering putative TSS)\n\n\t\tRegion sizes:\n\
            \t\t-size <#> (size of regions to check for significant TSS clusters,\
            \ default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to calculate\
            \ RNA/input read density,default: -100,500)\n\t\t-antisenseRegion <#>,<#>\
            \ (Size of region to calculate RNA/input read density,default: -500,100)\n\
            \t\t-rnaRegion <#>,<#> (Size of region to calculate RNA coverage for FP\
            \ identification,default: -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold\
            \ <#> (minimum RNAseq read density for stable transcript, def: 2)\n\t\t\
            -biThreshold <#> (minimum divergent csRNAseq signal for bi/divergent TSS,\
            \ def: 2)\n\t\t-dist <#> (minimum distance from annotated TSS to assign\
            \ 'distal' annotation, def: 500)\n\n\t* Tag Directories are assumed to\
            \ represent the 5' ends of RNA in sense orientation\n\n"
          generated_using: *id004
        - !Command
          command: *id009
          positional: []
          named:
          - !Flag
            description: (*see note below)
            synonyms:
            - -o
            - -i
            args: !SimpleFlagArg
              name: prefix
            optional: true
          - !Flag
            description: (*see note below)
            synonyms:
            - -rna
            args: !SimpleFlagArg
              name: RNAseq tag directory
            optional: true
          - !Flag
            description: (HOMER genome identifier or FASTA file)
            synonyms:
            - -genome
            args: !SimpleFlagArg
              name: genome
            optional: true
          - !Flag
            description: (gene annotation, by default will use genome annotation if
              available)
            synonyms:
            - -gtf
            args: !SimpleFlagArg
              name: gtf file
            optional: true
          - !Flag
            description: '<#> (minimum number of norm. tags per TSS cluster, default:
              7)'
            synonyms:
            - -ntagThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (size of regions to check for significant TSS clusters,
              default: 150)'
            synonyms:
            - -size
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '(include single exon genes for TP/FP calculations, default:
              only use multiexon genes)'
            synonyms:
            - -includeSingleExons
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: (Include a metagene plot of read distributions)
            synonyms:
            - -metagene
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: <#> (max CPUs to use)
            synonyms:
            - -cpu
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
              30000000000)'
            synonyms:
            - -maxInputLog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
              30000000000)'
            synonyms:
            - -maxRNALog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum CDF difference to consider for TP/FP calculation,
              default: 0.15)'
            synonyms:
            - -minDistDiff
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (if annotation-based TP/FP threshold calculation failes,
              default: 1)'
            synonyms:
            - -defaultLog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (number of normalizaed reads added to input/target read
              counts, default: 1.0)'
            synonyms:
            - -pseudoCount
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (local fold enrichment used for initial TSS identification,
              default: 2)'
            synonyms:
            - -L
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: (only use RNAseq for stability calculations, not for filtering
              putative TSS)
            synonyms:
            - -noFilterRNA
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
              -100,500)'
            synonyms:
            - -senseRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
              -500,100)'
            synonyms:
            - -antisenseRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA coverage for FP
              identification,default: -150,-10)'
            synonyms:
            - -rnaRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum RNAseq read density for stable transcript,
              def: 2)'
            synonyms:
            - -stableThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum divergent csRNAseq signal for bi/divergent
              TSS, def: 2)'
            synonyms:
            - -biThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: "<#> (minimum distance from annotated TSS to assign 'distal'\
              \ annotation, def: 500)"
            synonyms:
            - -dist
            args: !EmptyFlagArg {}
            optional: true
          parent: *id003
          subcommands: []
          usage: []
          help_flag:
          usage_flag:
          version_flag:
          help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\
            \t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\
            \t\t-rna <RNAseq tag directory> (*see note below)\n\t\t-genome <genome>\
            \ (HOMER genome identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation,\
            \ by default will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold\
            \ <#> (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t\
            -size <#> (size of regions to check for significant TSS clusters, default:\
            \ 150)\n\t\t-includeSingleExons (include single exon genes for TP/FP calculations,\
            \ default: only use multiexon genes)\n\t\t-metagene (Include a metagene\
            \ plot of read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\t\
            Enrichment threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2\
            \ fold enrichment vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold\
            \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\
            \t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP calculation,\
            \ default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP\
            \ threshold calculation failes, default: 1)\n\t\t-pseudoCount <#> (number\
            \ of normalizaed reads added to input/target read counts, default: 1.0)\n\
            \t\t-ntagThreshold <#> (minimum number of norm. tags per TSS cluster,\
            \ default: 7)\n\t\t-L <#> (local fold enrichment used for initial TSS\
            \ identification, default: 2)\n\t\t-noFilterRNA (only use RNAseq for stability\
            \ calculations, not for filtering putative TSS)\n\n\t\tRegion sizes:\n\
            \t\t-size <#> (size of regions to check for significant TSS clusters,\
            \ default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to calculate\
            \ RNA/input read density,default: -100,500)\n\t\t-antisenseRegion <#>,<#>\
            \ (Size of region to calculate RNA/input read density,default: -500,100)\n\
            \t\t-rnaRegion <#>,<#> (Size of region to calculate RNA coverage for FP\
            \ identification,default: -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold\
            \ <#> (minimum RNAseq read density for stable transcript, def: 2)\n\t\t\
            -biThreshold <#> (minimum divergent csRNAseq signal for bi/divergent TSS,\
            \ def: 2)\n\t\t-dist <#> (minimum distance from annotated TSS to assign\
            \ 'distal' annotation, def: 500)\n\n\t* Tag Directories are assumed to\
            \ represent the 5' ends of RNA in sense orientation\n\n"
          generated_using: *id004
        - !Command
          command: *id010
          positional: []
          named:
          - !Flag
            description: (*see note below)
            synonyms:
            - -o
            - -i
            args: !SimpleFlagArg
              name: prefix
            optional: true
          - !Flag
            description: (*see note below)
            synonyms:
            - -rna
            args: !SimpleFlagArg
              name: RNAseq tag directory
            optional: true
          - !Flag
            description: (HOMER genome identifier or FASTA file)
            synonyms:
            - -genome
            args: !SimpleFlagArg
              name: genome
            optional: true
          - !Flag
            description: (gene annotation, by default will use genome annotation if
              available)
            synonyms:
            - -gtf
            args: !SimpleFlagArg
              name: gtf file
            optional: true
          - !Flag
            description: '<#> (minimum number of norm. tags per TSS cluster, default:
              7)'
            synonyms:
            - -ntagThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (size of regions to check for significant TSS clusters,
              default: 150)'
            synonyms:
            - -size
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '(include single exon genes for TP/FP calculations, default:
              only use multiexon genes)'
            synonyms:
            - -includeSingleExons
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: (Include a metagene plot of read distributions)
            synonyms:
            - -metagene
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: <#> (max CPUs to use)
            synonyms:
            - -cpu
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
              30000000000)'
            synonyms:
            - -maxInputLog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
              30000000000)'
            synonyms:
            - -maxRNALog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum CDF difference to consider for TP/FP calculation,
              default: 0.15)'
            synonyms:
            - -minDistDiff
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (if annotation-based TP/FP threshold calculation failes,
              default: 1)'
            synonyms:
            - -defaultLog2Fold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (number of normalizaed reads added to input/target read
              counts, default: 1.0)'
            synonyms:
            - -pseudoCount
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (local fold enrichment used for initial TSS identification,
              default: 2)'
            synonyms:
            - -L
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: (only use RNAseq for stability calculations, not for filtering
              putative TSS)
            synonyms:
            - -noFilterRNA
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
              -100,500)'
            synonyms:
            - -senseRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
              -500,100)'
            synonyms:
            - -antisenseRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#>,<#> (Size of region to calculate RNA coverage for FP
              identification,default: -150,-10)'
            synonyms:
            - -rnaRegion
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum RNAseq read density for stable transcript,
              def: 2)'
            synonyms:
            - -stableThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: '<#> (minimum divergent csRNAseq signal for bi/divergent
              TSS, def: 2)'
            synonyms:
            - -biThreshold
            args: !EmptyFlagArg {}
            optional: true
          - !Flag
            description: "<#> (minimum distance from annotated TSS to assign 'distal'\
              \ annotation, def: 500)"
            synonyms:
            - -dist
            args: !EmptyFlagArg {}
            optional: true
          parent: *id003
          subcommands: []
          usage: []
          help_flag:
          usage_flag:
          version_flag:
          help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\
            \t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\
            \t\t-rna <RNAseq tag directory> (*see note below)\n\t\t-genome <genome>\
            \ (HOMER genome identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation,\
            \ by default will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold\
            \ <#> (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t\
            -size <#> (size of regions to check for significant TSS clusters, default:\
            \ 150)\n\t\t-includeSingleExons (include single exon genes for TP/FP calculations,\
            \ default: only use multiexon genes)\n\t\t-metagene (Include a metagene\
            \ plot of read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\t\
            Enrichment threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2\
            \ fold enrichment vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold\
            \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\
            \t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP calculation,\
            \ default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP\
            \ threshold calculation failes, default: 1)\n\t\t-pseudoCount <#> (number\
            \ of normalizaed reads added to input/target read counts, default: 1.0)\n\
            \t\t-ntagThreshold <#> (minimum number of norm. tags per TSS cluster,\
            \ default: 7)\n\t\t-L <#> (local fold enrichment used for initial TSS\
            \ identification, default: 2)\n\t\t-noFilterRNA (only use RNAseq for stability\
            \ calculations, not for filtering putative TSS)\n\n\t\tRegion sizes:\n\
            \t\t-size <#> (size of regions to check for significant TSS clusters,\
            \ default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to calculate\
            \ RNA/input read density,default: -100,500)\n\t\t-antisenseRegion <#>,<#>\
            \ (Size of region to calculate RNA/input read density,default: -500,100)\n\
            \t\t-rnaRegion <#>,<#> (Size of region to calculate RNA coverage for FP\
            \ identification,default: -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold\
            \ <#> (minimum RNAseq read density for stable transcript, def: 2)\n\t\t\
            -biThreshold <#> (minimum divergent csRNAseq signal for bi/divergent TSS,\
            \ def: 2)\n\t\t-dist <#> (minimum distance from annotated TSS to assign\
            \ 'distal' annotation, def: 500)\n\n\t* Tag Directories are assumed to\
            \ represent the 5' ends of RNA in sense orientation\n\n"
          generated_using: *id004
        usage: []
        help_flag:
        usage_flag:
        version_flag:
        help_text: "!!! First argument needs to be a <tag directory>\n\n\tUsage: findPeaks\
          \ <tag directory> [options]\n\n\tFinds peaks in the provided tag directory.\
          \  By default, peak list printed to stdout\n\n\tGeneral analysis options:\n\
          \t\t-o <filename|auto> (file name for to output peaks, default: stdout)\n\
          \t\t\t\"-o auto\" will send output to \"<tag directory>/peaks.txt\", \"\
          .../regions.txt\",\n\t\t\tor \".../transcripts.txt\" depending on the \"\
          -style\" option\n\t\t-style <option> (Specialized options for specific analysis\
          \ strategies)\n\t\t\tfactor (transcription factor ChIP-Seq, uses -center,\
          \ output: peaks.txt,  default)\n\t\t\thistone (histone modification ChIP-Seq,\
          \ region based, uses -region -size 500 -L 0, regions.txt)\n\t\t\tgroseq\
          \ (de novo transcript identification from GroSeq data, transcripts.txt)\n\
          \t\t\ttss (TSS identification from 5' RNA sequencing, tss.txt)\n\t\t\tdnase\
          \ (Hypersensitivity [crawford style (nicking)], peaks.txt)\n\t\t\tsuper\
          \ (Super Enhancers, superEnhancers.txt)\n\t\t\tsuperhistone (Super Enhancers\
          \ from H3K27ac data, superEnhancers.txt)\n\t\t\tmC (Cytosine methylation\
          \ (BS-seq/methylC-seq), regions.txt)\n\t\t\tdamid (DamID enrichment from\
          \ DpnI digestion, regions.txt)\n\t\t\tclip (CLIP-Seq enrichment, strand\
          \ specific, peaks.txt)\n\n\tchipseq/histone options:\n\t\t-i <input tag\
          \ directory> (Experiment to use as IgG/Input/Control)\n\t\t-size <#> (Peak\
          \ size, default: auto)\n\t\t-minDist <#> (minimum distance between peaks,\
          \ default: peak size x2)\n\t\t-gsize <#> (Set effective mappable genome\
          \ size, default: 2e9)\n\t\t-fragLength <#|auto> (Approximate fragment length,\
          \ default: auto)\n\t\t-inputFragLength <#|auto> (Approximate fragment length\
          \ of input tags, default: auto)\n\t\t-tbp <#> (Maximum tags per bp to count,\
          \ 0 = no limit, default: auto)\n\t\t-inputtbp <#> (Maximum tags per bp to\
          \ count in input, 0 = no limit, default: auto)\n\t\t-strand <both|separate>\
          \ (find peaks using tags on both strands or separate, default:both)\n\t\t\
          -norm # (Tag count to normalize to, default 10000000)\n\t\t-region (extends\
          \ start/stop coordinates to cover full region considered \"enriched\")\n\
          \t\t\t-regionRes <#> (number of fractions peaks are divided in when extending\
          \ 'regions', def: 4)\n\t\t-center (Centers peaks on maximum tag overlap\
          \ and calculates focus ratios)\n\t\t-nfr (Centers peaks on most likely nucleosome\
          \ free region [works best with mnase data])\n\t\t\t(-center and -nfr can\
          \ be performed later with \"getPeakTags\"\n\n\tPeak Filtering options: (set\
          \ -F/-L/-C to 0 to skip)\n\t\t-F <#> (fold enrichment over input tag count,\
          \ default: 4.0)\n\t\t  -P <#> (poisson p-value threshold relative to input\
          \ tag count, default: 0.0001)\n\t\t-L <#> (fold enrichment over local tag\
          \ count, default: 4.0)\n\t\t  -LP <#> (poisson p-value threshold relative\
          \ to local tag count, default: 0.0001)\n\t\t-C <#> (fold enrichment limit\
          \ of expected unique tag positions, default: 2.0)\n\t\t-localSize <#> (region\
          \ to check for local tag enrichment, default: 10000)\n\t\t-inputSize <#>\
          \ (Size of region to search for control tags, default: 2x peak size)\n\t\
          \t-fdr <#> (False discovery rate, default = 0.001)\n\t\t-poisson <#> (Set\
          \ poisson p-value cutoff, default: uses fdr)\n\t\t-tagThreshold <#> (Set\
          \ # of tags to define a peak, default: 25)\n\t\t-ntagThreshold <#> (Set\
          \ # of normalized tags to define a peak, by default uses 1e7 for norm)\n\
          \t\t-minTagThreshold <#> (Absolute minimum tags per peak, default: expected\
          \ tags per peak)\n\n\tSuperEnhancer Options: (Need to specify \"-style super\"\
          ):\n\t\t-superSlope <#> (Slope threshold to identify super vs. typical enh.,\
          \ default: 1.00)\n\t\t-superWindow <#> (moving window/number of peaks to\
          \ use to calculate slope, default: 10)\n\t\t-typical <filename> (Output\
          \ typical enhancers to this file, default: not used)\n\t\t-inputPeaks <filename>\
          \ (initial peaks to use for super enhancer merging/scoring)\n\t\t-excludePeaks\
          \ <filename> (regions to exclude from analysis, i.e. TSS regions for H3K27ac)\n\
          \n\tMethylC-Seq/BS-Seq options (Need to specify \"-style mC\"):\n\t\t-unmethylC\
          \ / -methylC (find unmethylated/methylated regions, default: -unmethyC)\n\
          \t\t-mCthresh <#> (methylation threshold of regions, default: avg methylation/2)\n\
          \t\t-minNumC <#> (Minimum number of cytosines per methylation peak, default:\
          \ 6)\n\n\tGroSeq Options (Need to specify \"-style groseq\"):\n\t\t-tssSize\
          \ <#> (size of region for initiation detection/artifact size, default: 250)\n\
          \t\t-minBodySize <#> (size of regoin for transcript body detection, default:\
          \ 1000)\n\t\t-tssFold <#> (fold enrichment for new initiation dectection,\
          \ default: 4.0)\n\t\t-bodyFold <#> (fold enrichment for new transcript dectection,\
          \ default: 4.0)\n\t\t-endFold <#> (end transcript when levels are this much\
          \ less than the start, default: 10.0)\n\t\t-method <fold|level> (method\
          \ used for identifying new transcripts, default: fold)\n\t\t-fragLength\
          \ <#> (Approximate fragment length, default: 150)\n\t\t-uniqmap <directory>\
          \ (directory of binary files specifying uniquely mappable locations)\n\t\
          \t\tDownload from http://biowhat.ucsd.edu/homer/groseq/\n\t\t-confPvalue\
          \ <#> (confidence p-value: 1.00e-05)\n\t\t-minReadDepth <#> (Minimum initial\
          \ read depth for transcripts, default: auto)\n\t\t-pseudoCount <#> (Pseudo\
          \ tag count, default: 2.0)\n\t\t-rev (reverse strand of reads - for first-strand\
          \ rna-seq/gro-seq)\n\t\t-gtf <filename> (Output de novo transcripts in GTF\
          \ format)\n\t\t\t\"-o auto\" will produce <dir>/transcripts.txt and <dir>/transcripts.gtf\n\
          \t\t\n\n\tNo genome selected (\"none\") - limits what you can do\n\tPeak\
          \ file = 0.178757123489962.tss.tmp\n\tGenome = none\n\tOrganism = unknown\n\
          \tWill count tags on strand: +\n\tFragment Length set to 1\n\tWill use up\
          \ to 3 CPUs in parts that can use them\n\tPeak/BED file conversion summary:\n\
          \t\tBED/Header formatted lines: 0\n\t\tpeakfile formatted lines: 0\n\t\t\
          Duplicated Peak IDs: 0\n\n\tPeak File Statistics:\n\t\tTotal Peaks: 0\n\t\
          \tRedundant Peak IDs: 0\n\t\tPeaks lacking information: 0 (need at least\
          \ 5 columns per peak)\n\t\tPeaks with misformatted coordinates: 0 (should\
          \ be integer)\n\t\tPeaks with misformatted strand: 0 (should be either +/-\
          \ or 0/1)\n\n\tOnly 0 peaks found! If many more expected, maybe the file\
          \ format is for Macs only\n\tTry running the command: changeNewLine.pl \"\
          0.744680606135265.clean.pos\"\n\n\tReading Positions...\n\t-----------------------\n\
          \tFinding Closest TSS...\n\tSkipping TSS assignment (can't find file for\
          \ genome none)\n\t\tCan't find promoterFile none/none.tss\n\tCould not find\
          \ basic annotation file (none/none.annotation)\n\tCould not find full/detailed\
          \ annotation file (none/none.annotation)\n\tCounting Tags in Peaks from\
          \ each directory...\n\tOrganism: unknown\n\tOutputing Annotation File...\n\
          \tDone annotating peaks file\n\n\n\tNo genome selected (\"none\") - limits\
          \ what you can do\n\tPeak file = 0.178757123489962.tss.tmp\n\tGenome = none\n\
          \tOrganism = unknown\n\tWill count tags on strand: +\n\tFragment Length\
          \ set to 1\n\tPeak Region set to 150\n\tTag Directories:\n--help not recognized\n\
          \n\n\tUsage: annotatePeaks.pl <peak file | tss> <genome version>  [additional\
          \ options...]\n\n\tAvailable Genomes (required argument): (name,org,directory,default\
          \ promoter set)\n\t\t\t-- or --\n\t\tCustom: provide the path to genome\
          \ FASTA files (directory or single file)\n\t\tIf no genome is available,\
          \ specify 'none'.\n\t\tIf using FASTA file or none, may want to specify\
          \ '-organism <...>'\n\n\tUser defined annotation files (default is UCSC\
          \ refGene annotation):\n\t\tannotatePeaks.pl accepts GTF (gene transfer\
          \ formatted) files to annotate positions relative\n\t\tto custom annotations,\
          \ such as those from de novo transcript discovery or Gencode.\n\t\t-gtf\
          \ <gtf format file> (Use -gff and -gff3 if appropriate, but GTF is better)\n\
          \t\t-gid (by default the GTF file is processed by transcript_id, use this\
          \ option for gene_id)\n\t\t-ann <custom homer annotation file> (created\
          \ by assignGenomeAnnotation, see website)\n\n\tPeak vs. tss/tts/rna mode\
          \ (works with custom GTF file):\n\t\tIf the first argument is \"tss\" (i.e.\
          \ annotatePeaks.pl tss hg18 ...) then a TSS centric\n\t\tanalysis will be\
          \ carried out.  Tag counts and motifs will be found relative to the TSS.\n\
          \t\t(no position file needed) [\"tts\" now works too - e.g. 3' end of gene]\n\
          \t\t[\"rna\" specifies gene bodies, will automaticall set \"-size given\"\
          ]\n\t\tNOTE: The default TSS peak size is 4000 bp, i.e. +/- 2kb (change\
          \ with -size option)\n\t\t-list <gene id list> (subset of genes to perform\
          \ analysis [unigene, gene id, accession,\n\t\t\t probe, etc.], default =\
          \ all promoters)\n\t\t-cTSS <promoter position file i.e. peak file> (should\
          \ be centered on TSS)\n\n\tPrimary Annotation Options:\n\t\t-mask (Masked\
          \ repeats, can also add 'r' to end of genome name)\n\t\t-m <motif file 1>\
          \ [motif file 2] ... (list of motifs to find in peaks)\n\t\t\t-mscore (reports\
          \ the highest log-odds score within the peak)\n\t\t\t-nmotifs (reports the\
          \ number of motifs per peak)\n\t\t\t-mdist (reports distance to closest\
          \ motif)\n\t\t\t-mfasta <filename> (reports sites in a fasta file - for\
          \ building new motifs)\n\t\t\t-fm <motif file 1> [motif file 2] (list of\
          \ motifs to filter from above)\n\t\t\t-rmrevopp <#> (only count sites found\
          \ within <#> on both strands once, i.e. palindromic)\n\t\t\t-matrix <prefix>\
          \ (outputs a motif co-occurrence files:\n\t\t\t\tprefix.count.matrix.txt\
          \ - number of peaks with motif co-occurrence\n\t\t\t\tprefix.ratio.matrix.txt\
          \ - ratio of observed vs. expected  co-occurrence\n\t\t\t\tprefix.logPvalue.matrix.txt\
          \ - co-occurrence enrichment\n\t\t\t\tprefix.stats.txt - table of pair-wise\
          \ motif co-occurrence statistics\n\t\t\t\tadditional options:\n\t\t\t\t\
          -matrixMinDist <#> (minimum distance between motif pairs - to avoid overlap,\
          \ default: 4)\n\t\t\t\t-matrixMaxDist <#> (maximum distance between motif\
          \ pairs)\n\t\t\t-mbed <filename> (Output motif positions to a BED file to\
          \ load at UCSC (or -mpeak))\n\t\t\t-mlogic <filename> (will output stats\
          \ on common motif orientations)\n\t\t-d <tag directory 1> [tag directory\
          \ 2] ... (list of experiment directories to show\n\t\t\ttag counts for)\
          \ NOTE: -dfile <file> where file is a list of directories in first column\n\
          \t\t-bedGraph <bedGraph file 1> [bedGraph file 2] ... (read coverage counts\
          \ from bedGraph files)\n\t\t-wig <wiggle file 1> [wiggle file 2] ... (read\
          \ coverage counts from wiggle files)\n\t\t-p <peak file> [peak file 2] ...\
          \ (to find nearest peaks)\n\t\t\t-pdist to report only distance (-pdist2\
          \ gives directional distance)\n\t\t\t-pcount to report number of peaks within\
          \ region\n\t\t-vcf <VCF file> (annotate peaks with genetic variation infomation,\
          \ one col per individual)\n\t\t\t-editDistance (Computes the # bp changes\
          \ relative to reference)\n\t\t\t-individuals <name1> [name2] ... (restrict\
          \ analysis to these individuals)\n\t\t-gene <data file> ... (Adds additional\
          \ data to result based on the closest gene.\n\t\t\tThis is useful for adding\
          \ gene expression data.  The file must have a header,\n\t\t\tand the first\
          \ column must be a GeneID, Accession number, etc.  If the peak\n\t\t\tcannot\
          \ be mapped to data in the file then the entry will be left empty.\n\t\t\
          -go <output directory> (perform GO analysis using genes near peaks)\n\t\t\
          -genomeOntology <output directory> (perform genomeOntology analysis on peaks)\n\
          \t\t\t-gsize <#> (Genome size for genomeOntology analysis, default: 2e9)\n\
          \n\tAnnotation vs. Histogram mode:\n\t\t-hist <bin size in bp> (i.e 1, 2,\
          \ 5, 10, 20, 50, 100 etc.)\n\t\tThe -hist option can be used to generate\
          \ histograms of position dependent features relative\n\t\tto the center\
          \ of peaks.  This is primarily meant to be used with -d and -m options to\
          \ map\n\t\tdistribution of motifs and ChIP-Seq tags.  For ChIP-Seq peaks\
          \ for a Transcription factor\n\t\tyou might want to use the -center option\
          \ (below) to center peaks on the known motif\n\t\t** If using \"-size given\"\
          , histogram will be scaled to each region (i.e. 0-100%), with\n\t\tthe -hist\
          \ parameter being the number of bins to divide each region into.\n\t\t\t\
          Histogram Mode specific Options:\n\t\t\t-nuc (calculated mononucleotide\
          \ frequencies at each position,\n\t\t\t\tWill report by default if extracting\
          \ sequence for other purposes like motifs)\n\t\t\t-di (calculated dinucleotide\
          \ frequencies at each position)\n\t\t\t-histNorm <#> (normalize the total\
          \ tag count for each region to 1, where <#> is the\n\t\t\t\tminimum tag\
          \ total per region - use to avoid tag spikes from low coverage\n\t\t\t-ghist\
          \ (outputs profiles for each gene, for peak shape clustering)\n\t\t\t-rm\
          \ <#> (remove occurrences of same motif that occur within # bp)\n\n\tPeak\
          \ Centering: (other options are ignored)\n\t\t-center <motif file> (This\
          \ will re-center peaks on the specified motif, or remove peak\n\t\t\tif\
          \ there is no motif in the peak.  ONLY recentering will be performed, and\
          \ all other\n\t\t\toptions will be ignored.  This will output a new peak\
          \ file that can then be reanalyzed\n\t\t\tto reveal fine-grain structure\
          \ in peaks (It is advised to use -size < 200) with this\n\t\t\tto keep peaks\
          \ from moving too far (-mirror flips the position)\n\t\t-multi (returns\
          \ genomic positions of all sites instead of just the closest to center)\n\
          \n\tGenome comparisons (need genome & liftOver)\n\t\t-cmpGenome <genome1>\
          \ [genome2] (Genomes to compare for sequence/motifs)\n\t\t-cmpLiftover <liftover1>\
          \ [genome2] (Genomes to compare for sequence/motifs)\n\n\tNormalization\
          \ options:\n\t\t-fpkm (normalize read counts to million reads or fragments\
          \ per kilobase mapped)\n\t\t-raw (do not adjust the tag counts based on\
          \ total tags sequenced, -noadj works too)\n\t\t-norm <#> (normalize tags\
          \ to this tag count, default=1e7, 0=average tag count in all directories)\n\
          \t\t-normLength <#> (Fragment length to normlize to for experiments with\
          \ different lens, def: 100)\n\t\t-log (output tag counts as log2(x+1+rand)\
          \ values - for scatter plots)\n\t\t-sqrt (output tag counts as sqrt(x+rand)\
          \ values - for scatter plots)\n\t\t-ratio (process tag values as ratios\
          \ - i.e. chip-seq, or mCpG/CpG)\n\n\tAdvanced normalization options: (-rlog\
          \ and -vst require R and DESeq2 to be installed)\n\t\t-rlog (quantile/variance\
          \ normalization on reported genes using DESeq2 rlog funcition, needs R)\n\
          \t\t-vst (quantile/variance normalization on reported genes using DESeq2\
          \ vst function, needs R)\n\n\tAdvanced Options:\n\t\t-len <#> / -fragLength\
          \ <#> (Fragment length, default=auto, might want to set to 1 for 5'RNA)\n\
          \t\t-size <#> (Peak size[from center of peak], default=inferred from peak\
          \ file)\n\t\t\t-size #,# (i.e. -size -10,50 count tags from -10 bp to +50\
          \ bp from center)\n\t\t\t-size \"given\" (count tags etc. using the actual\
          \ regions - for variable length regions)\n\t\t-strand <+|-|both> (Count\
          \ tags on specific strands relative to peak, default: both)\n\t\t-pc <#>\
          \ (maximum number of tags to count per bp, default=0 [no maximum], -tbp\
          \ <#> works too)\n\t\t-CpG (Calculate CpG/GC content)\n\t\t-nfr (report\
          \ nuclesome free region scores instead of tag counts, also -nfrSize <#>)\n\
          \t\t-norevopp (do not search for motifs on the opposite strand [works with\
          \ -center too])\n\t\t-gwasCatalog <gwasCatalog file from UCSC> (list overlapping\
          \ GWAS risk SNPs)\n\t\t-pdist (only report distance to nearest peak using\
          \ -p, not peak name)\n\t\t-map <mapping file> (mapping between peak IDs\
          \ and promoter IDs, overrides closest assignment)\n\t\t-noann, -nogene (skip\
          \ genome annotation step, skip TSS annotation)\n\t\t-homer1/-homer2 (by\
          \ default, the new version of homer [-homer2] is used for finding motifs)\n\
          \t\t-cpu <#> (Number of processors to use when possible - only some parts\
          \ utilize multiple cores)\n\t\t-noblanks (remove peaks/rows with missing\
          \ data)\n\n\tPeak/BED file conversion summary:\n\t\tBED/Header formatted\
          \ lines: 0\n\t\tpeakfile formatted lines: 0\n\t\tDuplicated Peak IDs: 0\n\
          \n\tPeak File Statistics:\n\t\tTotal Peaks: 0\n\t\tRedundant Peak IDs: 0\n\
          \t\tPeaks lacking information: 0 (need at least 5 columns per peak)\n\t\t\
          Peaks with misformatted coordinates: 0 (should be integer)\n\t\tPeaks with\
          \ misformatted strand: 0 (should be either +/- or 0/1)\n\n\tOnly 0 peaks\
          \ found! If many more expected, maybe the file format is for Macs only\n\
          \tTry running the command: changeNewLine.pl \"0.0945234839609874.2.tmp\"\
          \n\n\n\tNo genome selected (\"none\") - limits what you can do\n\tPeak file\
          \ = 0.178757123489962.tmp\n\tGenome = none\n\tOrganism = unknown\n\tWill\
          \ count tags on strand: +\n\tFragment Length set to 1\n\tTag Directories:\n\
          --help not recognized\n\n\n\tUsage: annotatePeaks.pl <peak file | tss> <genome\
          \ version>  [additional options...]\n\n\tAvailable Genomes (required argument):\
          \ (name,org,directory,default promoter set)\n\t\t\t-- or --\n\t\tCustom:\
          \ provide the path to genome FASTA files (directory or single file)\n\t\t\
          If no genome is available, specify 'none'.\n\t\tIf using FASTA file or none,\
          \ may want to specify '-organism <...>'\n\n\tUser defined annotation files\
          \ (default is UCSC refGene annotation):\n\t\tannotatePeaks.pl accepts GTF\
          \ (gene transfer formatted) files to annotate positions relative\n\t\tto\
          \ custom annotations, such as those from de novo transcript discovery or\
          \ Gencode.\n\t\t-gtf <gtf format file> (Use -gff and -gff3 if appropriate,\
          \ but GTF is better)\n\t\t-gid (by default the GTF file is processed by\
          \ transcript_id, use this option for gene_id)\n\t\t-ann <custom homer annotation\
          \ file> (created by assignGenomeAnnotation, see website)\n\n\tPeak vs. tss/tts/rna\
          \ mode (works with custom GTF file):\n\t\tIf the first argument is \"tss\"\
          \ (i.e. annotatePeaks.pl tss hg18 ...) then a TSS centric\n\t\tanalysis\
          \ will be carried out.  Tag counts and motifs will be found relative to\
          \ the TSS.\n\t\t(no position file needed) [\"tts\" now works too - e.g.\
          \ 3' end of gene]\n\t\t[\"rna\" specifies gene bodies, will automaticall\
          \ set \"-size given\"]\n\t\tNOTE: The default TSS peak size is 4000 bp,\
          \ i.e. +/- 2kb (change with -size option)\n\t\t-list <gene id list> (subset\
          \ of genes to perform analysis [unigene, gene id, accession,\n\t\t\t probe,\
          \ etc.], default = all promoters)\n\t\t-cTSS <promoter position file i.e.\
          \ peak file> (should be centered on TSS)\n\n\tPrimary Annotation Options:\n\
          \t\t-mask (Masked repeats, can also add 'r' to end of genome name)\n\t\t\
          -m <motif file 1> [motif file 2] ... (list of motifs to find in peaks)\n\
          \t\t\t-mscore (reports the highest log-odds score within the peak)\n\t\t\
          \t-nmotifs (reports the number of motifs per peak)\n\t\t\t-mdist (reports\
          \ distance to closest motif)\n\t\t\t-mfasta <filename> (reports sites in\
          \ a fasta file - for building new motifs)\n\t\t\t-fm <motif file 1> [motif\
          \ file 2] (list of motifs to filter from above)\n\t\t\t-rmrevopp <#> (only\
          \ count sites found within <#> on both strands once, i.e. palindromic)\n\
          \t\t\t-matrix <prefix> (outputs a motif co-occurrence files:\n\t\t\t\tprefix.count.matrix.txt\
          \ - number of peaks with motif co-occurrence\n\t\t\t\tprefix.ratio.matrix.txt\
          \ - ratio of observed vs. expected  co-occurrence\n\t\t\t\tprefix.logPvalue.matrix.txt\
          \ - co-occurrence enrichment\n\t\t\t\tprefix.stats.txt - table of pair-wise\
          \ motif co-occurrence statistics\n\t\t\t\tadditional options:\n\t\t\t\t\
          -matrixMinDist <#> (minimum distance between motif pairs - to avoid overlap,\
          \ default: 4)\n\t\t\t\t-matrixMaxDist <#> (maximum distance between motif\
          \ pairs)\n\t\t\t-mbed <filename> (Output motif positions to a BED file to\
          \ load at UCSC (or -mpeak))\n\t\t\t-mlogic <filename> (will output stats\
          \ on common motif orientations)\n\t\t-d <tag directory 1> [tag directory\
          \ 2] ... (list of experiment directories to show\n\t\t\ttag counts for)\
          \ NOTE: -dfile <file> where file is a list of directories in first column\n\
          \t\t-bedGraph <bedGraph file 1> [bedGraph file 2] ... (read coverage counts\
          \ from bedGraph files)\n\t\t-wig <wiggle file 1> [wiggle file 2] ... (read\
          \ coverage counts from wiggle files)\n\t\t-p <peak file> [peak file 2] ...\
          \ (to find nearest peaks)\n\t\t\t-pdist to report only distance (-pdist2\
          \ gives directional distance)\n\t\t\t-pcount to report number of peaks within\
          \ region\n\t\t-vcf <VCF file> (annotate peaks with genetic variation infomation,\
          \ one col per individual)\n\t\t\t-editDistance (Computes the # bp changes\
          \ relative to reference)\n\t\t\t-individuals <name1> [name2] ... (restrict\
          \ analysis to these individuals)\n\t\t-gene <data file> ... (Adds additional\
          \ data to result based on the closest gene.\n\t\t\tThis is useful for adding\
          \ gene expression data.  The file must have a header,\n\t\t\tand the first\
          \ column must be a GeneID, Accession number, etc.  If the peak\n\t\t\tcannot\
          \ be mapped to data in the file then the entry will be left empty.\n\t\t\
          -go <output directory> (perform GO analysis using genes near peaks)\n\t\t\
          -genomeOntology <output directory> (perform genomeOntology analysis on peaks)\n\
          \t\t\t-gsize <#> (Genome size for genomeOntology analysis, default: 2e9)\n\
          \n\tAnnotation vs. Histogram mode:\n\t\t-hist <bin size in bp> (i.e 1, 2,\
          \ 5, 10, 20, 50, 100 etc.)\n\t\tThe -hist option can be used to generate\
          \ histograms of position dependent features relative\n\t\tto the center\
          \ of peaks.  This is primarily meant to be used with -d and -m options to\
          \ map\n\t\tdistribution of motifs and ChIP-Seq tags.  For ChIP-Seq peaks\
          \ for a Transcription factor\n\t\tyou might want to use the -center option\
          \ (below) to center peaks on the known motif\n\t\t** If using \"-size given\"\
          , histogram will be scaled to each region (i.e. 0-100%), with\n\t\tthe -hist\
          \ parameter being the number of bins to divide each region into.\n\t\t\t\
          Histogram Mode specific Options:\n\t\t\t-nuc (calculated mononucleotide\
          \ frequencies at each position,\n\t\t\t\tWill report by default if extracting\
          \ sequence for other purposes like motifs)\n\t\t\t-di (calculated dinucleotide\
          \ frequencies at each position)\n\t\t\t-histNorm <#> (normalize the total\
          \ tag count for each region to 1, where <#> is the\n\t\t\t\tminimum tag\
          \ total per region - use to avoid tag spikes from low coverage\n\t\t\t-ghist\
          \ (outputs profiles for each gene, for peak shape clustering)\n\t\t\t-rm\
          \ <#> (remove occurrences of same motif that occur within # bp)\n\n\tPeak\
          \ Centering: (other options are ignored)\n\t\t-center <motif file> (This\
          \ will re-center peaks on the specified motif, or remove peak\n\t\t\tif\
          \ there is no motif in the peak.  ONLY recentering will be performed, and\
          \ all other\n\t\t\toptions will be ignored.  This will output a new peak\
          \ file that can then be reanalyzed\n\t\t\tto reveal fine-grain structure\
          \ in peaks (It is advised to use -size < 200) with this\n\t\t\tto keep peaks\
          \ from moving too far (-mirror flips the position)\n\t\t-multi (returns\
          \ genomic positions of all sites instead of just the closest to center)\n\
          \n\tGenome comparisons (need genome & liftOver)\n\t\t-cmpGenome <genome1>\
          \ [genome2] (Genomes to compare for sequence/motifs)\n\t\t-cmpLiftover <liftover1>\
          \ [genome2] (Genomes to compare for sequence/motifs)\n\n\tNormalization\
          \ options:\n\t\t-fpkm (normalize read counts to million reads or fragments\
          \ per kilobase mapped)\n\t\t-raw (do not adjust the tag counts based on\
          \ total tags sequenced, -noadj works too)\n\t\t-norm <#> (normalize tags\
          \ to this tag count, default=1e7, 0=average tag count in all directories)\n\
          \t\t-normLength <#> (Fragment length to normlize to for experiments with\
          \ different lens, def: 100)\n\t\t-log (output tag counts as log2(x+1+rand)\
          \ values - for scatter plots)\n\t\t-sqrt (output tag counts as sqrt(x+rand)\
          \ values - for scatter plots)\n\t\t-ratio (process tag values as ratios\
          \ - i.e. chip-seq, or mCpG/CpG)\n\n\tAdvanced normalization options: (-rlog\
          \ and -vst require R and DESeq2 to be installed)\n\t\t-rlog (quantile/variance\
          \ normalization on reported genes using DESeq2 rlog funcition, needs R)\n\
          \t\t-vst (quantile/variance normalization on reported genes using DESeq2\
          \ vst function, needs R)\n\n\tAdvanced Options:\n\t\t-len <#> / -fragLength\
          \ <#> (Fragment length, default=auto, might want to set to 1 for 5'RNA)\n\
          \t\t-size <#> (Peak size[from center of peak], default=inferred from peak\
          \ file)\n\t\t\t-size #,# (i.e. -size -10,50 count tags from -10 bp to +50\
          \ bp from center)\n\t\t\t-size \"given\" (count tags etc. using the actual\
          \ regions - for variable length regions)\n\t\t-strand <+|-|both> (Count\
          \ tags on specific strands relative to peak, default: both)\n\t\t-pc <#>\
          \ (maximum number of tags to count per bp, default=0 [no maximum], -tbp\
          \ <#> works too)\n\t\t-CpG (Calculate CpG/GC content)\n\t\t-nfr (report\
          \ nuclesome free region scores instead of tag counts, also -nfrSize <#>)\n\
          \t\t-norevopp (do not search for motifs on the opposite strand [works with\
          \ -center too])\n\t\t-gwasCatalog <gwasCatalog file from UCSC> (list overlapping\
          \ GWAS risk SNPs)\n\t\t-pdist (only report distance to nearest peak using\
          \ -p, not peak name)\n\t\t-map <mapping file> (mapping between peak IDs\
          \ and promoter IDs, overrides closest assignment)\n\t\t-noann, -nogene (skip\
          \ genome annotation step, skip TSS annotation)\n\t\t-homer1/-homer2 (by\
          \ default, the new version of homer [-homer2] is used for finding motifs)\n\
          \t\t-cpu <#> (Number of processors to use when possible - only some parts\
          \ utilize multiple cores)\n\t\t-noblanks (remove peaks/rows with missing\
          \ data)\n\nCould not open --help//tagInfo.txt\n"
        generated_using: *id004
      subcommands: []
      usage: []
      help_flag:
      usage_flag:
      version_flag:
      help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\
        \t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\t\
        \t-rna <RNAseq tag directory> (*see note below)\n\t\t-genome <genome> (HOMER\
        \ genome identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation,\
        \ by default will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold\
        \ <#> (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t-size\
        \ <#> (size of regions to check for significant TSS clusters, default: 150)\n\
        \t\t-includeSingleExons (include single exon genes for TP/FP calculations,\
        \ default: only use multiexon genes)\n\t\t-metagene (Include a metagene plot\
        \ of read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\tEnrichment\
        \ threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2 fold enrichment\
        \ vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold <#> (maximum\
        \ log2 fold enrichment vs. input or RNA, default: 30000000000)\n\t\t-minDistDiff\
        \ <#> (minimum CDF difference to consider for TP/FP calculation, default:\
        \ 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP threshold calculation\
        \ failes, default: 1)\n\t\t-pseudoCount <#> (number of normalizaed reads added\
        \ to input/target read counts, default: 1.0)\n\t\t-ntagThreshold <#> (minimum\
        \ number of norm. tags per TSS cluster, default: 7)\n\t\t-L <#> (local fold\
        \ enrichment used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
        \ (only use RNAseq for stability calculations, not for filtering putative\
        \ TSS)\n\n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check for significant\
        \ TSS clusters, default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to\
        \ calculate RNA/input read density,default: -100,500)\n\t\t-antisenseRegion\
        \ <#>,<#> (Size of region to calculate RNA/input read density,default: -500,100)\n\
        \t\t-rnaRegion <#>,<#> (Size of region to calculate RNA coverage for FP identification,default:\
        \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum\
        \ RNAseq read density for stable transcript, def: 2)\n\t\t-biThreshold <#>\
        \ (minimum divergent csRNAseq signal for bi/divergent TSS, def: 2)\n\t\t-dist\
        \ <#> (minimum distance from annotated TSS to assign 'distal' annotation,\
        \ def: 500)\n\n\t* Tag Directories are assumed to represent the 5' ends of\
        \ RNA in sense orientation\n\n"
      generated_using: *id004
    - !Command
      command: *id005
      positional: []
      named:
      - !Flag
        description: (*see note below)
        synonyms:
        - -o
        - -i
        args: !SimpleFlagArg
          name: prefix
        optional: true
      - !Flag
        description: (*see note below)
        synonyms:
        - -rna
        args: !SimpleFlagArg
          name: RNAseq tag directory
        optional: true
      - !Flag
        description: (HOMER genome identifier or FASTA file)
        synonyms:
        - -genome
        args: !SimpleFlagArg
          name: genome
        optional: true
      - !Flag
        description: (gene annotation, by default will use genome annotation if available)
        synonyms:
        - -gtf
        args: !SimpleFlagArg
          name: gtf file
        optional: true
      - !Flag
        description: '<#> (minimum number of norm. tags per TSS cluster, default:
          7)'
        synonyms:
        - -ntagThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (size of regions to check for significant TSS clusters,
          default: 150)'
        synonyms:
        - -size
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '(include single exon genes for TP/FP calculations, default:
          only use multiexon genes)'
        synonyms:
        - -includeSingleExons
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: (Include a metagene plot of read distributions)
        synonyms:
        - -metagene
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: <#> (max CPUs to use)
        synonyms:
        - -cpu
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
          30000000000)'
        synonyms:
        - -maxInputLog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
          30000000000)'
        synonyms:
        - -maxRNALog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum CDF difference to consider for TP/FP calculation,
          default: 0.15)'
        synonyms:
        - -minDistDiff
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (if annotation-based TP/FP threshold calculation failes,
          default: 1)'
        synonyms:
        - -defaultLog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (number of normalizaed reads added to input/target read
          counts, default: 1.0)'
        synonyms:
        - -pseudoCount
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (local fold enrichment used for initial TSS identification,
          default: 2)'
        synonyms:
        - -L
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: (only use RNAseq for stability calculations, not for filtering
          putative TSS)
        synonyms:
        - -noFilterRNA
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
          -100,500)'
        synonyms:
        - -senseRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
          -500,100)'
        synonyms:
        - -antisenseRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA coverage for FP identification,default:
          -150,-10)'
        synonyms:
        - -rnaRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum RNAseq read density for stable transcript, def:
          2)'
        synonyms:
        - -stableThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum divergent csRNAseq signal for bi/divergent TSS,
          def: 2)'
        synonyms:
        - -biThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: "<#> (minimum distance from annotated TSS to assign 'distal'\
          \ annotation, def: 500)"
        synonyms:
        - -dist
        args: !EmptyFlagArg {}
        optional: true
      parent: *id011
      subcommands: []
      usage: []
      help_flag:
      usage_flag:
      version_flag:
      help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\
        \t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\t\
        \t-rna <RNAseq tag directory> (*see note below)\n\t\t-genome <genome> (HOMER\
        \ genome identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation,\
        \ by default will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold\
        \ <#> (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t-size\
        \ <#> (size of regions to check for significant TSS clusters, default: 150)\n\
        \t\t-includeSingleExons (include single exon genes for TP/FP calculations,\
        \ default: only use multiexon genes)\n\t\t-metagene (Include a metagene plot\
        \ of read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\tEnrichment\
        \ threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2 fold enrichment\
        \ vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold <#> (maximum\
        \ log2 fold enrichment vs. input or RNA, default: 30000000000)\n\t\t-minDistDiff\
        \ <#> (minimum CDF difference to consider for TP/FP calculation, default:\
        \ 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP threshold calculation\
        \ failes, default: 1)\n\t\t-pseudoCount <#> (number of normalizaed reads added\
        \ to input/target read counts, default: 1.0)\n\t\t-ntagThreshold <#> (minimum\
        \ number of norm. tags per TSS cluster, default: 7)\n\t\t-L <#> (local fold\
        \ enrichment used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
        \ (only use RNAseq for stability calculations, not for filtering putative\
        \ TSS)\n\n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check for significant\
        \ TSS clusters, default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to\
        \ calculate RNA/input read density,default: -100,500)\n\t\t-antisenseRegion\
        \ <#>,<#> (Size of region to calculate RNA/input read density,default: -500,100)\n\
        \t\t-rnaRegion <#>,<#> (Size of region to calculate RNA coverage for FP identification,default:\
        \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum\
        \ RNAseq read density for stable transcript, def: 2)\n\t\t-biThreshold <#>\
        \ (minimum divergent csRNAseq signal for bi/divergent TSS, def: 2)\n\t\t-dist\
        \ <#> (minimum distance from annotated TSS to assign 'distal' annotation,\
        \ def: 500)\n\n\t* Tag Directories are assumed to represent the 5' ends of\
        \ RNA in sense orientation\n\n"
      generated_using: *id004
    - !Command
      command: *id006
      positional: []
      named:
      - !Flag
        description: (*see note below)
        synonyms:
        - -o
        - -i
        args: !SimpleFlagArg
          name: prefix
        optional: true
      - !Flag
        description: (*see note below)
        synonyms:
        - -rna
        args: !SimpleFlagArg
          name: RNAseq tag directory
        optional: true
      - !Flag
        description: (HOMER genome identifier or FASTA file)
        synonyms:
        - -genome
        args: !SimpleFlagArg
          name: genome
        optional: true
      - !Flag
        description: (gene annotation, by default will use genome annotation if available)
        synonyms:
        - -gtf
        args: !SimpleFlagArg
          name: gtf file
        optional: true
      - !Flag
        description: '<#> (minimum number of norm. tags per TSS cluster, default:
          7)'
        synonyms:
        - -ntagThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (size of regions to check for significant TSS clusters,
          default: 150)'
        synonyms:
        - -size
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '(include single exon genes for TP/FP calculations, default:
          only use multiexon genes)'
        synonyms:
        - -includeSingleExons
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: (Include a metagene plot of read distributions)
        synonyms:
        - -metagene
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: <#> (max CPUs to use)
        synonyms:
        - -cpu
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
          30000000000)'
        synonyms:
        - -maxInputLog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
          30000000000)'
        synonyms:
        - -maxRNALog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum CDF difference to consider for TP/FP calculation,
          default: 0.15)'
        synonyms:
        - -minDistDiff
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (if annotation-based TP/FP threshold calculation failes,
          default: 1)'
        synonyms:
        - -defaultLog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (number of normalizaed reads added to input/target read
          counts, default: 1.0)'
        synonyms:
        - -pseudoCount
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (local fold enrichment used for initial TSS identification,
          default: 2)'
        synonyms:
        - -L
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: (only use RNAseq for stability calculations, not for filtering
          putative TSS)
        synonyms:
        - -noFilterRNA
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
          -100,500)'
        synonyms:
        - -senseRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
          -500,100)'
        synonyms:
        - -antisenseRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA coverage for FP identification,default:
          -150,-10)'
        synonyms:
        - -rnaRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum RNAseq read density for stable transcript, def:
          2)'
        synonyms:
        - -stableThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum divergent csRNAseq signal for bi/divergent TSS,
          def: 2)'
        synonyms:
        - -biThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: "<#> (minimum distance from annotated TSS to assign 'distal'\
          \ annotation, def: 500)"
        synonyms:
        - -dist
        args: !EmptyFlagArg {}
        optional: true
      parent: *id011
      subcommands: []
      usage: []
      help_flag:
      usage_flag:
      version_flag:
      help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\
        \t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\t\
        \t-rna <RNAseq tag directory> (*see note below)\n\t\t-genome <genome> (HOMER\
        \ genome identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation,\
        \ by default will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold\
        \ <#> (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t-size\
        \ <#> (size of regions to check for significant TSS clusters, default: 150)\n\
        \t\t-includeSingleExons (include single exon genes for TP/FP calculations,\
        \ default: only use multiexon genes)\n\t\t-metagene (Include a metagene plot\
        \ of read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\tEnrichment\
        \ threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2 fold enrichment\
        \ vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold <#> (maximum\
        \ log2 fold enrichment vs. input or RNA, default: 30000000000)\n\t\t-minDistDiff\
        \ <#> (minimum CDF difference to consider for TP/FP calculation, default:\
        \ 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP threshold calculation\
        \ failes, default: 1)\n\t\t-pseudoCount <#> (number of normalizaed reads added\
        \ to input/target read counts, default: 1.0)\n\t\t-ntagThreshold <#> (minimum\
        \ number of norm. tags per TSS cluster, default: 7)\n\t\t-L <#> (local fold\
        \ enrichment used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
        \ (only use RNAseq for stability calculations, not for filtering putative\
        \ TSS)\n\n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check for significant\
        \ TSS clusters, default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to\
        \ calculate RNA/input read density,default: -100,500)\n\t\t-antisenseRegion\
        \ <#>,<#> (Size of region to calculate RNA/input read density,default: -500,100)\n\
        \t\t-rnaRegion <#>,<#> (Size of region to calculate RNA coverage for FP identification,default:\
        \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum\
        \ RNAseq read density for stable transcript, def: 2)\n\t\t-biThreshold <#>\
        \ (minimum divergent csRNAseq signal for bi/divergent TSS, def: 2)\n\t\t-dist\
        \ <#> (minimum distance from annotated TSS to assign 'distal' annotation,\
        \ def: 500)\n\n\t* Tag Directories are assumed to represent the 5' ends of\
        \ RNA in sense orientation\n\n"
      generated_using: *id004
    - !Command
      command: *id007
      positional: []
      named:
      - !Flag
        description: (*see note below)
        synonyms:
        - -o
        - -i
        args: !SimpleFlagArg
          name: prefix
        optional: true
      - !Flag
        description: (*see note below)
        synonyms:
        - -rna
        args: !SimpleFlagArg
          name: RNAseq tag directory
        optional: true
      - !Flag
        description: (HOMER genome identifier or FASTA file)
        synonyms:
        - -genome
        args: !SimpleFlagArg
          name: genome
        optional: true
      - !Flag
        description: (gene annotation, by default will use genome annotation if available)
        synonyms:
        - -gtf
        args: !SimpleFlagArg
          name: gtf file
        optional: true
      - !Flag
        description: '<#> (minimum number of norm. tags per TSS cluster, default:
          7)'
        synonyms:
        - -ntagThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (size of regions to check for significant TSS clusters,
          default: 150)'
        synonyms:
        - -size
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '(include single exon genes for TP/FP calculations, default:
          only use multiexon genes)'
        synonyms:
        - -includeSingleExons
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: (Include a metagene plot of read distributions)
        synonyms:
        - -metagene
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: <#> (max CPUs to use)
        synonyms:
        - -cpu
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
          30000000000)'
        synonyms:
        - -maxInputLog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
          30000000000)'
        synonyms:
        - -maxRNALog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum CDF difference to consider for TP/FP calculation,
          default: 0.15)'
        synonyms:
        - -minDistDiff
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (if annotation-based TP/FP threshold calculation failes,
          default: 1)'
        synonyms:
        - -defaultLog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (number of normalizaed reads added to input/target read
          counts, default: 1.0)'
        synonyms:
        - -pseudoCount
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (local fold enrichment used for initial TSS identification,
          default: 2)'
        synonyms:
        - -L
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: (only use RNAseq for stability calculations, not for filtering
          putative TSS)
        synonyms:
        - -noFilterRNA
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
          -100,500)'
        synonyms:
        - -senseRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
          -500,100)'
        synonyms:
        - -antisenseRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA coverage for FP identification,default:
          -150,-10)'
        synonyms:
        - -rnaRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum RNAseq read density for stable transcript, def:
          2)'
        synonyms:
        - -stableThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum divergent csRNAseq signal for bi/divergent TSS,
          def: 2)'
        synonyms:
        - -biThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: "<#> (minimum distance from annotated TSS to assign 'distal'\
          \ annotation, def: 500)"
        synonyms:
        - -dist
        args: !EmptyFlagArg {}
        optional: true
      parent: *id011
      subcommands: []
      usage: []
      help_flag:
      usage_flag:
      version_flag:
      help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\
        \t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\t\
        \t-rna <RNAseq tag directory> (*see note below)\n\t\t-genome <genome> (HOMER\
        \ genome identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation,\
        \ by default will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold\
        \ <#> (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t-size\
        \ <#> (size of regions to check for significant TSS clusters, default: 150)\n\
        \t\t-includeSingleExons (include single exon genes for TP/FP calculations,\
        \ default: only use multiexon genes)\n\t\t-metagene (Include a metagene plot\
        \ of read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\tEnrichment\
        \ threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2 fold enrichment\
        \ vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold <#> (maximum\
        \ log2 fold enrichment vs. input or RNA, default: 30000000000)\n\t\t-minDistDiff\
        \ <#> (minimum CDF difference to consider for TP/FP calculation, default:\
        \ 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP threshold calculation\
        \ failes, default: 1)\n\t\t-pseudoCount <#> (number of normalizaed reads added\
        \ to input/target read counts, default: 1.0)\n\t\t-ntagThreshold <#> (minimum\
        \ number of norm. tags per TSS cluster, default: 7)\n\t\t-L <#> (local fold\
        \ enrichment used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
        \ (only use RNAseq for stability calculations, not for filtering putative\
        \ TSS)\n\n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check for significant\
        \ TSS clusters, default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to\
        \ calculate RNA/input read density,default: -100,500)\n\t\t-antisenseRegion\
        \ <#>,<#> (Size of region to calculate RNA/input read density,default: -500,100)\n\
        \t\t-rnaRegion <#>,<#> (Size of region to calculate RNA coverage for FP identification,default:\
        \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum\
        \ RNAseq read density for stable transcript, def: 2)\n\t\t-biThreshold <#>\
        \ (minimum divergent csRNAseq signal for bi/divergent TSS, def: 2)\n\t\t-dist\
        \ <#> (minimum distance from annotated TSS to assign 'distal' annotation,\
        \ def: 500)\n\n\t* Tag Directories are assumed to represent the 5' ends of\
        \ RNA in sense orientation\n\n"
      generated_using: *id004
    - !Command
      command: *id008
      positional: []
      named:
      - !Flag
        description: (*see note below)
        synonyms:
        - -o
        - -i
        args: !SimpleFlagArg
          name: prefix
        optional: true
      - !Flag
        description: (*see note below)
        synonyms:
        - -rna
        args: !SimpleFlagArg
          name: RNAseq tag directory
        optional: true
      - !Flag
        description: (HOMER genome identifier or FASTA file)
        synonyms:
        - -genome
        args: !SimpleFlagArg
          name: genome
        optional: true
      - !Flag
        description: (gene annotation, by default will use genome annotation if available)
        synonyms:
        - -gtf
        args: !SimpleFlagArg
          name: gtf file
        optional: true
      - !Flag
        description: '<#> (minimum number of norm. tags per TSS cluster, default:
          7)'
        synonyms:
        - -ntagThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (size of regions to check for significant TSS clusters,
          default: 150)'
        synonyms:
        - -size
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '(include single exon genes for TP/FP calculations, default:
          only use multiexon genes)'
        synonyms:
        - -includeSingleExons
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: (Include a metagene plot of read distributions)
        synonyms:
        - -metagene
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: <#> (max CPUs to use)
        synonyms:
        - -cpu
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
          30000000000)'
        synonyms:
        - -maxInputLog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
          30000000000)'
        synonyms:
        - -maxRNALog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum CDF difference to consider for TP/FP calculation,
          default: 0.15)'
        synonyms:
        - -minDistDiff
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (if annotation-based TP/FP threshold calculation failes,
          default: 1)'
        synonyms:
        - -defaultLog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (number of normalizaed reads added to input/target read
          counts, default: 1.0)'
        synonyms:
        - -pseudoCount
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (local fold enrichment used for initial TSS identification,
          default: 2)'
        synonyms:
        - -L
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: (only use RNAseq for stability calculations, not for filtering
          putative TSS)
        synonyms:
        - -noFilterRNA
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
          -100,500)'
        synonyms:
        - -senseRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
          -500,100)'
        synonyms:
        - -antisenseRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA coverage for FP identification,default:
          -150,-10)'
        synonyms:
        - -rnaRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum RNAseq read density for stable transcript, def:
          2)'
        synonyms:
        - -stableThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum divergent csRNAseq signal for bi/divergent TSS,
          def: 2)'
        synonyms:
        - -biThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: "<#> (minimum distance from annotated TSS to assign 'distal'\
          \ annotation, def: 500)"
        synonyms:
        - -dist
        args: !EmptyFlagArg {}
        optional: true
      parent: *id011
      subcommands: []
      usage: []
      help_flag:
      usage_flag:
      version_flag:
      help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\
        \t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\t\
        \t-rna <RNAseq tag directory> (*see note below)\n\t\t-genome <genome> (HOMER\
        \ genome identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation,\
        \ by default will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold\
        \ <#> (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t-size\
        \ <#> (size of regions to check for significant TSS clusters, default: 150)\n\
        \t\t-includeSingleExons (include single exon genes for TP/FP calculations,\
        \ default: only use multiexon genes)\n\t\t-metagene (Include a metagene plot\
        \ of read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\tEnrichment\
        \ threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2 fold enrichment\
        \ vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold <#> (maximum\
        \ log2 fold enrichment vs. input or RNA, default: 30000000000)\n\t\t-minDistDiff\
        \ <#> (minimum CDF difference to consider for TP/FP calculation, default:\
        \ 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP threshold calculation\
        \ failes, default: 1)\n\t\t-pseudoCount <#> (number of normalizaed reads added\
        \ to input/target read counts, default: 1.0)\n\t\t-ntagThreshold <#> (minimum\
        \ number of norm. tags per TSS cluster, default: 7)\n\t\t-L <#> (local fold\
        \ enrichment used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
        \ (only use RNAseq for stability calculations, not for filtering putative\
        \ TSS)\n\n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check for significant\
        \ TSS clusters, default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to\
        \ calculate RNA/input read density,default: -100,500)\n\t\t-antisenseRegion\
        \ <#>,<#> (Size of region to calculate RNA/input read density,default: -500,100)\n\
        \t\t-rnaRegion <#>,<#> (Size of region to calculate RNA coverage for FP identification,default:\
        \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum\
        \ RNAseq read density for stable transcript, def: 2)\n\t\t-biThreshold <#>\
        \ (minimum divergent csRNAseq signal for bi/divergent TSS, def: 2)\n\t\t-dist\
        \ <#> (minimum distance from annotated TSS to assign 'distal' annotation,\
        \ def: 500)\n\n\t* Tag Directories are assumed to represent the 5' ends of\
        \ RNA in sense orientation\n\n"
      generated_using: *id004
    - !Command
      command: *id009
      positional: []
      named:
      - !Flag
        description: (*see note below)
        synonyms:
        - -o
        - -i
        args: !SimpleFlagArg
          name: prefix
        optional: true
      - !Flag
        description: (*see note below)
        synonyms:
        - -rna
        args: !SimpleFlagArg
          name: RNAseq tag directory
        optional: true
      - !Flag
        description: (HOMER genome identifier or FASTA file)
        synonyms:
        - -genome
        args: !SimpleFlagArg
          name: genome
        optional: true
      - !Flag
        description: (gene annotation, by default will use genome annotation if available)
        synonyms:
        - -gtf
        args: !SimpleFlagArg
          name: gtf file
        optional: true
      - !Flag
        description: '<#> (minimum number of norm. tags per TSS cluster, default:
          7)'
        synonyms:
        - -ntagThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (size of regions to check for significant TSS clusters,
          default: 150)'
        synonyms:
        - -size
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '(include single exon genes for TP/FP calculations, default:
          only use multiexon genes)'
        synonyms:
        - -includeSingleExons
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: (Include a metagene plot of read distributions)
        synonyms:
        - -metagene
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: <#> (max CPUs to use)
        synonyms:
        - -cpu
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
          30000000000)'
        synonyms:
        - -maxInputLog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
          30000000000)'
        synonyms:
        - -maxRNALog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum CDF difference to consider for TP/FP calculation,
          default: 0.15)'
        synonyms:
        - -minDistDiff
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (if annotation-based TP/FP threshold calculation failes,
          default: 1)'
        synonyms:
        - -defaultLog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (number of normalizaed reads added to input/target read
          counts, default: 1.0)'
        synonyms:
        - -pseudoCount
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (local fold enrichment used for initial TSS identification,
          default: 2)'
        synonyms:
        - -L
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: (only use RNAseq for stability calculations, not for filtering
          putative TSS)
        synonyms:
        - -noFilterRNA
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
          -100,500)'
        synonyms:
        - -senseRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
          -500,100)'
        synonyms:
        - -antisenseRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA coverage for FP identification,default:
          -150,-10)'
        synonyms:
        - -rnaRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum RNAseq read density for stable transcript, def:
          2)'
        synonyms:
        - -stableThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum divergent csRNAseq signal for bi/divergent TSS,
          def: 2)'
        synonyms:
        - -biThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: "<#> (minimum distance from annotated TSS to assign 'distal'\
          \ annotation, def: 500)"
        synonyms:
        - -dist
        args: !EmptyFlagArg {}
        optional: true
      parent: *id011
      subcommands: []
      usage: []
      help_flag:
      usage_flag:
      version_flag:
      help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\
        \t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\t\
        \t-rna <RNAseq tag directory> (*see note below)\n\t\t-genome <genome> (HOMER\
        \ genome identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation,\
        \ by default will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold\
        \ <#> (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t-size\
        \ <#> (size of regions to check for significant TSS clusters, default: 150)\n\
        \t\t-includeSingleExons (include single exon genes for TP/FP calculations,\
        \ default: only use multiexon genes)\n\t\t-metagene (Include a metagene plot\
        \ of read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\tEnrichment\
        \ threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2 fold enrichment\
        \ vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold <#> (maximum\
        \ log2 fold enrichment vs. input or RNA, default: 30000000000)\n\t\t-minDistDiff\
        \ <#> (minimum CDF difference to consider for TP/FP calculation, default:\
        \ 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP threshold calculation\
        \ failes, default: 1)\n\t\t-pseudoCount <#> (number of normalizaed reads added\
        \ to input/target read counts, default: 1.0)\n\t\t-ntagThreshold <#> (minimum\
        \ number of norm. tags per TSS cluster, default: 7)\n\t\t-L <#> (local fold\
        \ enrichment used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
        \ (only use RNAseq for stability calculations, not for filtering putative\
        \ TSS)\n\n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check for significant\
        \ TSS clusters, default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to\
        \ calculate RNA/input read density,default: -100,500)\n\t\t-antisenseRegion\
        \ <#>,<#> (Size of region to calculate RNA/input read density,default: -500,100)\n\
        \t\t-rnaRegion <#>,<#> (Size of region to calculate RNA coverage for FP identification,default:\
        \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum\
        \ RNAseq read density for stable transcript, def: 2)\n\t\t-biThreshold <#>\
        \ (minimum divergent csRNAseq signal for bi/divergent TSS, def: 2)\n\t\t-dist\
        \ <#> (minimum distance from annotated TSS to assign 'distal' annotation,\
        \ def: 500)\n\n\t* Tag Directories are assumed to represent the 5' ends of\
        \ RNA in sense orientation\n\n"
      generated_using: *id004
    - !Command
      command: *id010
      positional: []
      named:
      - !Flag
        description: (*see note below)
        synonyms:
        - -o
        - -i
        args: !SimpleFlagArg
          name: prefix
        optional: true
      - !Flag
        description: (*see note below)
        synonyms:
        - -rna
        args: !SimpleFlagArg
          name: RNAseq tag directory
        optional: true
      - !Flag
        description: (HOMER genome identifier or FASTA file)
        synonyms:
        - -genome
        args: !SimpleFlagArg
          name: genome
        optional: true
      - !Flag
        description: (gene annotation, by default will use genome annotation if available)
        synonyms:
        - -gtf
        args: !SimpleFlagArg
          name: gtf file
        optional: true
      - !Flag
        description: '<#> (minimum number of norm. tags per TSS cluster, default:
          7)'
        synonyms:
        - -ntagThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (size of regions to check for significant TSS clusters,
          default: 150)'
        synonyms:
        - -size
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '(include single exon genes for TP/FP calculations, default:
          only use multiexon genes)'
        synonyms:
        - -includeSingleExons
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: (Include a metagene plot of read distributions)
        synonyms:
        - -metagene
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: <#> (max CPUs to use)
        synonyms:
        - -cpu
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
          30000000000)'
        synonyms:
        - -maxInputLog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (maximum log2 fold enrichment vs. input or RNA, default:
          30000000000)'
        synonyms:
        - -maxRNALog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum CDF difference to consider for TP/FP calculation,
          default: 0.15)'
        synonyms:
        - -minDistDiff
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (if annotation-based TP/FP threshold calculation failes,
          default: 1)'
        synonyms:
        - -defaultLog2Fold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (number of normalizaed reads added to input/target read
          counts, default: 1.0)'
        synonyms:
        - -pseudoCount
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (local fold enrichment used for initial TSS identification,
          default: 2)'
        synonyms:
        - -L
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: (only use RNAseq for stability calculations, not for filtering
          putative TSS)
        synonyms:
        - -noFilterRNA
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
          -100,500)'
        synonyms:
        - -senseRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
          -500,100)'
        synonyms:
        - -antisenseRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#>,<#> (Size of region to calculate RNA coverage for FP identification,default:
          -150,-10)'
        synonyms:
        - -rnaRegion
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum RNAseq read density for stable transcript, def:
          2)'
        synonyms:
        - -stableThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: '<#> (minimum divergent csRNAseq signal for bi/divergent TSS,
          def: 2)'
        synonyms:
        - -biThreshold
        args: !EmptyFlagArg {}
        optional: true
      - !Flag
        description: "<#> (minimum distance from annotated TSS to assign 'distal'\
          \ annotation, def: 500)"
        synonyms:
        - -dist
        args: !EmptyFlagArg {}
        optional: true
      parent: *id011
      subcommands: []
      usage: []
      help_flag:
      usage_flag:
      version_flag:
      help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\
        \t\t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\t\
        \t-rna <RNAseq tag directory> (*see note below)\n\t\t-genome <genome> (HOMER\
        \ genome identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation,\
        \ by default will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold\
        \ <#> (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t-size\
        \ <#> (size of regions to check for significant TSS clusters, default: 150)\n\
        \t\t-includeSingleExons (include single exon genes for TP/FP calculations,\
        \ default: only use multiexon genes)\n\t\t-metagene (Include a metagene plot\
        \ of read distributions)\n\t\t-cpu <#> (max CPUs to use)\n\n\t\tEnrichment\
        \ threshold options:\n\t\t-maxInputLog2Fold <#> (maximum log2 fold enrichment\
        \ vs. input or RNA, default: 30000000000)\n\t\t-maxRNALog2Fold <#> (maximum\
        \ log2 fold enrichment vs. input or RNA, default: 30000000000)\n\t\t-minDistDiff\
        \ <#> (minimum CDF difference to consider for TP/FP calculation, default:\
        \ 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP threshold calculation\
        \ failes, default: 1)\n\t\t-pseudoCount <#> (number of normalizaed reads added\
        \ to input/target read counts, default: 1.0)\n\t\t-ntagThreshold <#> (minimum\
        \ number of norm. tags per TSS cluster, default: 7)\n\t\t-L <#> (local fold\
        \ enrichment used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
        \ (only use RNAseq for stability calculations, not for filtering putative\
        \ TSS)\n\n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check for significant\
        \ TSS clusters, default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to\
        \ calculate RNA/input read density,default: -100,500)\n\t\t-antisenseRegion\
        \ <#>,<#> (Size of region to calculate RNA/input read density,default: -500,100)\n\
        \t\t-rnaRegion <#>,<#> (Size of region to calculate RNA coverage for FP identification,default:\
        \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum\
        \ RNAseq read density for stable transcript, def: 2)\n\t\t-biThreshold <#>\
        \ (minimum divergent csRNAseq signal for bi/divergent TSS, def: 2)\n\t\t-dist\
        \ <#> (minimum distance from annotated TSS to assign 'distal' annotation,\
        \ def: 500)\n\n\t* Tag Directories are assumed to represent the 5' ends of\
        \ RNA in sense orientation\n\n"
      generated_using: *id004
    usage: []
    help_flag:
    usage_flag:
    version_flag:
    help_text: "!!! First argument needs to be a <tag directory>\n\n\tUsage: findPeaks\
      \ <tag directory> [options]\n\n\tFinds peaks in the provided tag directory.\
      \  By default, peak list printed to stdout\n\n\tGeneral analysis options:\n\t\
      \t-o <filename|auto> (file name for to output peaks, default: stdout)\n\t\t\t\
      \"-o auto\" will send output to \"<tag directory>/peaks.txt\", \".../regions.txt\"\
      ,\n\t\t\tor \".../transcripts.txt\" depending on the \"-style\" option\n\t\t\
      -style <option> (Specialized options for specific analysis strategies)\n\t\t\
      \tfactor (transcription factor ChIP-Seq, uses -center, output: peaks.txt,  default)\n\
      \t\t\thistone (histone modification ChIP-Seq, region based, uses -region -size\
      \ 500 -L 0, regions.txt)\n\t\t\tgroseq (de novo transcript identification from\
      \ GroSeq data, transcripts.txt)\n\t\t\ttss (TSS identification from 5' RNA sequencing,\
      \ tss.txt)\n\t\t\tdnase (Hypersensitivity [crawford style (nicking)], peaks.txt)\n\
      \t\t\tsuper (Super Enhancers, superEnhancers.txt)\n\t\t\tsuperhistone (Super\
      \ Enhancers from H3K27ac data, superEnhancers.txt)\n\t\t\tmC (Cytosine methylation\
      \ (BS-seq/methylC-seq), regions.txt)\n\t\t\tdamid (DamID enrichment from DpnI\
      \ digestion, regions.txt)\n\t\t\tclip (CLIP-Seq enrichment, strand specific,\
      \ peaks.txt)\n\n\tchipseq/histone options:\n\t\t-i <input tag directory> (Experiment\
      \ to use as IgG/Input/Control)\n\t\t-size <#> (Peak size, default: auto)\n\t\
      \t-minDist <#> (minimum distance between peaks, default: peak size x2)\n\t\t\
      -gsize <#> (Set effective mappable genome size, default: 2e9)\n\t\t-fragLength\
      \ <#|auto> (Approximate fragment length, default: auto)\n\t\t-inputFragLength\
      \ <#|auto> (Approximate fragment length of input tags, default: auto)\n\t\t\
      -tbp <#> (Maximum tags per bp to count, 0 = no limit, default: auto)\n\t\t-inputtbp\
      \ <#> (Maximum tags per bp to count in input, 0 = no limit, default: auto)\n\
      \t\t-strand <both|separate> (find peaks using tags on both strands or separate,\
      \ default:both)\n\t\t-norm # (Tag count to normalize to, default 10000000)\n\
      \t\t-region (extends start/stop coordinates to cover full region considered\
      \ \"enriched\")\n\t\t\t-regionRes <#> (number of fractions peaks are divided\
      \ in when extending 'regions', def: 4)\n\t\t-center (Centers peaks on maximum\
      \ tag overlap and calculates focus ratios)\n\t\t-nfr (Centers peaks on most\
      \ likely nucleosome free region [works best with mnase data])\n\t\t\t(-center\
      \ and -nfr can be performed later with \"getPeakTags\"\n\n\tPeak Filtering options:\
      \ (set -F/-L/-C to 0 to skip)\n\t\t-F <#> (fold enrichment over input tag count,\
      \ default: 4.0)\n\t\t  -P <#> (poisson p-value threshold relative to input tag\
      \ count, default: 0.0001)\n\t\t-L <#> (fold enrichment over local tag count,\
      \ default: 4.0)\n\t\t  -LP <#> (poisson p-value threshold relative to local\
      \ tag count, default: 0.0001)\n\t\t-C <#> (fold enrichment limit of expected\
      \ unique tag positions, default: 2.0)\n\t\t-localSize <#> (region to check for\
      \ local tag enrichment, default: 10000)\n\t\t-inputSize <#> (Size of region\
      \ to search for control tags, default: 2x peak size)\n\t\t-fdr <#> (False discovery\
      \ rate, default = 0.001)\n\t\t-poisson <#> (Set poisson p-value cutoff, default:\
      \ uses fdr)\n\t\t-tagThreshold <#> (Set # of tags to define a peak, default:\
      \ 25)\n\t\t-ntagThreshold <#> (Set # of normalized tags to define a peak, by\
      \ default uses 1e7 for norm)\n\t\t-minTagThreshold <#> (Absolute minimum tags\
      \ per peak, default: expected tags per peak)\n\n\tSuperEnhancer Options: (Need\
      \ to specify \"-style super\"):\n\t\t-superSlope <#> (Slope threshold to identify\
      \ super vs. typical enh., default: 1.00)\n\t\t-superWindow <#> (moving window/number\
      \ of peaks to use to calculate slope, default: 10)\n\t\t-typical <filename>\
      \ (Output typical enhancers to this file, default: not used)\n\t\t-inputPeaks\
      \ <filename> (initial peaks to use for super enhancer merging/scoring)\n\t\t\
      -excludePeaks <filename> (regions to exclude from analysis, i.e. TSS regions\
      \ for H3K27ac)\n\n\tMethylC-Seq/BS-Seq options (Need to specify \"-style mC\"\
      ):\n\t\t-unmethylC / -methylC (find unmethylated/methylated regions, default:\
      \ -unmethyC)\n\t\t-mCthresh <#> (methylation threshold of regions, default:\
      \ avg methylation/2)\n\t\t-minNumC <#> (Minimum number of cytosines per methylation\
      \ peak, default: 6)\n\n\tGroSeq Options (Need to specify \"-style groseq\"):\n\
      \t\t-tssSize <#> (size of region for initiation detection/artifact size, default:\
      \ 250)\n\t\t-minBodySize <#> (size of regoin for transcript body detection,\
      \ default: 1000)\n\t\t-tssFold <#> (fold enrichment for new initiation dectection,\
      \ default: 4.0)\n\t\t-bodyFold <#> (fold enrichment for new transcript dectection,\
      \ default: 4.0)\n\t\t-endFold <#> (end transcript when levels are this much\
      \ less than the start, default: 10.0)\n\t\t-method <fold|level> (method used\
      \ for identifying new transcripts, default: fold)\n\t\t-fragLength <#> (Approximate\
      \ fragment length, default: 150)\n\t\t-uniqmap <directory> (directory of binary\
      \ files specifying uniquely mappable locations)\n\t\t\tDownload from http://biowhat.ucsd.edu/homer/groseq/\n\
      \t\t-confPvalue <#> (confidence p-value: 1.00e-05)\n\t\t-minReadDepth <#> (Minimum\
      \ initial read depth for transcripts, default: auto)\n\t\t-pseudoCount <#> (Pseudo\
      \ tag count, default: 2.0)\n\t\t-rev (reverse strand of reads - for first-strand\
      \ rna-seq/gro-seq)\n\t\t-gtf <filename> (Output de novo transcripts in GTF format)\n\
      \t\t\t\"-o auto\" will produce <dir>/transcripts.txt and <dir>/transcripts.gtf\n\
      \t\t\n\n\tNo genome selected (\"none\") - limits what you can do\n\tPeak file\
      \ = 0.178757123489962.tss.tmp\n\tGenome = none\n\tOrganism = unknown\n\tWill\
      \ count tags on strand: +\n\tFragment Length set to 1\n\tWill use up to 3 CPUs\
      \ in parts that can use them\n\tPeak/BED file conversion summary:\n\t\tBED/Header\
      \ formatted lines: 0\n\t\tpeakfile formatted lines: 0\n\t\tDuplicated Peak IDs:\
      \ 0\n\n\tPeak File Statistics:\n\t\tTotal Peaks: 0\n\t\tRedundant Peak IDs:\
      \ 0\n\t\tPeaks lacking information: 0 (need at least 5 columns per peak)\n\t\
      \tPeaks with misformatted coordinates: 0 (should be integer)\n\t\tPeaks with\
      \ misformatted strand: 0 (should be either +/- or 0/1)\n\n\tOnly 0 peaks found!\
      \ If many more expected, maybe the file format is for Macs only\n\tTry running\
      \ the command: changeNewLine.pl \"0.744680606135265.clean.pos\"\n\n\tReading\
      \ Positions...\n\t-----------------------\n\tFinding Closest TSS...\n\tSkipping\
      \ TSS assignment (can't find file for genome none)\n\t\tCan't find promoterFile\
      \ none/none.tss\n\tCould not find basic annotation file (none/none.annotation)\n\
      \tCould not find full/detailed annotation file (none/none.annotation)\n\tCounting\
      \ Tags in Peaks from each directory...\n\tOrganism: unknown\n\tOutputing Annotation\
      \ File...\n\tDone annotating peaks file\n\n\n\tNo genome selected (\"none\"\
      ) - limits what you can do\n\tPeak file = 0.178757123489962.tss.tmp\n\tGenome\
      \ = none\n\tOrganism = unknown\n\tWill count tags on strand: +\n\tFragment Length\
      \ set to 1\n\tPeak Region set to 150\n\tTag Directories:\n--help not recognized\n\
      \n\n\tUsage: annotatePeaks.pl <peak file | tss> <genome version>  [additional\
      \ options...]\n\n\tAvailable Genomes (required argument): (name,org,directory,default\
      \ promoter set)\n\t\t\t-- or --\n\t\tCustom: provide the path to genome FASTA\
      \ files (directory or single file)\n\t\tIf no genome is available, specify 'none'.\n\
      \t\tIf using FASTA file or none, may want to specify '-organism <...>'\n\n\t\
      User defined annotation files (default is UCSC refGene annotation):\n\t\tannotatePeaks.pl\
      \ accepts GTF (gene transfer formatted) files to annotate positions relative\n\
      \t\tto custom annotations, such as those from de novo transcript discovery or\
      \ Gencode.\n\t\t-gtf <gtf format file> (Use -gff and -gff3 if appropriate, but\
      \ GTF is better)\n\t\t-gid (by default the GTF file is processed by transcript_id,\
      \ use this option for gene_id)\n\t\t-ann <custom homer annotation file> (created\
      \ by assignGenomeAnnotation, see website)\n\n\tPeak vs. tss/tts/rna mode (works\
      \ with custom GTF file):\n\t\tIf the first argument is \"tss\" (i.e. annotatePeaks.pl\
      \ tss hg18 ...) then a TSS centric\n\t\tanalysis will be carried out.  Tag counts\
      \ and motifs will be found relative to the TSS.\n\t\t(no position file needed)\
      \ [\"tts\" now works too - e.g. 3' end of gene]\n\t\t[\"rna\" specifies gene\
      \ bodies, will automaticall set \"-size given\"]\n\t\tNOTE: The default TSS\
      \ peak size is 4000 bp, i.e. +/- 2kb (change with -size option)\n\t\t-list <gene\
      \ id list> (subset of genes to perform analysis [unigene, gene id, accession,\n\
      \t\t\t probe, etc.], default = all promoters)\n\t\t-cTSS <promoter position\
      \ file i.e. peak file> (should be centered on TSS)\n\n\tPrimary Annotation Options:\n\
      \t\t-mask (Masked repeats, can also add 'r' to end of genome name)\n\t\t-m <motif\
      \ file 1> [motif file 2] ... (list of motifs to find in peaks)\n\t\t\t-mscore\
      \ (reports the highest log-odds score within the peak)\n\t\t\t-nmotifs (reports\
      \ the number of motifs per peak)\n\t\t\t-mdist (reports distance to closest\
      \ motif)\n\t\t\t-mfasta <filename> (reports sites in a fasta file - for building\
      \ new motifs)\n\t\t\t-fm <motif file 1> [motif file 2] (list of motifs to filter\
      \ from above)\n\t\t\t-rmrevopp <#> (only count sites found within <#> on both\
      \ strands once, i.e. palindromic)\n\t\t\t-matrix <prefix> (outputs a motif co-occurrence\
      \ files:\n\t\t\t\tprefix.count.matrix.txt - number of peaks with motif co-occurrence\n\
      \t\t\t\tprefix.ratio.matrix.txt - ratio of observed vs. expected  co-occurrence\n\
      \t\t\t\tprefix.logPvalue.matrix.txt - co-occurrence enrichment\n\t\t\t\tprefix.stats.txt\
      \ - table of pair-wise motif co-occurrence statistics\n\t\t\t\tadditional options:\n\
      \t\t\t\t-matrixMinDist <#> (minimum distance between motif pairs - to avoid\
      \ overlap, default: 4)\n\t\t\t\t-matrixMaxDist <#> (maximum distance between\
      \ motif pairs)\n\t\t\t-mbed <filename> (Output motif positions to a BED file\
      \ to load at UCSC (or -mpeak))\n\t\t\t-mlogic <filename> (will output stats\
      \ on common motif orientations)\n\t\t-d <tag directory 1> [tag directory 2]\
      \ ... (list of experiment directories to show\n\t\t\ttag counts for) NOTE: -dfile\
      \ <file> where file is a list of directories in first column\n\t\t-bedGraph\
      \ <bedGraph file 1> [bedGraph file 2] ... (read coverage counts from bedGraph\
      \ files)\n\t\t-wig <wiggle file 1> [wiggle file 2] ... (read coverage counts\
      \ from wiggle files)\n\t\t-p <peak file> [peak file 2] ... (to find nearest\
      \ peaks)\n\t\t\t-pdist to report only distance (-pdist2 gives directional distance)\n\
      \t\t\t-pcount to report number of peaks within region\n\t\t-vcf <VCF file> (annotate\
      \ peaks with genetic variation infomation, one col per individual)\n\t\t\t-editDistance\
      \ (Computes the # bp changes relative to reference)\n\t\t\t-individuals <name1>\
      \ [name2] ... (restrict analysis to these individuals)\n\t\t-gene <data file>\
      \ ... (Adds additional data to result based on the closest gene.\n\t\t\tThis\
      \ is useful for adding gene expression data.  The file must have a header,\n\
      \t\t\tand the first column must be a GeneID, Accession number, etc.  If the\
      \ peak\n\t\t\tcannot be mapped to data in the file then the entry will be left\
      \ empty.\n\t\t-go <output directory> (perform GO analysis using genes near peaks)\n\
      \t\t-genomeOntology <output directory> (perform genomeOntology analysis on peaks)\n\
      \t\t\t-gsize <#> (Genome size for genomeOntology analysis, default: 2e9)\n\n\
      \tAnnotation vs. Histogram mode:\n\t\t-hist <bin size in bp> (i.e 1, 2, 5, 10,\
      \ 20, 50, 100 etc.)\n\t\tThe -hist option can be used to generate histograms\
      \ of position dependent features relative\n\t\tto the center of peaks.  This\
      \ is primarily meant to be used with -d and -m options to map\n\t\tdistribution\
      \ of motifs and ChIP-Seq tags.  For ChIP-Seq peaks for a Transcription factor\n\
      \t\tyou might want to use the -center option (below) to center peaks on the\
      \ known motif\n\t\t** If using \"-size given\", histogram will be scaled to\
      \ each region (i.e. 0-100%), with\n\t\tthe -hist parameter being the number\
      \ of bins to divide each region into.\n\t\t\tHistogram Mode specific Options:\n\
      \t\t\t-nuc (calculated mononucleotide frequencies at each position,\n\t\t\t\t\
      Will report by default if extracting sequence for other purposes like motifs)\n\
      \t\t\t-di (calculated dinucleotide frequencies at each position)\n\t\t\t-histNorm\
      \ <#> (normalize the total tag count for each region to 1, where <#> is the\n\
      \t\t\t\tminimum tag total per region - use to avoid tag spikes from low coverage\n\
      \t\t\t-ghist (outputs profiles for each gene, for peak shape clustering)\n\t\
      \t\t-rm <#> (remove occurrences of same motif that occur within # bp)\n\n\t\
      Peak Centering: (other options are ignored)\n\t\t-center <motif file> (This\
      \ will re-center peaks on the specified motif, or remove peak\n\t\t\tif there\
      \ is no motif in the peak.  ONLY recentering will be performed, and all other\n\
      \t\t\toptions will be ignored.  This will output a new peak file that can then\
      \ be reanalyzed\n\t\t\tto reveal fine-grain structure in peaks (It is advised\
      \ to use -size < 200) with this\n\t\t\tto keep peaks from moving too far (-mirror\
      \ flips the position)\n\t\t-multi (returns genomic positions of all sites instead\
      \ of just the closest to center)\n\n\tGenome comparisons (need genome & liftOver)\n\
      \t\t-cmpGenome <genome1> [genome2] (Genomes to compare for sequence/motifs)\n\
      \t\t-cmpLiftover <liftover1> [genome2] (Genomes to compare for sequence/motifs)\n\
      \n\tNormalization options:\n\t\t-fpkm (normalize read counts to million reads\
      \ or fragments per kilobase mapped)\n\t\t-raw (do not adjust the tag counts\
      \ based on total tags sequenced, -noadj works too)\n\t\t-norm <#> (normalize\
      \ tags to this tag count, default=1e7, 0=average tag count in all directories)\n\
      \t\t-normLength <#> (Fragment length to normlize to for experiments with different\
      \ lens, def: 100)\n\t\t-log (output tag counts as log2(x+1+rand) values - for\
      \ scatter plots)\n\t\t-sqrt (output tag counts as sqrt(x+rand) values - for\
      \ scatter plots)\n\t\t-ratio (process tag values as ratios - i.e. chip-seq,\
      \ or mCpG/CpG)\n\n\tAdvanced normalization options: (-rlog and -vst require\
      \ R and DESeq2 to be installed)\n\t\t-rlog (quantile/variance normalization\
      \ on reported genes using DESeq2 rlog funcition, needs R)\n\t\t-vst (quantile/variance\
      \ normalization on reported genes using DESeq2 vst function, needs R)\n\n\t\
      Advanced Options:\n\t\t-len <#> / -fragLength <#> (Fragment length, default=auto,\
      \ might want to set to 1 for 5'RNA)\n\t\t-size <#> (Peak size[from center of\
      \ peak], default=inferred from peak file)\n\t\t\t-size #,# (i.e. -size -10,50\
      \ count tags from -10 bp to +50 bp from center)\n\t\t\t-size \"given\" (count\
      \ tags etc. using the actual regions - for variable length regions)\n\t\t-strand\
      \ <+|-|both> (Count tags on specific strands relative to peak, default: both)\n\
      \t\t-pc <#> (maximum number of tags to count per bp, default=0 [no maximum],\
      \ -tbp <#> works too)\n\t\t-CpG (Calculate CpG/GC content)\n\t\t-nfr (report\
      \ nuclesome free region scores instead of tag counts, also -nfrSize <#>)\n\t\
      \t-norevopp (do not search for motifs on the opposite strand [works with -center\
      \ too])\n\t\t-gwasCatalog <gwasCatalog file from UCSC> (list overlapping GWAS\
      \ risk SNPs)\n\t\t-pdist (only report distance to nearest peak using -p, not\
      \ peak name)\n\t\t-map <mapping file> (mapping between peak IDs and promoter\
      \ IDs, overrides closest assignment)\n\t\t-noann, -nogene (skip genome annotation\
      \ step, skip TSS annotation)\n\t\t-homer1/-homer2 (by default, the new version\
      \ of homer [-homer2] is used for finding motifs)\n\t\t-cpu <#> (Number of processors\
      \ to use when possible - only some parts utilize multiple cores)\n\t\t-noblanks\
      \ (remove peaks/rows with missing data)\n\n\tPeak/BED file conversion summary:\n\
      \t\tBED/Header formatted lines: 0\n\t\tpeakfile formatted lines: 0\n\t\tDuplicated\
      \ Peak IDs: 0\n\n\tPeak File Statistics:\n\t\tTotal Peaks: 0\n\t\tRedundant\
      \ Peak IDs: 0\n\t\tPeaks lacking information: 0 (need at least 5 columns per\
      \ peak)\n\t\tPeaks with misformatted coordinates: 0 (should be integer)\n\t\t\
      Peaks with misformatted strand: 0 (should be either +/- or 0/1)\n\n\tOnly 0\
      \ peaks found! If many more expected, maybe the file format is for Macs only\n\
      \tTry running the command: changeNewLine.pl \"0.0945234839609874.2.tmp\"\n\n\
      \n\tNo genome selected (\"none\") - limits what you can do\n\tPeak file = 0.178757123489962.tmp\n\
      \tGenome = none\n\tOrganism = unknown\n\tWill count tags on strand: +\n\tFragment\
      \ Length set to 1\n\tTag Directories:\n--help not recognized\n\n\n\tUsage: annotatePeaks.pl\
      \ <peak file | tss> <genome version>  [additional options...]\n\n\tAvailable\
      \ Genomes (required argument): (name,org,directory,default promoter set)\n\t\
      \t\t-- or --\n\t\tCustom: provide the path to genome FASTA files (directory\
      \ or single file)\n\t\tIf no genome is available, specify 'none'.\n\t\tIf using\
      \ FASTA file or none, may want to specify '-organism <...>'\n\n\tUser defined\
      \ annotation files (default is UCSC refGene annotation):\n\t\tannotatePeaks.pl\
      \ accepts GTF (gene transfer formatted) files to annotate positions relative\n\
      \t\tto custom annotations, such as those from de novo transcript discovery or\
      \ Gencode.\n\t\t-gtf <gtf format file> (Use -gff and -gff3 if appropriate, but\
      \ GTF is better)\n\t\t-gid (by default the GTF file is processed by transcript_id,\
      \ use this option for gene_id)\n\t\t-ann <custom homer annotation file> (created\
      \ by assignGenomeAnnotation, see website)\n\n\tPeak vs. tss/tts/rna mode (works\
      \ with custom GTF file):\n\t\tIf the first argument is \"tss\" (i.e. annotatePeaks.pl\
      \ tss hg18 ...) then a TSS centric\n\t\tanalysis will be carried out.  Tag counts\
      \ and motifs will be found relative to the TSS.\n\t\t(no position file needed)\
      \ [\"tts\" now works too - e.g. 3' end of gene]\n\t\t[\"rna\" specifies gene\
      \ bodies, will automaticall set \"-size given\"]\n\t\tNOTE: The default TSS\
      \ peak size is 4000 bp, i.e. +/- 2kb (change with -size option)\n\t\t-list <gene\
      \ id list> (subset of genes to perform analysis [unigene, gene id, accession,\n\
      \t\t\t probe, etc.], default = all promoters)\n\t\t-cTSS <promoter position\
      \ file i.e. peak file> (should be centered on TSS)\n\n\tPrimary Annotation Options:\n\
      \t\t-mask (Masked repeats, can also add 'r' to end of genome name)\n\t\t-m <motif\
      \ file 1> [motif file 2] ... (list of motifs to find in peaks)\n\t\t\t-mscore\
      \ (reports the highest log-odds score within the peak)\n\t\t\t-nmotifs (reports\
      \ the number of motifs per peak)\n\t\t\t-mdist (reports distance to closest\
      \ motif)\n\t\t\t-mfasta <filename> (reports sites in a fasta file - for building\
      \ new motifs)\n\t\t\t-fm <motif file 1> [motif file 2] (list of motifs to filter\
      \ from above)\n\t\t\t-rmrevopp <#> (only count sites found within <#> on both\
      \ strands once, i.e. palindromic)\n\t\t\t-matrix <prefix> (outputs a motif co-occurrence\
      \ files:\n\t\t\t\tprefix.count.matrix.txt - number of peaks with motif co-occurrence\n\
      \t\t\t\tprefix.ratio.matrix.txt - ratio of observed vs. expected  co-occurrence\n\
      \t\t\t\tprefix.logPvalue.matrix.txt - co-occurrence enrichment\n\t\t\t\tprefix.stats.txt\
      \ - table of pair-wise motif co-occurrence statistics\n\t\t\t\tadditional options:\n\
      \t\t\t\t-matrixMinDist <#> (minimum distance between motif pairs - to avoid\
      \ overlap, default: 4)\n\t\t\t\t-matrixMaxDist <#> (maximum distance between\
      \ motif pairs)\n\t\t\t-mbed <filename> (Output motif positions to a BED file\
      \ to load at UCSC (or -mpeak))\n\t\t\t-mlogic <filename> (will output stats\
      \ on common motif orientations)\n\t\t-d <tag directory 1> [tag directory 2]\
      \ ... (list of experiment directories to show\n\t\t\ttag counts for) NOTE: -dfile\
      \ <file> where file is a list of directories in first column\n\t\t-bedGraph\
      \ <bedGraph file 1> [bedGraph file 2] ... (read coverage counts from bedGraph\
      \ files)\n\t\t-wig <wiggle file 1> [wiggle file 2] ... (read coverage counts\
      \ from wiggle files)\n\t\t-p <peak file> [peak file 2] ... (to find nearest\
      \ peaks)\n\t\t\t-pdist to report only distance (-pdist2 gives directional distance)\n\
      \t\t\t-pcount to report number of peaks within region\n\t\t-vcf <VCF file> (annotate\
      \ peaks with genetic variation infomation, one col per individual)\n\t\t\t-editDistance\
      \ (Computes the # bp changes relative to reference)\n\t\t\t-individuals <name1>\
      \ [name2] ... (restrict analysis to these individuals)\n\t\t-gene <data file>\
      \ ... (Adds additional data to result based on the closest gene.\n\t\t\tThis\
      \ is useful for adding gene expression data.  The file must have a header,\n\
      \t\t\tand the first column must be a GeneID, Accession number, etc.  If the\
      \ peak\n\t\t\tcannot be mapped to data in the file then the entry will be left\
      \ empty.\n\t\t-go <output directory> (perform GO analysis using genes near peaks)\n\
      \t\t-genomeOntology <output directory> (perform genomeOntology analysis on peaks)\n\
      \t\t\t-gsize <#> (Genome size for genomeOntology analysis, default: 2e9)\n\n\
      \tAnnotation vs. Histogram mode:\n\t\t-hist <bin size in bp> (i.e 1, 2, 5, 10,\
      \ 20, 50, 100 etc.)\n\t\tThe -hist option can be used to generate histograms\
      \ of position dependent features relative\n\t\tto the center of peaks.  This\
      \ is primarily meant to be used with -d and -m options to map\n\t\tdistribution\
      \ of motifs and ChIP-Seq tags.  For ChIP-Seq peaks for a Transcription factor\n\
      \t\tyou might want to use the -center option (below) to center peaks on the\
      \ known motif\n\t\t** If using \"-size given\", histogram will be scaled to\
      \ each region (i.e. 0-100%), with\n\t\tthe -hist parameter being the number\
      \ of bins to divide each region into.\n\t\t\tHistogram Mode specific Options:\n\
      \t\t\t-nuc (calculated mononucleotide frequencies at each position,\n\t\t\t\t\
      Will report by default if extracting sequence for other purposes like motifs)\n\
      \t\t\t-di (calculated dinucleotide frequencies at each position)\n\t\t\t-histNorm\
      \ <#> (normalize the total tag count for each region to 1, where <#> is the\n\
      \t\t\t\tminimum tag total per region - use to avoid tag spikes from low coverage\n\
      \t\t\t-ghist (outputs profiles for each gene, for peak shape clustering)\n\t\
      \t\t-rm <#> (remove occurrences of same motif that occur within # bp)\n\n\t\
      Peak Centering: (other options are ignored)\n\t\t-center <motif file> (This\
      \ will re-center peaks on the specified motif, or remove peak\n\t\t\tif there\
      \ is no motif in the peak.  ONLY recentering will be performed, and all other\n\
      \t\t\toptions will be ignored.  This will output a new peak file that can then\
      \ be reanalyzed\n\t\t\tto reveal fine-grain structure in peaks (It is advised\
      \ to use -size < 200) with this\n\t\t\tto keep peaks from moving too far (-mirror\
      \ flips the position)\n\t\t-multi (returns genomic positions of all sites instead\
      \ of just the closest to center)\n\n\tGenome comparisons (need genome & liftOver)\n\
      \t\t-cmpGenome <genome1> [genome2] (Genomes to compare for sequence/motifs)\n\
      \t\t-cmpLiftover <liftover1> [genome2] (Genomes to compare for sequence/motifs)\n\
      \n\tNormalization options:\n\t\t-fpkm (normalize read counts to million reads\
      \ or fragments per kilobase mapped)\n\t\t-raw (do not adjust the tag counts\
      \ based on total tags sequenced, -noadj works too)\n\t\t-norm <#> (normalize\
      \ tags to this tag count, default=1e7, 0=average tag count in all directories)\n\
      \t\t-normLength <#> (Fragment length to normlize to for experiments with different\
      \ lens, def: 100)\n\t\t-log (output tag counts as log2(x+1+rand) values - for\
      \ scatter plots)\n\t\t-sqrt (output tag counts as sqrt(x+rand) values - for\
      \ scatter plots)\n\t\t-ratio (process tag values as ratios - i.e. chip-seq,\
      \ or mCpG/CpG)\n\n\tAdvanced normalization options: (-rlog and -vst require\
      \ R and DESeq2 to be installed)\n\t\t-rlog (quantile/variance normalization\
      \ on reported genes using DESeq2 rlog funcition, needs R)\n\t\t-vst (quantile/variance\
      \ normalization on reported genes using DESeq2 vst function, needs R)\n\n\t\
      Advanced Options:\n\t\t-len <#> / -fragLength <#> (Fragment length, default=auto,\
      \ might want to set to 1 for 5'RNA)\n\t\t-size <#> (Peak size[from center of\
      \ peak], default=inferred from peak file)\n\t\t\t-size #,# (i.e. -size -10,50\
      \ count tags from -10 bp to +50 bp from center)\n\t\t\t-size \"given\" (count\
      \ tags etc. using the actual regions - for variable length regions)\n\t\t-strand\
      \ <+|-|both> (Count tags on specific strands relative to peak, default: both)\n\
      \t\t-pc <#> (maximum number of tags to count per bp, default=0 [no maximum],\
      \ -tbp <#> works too)\n\t\t-CpG (Calculate CpG/GC content)\n\t\t-nfr (report\
      \ nuclesome free region scores instead of tag counts, also -nfrSize <#>)\n\t\
      \t-norevopp (do not search for motifs on the opposite strand [works with -center\
      \ too])\n\t\t-gwasCatalog <gwasCatalog file from UCSC> (list overlapping GWAS\
      \ risk SNPs)\n\t\t-pdist (only report distance to nearest peak using -p, not\
      \ peak name)\n\t\t-map <mapping file> (mapping between peak IDs and promoter\
      \ IDs, overrides closest assignment)\n\t\t-noann, -nogene (skip genome annotation\
      \ step, skip TSS annotation)\n\t\t-homer1/-homer2 (by default, the new version\
      \ of homer [-homer2] is used for finding motifs)\n\t\t-cpu <#> (Number of processors\
      \ to use when possible - only some parts utilize multiple cores)\n\t\t-noblanks\
      \ (remove peaks/rows with missing data)\n\nCould not open --help//tagInfo.txt\n"
    generated_using: *id004
  subcommands: []
  usage: []
  help_flag:
  usage_flag:
  version_flag:
  help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\t\
    \t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\t\t-rna\
    \ <RNAseq tag directory> (*see note below)\n\t\t-genome <genome> (HOMER genome\
    \ identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation, by default\
    \ will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold <#> (minimum\
    \ number of norm. tags per TSS cluster, default: 7)\n\t\t-size <#> (size of regions\
    \ to check for significant TSS clusters, default: 150)\n\t\t-includeSingleExons\
    \ (include single exon genes for TP/FP calculations, default: only use multiexon\
    \ genes)\n\t\t-metagene (Include a metagene plot of read distributions)\n\t\t\
    -cpu <#> (max CPUs to use)\n\n\t\tEnrichment threshold options:\n\t\t-maxInputLog2Fold\
    \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\t\
    \t-maxRNALog2Fold <#> (maximum log2 fold enrichment vs. input or RNA, default:\
    \ 30000000000)\n\t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP\
    \ calculation, default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP\
    \ threshold calculation failes, default: 1)\n\t\t-pseudoCount <#> (number of normalizaed\
    \ reads added to input/target read counts, default: 1.0)\n\t\t-ntagThreshold <#>\
    \ (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t-L <#> (local\
    \ fold enrichment used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
    \ (only use RNAseq for stability calculations, not for filtering putative TSS)\n\
    \n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check for significant TSS\
    \ clusters, default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to calculate\
    \ RNA/input read density,default: -100,500)\n\t\t-antisenseRegion <#>,<#> (Size\
    \ of region to calculate RNA/input read density,default: -500,100)\n\t\t-rnaRegion\
    \ <#>,<#> (Size of region to calculate RNA coverage for FP identification,default:\
    \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum RNAseq\
    \ read density for stable transcript, def: 2)\n\t\t-biThreshold <#> (minimum divergent\
    \ csRNAseq signal for bi/divergent TSS, def: 2)\n\t\t-dist <#> (minimum distance\
    \ from annotated TSS to assign 'distal' annotation, def: 500)\n\n\t* Tag Directories\
    \ are assumed to represent the 5' ends of RNA in sense orientation\n\n"
  generated_using: *id004
- !Command
  command: *id005
  positional: []
  named:
  - !Flag
    description: (*see note below)
    synonyms:
    - -o
    - -i
    args: !SimpleFlagArg
      name: prefix
    optional: true
  - !Flag
    description: (*see note below)
    synonyms:
    - -rna
    args: !SimpleFlagArg
      name: RNAseq tag directory
    optional: true
  - !Flag
    description: (HOMER genome identifier or FASTA file)
    synonyms:
    - -genome
    args: !SimpleFlagArg
      name: genome
    optional: true
  - !Flag
    description: (gene annotation, by default will use genome annotation if available)
    synonyms:
    - -gtf
    args: !SimpleFlagArg
      name: gtf file
    optional: true
  - !Flag
    description: '<#> (minimum number of norm. tags per TSS cluster, default: 7)'
    synonyms:
    - -ntagThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (size of regions to check for significant TSS clusters, default:
      150)'
    synonyms:
    - -size
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '(include single exon genes for TP/FP calculations, default: only
      use multiexon genes)'
    synonyms:
    - -includeSingleExons
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: (Include a metagene plot of read distributions)
    synonyms:
    - -metagene
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: <#> (max CPUs to use)
    synonyms:
    - -cpu
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)'
    synonyms:
    - -maxInputLog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)'
    synonyms:
    - -maxRNALog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum CDF difference to consider for TP/FP calculation, default:
      0.15)'
    synonyms:
    - -minDistDiff
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (if annotation-based TP/FP threshold calculation failes, default:
      1)'
    synonyms:
    - -defaultLog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (number of normalizaed reads added to input/target read counts,
      default: 1.0)'
    synonyms:
    - -pseudoCount
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (local fold enrichment used for initial TSS identification,
      default: 2)'
    synonyms:
    - -L
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: (only use RNAseq for stability calculations, not for filtering putative
      TSS)
    synonyms:
    - -noFilterRNA
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
      -100,500)'
    synonyms:
    - -senseRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
      -500,100)'
    synonyms:
    - -antisenseRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA coverage for FP identification,default:
      -150,-10)'
    synonyms:
    - -rnaRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum RNAseq read density for stable transcript, def: 2)'
    synonyms:
    - -stableThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum divergent csRNAseq signal for bi/divergent TSS, def:
      2)'
    synonyms:
    - -biThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: "<#> (minimum distance from annotated TSS to assign 'distal' annotation,\
      \ def: 500)"
    synonyms:
    - -dist
    args: !EmptyFlagArg {}
    optional: true
  parent: *id012
  subcommands: []
  usage: []
  help_flag:
  usage_flag:
  version_flag:
  help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\t\
    \t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\t\t-rna\
    \ <RNAseq tag directory> (*see note below)\n\t\t-genome <genome> (HOMER genome\
    \ identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation, by default\
    \ will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold <#> (minimum\
    \ number of norm. tags per TSS cluster, default: 7)\n\t\t-size <#> (size of regions\
    \ to check for significant TSS clusters, default: 150)\n\t\t-includeSingleExons\
    \ (include single exon genes for TP/FP calculations, default: only use multiexon\
    \ genes)\n\t\t-metagene (Include a metagene plot of read distributions)\n\t\t\
    -cpu <#> (max CPUs to use)\n\n\t\tEnrichment threshold options:\n\t\t-maxInputLog2Fold\
    \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\t\
    \t-maxRNALog2Fold <#> (maximum log2 fold enrichment vs. input or RNA, default:\
    \ 30000000000)\n\t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP\
    \ calculation, default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP\
    \ threshold calculation failes, default: 1)\n\t\t-pseudoCount <#> (number of normalizaed\
    \ reads added to input/target read counts, default: 1.0)\n\t\t-ntagThreshold <#>\
    \ (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t-L <#> (local\
    \ fold enrichment used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
    \ (only use RNAseq for stability calculations, not for filtering putative TSS)\n\
    \n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check for significant TSS\
    \ clusters, default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to calculate\
    \ RNA/input read density,default: -100,500)\n\t\t-antisenseRegion <#>,<#> (Size\
    \ of region to calculate RNA/input read density,default: -500,100)\n\t\t-rnaRegion\
    \ <#>,<#> (Size of region to calculate RNA coverage for FP identification,default:\
    \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum RNAseq\
    \ read density for stable transcript, def: 2)\n\t\t-biThreshold <#> (minimum divergent\
    \ csRNAseq signal for bi/divergent TSS, def: 2)\n\t\t-dist <#> (minimum distance\
    \ from annotated TSS to assign 'distal' annotation, def: 500)\n\n\t* Tag Directories\
    \ are assumed to represent the 5' ends of RNA in sense orientation\n\n"
  generated_using: *id004
- !Command
  command: *id006
  positional: []
  named:
  - !Flag
    description: (*see note below)
    synonyms:
    - -o
    - -i
    args: !SimpleFlagArg
      name: prefix
    optional: true
  - !Flag
    description: (*see note below)
    synonyms:
    - -rna
    args: !SimpleFlagArg
      name: RNAseq tag directory
    optional: true
  - !Flag
    description: (HOMER genome identifier or FASTA file)
    synonyms:
    - -genome
    args: !SimpleFlagArg
      name: genome
    optional: true
  - !Flag
    description: (gene annotation, by default will use genome annotation if available)
    synonyms:
    - -gtf
    args: !SimpleFlagArg
      name: gtf file
    optional: true
  - !Flag
    description: '<#> (minimum number of norm. tags per TSS cluster, default: 7)'
    synonyms:
    - -ntagThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (size of regions to check for significant TSS clusters, default:
      150)'
    synonyms:
    - -size
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '(include single exon genes for TP/FP calculations, default: only
      use multiexon genes)'
    synonyms:
    - -includeSingleExons
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: (Include a metagene plot of read distributions)
    synonyms:
    - -metagene
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: <#> (max CPUs to use)
    synonyms:
    - -cpu
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)'
    synonyms:
    - -maxInputLog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)'
    synonyms:
    - -maxRNALog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum CDF difference to consider for TP/FP calculation, default:
      0.15)'
    synonyms:
    - -minDistDiff
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (if annotation-based TP/FP threshold calculation failes, default:
      1)'
    synonyms:
    - -defaultLog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (number of normalizaed reads added to input/target read counts,
      default: 1.0)'
    synonyms:
    - -pseudoCount
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (local fold enrichment used for initial TSS identification,
      default: 2)'
    synonyms:
    - -L
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: (only use RNAseq for stability calculations, not for filtering putative
      TSS)
    synonyms:
    - -noFilterRNA
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
      -100,500)'
    synonyms:
    - -senseRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
      -500,100)'
    synonyms:
    - -antisenseRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA coverage for FP identification,default:
      -150,-10)'
    synonyms:
    - -rnaRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum RNAseq read density for stable transcript, def: 2)'
    synonyms:
    - -stableThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum divergent csRNAseq signal for bi/divergent TSS, def:
      2)'
    synonyms:
    - -biThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: "<#> (minimum distance from annotated TSS to assign 'distal' annotation,\
      \ def: 500)"
    synonyms:
    - -dist
    args: !EmptyFlagArg {}
    optional: true
  parent: *id012
  subcommands: []
  usage: []
  help_flag:
  usage_flag:
  version_flag:
  help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\t\
    \t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\t\t-rna\
    \ <RNAseq tag directory> (*see note below)\n\t\t-genome <genome> (HOMER genome\
    \ identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation, by default\
    \ will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold <#> (minimum\
    \ number of norm. tags per TSS cluster, default: 7)\n\t\t-size <#> (size of regions\
    \ to check for significant TSS clusters, default: 150)\n\t\t-includeSingleExons\
    \ (include single exon genes for TP/FP calculations, default: only use multiexon\
    \ genes)\n\t\t-metagene (Include a metagene plot of read distributions)\n\t\t\
    -cpu <#> (max CPUs to use)\n\n\t\tEnrichment threshold options:\n\t\t-maxInputLog2Fold\
    \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\t\
    \t-maxRNALog2Fold <#> (maximum log2 fold enrichment vs. input or RNA, default:\
    \ 30000000000)\n\t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP\
    \ calculation, default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP\
    \ threshold calculation failes, default: 1)\n\t\t-pseudoCount <#> (number of normalizaed\
    \ reads added to input/target read counts, default: 1.0)\n\t\t-ntagThreshold <#>\
    \ (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t-L <#> (local\
    \ fold enrichment used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
    \ (only use RNAseq for stability calculations, not for filtering putative TSS)\n\
    \n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check for significant TSS\
    \ clusters, default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to calculate\
    \ RNA/input read density,default: -100,500)\n\t\t-antisenseRegion <#>,<#> (Size\
    \ of region to calculate RNA/input read density,default: -500,100)\n\t\t-rnaRegion\
    \ <#>,<#> (Size of region to calculate RNA coverage for FP identification,default:\
    \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum RNAseq\
    \ read density for stable transcript, def: 2)\n\t\t-biThreshold <#> (minimum divergent\
    \ csRNAseq signal for bi/divergent TSS, def: 2)\n\t\t-dist <#> (minimum distance\
    \ from annotated TSS to assign 'distal' annotation, def: 500)\n\n\t* Tag Directories\
    \ are assumed to represent the 5' ends of RNA in sense orientation\n\n"
  generated_using: *id004
- !Command
  command: *id007
  positional: []
  named:
  - !Flag
    description: (*see note below)
    synonyms:
    - -o
    - -i
    args: !SimpleFlagArg
      name: prefix
    optional: true
  - !Flag
    description: (*see note below)
    synonyms:
    - -rna
    args: !SimpleFlagArg
      name: RNAseq tag directory
    optional: true
  - !Flag
    description: (HOMER genome identifier or FASTA file)
    synonyms:
    - -genome
    args: !SimpleFlagArg
      name: genome
    optional: true
  - !Flag
    description: (gene annotation, by default will use genome annotation if available)
    synonyms:
    - -gtf
    args: !SimpleFlagArg
      name: gtf file
    optional: true
  - !Flag
    description: '<#> (minimum number of norm. tags per TSS cluster, default: 7)'
    synonyms:
    - -ntagThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (size of regions to check for significant TSS clusters, default:
      150)'
    synonyms:
    - -size
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '(include single exon genes for TP/FP calculations, default: only
      use multiexon genes)'
    synonyms:
    - -includeSingleExons
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: (Include a metagene plot of read distributions)
    synonyms:
    - -metagene
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: <#> (max CPUs to use)
    synonyms:
    - -cpu
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)'
    synonyms:
    - -maxInputLog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)'
    synonyms:
    - -maxRNALog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum CDF difference to consider for TP/FP calculation, default:
      0.15)'
    synonyms:
    - -minDistDiff
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (if annotation-based TP/FP threshold calculation failes, default:
      1)'
    synonyms:
    - -defaultLog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (number of normalizaed reads added to input/target read counts,
      default: 1.0)'
    synonyms:
    - -pseudoCount
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (local fold enrichment used for initial TSS identification,
      default: 2)'
    synonyms:
    - -L
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: (only use RNAseq for stability calculations, not for filtering putative
      TSS)
    synonyms:
    - -noFilterRNA
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
      -100,500)'
    synonyms:
    - -senseRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
      -500,100)'
    synonyms:
    - -antisenseRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA coverage for FP identification,default:
      -150,-10)'
    synonyms:
    - -rnaRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum RNAseq read density for stable transcript, def: 2)'
    synonyms:
    - -stableThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum divergent csRNAseq signal for bi/divergent TSS, def:
      2)'
    synonyms:
    - -biThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: "<#> (minimum distance from annotated TSS to assign 'distal' annotation,\
      \ def: 500)"
    synonyms:
    - -dist
    args: !EmptyFlagArg {}
    optional: true
  parent: *id012
  subcommands: []
  usage: []
  help_flag:
  usage_flag:
  version_flag:
  help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\t\
    \t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\t\t-rna\
    \ <RNAseq tag directory> (*see note below)\n\t\t-genome <genome> (HOMER genome\
    \ identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation, by default\
    \ will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold <#> (minimum\
    \ number of norm. tags per TSS cluster, default: 7)\n\t\t-size <#> (size of regions\
    \ to check for significant TSS clusters, default: 150)\n\t\t-includeSingleExons\
    \ (include single exon genes for TP/FP calculations, default: only use multiexon\
    \ genes)\n\t\t-metagene (Include a metagene plot of read distributions)\n\t\t\
    -cpu <#> (max CPUs to use)\n\n\t\tEnrichment threshold options:\n\t\t-maxInputLog2Fold\
    \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\t\
    \t-maxRNALog2Fold <#> (maximum log2 fold enrichment vs. input or RNA, default:\
    \ 30000000000)\n\t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP\
    \ calculation, default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP\
    \ threshold calculation failes, default: 1)\n\t\t-pseudoCount <#> (number of normalizaed\
    \ reads added to input/target read counts, default: 1.0)\n\t\t-ntagThreshold <#>\
    \ (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t-L <#> (local\
    \ fold enrichment used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
    \ (only use RNAseq for stability calculations, not for filtering putative TSS)\n\
    \n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check for significant TSS\
    \ clusters, default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to calculate\
    \ RNA/input read density,default: -100,500)\n\t\t-antisenseRegion <#>,<#> (Size\
    \ of region to calculate RNA/input read density,default: -500,100)\n\t\t-rnaRegion\
    \ <#>,<#> (Size of region to calculate RNA coverage for FP identification,default:\
    \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum RNAseq\
    \ read density for stable transcript, def: 2)\n\t\t-biThreshold <#> (minimum divergent\
    \ csRNAseq signal for bi/divergent TSS, def: 2)\n\t\t-dist <#> (minimum distance\
    \ from annotated TSS to assign 'distal' annotation, def: 500)\n\n\t* Tag Directories\
    \ are assumed to represent the 5' ends of RNA in sense orientation\n\n"
  generated_using: *id004
- !Command
  command: *id008
  positional: []
  named:
  - !Flag
    description: (*see note below)
    synonyms:
    - -o
    - -i
    args: !SimpleFlagArg
      name: prefix
    optional: true
  - !Flag
    description: (*see note below)
    synonyms:
    - -rna
    args: !SimpleFlagArg
      name: RNAseq tag directory
    optional: true
  - !Flag
    description: (HOMER genome identifier or FASTA file)
    synonyms:
    - -genome
    args: !SimpleFlagArg
      name: genome
    optional: true
  - !Flag
    description: (gene annotation, by default will use genome annotation if available)
    synonyms:
    - -gtf
    args: !SimpleFlagArg
      name: gtf file
    optional: true
  - !Flag
    description: '<#> (minimum number of norm. tags per TSS cluster, default: 7)'
    synonyms:
    - -ntagThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (size of regions to check for significant TSS clusters, default:
      150)'
    synonyms:
    - -size
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '(include single exon genes for TP/FP calculations, default: only
      use multiexon genes)'
    synonyms:
    - -includeSingleExons
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: (Include a metagene plot of read distributions)
    synonyms:
    - -metagene
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: <#> (max CPUs to use)
    synonyms:
    - -cpu
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)'
    synonyms:
    - -maxInputLog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)'
    synonyms:
    - -maxRNALog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum CDF difference to consider for TP/FP calculation, default:
      0.15)'
    synonyms:
    - -minDistDiff
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (if annotation-based TP/FP threshold calculation failes, default:
      1)'
    synonyms:
    - -defaultLog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (number of normalizaed reads added to input/target read counts,
      default: 1.0)'
    synonyms:
    - -pseudoCount
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (local fold enrichment used for initial TSS identification,
      default: 2)'
    synonyms:
    - -L
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: (only use RNAseq for stability calculations, not for filtering putative
      TSS)
    synonyms:
    - -noFilterRNA
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
      -100,500)'
    synonyms:
    - -senseRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
      -500,100)'
    synonyms:
    - -antisenseRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA coverage for FP identification,default:
      -150,-10)'
    synonyms:
    - -rnaRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum RNAseq read density for stable transcript, def: 2)'
    synonyms:
    - -stableThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum divergent csRNAseq signal for bi/divergent TSS, def:
      2)'
    synonyms:
    - -biThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: "<#> (minimum distance from annotated TSS to assign 'distal' annotation,\
      \ def: 500)"
    synonyms:
    - -dist
    args: !EmptyFlagArg {}
    optional: true
  parent: *id012
  subcommands: []
  usage: []
  help_flag:
  usage_flag:
  version_flag:
  help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\t\
    \t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\t\t-rna\
    \ <RNAseq tag directory> (*see note below)\n\t\t-genome <genome> (HOMER genome\
    \ identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation, by default\
    \ will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold <#> (minimum\
    \ number of norm. tags per TSS cluster, default: 7)\n\t\t-size <#> (size of regions\
    \ to check for significant TSS clusters, default: 150)\n\t\t-includeSingleExons\
    \ (include single exon genes for TP/FP calculations, default: only use multiexon\
    \ genes)\n\t\t-metagene (Include a metagene plot of read distributions)\n\t\t\
    -cpu <#> (max CPUs to use)\n\n\t\tEnrichment threshold options:\n\t\t-maxInputLog2Fold\
    \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\t\
    \t-maxRNALog2Fold <#> (maximum log2 fold enrichment vs. input or RNA, default:\
    \ 30000000000)\n\t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP\
    \ calculation, default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP\
    \ threshold calculation failes, default: 1)\n\t\t-pseudoCount <#> (number of normalizaed\
    \ reads added to input/target read counts, default: 1.0)\n\t\t-ntagThreshold <#>\
    \ (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t-L <#> (local\
    \ fold enrichment used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
    \ (only use RNAseq for stability calculations, not for filtering putative TSS)\n\
    \n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check for significant TSS\
    \ clusters, default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to calculate\
    \ RNA/input read density,default: -100,500)\n\t\t-antisenseRegion <#>,<#> (Size\
    \ of region to calculate RNA/input read density,default: -500,100)\n\t\t-rnaRegion\
    \ <#>,<#> (Size of region to calculate RNA coverage for FP identification,default:\
    \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum RNAseq\
    \ read density for stable transcript, def: 2)\n\t\t-biThreshold <#> (minimum divergent\
    \ csRNAseq signal for bi/divergent TSS, def: 2)\n\t\t-dist <#> (minimum distance\
    \ from annotated TSS to assign 'distal' annotation, def: 500)\n\n\t* Tag Directories\
    \ are assumed to represent the 5' ends of RNA in sense orientation\n\n"
  generated_using: *id004
- !Command
  command: *id009
  positional: []
  named:
  - !Flag
    description: (*see note below)
    synonyms:
    - -o
    - -i
    args: !SimpleFlagArg
      name: prefix
    optional: true
  - !Flag
    description: (*see note below)
    synonyms:
    - -rna
    args: !SimpleFlagArg
      name: RNAseq tag directory
    optional: true
  - !Flag
    description: (HOMER genome identifier or FASTA file)
    synonyms:
    - -genome
    args: !SimpleFlagArg
      name: genome
    optional: true
  - !Flag
    description: (gene annotation, by default will use genome annotation if available)
    synonyms:
    - -gtf
    args: !SimpleFlagArg
      name: gtf file
    optional: true
  - !Flag
    description: '<#> (minimum number of norm. tags per TSS cluster, default: 7)'
    synonyms:
    - -ntagThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (size of regions to check for significant TSS clusters, default:
      150)'
    synonyms:
    - -size
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '(include single exon genes for TP/FP calculations, default: only
      use multiexon genes)'
    synonyms:
    - -includeSingleExons
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: (Include a metagene plot of read distributions)
    synonyms:
    - -metagene
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: <#> (max CPUs to use)
    synonyms:
    - -cpu
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)'
    synonyms:
    - -maxInputLog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)'
    synonyms:
    - -maxRNALog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum CDF difference to consider for TP/FP calculation, default:
      0.15)'
    synonyms:
    - -minDistDiff
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (if annotation-based TP/FP threshold calculation failes, default:
      1)'
    synonyms:
    - -defaultLog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (number of normalizaed reads added to input/target read counts,
      default: 1.0)'
    synonyms:
    - -pseudoCount
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (local fold enrichment used for initial TSS identification,
      default: 2)'
    synonyms:
    - -L
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: (only use RNAseq for stability calculations, not for filtering putative
      TSS)
    synonyms:
    - -noFilterRNA
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
      -100,500)'
    synonyms:
    - -senseRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
      -500,100)'
    synonyms:
    - -antisenseRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA coverage for FP identification,default:
      -150,-10)'
    synonyms:
    - -rnaRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum RNAseq read density for stable transcript, def: 2)'
    synonyms:
    - -stableThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum divergent csRNAseq signal for bi/divergent TSS, def:
      2)'
    synonyms:
    - -biThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: "<#> (minimum distance from annotated TSS to assign 'distal' annotation,\
      \ def: 500)"
    synonyms:
    - -dist
    args: !EmptyFlagArg {}
    optional: true
  parent: *id012
  subcommands: []
  usage: []
  help_flag:
  usage_flag:
  version_flag:
  help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\t\
    \t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\t\t-rna\
    \ <RNAseq tag directory> (*see note below)\n\t\t-genome <genome> (HOMER genome\
    \ identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation, by default\
    \ will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold <#> (minimum\
    \ number of norm. tags per TSS cluster, default: 7)\n\t\t-size <#> (size of regions\
    \ to check for significant TSS clusters, default: 150)\n\t\t-includeSingleExons\
    \ (include single exon genes for TP/FP calculations, default: only use multiexon\
    \ genes)\n\t\t-metagene (Include a metagene plot of read distributions)\n\t\t\
    -cpu <#> (max CPUs to use)\n\n\t\tEnrichment threshold options:\n\t\t-maxInputLog2Fold\
    \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\t\
    \t-maxRNALog2Fold <#> (maximum log2 fold enrichment vs. input or RNA, default:\
    \ 30000000000)\n\t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP\
    \ calculation, default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP\
    \ threshold calculation failes, default: 1)\n\t\t-pseudoCount <#> (number of normalizaed\
    \ reads added to input/target read counts, default: 1.0)\n\t\t-ntagThreshold <#>\
    \ (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t-L <#> (local\
    \ fold enrichment used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
    \ (only use RNAseq for stability calculations, not for filtering putative TSS)\n\
    \n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check for significant TSS\
    \ clusters, default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to calculate\
    \ RNA/input read density,default: -100,500)\n\t\t-antisenseRegion <#>,<#> (Size\
    \ of region to calculate RNA/input read density,default: -500,100)\n\t\t-rnaRegion\
    \ <#>,<#> (Size of region to calculate RNA coverage for FP identification,default:\
    \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum RNAseq\
    \ read density for stable transcript, def: 2)\n\t\t-biThreshold <#> (minimum divergent\
    \ csRNAseq signal for bi/divergent TSS, def: 2)\n\t\t-dist <#> (minimum distance\
    \ from annotated TSS to assign 'distal' annotation, def: 500)\n\n\t* Tag Directories\
    \ are assumed to represent the 5' ends of RNA in sense orientation\n\n"
  generated_using: *id004
- !Command
  command: *id010
  positional: []
  named:
  - !Flag
    description: (*see note below)
    synonyms:
    - -o
    - -i
    args: !SimpleFlagArg
      name: prefix
    optional: true
  - !Flag
    description: (*see note below)
    synonyms:
    - -rna
    args: !SimpleFlagArg
      name: RNAseq tag directory
    optional: true
  - !Flag
    description: (HOMER genome identifier or FASTA file)
    synonyms:
    - -genome
    args: !SimpleFlagArg
      name: genome
    optional: true
  - !Flag
    description: (gene annotation, by default will use genome annotation if available)
    synonyms:
    - -gtf
    args: !SimpleFlagArg
      name: gtf file
    optional: true
  - !Flag
    description: '<#> (minimum number of norm. tags per TSS cluster, default: 7)'
    synonyms:
    - -ntagThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (size of regions to check for significant TSS clusters, default:
      150)'
    synonyms:
    - -size
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '(include single exon genes for TP/FP calculations, default: only
      use multiexon genes)'
    synonyms:
    - -includeSingleExons
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: (Include a metagene plot of read distributions)
    synonyms:
    - -metagene
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: <#> (max CPUs to use)
    synonyms:
    - -cpu
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)'
    synonyms:
    - -maxInputLog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)'
    synonyms:
    - -maxRNALog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum CDF difference to consider for TP/FP calculation, default:
      0.15)'
    synonyms:
    - -minDistDiff
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (if annotation-based TP/FP threshold calculation failes, default:
      1)'
    synonyms:
    - -defaultLog2Fold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (number of normalizaed reads added to input/target read counts,
      default: 1.0)'
    synonyms:
    - -pseudoCount
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (local fold enrichment used for initial TSS identification,
      default: 2)'
    synonyms:
    - -L
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: (only use RNAseq for stability calculations, not for filtering putative
      TSS)
    synonyms:
    - -noFilterRNA
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
      -100,500)'
    synonyms:
    - -senseRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA/input read density,default:
      -500,100)'
    synonyms:
    - -antisenseRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#>,<#> (Size of region to calculate RNA coverage for FP identification,default:
      -150,-10)'
    synonyms:
    - -rnaRegion
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum RNAseq read density for stable transcript, def: 2)'
    synonyms:
    - -stableThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: '<#> (minimum divergent csRNAseq signal for bi/divergent TSS, def:
      2)'
    synonyms:
    - -biThreshold
    args: !EmptyFlagArg {}
    optional: true
  - !Flag
    description: "<#> (minimum distance from annotated TSS to assign 'distal' annotation,\
      \ def: 500)"
    synonyms:
    - -dist
    args: !EmptyFlagArg {}
    optional: true
  parent: *id012
  subcommands: []
  usage: []
  help_flag:
  usage_flag:
  version_flag:
  help_text: "\n\tfindcsRNATSS.pl <csRNA tag directory> [options]\n\n\tOptions:\n\t\
    \t-o <prefix> \n\t\t-i <csRNA input tag directory> (*see note below)\n\t\t-rna\
    \ <RNAseq tag directory> (*see note below)\n\t\t-genome <genome> (HOMER genome\
    \ identifier or FASTA file)\n\t\t-gtf <gtf file> (gene annotation, by default\
    \ will use genome annotation if available)\n\t\t\n\t\t-ntagThreshold <#> (minimum\
    \ number of norm. tags per TSS cluster, default: 7)\n\t\t-size <#> (size of regions\
    \ to check for significant TSS clusters, default: 150)\n\t\t-includeSingleExons\
    \ (include single exon genes for TP/FP calculations, default: only use multiexon\
    \ genes)\n\t\t-metagene (Include a metagene plot of read distributions)\n\t\t\
    -cpu <#> (max CPUs to use)\n\n\t\tEnrichment threshold options:\n\t\t-maxInputLog2Fold\
    \ <#> (maximum log2 fold enrichment vs. input or RNA, default: 30000000000)\n\t\
    \t-maxRNALog2Fold <#> (maximum log2 fold enrichment vs. input or RNA, default:\
    \ 30000000000)\n\t\t-minDistDiff <#> (minimum CDF difference to consider for TP/FP\
    \ calculation, default: 0.15)\n\t\t-defaultLog2Fold <#> (if annotation-based TP/FP\
    \ threshold calculation failes, default: 1)\n\t\t-pseudoCount <#> (number of normalizaed\
    \ reads added to input/target read counts, default: 1.0)\n\t\t-ntagThreshold <#>\
    \ (minimum number of norm. tags per TSS cluster, default: 7)\n\t\t-L <#> (local\
    \ fold enrichment used for initial TSS identification, default: 2)\n\t\t-noFilterRNA\
    \ (only use RNAseq for stability calculations, not for filtering putative TSS)\n\
    \n\t\tRegion sizes:\n\t\t-size <#> (size of regions to check for significant TSS\
    \ clusters, default: 150)\n\t\t-senseRegion <#>,<#> (Size of region to calculate\
    \ RNA/input read density,default: -100,500)\n\t\t-antisenseRegion <#>,<#> (Size\
    \ of region to calculate RNA/input read density,default: -500,100)\n\t\t-rnaRegion\
    \ <#>,<#> (Size of region to calculate RNA coverage for FP identification,default:\
    \ -150,-10)\n\n\t\tAnnotation options:\n\t\t-stableThreshold <#> (minimum RNAseq\
    \ read density for stable transcript, def: 2)\n\t\t-biThreshold <#> (minimum divergent\
    \ csRNAseq signal for bi/divergent TSS, def: 2)\n\t\t-dist <#> (minimum distance\
    \ from annotated TSS to assign 'distal' annotation, def: 500)\n\n\t* Tag Directories\
    \ are assumed to represent the 5' ends of RNA in sense orientation\n\n"
  generated_using: *id004
usage: []
help_flag:
usage_flag:
version_flag:
help_text: "!!! First argument needs to be a <tag directory>\n\n\tUsage: findPeaks\
  \ <tag directory> [options]\n\n\tFinds peaks in the provided tag directory.  By\
  \ default, peak list printed to stdout\n\n\tGeneral analysis options:\n\t\t-o <filename|auto>\
  \ (file name for to output peaks, default: stdout)\n\t\t\t\"-o auto\" will send\
  \ output to \"<tag directory>/peaks.txt\", \".../regions.txt\",\n\t\t\tor \".../transcripts.txt\"\
  \ depending on the \"-style\" option\n\t\t-style <option> (Specialized options for\
  \ specific analysis strategies)\n\t\t\tfactor (transcription factor ChIP-Seq, uses\
  \ -center, output: peaks.txt,  default)\n\t\t\thistone (histone modification ChIP-Seq,\
  \ region based, uses -region -size 500 -L 0, regions.txt)\n\t\t\tgroseq (de novo\
  \ transcript identification from GroSeq data, transcripts.txt)\n\t\t\ttss (TSS identification\
  \ from 5' RNA sequencing, tss.txt)\n\t\t\tdnase (Hypersensitivity [crawford style\
  \ (nicking)], peaks.txt)\n\t\t\tsuper (Super Enhancers, superEnhancers.txt)\n\t\t\
  \tsuperhistone (Super Enhancers from H3K27ac data, superEnhancers.txt)\n\t\t\tmC\
  \ (Cytosine methylation (BS-seq/methylC-seq), regions.txt)\n\t\t\tdamid (DamID enrichment\
  \ from DpnI digestion, regions.txt)\n\t\t\tclip (CLIP-Seq enrichment, strand specific,\
  \ peaks.txt)\n\n\tchipseq/histone options:\n\t\t-i <input tag directory> (Experiment\
  \ to use as IgG/Input/Control)\n\t\t-size <#> (Peak size, default: auto)\n\t\t-minDist\
  \ <#> (minimum distance between peaks, default: peak size x2)\n\t\t-gsize <#> (Set\
  \ effective mappable genome size, default: 2e9)\n\t\t-fragLength <#|auto> (Approximate\
  \ fragment length, default: auto)\n\t\t-inputFragLength <#|auto> (Approximate fragment\
  \ length of input tags, default: auto)\n\t\t-tbp <#> (Maximum tags per bp to count,\
  \ 0 = no limit, default: auto)\n\t\t-inputtbp <#> (Maximum tags per bp to count\
  \ in input, 0 = no limit, default: auto)\n\t\t-strand <both|separate> (find peaks\
  \ using tags on both strands or separate, default:both)\n\t\t-norm # (Tag count\
  \ to normalize to, default 10000000)\n\t\t-region (extends start/stop coordinates\
  \ to cover full region considered \"enriched\")\n\t\t\t-regionRes <#> (number of\
  \ fractions peaks are divided in when extending 'regions', def: 4)\n\t\t-center\
  \ (Centers peaks on maximum tag overlap and calculates focus ratios)\n\t\t-nfr (Centers\
  \ peaks on most likely nucleosome free region [works best with mnase data])\n\t\t\
  \t(-center and -nfr can be performed later with \"getPeakTags\"\n\n\tPeak Filtering\
  \ options: (set -F/-L/-C to 0 to skip)\n\t\t-F <#> (fold enrichment over input tag\
  \ count, default: 4.0)\n\t\t  -P <#> (poisson p-value threshold relative to input\
  \ tag count, default: 0.0001)\n\t\t-L <#> (fold enrichment over local tag count,\
  \ default: 4.0)\n\t\t  -LP <#> (poisson p-value threshold relative to local tag\
  \ count, default: 0.0001)\n\t\t-C <#> (fold enrichment limit of expected unique\
  \ tag positions, default: 2.0)\n\t\t-localSize <#> (region to check for local tag\
  \ enrichment, default: 10000)\n\t\t-inputSize <#> (Size of region to search for\
  \ control tags, default: 2x peak size)\n\t\t-fdr <#> (False discovery rate, default\
  \ = 0.001)\n\t\t-poisson <#> (Set poisson p-value cutoff, default: uses fdr)\n\t\
  \t-tagThreshold <#> (Set # of tags to define a peak, default: 25)\n\t\t-ntagThreshold\
  \ <#> (Set # of normalized tags to define a peak, by default uses 1e7 for norm)\n\
  \t\t-minTagThreshold <#> (Absolute minimum tags per peak, default: expected tags\
  \ per peak)\n\n\tSuperEnhancer Options: (Need to specify \"-style super\"):\n\t\t\
  -superSlope <#> (Slope threshold to identify super vs. typical enh., default: 1.00)\n\
  \t\t-superWindow <#> (moving window/number of peaks to use to calculate slope, default:\
  \ 10)\n\t\t-typical <filename> (Output typical enhancers to this file, default:\
  \ not used)\n\t\t-inputPeaks <filename> (initial peaks to use for super enhancer\
  \ merging/scoring)\n\t\t-excludePeaks <filename> (regions to exclude from analysis,\
  \ i.e. TSS regions for H3K27ac)\n\n\tMethylC-Seq/BS-Seq options (Need to specify\
  \ \"-style mC\"):\n\t\t-unmethylC / -methylC (find unmethylated/methylated regions,\
  \ default: -unmethyC)\n\t\t-mCthresh <#> (methylation threshold of regions, default:\
  \ avg methylation/2)\n\t\t-minNumC <#> (Minimum number of cytosines per methylation\
  \ peak, default: 6)\n\n\tGroSeq Options (Need to specify \"-style groseq\"):\n\t\
  \t-tssSize <#> (size of region for initiation detection/artifact size, default:\
  \ 250)\n\t\t-minBodySize <#> (size of regoin for transcript body detection, default:\
  \ 1000)\n\t\t-tssFold <#> (fold enrichment for new initiation dectection, default:\
  \ 4.0)\n\t\t-bodyFold <#> (fold enrichment for new transcript dectection, default:\
  \ 4.0)\n\t\t-endFold <#> (end transcript when levels are this much less than the\
  \ start, default: 10.0)\n\t\t-method <fold|level> (method used for identifying new\
  \ transcripts, default: fold)\n\t\t-fragLength <#> (Approximate fragment length,\
  \ default: 150)\n\t\t-uniqmap <directory> (directory of binary files specifying\
  \ uniquely mappable locations)\n\t\t\tDownload from http://biowhat.ucsd.edu/homer/groseq/\n\
  \t\t-confPvalue <#> (confidence p-value: 1.00e-05)\n\t\t-minReadDepth <#> (Minimum\
  \ initial read depth for transcripts, default: auto)\n\t\t-pseudoCount <#> (Pseudo\
  \ tag count, default: 2.0)\n\t\t-rev (reverse strand of reads - for first-strand\
  \ rna-seq/gro-seq)\n\t\t-gtf <filename> (Output de novo transcripts in GTF format)\n\
  \t\t\t\"-o auto\" will produce <dir>/transcripts.txt and <dir>/transcripts.gtf\n\
  \t\t\n\n\tNo genome selected (\"none\") - limits what you can do\n\tPeak file =\
  \ 0.178757123489962.tss.tmp\n\tGenome = none\n\tOrganism = unknown\n\tWill count\
  \ tags on strand: +\n\tFragment Length set to 1\n\tWill use up to 3 CPUs in parts\
  \ that can use them\n\tPeak/BED file conversion summary:\n\t\tBED/Header formatted\
  \ lines: 0\n\t\tpeakfile formatted lines: 0\n\t\tDuplicated Peak IDs: 0\n\n\tPeak\
  \ File Statistics:\n\t\tTotal Peaks: 0\n\t\tRedundant Peak IDs: 0\n\t\tPeaks lacking\
  \ information: 0 (need at least 5 columns per peak)\n\t\tPeaks with misformatted\
  \ coordinates: 0 (should be integer)\n\t\tPeaks with misformatted strand: 0 (should\
  \ be either +/- or 0/1)\n\n\tOnly 0 peaks found! If many more expected, maybe the\
  \ file format is for Macs only\n\tTry running the command: changeNewLine.pl \"0.744680606135265.clean.pos\"\
  \n\n\tReading Positions...\n\t-----------------------\n\tFinding Closest TSS...\n\
  \tSkipping TSS assignment (can't find file for genome none)\n\t\tCan't find promoterFile\
  \ none/none.tss\n\tCould not find basic annotation file (none/none.annotation)\n\
  \tCould not find full/detailed annotation file (none/none.annotation)\n\tCounting\
  \ Tags in Peaks from each directory...\n\tOrganism: unknown\n\tOutputing Annotation\
  \ File...\n\tDone annotating peaks file\n\n\n\tNo genome selected (\"none\") - limits\
  \ what you can do\n\tPeak file = 0.178757123489962.tss.tmp\n\tGenome = none\n\t\
  Organism = unknown\n\tWill count tags on strand: +\n\tFragment Length set to 1\n\
  \tPeak Region set to 150\n\tTag Directories:\n--help not recognized\n\n\n\tUsage:\
  \ annotatePeaks.pl <peak file | tss> <genome version>  [additional options...]\n\
  \n\tAvailable Genomes (required argument): (name,org,directory,default promoter\
  \ set)\n\t\t\t-- or --\n\t\tCustom: provide the path to genome FASTA files (directory\
  \ or single file)\n\t\tIf no genome is available, specify 'none'.\n\t\tIf using\
  \ FASTA file or none, may want to specify '-organism <...>'\n\n\tUser defined annotation\
  \ files (default is UCSC refGene annotation):\n\t\tannotatePeaks.pl accepts GTF\
  \ (gene transfer formatted) files to annotate positions relative\n\t\tto custom\
  \ annotations, such as those from de novo transcript discovery or Gencode.\n\t\t\
  -gtf <gtf format file> (Use -gff and -gff3 if appropriate, but GTF is better)\n\t\
  \t-gid (by default the GTF file is processed by transcript_id, use this option for\
  \ gene_id)\n\t\t-ann <custom homer annotation file> (created by assignGenomeAnnotation,\
  \ see website)\n\n\tPeak vs. tss/tts/rna mode (works with custom GTF file):\n\t\t\
  If the first argument is \"tss\" (i.e. annotatePeaks.pl tss hg18 ...) then a TSS\
  \ centric\n\t\tanalysis will be carried out.  Tag counts and motifs will be found\
  \ relative to the TSS.\n\t\t(no position file needed) [\"tts\" now works too - e.g.\
  \ 3' end of gene]\n\t\t[\"rna\" specifies gene bodies, will automaticall set \"\
  -size given\"]\n\t\tNOTE: The default TSS peak size is 4000 bp, i.e. +/- 2kb (change\
  \ with -size option)\n\t\t-list <gene id list> (subset of genes to perform analysis\
  \ [unigene, gene id, accession,\n\t\t\t probe, etc.], default = all promoters)\n\
  \t\t-cTSS <promoter position file i.e. peak file> (should be centered on TSS)\n\n\
  \tPrimary Annotation Options:\n\t\t-mask (Masked repeats, can also add 'r' to end\
  \ of genome name)\n\t\t-m <motif file 1> [motif file 2] ... (list of motifs to find\
  \ in peaks)\n\t\t\t-mscore (reports the highest log-odds score within the peak)\n\
  \t\t\t-nmotifs (reports the number of motifs per peak)\n\t\t\t-mdist (reports distance\
  \ to closest motif)\n\t\t\t-mfasta <filename> (reports sites in a fasta file - for\
  \ building new motifs)\n\t\t\t-fm <motif file 1> [motif file 2] (list of motifs\
  \ to filter from above)\n\t\t\t-rmrevopp <#> (only count sites found within <#>\
  \ on both strands once, i.e. palindromic)\n\t\t\t-matrix <prefix> (outputs a motif\
  \ co-occurrence files:\n\t\t\t\tprefix.count.matrix.txt - number of peaks with motif\
  \ co-occurrence\n\t\t\t\tprefix.ratio.matrix.txt - ratio of observed vs. expected\
  \  co-occurrence\n\t\t\t\tprefix.logPvalue.matrix.txt - co-occurrence enrichment\n\
  \t\t\t\tprefix.stats.txt - table of pair-wise motif co-occurrence statistics\n\t\
  \t\t\tadditional options:\n\t\t\t\t-matrixMinDist <#> (minimum distance between\
  \ motif pairs - to avoid overlap, default: 4)\n\t\t\t\t-matrixMaxDist <#> (maximum\
  \ distance between motif pairs)\n\t\t\t-mbed <filename> (Output motif positions\
  \ to a BED file to load at UCSC (or -mpeak))\n\t\t\t-mlogic <filename> (will output\
  \ stats on common motif orientations)\n\t\t-d <tag directory 1> [tag directory 2]\
  \ ... (list of experiment directories to show\n\t\t\ttag counts for) NOTE: -dfile\
  \ <file> where file is a list of directories in first column\n\t\t-bedGraph <bedGraph\
  \ file 1> [bedGraph file 2] ... (read coverage counts from bedGraph files)\n\t\t\
  -wig <wiggle file 1> [wiggle file 2] ... (read coverage counts from wiggle files)\n\
  \t\t-p <peak file> [peak file 2] ... (to find nearest peaks)\n\t\t\t-pdist to report\
  \ only distance (-pdist2 gives directional distance)\n\t\t\t-pcount to report number\
  \ of peaks within region\n\t\t-vcf <VCF file> (annotate peaks with genetic variation\
  \ infomation, one col per individual)\n\t\t\t-editDistance (Computes the # bp changes\
  \ relative to reference)\n\t\t\t-individuals <name1> [name2] ... (restrict analysis\
  \ to these individuals)\n\t\t-gene <data file> ... (Adds additional data to result\
  \ based on the closest gene.\n\t\t\tThis is useful for adding gene expression data.\
  \  The file must have a header,\n\t\t\tand the first column must be a GeneID, Accession\
  \ number, etc.  If the peak\n\t\t\tcannot be mapped to data in the file then the\
  \ entry will be left empty.\n\t\t-go <output directory> (perform GO analysis using\
  \ genes near peaks)\n\t\t-genomeOntology <output directory> (perform genomeOntology\
  \ analysis on peaks)\n\t\t\t-gsize <#> (Genome size for genomeOntology analysis,\
  \ default: 2e9)\n\n\tAnnotation vs. Histogram mode:\n\t\t-hist <bin size in bp>\
  \ (i.e 1, 2, 5, 10, 20, 50, 100 etc.)\n\t\tThe -hist option can be used to generate\
  \ histograms of position dependent features relative\n\t\tto the center of peaks.\
  \  This is primarily meant to be used with -d and -m options to map\n\t\tdistribution\
  \ of motifs and ChIP-Seq tags.  For ChIP-Seq peaks for a Transcription factor\n\t\
  \tyou might want to use the -center option (below) to center peaks on the known\
  \ motif\n\t\t** If using \"-size given\", histogram will be scaled to each region\
  \ (i.e. 0-100%), with\n\t\tthe -hist parameter being the number of bins to divide\
  \ each region into.\n\t\t\tHistogram Mode specific Options:\n\t\t\t-nuc (calculated\
  \ mononucleotide frequencies at each position,\n\t\t\t\tWill report by default if\
  \ extracting sequence for other purposes like motifs)\n\t\t\t-di (calculated dinucleotide\
  \ frequencies at each position)\n\t\t\t-histNorm <#> (normalize the total tag count\
  \ for each region to 1, where <#> is the\n\t\t\t\tminimum tag total per region -\
  \ use to avoid tag spikes from low coverage\n\t\t\t-ghist (outputs profiles for\
  \ each gene, for peak shape clustering)\n\t\t\t-rm <#> (remove occurrences of same\
  \ motif that occur within # bp)\n\n\tPeak Centering: (other options are ignored)\n\
  \t\t-center <motif file> (This will re-center peaks on the specified motif, or remove\
  \ peak\n\t\t\tif there is no motif in the peak.  ONLY recentering will be performed,\
  \ and all other\n\t\t\toptions will be ignored.  This will output a new peak file\
  \ that can then be reanalyzed\n\t\t\tto reveal fine-grain structure in peaks (It\
  \ is advised to use -size < 200) with this\n\t\t\tto keep peaks from moving too\
  \ far (-mirror flips the position)\n\t\t-multi (returns genomic positions of all\
  \ sites instead of just the closest to center)\n\n\tGenome comparisons (need genome\
  \ & liftOver)\n\t\t-cmpGenome <genome1> [genome2] (Genomes to compare for sequence/motifs)\n\
  \t\t-cmpLiftover <liftover1> [genome2] (Genomes to compare for sequence/motifs)\n\
  \n\tNormalization options:\n\t\t-fpkm (normalize read counts to million reads or\
  \ fragments per kilobase mapped)\n\t\t-raw (do not adjust the tag counts based on\
  \ total tags sequenced, -noadj works too)\n\t\t-norm <#> (normalize tags to this\
  \ tag count, default=1e7, 0=average tag count in all directories)\n\t\t-normLength\
  \ <#> (Fragment length to normlize to for experiments with different lens, def:\
  \ 100)\n\t\t-log (output tag counts as log2(x+1+rand) values - for scatter plots)\n\
  \t\t-sqrt (output tag counts as sqrt(x+rand) values - for scatter plots)\n\t\t-ratio\
  \ (process tag values as ratios - i.e. chip-seq, or mCpG/CpG)\n\n\tAdvanced normalization\
  \ options: (-rlog and -vst require R and DESeq2 to be installed)\n\t\t-rlog (quantile/variance\
  \ normalization on reported genes using DESeq2 rlog funcition, needs R)\n\t\t-vst\
  \ (quantile/variance normalization on reported genes using DESeq2 vst function,\
  \ needs R)\n\n\tAdvanced Options:\n\t\t-len <#> / -fragLength <#> (Fragment length,\
  \ default=auto, might want to set to 1 for 5'RNA)\n\t\t-size <#> (Peak size[from\
  \ center of peak], default=inferred from peak file)\n\t\t\t-size #,# (i.e. -size\
  \ -10,50 count tags from -10 bp to +50 bp from center)\n\t\t\t-size \"given\" (count\
  \ tags etc. using the actual regions - for variable length regions)\n\t\t-strand\
  \ <+|-|both> (Count tags on specific strands relative to peak, default: both)\n\t\
  \t-pc <#> (maximum number of tags to count per bp, default=0 [no maximum], -tbp\
  \ <#> works too)\n\t\t-CpG (Calculate CpG/GC content)\n\t\t-nfr (report nuclesome\
  \ free region scores instead of tag counts, also -nfrSize <#>)\n\t\t-norevopp (do\
  \ not search for motifs on the opposite strand [works with -center too])\n\t\t-gwasCatalog\
  \ <gwasCatalog file from UCSC> (list overlapping GWAS risk SNPs)\n\t\t-pdist (only\
  \ report distance to nearest peak using -p, not peak name)\n\t\t-map <mapping file>\
  \ (mapping between peak IDs and promoter IDs, overrides closest assignment)\n\t\t\
  -noann, -nogene (skip genome annotation step, skip TSS annotation)\n\t\t-homer1/-homer2\
  \ (by default, the new version of homer [-homer2] is used for finding motifs)\n\t\
  \t-cpu <#> (Number of processors to use when possible - only some parts utilize\
  \ multiple cores)\n\t\t-noblanks (remove peaks/rows with missing data)\n\n\tPeak/BED\
  \ file conversion summary:\n\t\tBED/Header formatted lines: 0\n\t\tpeakfile formatted\
  \ lines: 0\n\t\tDuplicated Peak IDs: 0\n\n\tPeak File Statistics:\n\t\tTotal Peaks:\
  \ 0\n\t\tRedundant Peak IDs: 0\n\t\tPeaks lacking information: 0 (need at least\
  \ 5 columns per peak)\n\t\tPeaks with misformatted coordinates: 0 (should be integer)\n\
  \t\tPeaks with misformatted strand: 0 (should be either +/- or 0/1)\n\n\tOnly 0\
  \ peaks found! If many more expected, maybe the file format is for Macs only\n\t\
  Try running the command: changeNewLine.pl \"0.0945234839609874.2.tmp\"\n\n\n\tNo\
  \ genome selected (\"none\") - limits what you can do\n\tPeak file = 0.178757123489962.tmp\n\
  \tGenome = none\n\tOrganism = unknown\n\tWill count tags on strand: +\n\tFragment\
  \ Length set to 1\n\tTag Directories:\n--help not recognized\n\n\n\tUsage: annotatePeaks.pl\
  \ <peak file | tss> <genome version>  [additional options...]\n\n\tAvailable Genomes\
  \ (required argument): (name,org,directory,default promoter set)\n\t\t\t-- or --\n\
  \t\tCustom: provide the path to genome FASTA files (directory or single file)\n\t\
  \tIf no genome is available, specify 'none'.\n\t\tIf using FASTA file or none, may\
  \ want to specify '-organism <...>'\n\n\tUser defined annotation files (default\
  \ is UCSC refGene annotation):\n\t\tannotatePeaks.pl accepts GTF (gene transfer\
  \ formatted) files to annotate positions relative\n\t\tto custom annotations, such\
  \ as those from de novo transcript discovery or Gencode.\n\t\t-gtf <gtf format file>\
  \ (Use -gff and -gff3 if appropriate, but GTF is better)\n\t\t-gid (by default the\
  \ GTF file is processed by transcript_id, use this option for gene_id)\n\t\t-ann\
  \ <custom homer annotation file> (created by assignGenomeAnnotation, see website)\n\
  \n\tPeak vs. tss/tts/rna mode (works with custom GTF file):\n\t\tIf the first argument\
  \ is \"tss\" (i.e. annotatePeaks.pl tss hg18 ...) then a TSS centric\n\t\tanalysis\
  \ will be carried out.  Tag counts and motifs will be found relative to the TSS.\n\
  \t\t(no position file needed) [\"tts\" now works too - e.g. 3' end of gene]\n\t\t\
  [\"rna\" specifies gene bodies, will automaticall set \"-size given\"]\n\t\tNOTE:\
  \ The default TSS peak size is 4000 bp, i.e. +/- 2kb (change with -size option)\n\
  \t\t-list <gene id list> (subset of genes to perform analysis [unigene, gene id,\
  \ accession,\n\t\t\t probe, etc.], default = all promoters)\n\t\t-cTSS <promoter\
  \ position file i.e. peak file> (should be centered on TSS)\n\n\tPrimary Annotation\
  \ Options:\n\t\t-mask (Masked repeats, can also add 'r' to end of genome name)\n\
  \t\t-m <motif file 1> [motif file 2] ... (list of motifs to find in peaks)\n\t\t\
  \t-mscore (reports the highest log-odds score within the peak)\n\t\t\t-nmotifs (reports\
  \ the number of motifs per peak)\n\t\t\t-mdist (reports distance to closest motif)\n\
  \t\t\t-mfasta <filename> (reports sites in a fasta file - for building new motifs)\n\
  \t\t\t-fm <motif file 1> [motif file 2] (list of motifs to filter from above)\n\t\
  \t\t-rmrevopp <#> (only count sites found within <#> on both strands once, i.e.\
  \ palindromic)\n\t\t\t-matrix <prefix> (outputs a motif co-occurrence files:\n\t\
  \t\t\tprefix.count.matrix.txt - number of peaks with motif co-occurrence\n\t\t\t\
  \tprefix.ratio.matrix.txt - ratio of observed vs. expected  co-occurrence\n\t\t\t\
  \tprefix.logPvalue.matrix.txt - co-occurrence enrichment\n\t\t\t\tprefix.stats.txt\
  \ - table of pair-wise motif co-occurrence statistics\n\t\t\t\tadditional options:\n\
  \t\t\t\t-matrixMinDist <#> (minimum distance between motif pairs - to avoid overlap,\
  \ default: 4)\n\t\t\t\t-matrixMaxDist <#> (maximum distance between motif pairs)\n\
  \t\t\t-mbed <filename> (Output motif positions to a BED file to load at UCSC (or\
  \ -mpeak))\n\t\t\t-mlogic <filename> (will output stats on common motif orientations)\n\
  \t\t-d <tag directory 1> [tag directory 2] ... (list of experiment directories to\
  \ show\n\t\t\ttag counts for) NOTE: -dfile <file> where file is a list of directories\
  \ in first column\n\t\t-bedGraph <bedGraph file 1> [bedGraph file 2] ... (read coverage\
  \ counts from bedGraph files)\n\t\t-wig <wiggle file 1> [wiggle file 2] ... (read\
  \ coverage counts from wiggle files)\n\t\t-p <peak file> [peak file 2] ... (to find\
  \ nearest peaks)\n\t\t\t-pdist to report only distance (-pdist2 gives directional\
  \ distance)\n\t\t\t-pcount to report number of peaks within region\n\t\t-vcf <VCF\
  \ file> (annotate peaks with genetic variation infomation, one col per individual)\n\
  \t\t\t-editDistance (Computes the # bp changes relative to reference)\n\t\t\t-individuals\
  \ <name1> [name2] ... (restrict analysis to these individuals)\n\t\t-gene <data\
  \ file> ... (Adds additional data to result based on the closest gene.\n\t\t\tThis\
  \ is useful for adding gene expression data.  The file must have a header,\n\t\t\
  \tand the first column must be a GeneID, Accession number, etc.  If the peak\n\t\
  \t\tcannot be mapped to data in the file then the entry will be left empty.\n\t\t\
  -go <output directory> (perform GO analysis using genes near peaks)\n\t\t-genomeOntology\
  \ <output directory> (perform genomeOntology analysis on peaks)\n\t\t\t-gsize <#>\
  \ (Genome size for genomeOntology analysis, default: 2e9)\n\n\tAnnotation vs. Histogram\
  \ mode:\n\t\t-hist <bin size in bp> (i.e 1, 2, 5, 10, 20, 50, 100 etc.)\n\t\tThe\
  \ -hist option can be used to generate histograms of position dependent features\
  \ relative\n\t\tto the center of peaks.  This is primarily meant to be used with\
  \ -d and -m options to map\n\t\tdistribution of motifs and ChIP-Seq tags.  For ChIP-Seq\
  \ peaks for a Transcription factor\n\t\tyou might want to use the -center option\
  \ (below) to center peaks on the known motif\n\t\t** If using \"-size given\", histogram\
  \ will be scaled to each region (i.e. 0-100%), with\n\t\tthe -hist parameter being\
  \ the number of bins to divide each region into.\n\t\t\tHistogram Mode specific\
  \ Options:\n\t\t\t-nuc (calculated mononucleotide frequencies at each position,\n\
  \t\t\t\tWill report by default if extracting sequence for other purposes like motifs)\n\
  \t\t\t-di (calculated dinucleotide frequencies at each position)\n\t\t\t-histNorm\
  \ <#> (normalize the total tag count for each region to 1, where <#> is the\n\t\t\
  \t\tminimum tag total per region - use to avoid tag spikes from low coverage\n\t\
  \t\t-ghist (outputs profiles for each gene, for peak shape clustering)\n\t\t\t-rm\
  \ <#> (remove occurrences of same motif that occur within # bp)\n\n\tPeak Centering:\
  \ (other options are ignored)\n\t\t-center <motif file> (This will re-center peaks\
  \ on the specified motif, or remove peak\n\t\t\tif there is no motif in the peak.\
  \  ONLY recentering will be performed, and all other\n\t\t\toptions will be ignored.\
  \  This will output a new peak file that can then be reanalyzed\n\t\t\tto reveal\
  \ fine-grain structure in peaks (It is advised to use -size < 200) with this\n\t\
  \t\tto keep peaks from moving too far (-mirror flips the position)\n\t\t-multi (returns\
  \ genomic positions of all sites instead of just the closest to center)\n\n\tGenome\
  \ comparisons (need genome & liftOver)\n\t\t-cmpGenome <genome1> [genome2] (Genomes\
  \ to compare for sequence/motifs)\n\t\t-cmpLiftover <liftover1> [genome2] (Genomes\
  \ to compare for sequence/motifs)\n\n\tNormalization options:\n\t\t-fpkm (normalize\
  \ read counts to million reads or fragments per kilobase mapped)\n\t\t-raw (do not\
  \ adjust the tag counts based on total tags sequenced, -noadj works too)\n\t\t-norm\
  \ <#> (normalize tags to this tag count, default=1e7, 0=average tag count in all\
  \ directories)\n\t\t-normLength <#> (Fragment length to normlize to for experiments\
  \ with different lens, def: 100)\n\t\t-log (output tag counts as log2(x+1+rand)\
  \ values - for scatter plots)\n\t\t-sqrt (output tag counts as sqrt(x+rand) values\
  \ - for scatter plots)\n\t\t-ratio (process tag values as ratios - i.e. chip-seq,\
  \ or mCpG/CpG)\n\n\tAdvanced normalization options: (-rlog and -vst require R and\
  \ DESeq2 to be installed)\n\t\t-rlog (quantile/variance normalization on reported\
  \ genes using DESeq2 rlog funcition, needs R)\n\t\t-vst (quantile/variance normalization\
  \ on reported genes using DESeq2 vst function, needs R)\n\n\tAdvanced Options:\n\
  \t\t-len <#> / -fragLength <#> (Fragment length, default=auto, might want to set\
  \ to 1 for 5'RNA)\n\t\t-size <#> (Peak size[from center of peak], default=inferred\
  \ from peak file)\n\t\t\t-size #,# (i.e. -size -10,50 count tags from -10 bp to\
  \ +50 bp from center)\n\t\t\t-size \"given\" (count tags etc. using the actual regions\
  \ - for variable length regions)\n\t\t-strand <+|-|both> (Count tags on specific\
  \ strands relative to peak, default: both)\n\t\t-pc <#> (maximum number of tags\
  \ to count per bp, default=0 [no maximum], -tbp <#> works too)\n\t\t-CpG (Calculate\
  \ CpG/GC content)\n\t\t-nfr (report nuclesome free region scores instead of tag\
  \ counts, also -nfrSize <#>)\n\t\t-norevopp (do not search for motifs on the opposite\
  \ strand [works with -center too])\n\t\t-gwasCatalog <gwasCatalog file from UCSC>\
  \ (list overlapping GWAS risk SNPs)\n\t\t-pdist (only report distance to nearest\
  \ peak using -p, not peak name)\n\t\t-map <mapping file> (mapping between peak IDs\
  \ and promoter IDs, overrides closest assignment)\n\t\t-noann, -nogene (skip genome\
  \ annotation step, skip TSS annotation)\n\t\t-homer1/-homer2 (by default, the new\
  \ version of homer [-homer2] is used for finding motifs)\n\t\t-cpu <#> (Number of\
  \ processors to use when possible - only some parts utilize multiple cores)\n\t\t\
  -noblanks (remove peaks/rows with missing data)\n\nCould not open --help//tagInfo.txt\n"
generated_using: *id004
