version 1.0

task AnviScriptMergeCollections {
  input {
    String? contigs_db
    File? s_input_files
    String? output_file
  }
  command <<<
    anvi-script-merge-collections \
      ~{if defined(contigs_db) then ("--contigs-db " +  '"' + contigs_db + '"') else ""} \
      ~{if defined(s_input_files) then ("-i " +  '"' + s_input_files + '"') else ""} \
      ~{if defined(output_file) then ("--output-file " +  '"' + output_file + '"') else ""}
  >>>
  parameter_meta {
    contigs_db: "Anvi'o contigs database generated by 'anvi-gen- contigs'"
    s_input_files: "(S) [FILE(S) ...], --input-files FILE(S) [FILE(S) ...] Input file(s). TAB-delimited input files should have two columns, where the first column holds the contig name, and the second one the bin id. This is the standard ouptut of the program anvi-export-collection."
    output_file: "Output file name."
  }
}