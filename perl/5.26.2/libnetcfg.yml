!Command
command:
- libnetcfg
positional: []
named:
- !Flag
  description: the configuration
  synonyms:
  - -c
  args: !SimpleFlagArg
    name: change
  optional: true
- !Flag
  description: defaults from the old config (implies -c, non-interactive)
  synonyms:
  - -d
  args: !SimpleFlagArg
    name: use
  optional: true
- !Flag
  description: a specific file as the old config file
  synonyms:
  - -i
  args: !SimpleFlagArg
    name: use
  optional: true
- !Flag
  description: a specific file as the new config file
  synonyms:
  - -o
  args: !SimpleFlagArg
    name: use
  optional: true
- !Flag
  description: this help
  synonyms:
  - -h
  args: !SimpleFlagArg
    name: show
  optional: true
parent:
subcommands: []
usage: []
help_flag:
usage_flag:
version_flag:
help_text: "/usr/local/bin/libnetcfg: Usage: /usr/local/bin/libnetcfg [-c] [-d] [-i\
  \ oldconfigile] [-o newconfigfile] [-h]\nWithout options, the old configuration\
  \ is shown.\n\n   -c change the configuration\n   -d use defaults from the old config\
  \ (implies -c, non-interactive)\n   -i use a specific file as the old config file\n\
  \   -o use a specific file as the new config file\n   -h show this help\n\nThe default\
  \ name of the old configuration file is by default\n\"libnet.cfg\", unless otherwise\
  \ specified using the -i option,\nC<-i oldfile>, and it is searched first from the\
  \ current directory,\nand then from your module path.\n\nThe default name of the\
  \ new configuration file is \"libnet.cfg\", and by\ndefault it is written to the\
  \ current directory, unless otherwise\nspecified using the -o option.\n\n"
generated_using:
- -h
