!Command
command:
- gffutils-cli
- children
positional:
- !Positional
  description: "Database to use. If a GFF or GTF file is provided instead\nfor this\
    \ argument, a database will be created for you.\nThis can take some time (several\
    \ minutes), so it's best\nto create one ahead of time."
  position: 0
  name: db
  optional: false
- !Positional
  description: "Comma-separated list of IDs. These IDs, along with their\nchildren,\
    \ will be returned (subject to --limit and\n--exclude). If none provided, use\
    \ all genes in the\ndatabase (default: -)"
  position: 1
  name: ids
  optional: false
named:
- !Flag
  description: "Feature type (string), or level (integer). No children\nbelow this\
    \ level will be returned (default: -)"
  synonyms:
  - --limit
  args: !SimpleFlagArg
    name: LIMIT
  optional: true
- !Flag
  description: "Comma-separated list of featuretypes to filter out (like\ngrep -v)\
    \ (default: -)"
  synonyms:
  - --exclude
  args: !SimpleFlagArg
    name: EXCLUDE
  optional: true
- !Flag
  description: "Use this to suppress reporting the IDs you've provided.\n(default:\
    \ False)\n"
  synonyms:
  - --exclude-self
  args: !EmptyFlagArg {}
  optional: true
parent:
subcommands: []
usage: []
help_flag: !Flag
  description: show this help message and exit
  synonyms:
  - -h
  - --help
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "usage: gffutils-cli children [-h] [--limit LIMIT] [--exclude EXCLUDE]\n\
  \                             [--exclude-self]\n                             db\
  \ [ids]\n\n    Fetch children from the database according to ID.\n    \n\npositional\
  \ arguments:\n  db                 Database to use. If a GFF or GTF file is provided\
  \ instead\n                     for this argument, a database will be created for\
  \ you.\n                     This can take some time (several minutes), so it's\
  \ best\n                     to create one ahead of time.\n  ids               \
  \ Comma-separated list of IDs. These IDs, along with their\n                   \
  \  children, will be returned (subject to --limit and\n                     --exclude).\
  \ If none provided, use all genes in the\n                     database (default:\
  \ -)\n\noptional arguments:\n  -h, --help         show this help message and exit\n\
  \  --limit LIMIT      Feature type (string), or level (integer). No children\n \
  \                    below this level will be returned (default: -)\n  --exclude\
  \ EXCLUDE  Comma-separated list of featuretypes to filter out (like\n          \
  \           grep -v) (default: -)\n  --exclude-self     Use this to suppress reporting\
  \ the IDs you've provided.\n                     (default: False)\n"
generated_using:
- --help
