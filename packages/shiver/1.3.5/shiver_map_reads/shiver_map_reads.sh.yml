!Command
command:
- shiver_map_reads.sh
positional: []
named: []
parent:
subcommands: []
usage: []
help_flag:
usage_flag:
version_flag:
help_text: "Arguments for this script: (1) the initialisation directory you created\
  \ using the shiver_init.sh command; (2) the configuration file, containing all your\
  \ parameter choices etc.; (3) a fasta file of contigs (output from processing the\
  \ short reads with an assembly program); (4) A sample ID (\"SID\") used for naming\
  \ the output from this script (a sensible choice might be the contig file name minus\
  \ its path and extension); (5) the blast file created by the shiver_align_contigs.sh\
  \ command; (6) either the alignment of contigs to refs produced by the shiver_align_contigs.sh\
  \ command, or a fasta file containing a single reference to be used for mapping;\
  \ (7) the forward reads; (8) the reverse reads.\n"
generated_using:
- --help
