!Command
command:
- taxonkit
- reformat
positional: []
named:
- !Flag
  optional: true
  synonyms:
  - -P
  - --add-prefix
  description: add prefixes for all ranks, single prefix for a rank is defined by
    flag --prefix-X
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -d
  - --delimiter
  description: field delimiter in input lineage (default ";")
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - -F
  - --fill-miss-rank
  description: fill missing rank with lineage information of the next higher rank
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -f
  - --format
  description: output format, placeholders of rank are needed (default "{k};{p};{c};{o};{f};{g};{s}")
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - -i
  - --lineage-field
  description: field index of lineage. data should be tab-separated (default 2)
  args: !SimpleFlagArg
    name: int
- !Flag
  optional: true
  synonyms:
  - -r
  - --miss-rank-repl
  description: replacement string for missing rank
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - -p
  - --miss-rank-repl-prefix
  description: prefix for estimated taxon level (default "unclassified ")
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - -R
  - --miss-taxid-repl
  description: replacement string for missing taxid
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - --prefix-S
  description: prefix for subspecies, used along with flag -P/--add-prefix (default
    "S__")
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - --prefix-T
  description: prefix for strain, used along with flag -P/--add-prefix (default "T__")
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - --prefix-c
  description: prefix for class, used along with flag -P/--add-prefix (default "c__")
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - --prefix-f
  description: prefix for family, used along with flag -P/--add-prefix (default "f__")
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - --prefix-g
  description: prefix for genus, used along with flag -P/--add-prefix (default "g__")
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - --prefix-k
  description: prefix for superkingdom, used along with flag -P/--add-prefix (default
    "k__")
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - --prefix-o
  description: prefix for order, used along with flag -P/--add-prefix (default "o__")
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - --prefix-p
  description: prefix for phylum, used along with flag -P/--add-prefix (default "p__")
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - --prefix-s
  description: prefix for species, used along with flag -P/--add-prefix (default "s__")
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - --prefix-t
  description: prefix for subspecies/strain, used along with flag -P/--add-prefix
    (default "t__")
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - -S
  - --pseudo-strain
  description: use the node with lowest rank as strain name, only if which rank is
    lower than "species" and not "subpecies" nor "strain". This flag affects {t},
    {S}, {T}
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -t
  - --show-lineage-taxids
  description: show corresponding taxids of reformated lineage
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -T
  - --trim
  description: do not fill missing rank lower than current rank
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - --data-dir
  description: directory containing nodes.dmp and names.dmp (default "/root/.taxonkit")
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - --line-buffered
  description: use line buffering on output, i.e., immediately writing to stdin/file
    for every line of output
  args: !EmptyFlagArg {}
- !Flag
  optional: true
  synonyms:
  - -o
  - --out-file
  description: out file ("-" for stdout, suffix .gz for gzipped out) (default "-")
  args: !SimpleFlagArg
    name: string
- !Flag
  optional: true
  synonyms:
  - -j
  - --threads
  description: number of CPUs. 4 is enough (default 4)
  args: !SimpleFlagArg
    name: int
- !Flag
  optional: true
  synonyms:
  - --verbose
  description: print verbose information
  args: !EmptyFlagArg {}
parent:
subcommands: []
usage: []
help_flag: !Flag
  optional: true
  synonyms:
  - -h
  - --help
  description: help for reformat
  args: !EmptyFlagArg {}
usage_flag:
version_flag:
help_text: "Reformat lineage in canonical ranks\n\nOutput format can be formated by\
  \ flag --format, available placeholders:\n\n    {k}: superkingdom\n    {p}: phylum\n\
  \    {c}: class\n    {o}: order\n    {f}: family\n    {g}: genus\n    {s}: species\n\
  \    {t}: subspecies/strain\n    \n    {S}: subspecies\n    {T}: strain\n\nWhen\
  \ these's no nodes of rank \"subspecies\" nor \"stain\",\nyou can switch -S/--pseudo-strain\
  \ to use the node with lowest rank\nas subspecies/strain name, if which rank is\
  \ lower than \"species\". \nThis flag affects {t}, {S}, {T}.\n    \nOutput format\
  \ can contains some escape charactors like \"\\t\".\n\nThis command appends reformated\
  \ lineage to the input line.\nThe corresponding taxIDs of reformated lineage can\
  \ be provided as another\ncolumn by flag \"-t/--show-lineage-taxids\".\n\nUsage:\n\
  \  taxonkit reformat [flags]\n\nFlags:\n  -P, --add-prefix                     add\
  \ prefixes for all ranks, single prefix for a rank is defined by flag --prefix-X\n\
  \  -d, --delimiter string               field delimiter in input lineage (default\
  \ \";\")\n  -F, --fill-miss-rank                 fill missing rank with lineage\
  \ information of the next higher rank\n  -f, --format string                  output\
  \ format, placeholders of rank are needed (default \"{k};{p};{c};{o};{f};{g};{s}\"\
  )\n  -h, --help                           help for reformat\n  -i, --lineage-field\
  \ int              field index of lineage. data should be tab-separated (default\
  \ 2)\n  -r, --miss-rank-repl string          replacement string for missing rank\n\
  \  -p, --miss-rank-repl-prefix string   prefix for estimated taxon level (default\
  \ \"unclassified \")\n  -R, --miss-taxid-repl string         replacement string\
  \ for missing taxid\n      --prefix-S string                prefix for subspecies,\
  \ used along with flag -P/--add-prefix (default \"S__\")\n      --prefix-T string\
  \                prefix for strain, used along with flag -P/--add-prefix (default\
  \ \"T__\")\n      --prefix-c string                prefix for class, used along\
  \ with flag -P/--add-prefix (default \"c__\")\n      --prefix-f string         \
  \       prefix for family, used along with flag -P/--add-prefix (default \"f__\"\
  )\n      --prefix-g string                prefix for genus, used along with flag\
  \ -P/--add-prefix (default \"g__\")\n      --prefix-k string                prefix\
  \ for superkingdom, used along with flag -P/--add-prefix (default \"k__\")\n   \
  \   --prefix-o string                prefix for order, used along with flag -P/--add-prefix\
  \ (default \"o__\")\n      --prefix-p string                prefix for phylum, used\
  \ along with flag -P/--add-prefix (default \"p__\")\n      --prefix-s string   \
  \             prefix for species, used along with flag -P/--add-prefix (default\
  \ \"s__\")\n      --prefix-t string                prefix for subspecies/strain,\
  \ used along with flag -P/--add-prefix (default \"t__\")\n  -S, --pseudo-strain\
  \                  use the node with lowest rank as strain name, only if which rank\
  \ is lower than \"species\" and not \"subpecies\" nor \"strain\". This flag affects\
  \ {t}, {S}, {T}\n  -t, --show-lineage-taxids            show corresponding taxids\
  \ of reformated lineage\n  -T, --trim                           do not fill missing\
  \ rank lower than current rank\n\nGlobal Flags:\n      --data-dir string   directory\
  \ containing nodes.dmp and names.dmp (default \"/root/.taxonkit\")\n      --line-buffered\
  \     use line buffering on output, i.e., immediately writing to stdin/file for\
  \ every line of output\n  -o, --out-file string   out file (\"-\" for stdout, suffix\
  \ .gz for gzipped out) (default \"-\")\n  -j, --threads int       number of CPUs.\
  \ 4 is enough (default 4)\n      --verbose           print verbose information\n"
generated_using:
- --help
docker_image: quay.io/biocontainers/taxonkit:0.7.2--0
