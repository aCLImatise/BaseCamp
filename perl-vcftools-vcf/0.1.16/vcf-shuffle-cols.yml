!Command
positional:
- !Positional
  description: ''
  position: 0
  name: file.vcf.gz
  optional: false
named:
- !Flag
  description: The file with the correct order of the columns.
  synonyms:
  - -t
  - --template
  args: !SimpleFlagArg
    name: file
  optional: true
command:
- vcf-shuffle-cols
parent:
subcommands: []
help_flag: !Flag
  description: ', -?, --help                   This help message.'
  synonyms:
  - -h
  args: !EmptyFlagArg {}
  optional: true
usage_flag:
version_flag:
help_text: "About: Reorder columns to match the order in the template VCF.\nUsage:\
  \ vcf-shuffle-cols [OPTIONS] -t template.vcf.gz file.vcf.gz > out.vcf\nOptions:\n\
  \   -t, --template <file>            The file with the correct order of the columns.\n\
  \   -h, -?, --help                   This help message.\n\n"
generated_using:
- --help
