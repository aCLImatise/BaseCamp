!Command
positional:
- !Positional
  description: ''
  position: 0
  name: cat
  optional: false
- !Positional
  description: ''
  position: 1
  name: ref.fa
  optional: false
named: []
command:
- vcf-haplotypes
parent:
subcommands: []
help_flag:
usage_flag:
version_flag:
help_text: "Smartmatch is experimental at /tmp/tmpu6lzxab0/bin/vcf-haplotypes line\
  \ 185.\nUsage: cat ref.fa | vcf-haplotypes [OPTIONS] in.vcf.gz \nOptions:\n   -h,\
  \ -?, --help                   This help message.\n   -H, --haplotypes [<int>] \
  \        Apply variants for the given haplotypes (1,2).  If not given, both haplotypes\
  \ are applied.\n   -g, --gff filename               Reposition the features in the\
  \ given gff file with indels for each haplotypes\n   -s, --samples [<name>]    \
  \       Apply variants for the given samples. If not given, all variants are applied\n\
  \   -o, --output [<name>]            Write output files to the given directory.\
  \ If not given, set to './'\n   -g, --gff [<name>]               GFF input file\
  \ (optional).Examples:\n   # Get the consensus for one region. The fasta header\
  \ lines are then expected\n   # in the form \">chr:from-to\".\n   samtools faidx\
  \ ref.fa 8:11870-11890 | vcf-consensus in.vcf.gz > out.fa\n\n"
generated_using:
- --help
